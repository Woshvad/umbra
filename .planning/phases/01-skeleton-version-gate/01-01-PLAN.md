---
phase: 01-skeleton-version-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - daml/daml.yaml
  - DECISIONS.md
  - .gitignore
  - .env.example
  - README.md
autonomous: true
requirements: [LEDG-04, LEDG-01]
must_haves:
  truths:
    - "The installed Daml SDK is detected (daml version) and pinned to 2.10.4 in daml/daml.yaml before any template work"
    - "DECISIONS.md records SDK 2.10.4, the Daml 2.x HTTP JSON API line (NOT 3.x/cn-quickstart), the React-18 --legacy-peer-deps note, the Canton-sandbox ephemeral-party caveat, and the dev-only --allow-insecure-tokens caveat"
    - "The repo is scaffolded with .gitignore (excludes .env, .daml/, node_modules/, dist/, parties.json), .env.example with spec §15 keys, README.md run instructions (direct daml commands, no make), and the binding sources spec.md + Umbra design/ become git-tracked"
    - "daml/daml.yaml sets sdk-version 2.10.4, name umbra, version 0.1.0, source . (the folder CONTAINING the Umbra/ package dir — module headers stay Umbra.*), init-script Umbra.Setup:initialize, start-navigator false, and dependencies daml-prim/daml-stdlib/daml-script — with NO codegen block"
  artifacts:
    - path: "daml/daml.yaml"
      provides: "SDK pin + daml start wiring (the version gate config)"
      contains: "sdk-version: 2.10.4"
    - path: "DECISIONS.md"
      provides: "Version-gate decision record"
      contains: "2.10.4"
    - path: ".gitignore"
      provides: "Hygiene — never commit secrets or build artifacts"
      contains: ".env"
    - path: ".env.example"
      provides: "Spec §15 config keys (no real secrets)"
      contains: "ANTHROPIC_API_KEY="
    - path: "README.md"
      provides: "Run instructions (direct daml/node commands, no make)"
      min_lines: 15
  key_links:
    - from: "daml/daml.yaml"
      to: "Umbra.Setup:initialize"
      via: "init-script field"
      pattern: "init-script:\\s*Umbra\\.Setup:initialize"
    - from: "daml/daml.yaml"
      to: "Umbra/ module folder (package dir)"
      via: "source field points at the folder containing Umbra/"
      pattern: "source:\\s*\\."
---

<objective>
Pass the version gate (LEDG-04, spec's #1 risk) and lay the repo scaffold so every later layer codes against a known, pinned SDK and a frozen build config. This plan does ZERO Daml template authoring — it detects/pins the SDK, writes the canonical `daml/daml.yaml` that drives `daml start`, records the version decision, and creates repo hygiene files. It also commits the binding source inputs (`spec.md`, `Umbra design/`) so all later phases reference versioned sources.

Purpose: The installed SDK version and JSON API line determine codegen package versions, JSON API request shapes, and JWT token format for the entire build. Recording and pinning it here makes the build reproducible and prevents a mid-hackathon drift to the Daml 3.x / cn-quickstart line.

Output: `daml/daml.yaml` (pinned + wired), `DECISIONS.md`, `.gitignore`, `.env.example`, `README.md`, and `spec.md` + `Umbra design/` newly tracked in git.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@spec.md
@.planning/phases/01-skeleton-version-gate/01-CONTEXT.md
@.planning/phases/01-skeleton-version-gate/01-RESEARCH.md
@.planning/phases/01-skeleton-version-gate/01-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This phase (across plans 01-03) produces the frozen cross-layer contract. Plan 01 produces:
- **Files:** `daml/daml.yaml`, `DECISIONS.md`, `.gitignore`, `.env.example`, `README.md`; tracks `spec.md` + `Umbra design/`.
- **No Daml symbols** (templates/data/choices/scripts) are authored in this plan — they land in plans 02 (templates) and 03 (Setup/Tests).
The full symbol inventory for the phase: templates `Asset`/`Venue`/`Order`/`Round`/`RoundStats`/`TradeConfirmation`; data types `Side`/`OrderStatus`/`RoundStatus`/`Allocation`/`ClearResult`/`Parties`; choices `SubmitOrder`/`CloseRound`/`Clear`/`Split`/`Merge`/`Reassign`; scripts `initialize`/`exportParties`/`runCanonicalRound`/`test_setup_seeds`/`test_asset_split_merge`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Version gate — detect/pin SDK and write daml/daml.yaml + DECISIONS.md (LEDG-04)</name>
  <files>daml/daml.yaml, DECISIONS.md</files>
  <read_first>
    - spec.md §6 (Tech stack / version note) and §13 (project structure)
    - .planning/phases/01-skeleton-version-gate/01-RESEARCH.md "Version Gate Procedure (LEDG-04)" and "Pattern 1: Minimal daml.yaml for SDK 2.10.x" (copy-ready field list, all fields cited from Start.hs@v2.10.0)
    - .planning/STATE.md Blockers/Concerns (the P1 gate note)
  </read_first>
  <action>
    First run `daml version` to detect the installed SDK. The SDK is being installed out-of-band from the Windows tarball; if `daml` is NOT yet on PATH, the executor must wait/retry until it is, or finish the tarball install, BEFORE any build step — do NOT proceed to template work without a working `daml`. Confirm 2.10.4 is present/resolvable.

    Create `daml/daml.yaml` with exactly these fields (per RESEARCH Pattern 1): `sdk-version: 2.10.4`; `name: umbra`; `version: 0.1.0`; `source: .` — CRITICAL: `source:` is relative to daml.yaml and must point at the folder CONTAINING the package directory `Umbra/` (whose modules are headered `module Umbra.Asset where`, `Umbra.Auction`, `Umbra.Roles`, `Umbra.Setup`, `Umbra.Tests`, files at `daml/Umbra/*.daml`). Since `daml.yaml` lives in `daml/` and the package dir is `daml/Umbra/`, `source:` is `.` (the `daml/` folder). Do NOT use `source: Umbra` — that makes damlc resolve `Umbra.Asset` to the nonexistent `daml/Umbra/Umbra/Asset.daml` and `daml build` reports "no modules found" (the #1 "won't build" cause per Pitfall 2). `init-script: Umbra.Setup:initialize` (MUST be the fully module-qualified `Module:function` form — the module is `Umbra.Setup`, so the value is `Umbra.Setup:initialize`, per Pitfall 6); `start-navigator: false` (skips Navigator, speeds daml start); `dependencies:` listing `daml-prim`, `daml-stdlib`, `daml-script` (bundled with the SDK, resolved automatically — no npm install). Do NOT add a `codegen:` block in Phase 1 (it would make every daml start run daml2js with no consumer — defer to Phase 3 per the anti-pattern). Leave a commented `script-options:` hint for the parties.json export (`--output-file parties.json`) per RESEARCH "parties.json Export Mechanism" Option B, but keep it commented for now (plan 03 finalizes the export mechanism).

    Create root `DECISIONS.md` recording: (a) detected/installed SDK version = 2.10.4 (from `daml version`); (b) API line = Daml 2.x HTTP JSON API on :7575 — explicitly NOT Daml 3.x / Canton 3.x JSON Ledger API v2 / cn-quickstart (that is stretch §19); (c) React-18 peer-dep note: `@daml/react@2.10.4` peerDependencies = `react ^16.12.0 || ^17.0.0`, so the Phase-3 frontend installs with `--legacy-peer-deps` (or npm `overrides`); (d) Canton-sandbox caveat: `daml start` runs a Canton sandbox in 2.10.x, party IDs are `hint::<fingerprint>` and NOT stable across restarts → capture to parties.json, never hard-code; (e) dev-only caveat: `--allow-insecure-tokens` is dev-sandbox only, never for any deploy. This satisfies threat T-01-05 (insecure dev JWT misuse).
  </action>
  <verify>
    <automated>cd daml && daml build 2>&1 | tee /tmp/p1build.log; grep -q "sdk-version: 2.10.4" daml.yaml && grep -q "init-script: Umbra.Setup:initialize" daml.yaml && grep -qE "^source:[[:space:]]*\.$" daml.yaml && echo DAMLYAML_OK</automated>
    NOTE: `daml build` will FAIL at this point because no modules exist yet — that is expected. The binding assertion is the grep on daml.yaml fields (DAMLYAML_OK) plus DECISIONS.md grep below. Compilation green is the gate for plan 02.
  </verify>
  <acceptance_criteria>
    - `daml version` succeeds and reports 2.10.4 present (executor confirms before proceeding).
    - `grep -c "sdk-version: 2.10.4" daml/daml.yaml` == 1.
    - `grep -c "init-script: Umbra.Setup:initialize" daml/daml.yaml` == 1.
    - `daml/daml.yaml` declares `source: .` (the folder containing the Umbra/ package dir): `grep -cE "^source:[[:space:]]*\.$" daml/daml.yaml` == 1. It must NOT be `source: Umbra` (`grep -cE "^source:[[:space:]]*Umbra" daml/daml.yaml` == 0).
    - `daml/daml.yaml` contains NO uncommented `codegen:` key (`grep -v '^#' daml/daml.yaml | grep -c '^codegen:'` == 0).
    - `DECISIONS.md` exists and `grep -c "2.10.4" DECISIONS.md` >= 1 and `grep -ci "legacy-peer-deps" DECISIONS.md` >= 1 and `grep -ci "allow-insecure-tokens" DECISIONS.md` >= 1.
  </acceptance_criteria>
  <done>`daml version` confirms 2.10.4; `daml/daml.yaml` is pinned and wired (sdk-version, source: ., init-script Umbra.Setup:initialize, start-navigator:false, daml-prim/stdlib/script deps, no codegen block); `DECISIONS.md` records the five decision points above.</done>
</task>

<task type="auto">
  <name>Task 2: Repo scaffolding — .gitignore, .env.example, README.md, and track binding sources</name>
  <files>.gitignore, .env.example, README.md, spec.md, Umbra design/</files>
  <read_first>
    - spec.md §13 (project structure) and §15 (configuration & secrets — exact .env key list)
    - .planning/phases/01-skeleton-version-gate/01-CONTEXT.md "Repo scaffolding & hygiene" (the .gitignore exclude list + "commit the binding source inputs" instruction)
    - CLAUDE.md "Critical Project Rules" (NEVER add Claude as a git contributor; author/committer stays woshvad)
  </read_first>
  <action>
    Create root `.gitignore` excluding: `.env`, `.daml/` (Daml build artifacts), `node_modules/`, `dist/`, `.dist/`, build artifacts, `parties.json` (it holds ephemeral per-boot Canton party IDs — recommended gitignore per RESEARCH Runtime State Inventory), and the SDK download temp. Do NOT gitignore `spec.md` or `Umbra design/` — those must become tracked.

    Create root `.env.example` with exactly the spec §15 keys and placeholder (never-real) values: `ANTHROPIC_API_KEY=`, `JSON_API_URL=http://localhost:7575`, `SOLVER_PORT=4000`, `ROUND_SECONDS=60`, `PRODUCT_NAME=Umbra`. Add a comment noting `ANTHROPIC_API_KEY` is read ONLY by `solver/` (Phase 5) and is NEVER in the frontend, NEVER committed (satisfies threat T-01-04, ASVS V14). Do NOT create a real `.env`.

    Create root `README.md` (skeleton) documenting Phase-1 run instructions with DIRECT daml commands (no `make` — it is not installed; deferred to Phase 7): the project layout (daml/, solver/ stubbed, web/ stubbed), how to build (`cd daml && daml build`), how to run the sandbox + JSON API (`cd daml && daml start`, and the Windows hot-reload note: press `r` + `Enter`), and how to export parties.json (finalized in plan 03). Keep it concise but a stranger-runnable skeleton; it grows in Phase 7.

    Stage and track the currently-untracked binding sources `spec.md` and `Umbra design/` so every later phase references versioned sources. CRITICAL: when this work is committed (orchestrator handles the commit), the author/committer MUST stay `woshvad` — NO `Co-Authored-By`, NO "Generated with Claude" trailer, ever (CLAUDE.md strict rule). The executor must not introduce any Claude attribution into commit messages.
  </action>
  <verify>
    <automated>grep -q "^.env$" .gitignore && grep -q "^.daml/" .gitignore && grep -q "parties.json" .gitignore && grep -q "^ANTHROPIC_API_KEY=$" .env.example && grep -q "JSON_API_URL=http://localhost:7575" .env.example && grep -q "PRODUCT_NAME=Umbra" .env.example && test -f README.md && echo SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains `.env`, `.daml/`, `node_modules/`, `dist/`, `parties.json` (each greppable).
    - `.env.example` contains all five §15 keys (`ANTHROPIC_API_KEY=`, `JSON_API_URL=http://localhost:7575`, `SOLVER_PORT=4000`, `ROUND_SECONDS=60`, `PRODUCT_NAME=Umbra`); no real secret value present (`grep -E "ANTHROPIC_API_KEY=.+" .env.example` returns nothing).
    - `README.md` exists, is >= 15 lines, references `daml start` and the `r`+`Enter` Windows hot-reload note.
    - No real `.env` file is created (`test ! -f .env`).
    - `git ls-files spec.md` and `git ls-files "Umbra design"` show the binding sources are tracked (after staging).
  </acceptance_criteria>
  <done>`.gitignore`, `.env.example`, `README.md` exist and pass the grep gates; `spec.md` + `Umbra design/` are staged/tracked; no real `.env` exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → git history | Secrets or build artifacts must never cross into committed history |
| dev sandbox → any deploy | Dev-mode JWT/token settings must never be reused outside the local sandbox |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information disclosure | `.env` / `ANTHROPIC_API_KEY` | mitigate | `.env` gitignored; ship `.env.example` with empty placeholder only; key documented as read solely by `solver/` (per D: spec §15, ASVS V14) — no real secret ever committed |
| T-01-05 | Spoofing/Elevation | dev JWT / `--allow-insecure-tokens` | mitigate | Record in `DECISIONS.md` that `--allow-insecure-tokens` is dev-sandbox only, never for deploy; no external exposure in Phase 1 |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 1 installs NO external packages (pure-Daml skeleton; daml-prim/stdlib/script ship in the SDK tarball, not registry installs) — per RESEARCH Package Legitimacy Audit, no checkpoint needed; re-audit at Phase 3/5 |
</threat_model>

<verification>
- `daml version` reports 2.10.4 (gate passed).
- `daml/daml.yaml` greps for sdk-version 2.10.4, source: . (folder containing Umbra/), init-script Umbra.Setup:initialize, no codegen block.
- `DECISIONS.md` records all five version-gate decision points.
- `.gitignore` / `.env.example` / `README.md` pass their grep gates; no real `.env`.
- `spec.md` + `Umbra design/` tracked in git.
- (Compilation green is NOT expected here — modules arrive in plan 02. This plan's gate is config + scaffold correctness.)
</verification>

<success_criteria>
- LEDG-04 satisfied: SDK detected, pinned (2.10.4) in `daml/daml.yaml`, recorded in `DECISIONS.md` with API line + React-18 note + dev-token caveat, BEFORE any template work.
- Repo scaffold present and hygienic (no secrets/artifacts trackable); binding sources versioned.
- `daml/daml.yaml` is the correct `daml start` driver (source: . resolves the Umbra/ package dir) so plan 02 (templates) and plan 03 (Setup) can build on it.
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-version-gate/01-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
