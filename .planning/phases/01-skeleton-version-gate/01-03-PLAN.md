---
phase: 01-skeleton-version-gate
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - daml/Umbra/Setup.daml
  - daml/Umbra/Tests.daml
  - daml/daml.yaml
  - README.md
autonomous: true
requirements: [LEDG-02, LEDG-01]
must_haves:
  truths:
    - "Setup:initialize allocates Operator/BankA/BankB/BankC, creates one Venue{operator, desks=[A,B,C]}, and mints the §4 holdings: A→5000.0 USDCx; B→20.0 BONDX + 1000.0 USDCx; C→15.0 BONDX + 1000.0 USDCx — all Decimal literals carry a decimal point"
    - "Party IDs are written to a generated parties.json (gitignored) via daml script --output-file from the exportParties/initialize script's Parties return record"
    - "Setup:runCanonicalRound submits the three §4 orders (A Buy 10 @ 101.0, B Sell 8 @ 99.0, C Sell 5 @ 100.0) via Venue.SubmitOrder — the clearing assertion is deferred to Phase 2"
    - "daml test runs test_setup_seeds (asserts 4 parties + 5 Assets with the exact §4 quantities) and test_asset_split_merge (operator exercises Split then Merge preserving quantity) green"
    - "daml start compiles the project and boots the Canton sandbox + HTTP JSON API on :7575 (LEDG-01 runtime half) — verified manually via curl :7575/readyz"
  artifacts:
    - path: "daml/Umbra/Setup.daml"
      provides: "party allocation + §4 mint + Venue + runCanonicalRound + exportParties (LEDG-02)"
      contains: "allocatePartyWithHint"
    - path: "daml/Umbra/Tests.daml"
      provides: "test_setup_seeds (LEDG-02) + test_asset_split_merge (LEDG-03)"
      contains: "test_setup_seeds"
  key_links:
    - from: "daml/daml.yaml init-script"
      to: "Umbra.Setup.initialize"
      via: "init-script run by daml start"
      pattern: "init-script:\\s*Umbra\\.Setup:initialize"
    - from: "daml/Umbra/Setup.daml"
      to: "Umbra.Asset.Asset"
      via: "mintAsset createCmd Asset"
      pattern: "createCmd Asset"
    - from: "daml/Umbra/Setup.daml runCanonicalRound"
      to: "Umbra.Roles.Venue.SubmitOrder"
      via: "exerciseCmd venueCid SubmitOrder"
      pattern: "SubmitOrder"
---

<objective>
Seed the canonical §4 world and prove the skeleton runs end to end. Author `Setup.daml` (allocate the four parties, create the Venue, mint the §4 Assets, submit the canonical round, export party IDs) and `Tests.daml` (the two Wave-0 Daml Script tests), wire/confirm the `parties.json` export, and run the `daml start` smoke to satisfy the LEDG-01 runtime half.

Purpose: `Setup.daml` is the seed every later phase and test runs against; the §4 holdings are the single correctness reference for the whole build. Capturing party IDs to `parties.json` is required because the Canton sandbox yields `hint::<fingerprint>` IDs that are not stable across boots. The two tests are the Wave-0 validation scaffolds the VALIDATION contract requires.

Output: `daml/Umbra/Setup.daml`, `daml/Umbra/Tests.daml`, finalized `parties.json` export wiring in `daml/daml.yaml`, updated `README.md` export/test instructions, and a passing `daml start` smoke.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@spec.md
@.planning/phases/01-skeleton-version-gate/01-RESEARCH.md
@.planning/phases/01-skeleton-version-gate/01-VALIDATION.md
@.planning/phases/01-skeleton-version-gate/01-02-SUMMARY.md
</context>

<interface_context>
This plan consumes the frozen templates from plan 02: `Umbra.Asset.Asset` (mint via `createCmd Asset with operator, owner, symbol, quantity`), `Umbra.Roles.Venue` (`createCmd Venue with operator, desks`), `Umbra.Roles.Venue.SubmitOrder` (exercised by each desk), and the `Side(Buy/Sell)` type. Setup imports `Umbra.Asset`, `Umbra.Roles`, `Umbra.Auction`, and `Daml.Script`. The `Parties` record (operator/bankA/bankB/bankC, deriving Eq Show) is the export shape that becomes `parties.json`. RESEARCH "Pattern 4" has the copy-ready `allocateAll`/`mintAsset`/`initialize`/`exportParties` bodies; RESEARCH "parties.json Export Mechanism" documents both Option B (script-options in daml.yaml) and Option A (separate `daml script --output-file`) — pick one, default to trying Option B first then falling back to Option A.
</interface_context>

<artifacts_this_phase_produces>
Plan 03 produces:
- **Scripts (Setup.daml):** `allocateAll : Script Parties`, `mintAsset : Party -> Party -> Text -> Decimal -> Script (ContractId Asset)`, `initialize : Script Parties` (the daml.yaml init-script), `exportParties : Script Parties`, `runCanonicalRound : Script ()`.
- **Data type (Setup.daml):** `Parties { operator, bankA, bankB, bankC : Party }` (the parties.json export shape).
- **Tests (Tests.daml):** `test_setup_seeds : Script ()` (LEDG-02), `test_asset_split_merge : Script ()` (LEDG-03).
- **Generated/runtime:** `parties.json` (gitignored), running Canton sandbox + JSON API on :7575.
This completes the phase symbol inventory begun in plan 02.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Setup.daml — allocate parties + §4 mint + Venue + runCanonicalRound + exportParties (LEDG-02)</name>
  <files>daml/Umbra/Setup.daml</files>
  <read_first>
    - spec.md §4 (canonical fixture — exact parties, holdings, and the three orders) and §14 (Setup script — allocate, Venue, mint, parties export, RunCanonicalRound)
    - .planning/phases/01-skeleton-version-gate/01-RESEARCH.md "Pattern 4: Setup.daml" (copy-ready allocateAll/mintAsset/initialize/exportParties + runCanonicalRound bodies) and "Pitfall 1" (Decimal literals) and "Pitfall 3" (unstable party IDs → parties.json)
    - daml/Umbra/Asset.daml, daml/Umbra/Roles.daml, daml/Umbra/Auction.daml (the templates being seeded)
  </read_first>
  <action>
    Create `daml/Umbra/Setup.daml` with `module Umbra.Setup where` and imports `Daml.Script`, `Umbra.Asset`, `Umbra.Roles`, `Umbra.Auction`. Declare `data Parties = Parties with operator : Party; bankA : Party; bankB : Party; bankC : Party deriving (Eq, Show)` — the export shape that becomes parties.json.

    Author `allocateAll : Script Parties` using `allocatePartyWithHint` with hints "operator"/"bankA"/"bankB"/"bankC" and display names "Operator"/"BankA"/"BankB"/"BankC" (readable prefixes per RESEARCH; IDs still not stable across boots). Author `mintAsset : Party -> Party -> Text -> Decimal -> Script (ContractId Asset)` that `submit operator do createCmd Asset with operator, owner, symbol, quantity`.

    Author `initialize : Script Parties` (the daml.yaml init-script): allocate via `allocateAll`; create one `Venue with operator, desks = [bankA, bankB, bankC]` submitted by operator; mint the §4 holdings EXACTLY (every Decimal literal carries a decimal point per Pitfall 1) — BankA → "USDCx" 5000.0; BankB → "BONDX" 20.0 and "USDCx" 1000.0; BankC → "BONDX" 15.0 and "USDCx" 1000.0 (5 Asset contracts total). Return the `Parties` record. These quantities are NON-NEGOTIABLE (spec §4 single source of truth).

    Author `exportParties : Script Parties = allocateAll` (the separate-export script per RESEARCH Option A fallback — writes the Parties record as Daml-LF JSON when run with `--output-file parties.json`).

    Author `runCanonicalRound : Script ()` (spec §4 + §14): call `initialize`, query/find the Venue contract id for the operator, then submit the three §4 orders via `exerciseCmd venueCid SubmitOrder` — BankA: side = Buy, quantity = 10, limit = 101.0; BankB: side = Sell, quantity = 8, limit = 99.0; BankC: side = Sell, quantity = 5, limit = 100.0 (each `submit` by the respective desk, `roundId = "R1"`). Decimal limits carry decimal points; quantities are Int (no decimal point). Add a trailing comment that the clearing assertion (clearingPrice == 100.0, fills A=10/B=8/C=2) is DEFERRED to Phase 2 once Round.Clear is implemented — do NOT call Clear here (it would hit the loud placeholder and abort).
  </action>
  <verify>
    <automated>cd daml && daml build 2>&1 | tee /tmp/p3-setup.log; grep -qiE "error" /tmp/p3-setup.log && echo BUILD_HAS_ERRORS && exit 1; grep -q "allocatePartyWithHint" Umbra/Setup.daml && grep -q "5000.0" Umbra/Setup.daml && grep -q "20.0" Umbra/Setup.daml && grep -q "15.0" Umbra/Setup.daml && grep -q "SubmitOrder" Umbra/Setup.daml && grep -q "101.0" Umbra/Setup.daml && grep -q "99.0" Umbra/Setup.daml && echo SETUP_OK</automated>
  </verify>
  <acceptance_criteria>
    - `daml build` (in `daml/`) exits 0 with no `error` lines — the init-script `Umbra.Setup:initialize` now resolves (full LEDG-01 compile).
    - `grep -c "allocatePartyWithHint" daml/Umbra/Setup.daml` == 4 (four parties).
    - §4 mint quantities present: `grep -c "5000.0" Setup.daml` >= 1, `grep -c "20.0" Setup.daml` >= 1, `grep -c "15.0" Setup.daml` >= 1, `grep -c "1000.0" Setup.daml` >= 1 (each a decimal literal).
    - `grep -c "createCmd Asset" daml/Umbra/Setup.daml` >= 1 (mint path) and `grep -c "createCmd Venue\|create Venue" daml/Umbra/Setup.daml` >= 1.
    - runCanonicalRound submits 3 orders: `grep -c "SubmitOrder" Setup.daml` >= 3 (or a loop/list of 3); the three §4 limits 101.0 / 99.0 / 100.0 all present.
    - `runCanonicalRound` does NOT call `Clear` (`grep -c "exerciseCmd.*Clear\| Clear " Setup.daml` == 0) — clearing deferred to Phase 2.
  </acceptance_criteria>
  <done>`Setup.daml` compiles; `initialize` allocates 4 parties, creates the Venue, mints exactly the 5 §4 Assets with correct decimal quantities; `runCanonicalRound` submits the three §4 orders (no Clear call); `exportParties` provides the parties.json shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests.daml — test_setup_seeds (LEDG-02) + test_asset_split_merge (LEDG-03)</name>
  <files>daml/Umbra/Tests.daml</files>
  <read_first>
    - spec.md §16 (testing — note tests 1-6 are Phases 2-3; Phase 1 only needs the "seeds + compiles" smoke)
    - .planning/phases/01-skeleton-version-gate/01-VALIDATION.md "Wave 0 Requirements" and the Per-Task Verification Map (test_setup_seeds asserts 4 parties + 5 Assets + §4 quantities; test_asset_split_merge unit-tests operator Split/Merge)
    - daml/Umbra/Setup.daml (the initialize/allocateAll under test), daml/Umbra/Asset.daml (Split/Merge choices under test)
  </read_first>
  <behavior>
    - test_setup_seeds: after `initialize`, querying as operator finds exactly 5 Asset contracts whose quantities/symbols/owners match §4 (A: USDCx 5000.0; B: BONDX 20.0 + USDCx 1000.0; C: BONDX 15.0 + USDCx 1000.0), and the four parties are distinct/allocated.
    - test_asset_split_merge: operator mints one Asset, exercises Split into two, asserts the two quantities sum to the original (conservation), then exercises Merge back and asserts the merged quantity equals the original — proving operator-only Split/Merge preserve quantity (LEDG-03).
    - Edge: Split with splitQty out of range (<=0 or >= quantity) is rejected by the assertMsg — optionally assert this with `submitMustFail`.
  </behavior>
  <action>
    Create `daml/Umbra/Tests.daml` with `module Umbra.Tests where` and imports `Daml.Script`, `Umbra.Setup`, `Umbra.Asset`, `Umbra.Auction`, `DA.Assert` (or use `assert`/`assertMsg`). Author `test_setup_seeds : Script ()` that runs `initialize`, then `query @Asset operator` and asserts there are exactly 5 Asset contracts with the §4 symbols/quantities/owners (assert each expected holding is present — e.g. an Asset for bankA with symbol "USDCx" quantity 5000.0). Author `test_asset_split_merge : Script ()` that: allocates an operator + owner (or reuses `allocateAll`), mints one Asset (e.g. BONDX 10.0), exercises `Split` with splitQty 4.0, asserts the two resulting assets have quantities 4.0 and 6.0 (sum == 10.0, conservation), then exercises `Merge` and asserts the merged asset quantity == 10.0. Optionally add a `submitMustFail` for an out-of-range Split to prove the ensure/assert guard. All quantity literals are Decimal (decimal point). Keep the tests minimal — Phase 1 has no clearing/settlement/privacy logic to assert (those are Phases 2-3, spec §16 tests 1-6).
  </action>
  <verify>
    <automated>cd daml && daml test 2>&1 | tee /tmp/p3-test.log; grep -qiE "error|failed" /tmp/p3-test.log && echo TESTS_FAILED && exit 1; grep -q "test_setup_seeds" Umbra/Tests.daml && grep -q "test_asset_split_merge" Umbra/Tests.daml && echo TESTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `daml test` (in `daml/`) runs both scripts green with no `error`/`failed` lines in the log.
    - `grep -c "test_setup_seeds" daml/Umbra/Tests.daml` >= 1 and `grep -c "test_asset_split_merge" daml/Umbra/Tests.daml` >= 1.
    - test_setup_seeds asserts 5 Assets and at least the bankA USDCx 5000.0 holding (`grep -c "5000.0" Tests.daml` >= 1).
    - test_asset_split_merge exercises both `Split` and `Merge` (`grep -cE "exercise.*Split|Split with" Tests.daml` >= 1 and `grep -cE "exercise.*Merge|Merge with" Tests.daml` >= 1) and asserts conservation.
  </acceptance_criteria>
  <done>`daml test` runs `test_setup_seeds` (4 parties + 5 §4 Assets) and `test_asset_split_merge` (Split→Merge preserves quantity) green — the Wave-0 validation scaffolds exist and pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Finalize parties.json export + daml start smoke + README export/test docs (LEDG-01, LEDG-02)</name>
  <files>daml/daml.yaml, README.md</files>
  <read_first>
    - .planning/phases/01-skeleton-version-gate/01-RESEARCH.md "parties.json Export Mechanism (LEDG-02)" (Option A separate `daml script --output-file` vs Option B `script-options` in daml.yaml — Open Question 1) and "daml start Wiring (LEDG-01)" (the boot sequence + :7575/readyz)
    - daml/daml.yaml (the init-script + commented script-options hint from plan 01)
    - daml/Umbra/Setup.daml (initialize / exportParties)
  </read_first>
  <action>
    Finalize the parties.json export. Try Option B first: uncomment/add `script-options:` in `daml/daml.yaml` with `--output-file` and `parties.json` so `daml start`'s init-script run writes the `initialize` return value (the `Parties` record) to parties.json on each boot. If the write-directory under `daml start` is surprising on Windows (RESEARCH Open Question 1), fall back to Option A: a separate documented command `daml script --dar .daml/dist/umbra-0.1.0.dar --script-name Umbra.Setup:exportParties --ledger-host localhost --ledger-port <sandboxLedgerApiPort> --output-file parties.json` run against the live sandbox. Either satisfies LEDG-02 — the requirement is the party IDs land in `parties.json` as `{ operator, bankA, bankB, bankC }`. Confirm `parties.json` is gitignored (set in plan 01) so the ephemeral per-boot IDs are not committed.

    Run the `daml start` smoke (LEDG-01 runtime half): from `daml/`, run `daml start`; wait for the boot sequence (build → Canton sandbox + DAR upload → init-script Setup:initialize → JSON API on :7575); expect ~20-40s first boot and possibly a Windows JVM firewall prompt (allow localhost). Confirm readiness with `curl http://localhost:7575/readyz` returning ready/200. This is a long-running process — the executor starts it, verifies readyz, captures that parties.json was written, then stops the process. (This task's automated verify is the parties.json existence + structure check; the live :7575 boot is a manual-verification note since it's a long-running process.)

    Update `README.md` with the finalized export command and the test command (`cd daml && daml test`), and confirm the `daml start` + `r`+`Enter` hot-reload instructions from plan 01 are accurate.
  </action>
  <verify>
    <automated>cd daml && daml build 2>&1 | grep -qiE "error" && echo BUILD_BROKE && exit 1; (grep -q "script-options" daml.yaml || grep -qi "exportParties" ../README.md) && echo EXPORT_WIRED; test -f ../README.md && grep -qi "daml test" ../README.md && echo README_OK</automated>
    <human-check>From daml/, run `daml start`; wait for "JSON API started" / readiness; run `curl http://localhost:7575/readyz` → expect ready/200; confirm parties.json was written with { operator, bankA, bankB, bankC }; then stop the process. This proves LEDG-01 runtime half + LEDG-02 export. (Long-running process — not a daml test assertion.)</human-check>
  </verify>
  <acceptance_criteria>
    - The parties.json export is wired: EITHER `daml/daml.yaml` has `script-options:` with `--output-file` + `parties.json` (Option B), OR `README.md` documents the Option A `daml script --output-file parties.json` command.
    - `parties.json` is gitignored (`grep -c "parties.json" .gitignore` >= 1).
    - `README.md` documents `daml test` and the parties export, and references `daml start` + the `r`+`Enter` Windows hot-reload note.
    - MANUAL: `daml start` boots to JSON API ready on :7575 (`curl :7575/readyz` returns ready) and `parties.json` contains the four party IDs — captured in the SUMMARY as the LEDG-01 runtime / LEDG-02 export evidence.
  </acceptance_criteria>
  <done>parties.json export is wired (Option B or documented Option A) and gitignored; `daml start` boots the sandbox + JSON API on :7575 (manual smoke) and writes parties.json with the four party IDs; README documents the export + test commands. LEDG-01 (runtime) and LEDG-02 (export) satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sandbox → parties.json | Ephemeral Canton party IDs must be captured at allocation time, never hard-coded; parties.json holds per-boot IDs and must not be committed |
| dev JSON API → clients | `daml start` runs the JSON API in dev mode (`--allow-insecure-tokens`) — dev-sandbox only, no untrusted external clients in Phase 1 |
| seed integrity | The §4 mint quantities are the single correctness reference; a wrong literal silently corrupts every downstream test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Information disclosure | `parties.json` / ephemeral IDs | mitigate | `parties.json` gitignored (per-boot `hint::<fingerprint>` IDs never committed); IDs always read from the export, never hard-coded (RESEARCH Pitfall 3) |
| T-01-05 | Spoofing/Elevation | dev JSON API `--allow-insecure-tokens` | mitigate | Dev-sandbox only (recorded in DECISIONS.md, plan 01); no external exposure; per-party JWT auth is Phase 3 |
| T-01-07 | Tampering | §4 seed quantities | mitigate | `test_setup_seeds` asserts the exact §4 holdings (5 Assets, correct symbols/quantities/owners) so a wrong mint literal fails the test, not silently corrupts downstream |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 1 installs no external packages; Daml Script ships in the SDK — per RESEARCH Package Legitimacy Audit, no checkpoint needed |
</threat_model>

<verification>
- `daml build` green (init-script Umbra.Setup:initialize now resolves — full LEDG-01 compile).
- `daml test` runs test_setup_seeds + test_asset_split_merge green (Wave-0 validation scaffolds).
- §4 mint quantities asserted exactly by test_setup_seeds (5 Assets).
- parties.json export wired (Option B or documented Option A) and gitignored.
- MANUAL: `daml start` boots to JSON API ready on :7575; parties.json written with 4 IDs.
</verification>

<success_criteria>
- LEDG-02 satisfied: Setup allocates Operator/BankA/BankB/BankC, mints the exact §4 holdings, and party IDs land in parties.json.
- LEDG-01 (runtime half) satisfied: `daml start` compiles and boots sandbox + HTTP JSON API on :7575.
- The two Wave-0 Daml Script tests pass (`daml test` green) — the phase validation contract is met.
- The §4 fixture is seeded (clearing assertion correctly deferred to Phase 2 — runCanonicalRound does not call Clear).
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-version-gate/01-03-SUMMARY.md` when done.
</output>
