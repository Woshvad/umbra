---
phase: 02-clear-settle-on-ledger
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - daml/Umbra/Tests.daml
autonomous: true
requirements: [SETL-01, SETL-02, SETL-03, SETL-04, CLEAR-05]
must_haves:
  truths:
    - "test_settled_balances: after Round.Clear on the §4 fixture, balances are A:10/4000, B:12/1800, C:13/1200, with total BONDX and total USDCx conserved"
    - "test_atomicity: a round where a seller lacks the BONDX → Round.Clear fails via submitMustFail and NO balances change (all-or-nothing)"
    - "test_clear_rejects_bad_allocation: an allocation violating max-volume/limits/conservation, and a wrong clearingPrice (99.0), are both rejected by Round.Clear"
    - "A seedCanonicalAndClose helper opens a Round, submits the 3 §4 orders, CloseRound, and collects the Order/Asset ContractIds the real Clear needs"
    - "All four Phase-2 tests + the two Phase-1 tests pass under daml test (exit 0)"
  artifacts:
    - path: "daml/Umbra/Tests.daml"
      provides: "test_settled_balances, test_atomicity, test_clear_rejects_bad_allocation + seedCanonicalAndClose helper(s)"
      contains: "test_settled_balances"
  key_links:
    - from: "daml/Umbra/Tests.daml"
      to: "Round.Clear (real body)"
      via: "exerciseCmd ... Clear with the additive Option-B fields"
      pattern: "exerciseCmd .* Clear"
    - from: "daml/Umbra/Tests.daml"
      to: "query @Asset operator"
      via: "balance + conservation assertions after Clear"
      pattern: "query @Asset"
---

<objective>
Add the three on-ledger settlement tests (spec §16 tests 2–4) to `daml/Umbra/Tests.daml`, driving the real `Round.Clear` body from Plan 02: `test_settled_balances` (SETL-02), `test_atomicity` (SETL-03), `test_clear_rejects_bad_allocation` (SETL-04/CLEAR-05). Build the `seedCanonicalAndClose` helper(s) that open a Round, submit the three §4 orders, `CloseRound`, and collect the `ContractId Order` / `ContractId Asset` references the Option-B `Clear` signature requires.

Purpose: These tests are the phase's automated acceptance gate — they prove the atomic DvP settles the §4 fixture to the exact spec §4 balances, rolls back entirely on a failing leg, and rejects any wrong/unfair allocation on-ledger. `test_settled_balances` + `test_atomicity` together are what a HackCanton judge checks (spec §10: "the two things judges check").

Output: three new `Script ()` tests + the seed helper(s) in `Tests.daml`. `daml test` exit 0 with all four Phase-2 tests + the two Phase-1 tests green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/02-clear-settle-on-ledger/02-CONTEXT.md
@.planning/phases/02-clear-settle-on-ledger/02-RESEARCH.md
@.planning/phases/02-clear-settle-on-ledger/02-01-SUMMARY.md
@.planning/phases/02-clear-settle-on-ledger/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build seedCanonicalAndClose + an underfunded-seller variant helper</name>
  <files>daml/Umbra/Tests.daml</files>
  <read_first>
    - daml/Umbra/Tests.daml (whole file, now with `test_clears_at_100` from Plan 01): existing imports + the `query @Asset`/`queryContractId`/`submit`/`submitMustFail`/`exerciseCmd`/`createCmd`/`===`/`assertMsg`/`forA_` patterns. ADD to this file.
    - daml/Umbra/Setup.daml lines 49–93: `initialize` (seeds the §4 world — 4 parties, Venue, 5 Assets), `runCanonicalRound` (submits A Buy 10 @101.0, B Sell 8 @99.0, C Sell 5 @100.0 via `Venue.SubmitOrder`). NOTE: `runCanonicalRound` only SUBMITS orders — it never opens/closes a Round, so a Phase-2 seed helper is needed (RESEARCH A5).
    - daml/Umbra/Auction.daml (post Plan 02): the `Round` template (signatory operator, observer desks; `CloseRound` flips status to Closed), the `Order` template, and the real `Clear` signature with the additive `orderCids`/`buyerUsdcCid`/`sellerBondCids` fields.
    - daml/Umbra/Roles.daml: `Venue.SubmitOrder` (nonconsuming; controller desk) returns `ContractId Order`.
    - .planning/phases/02-clear-settle-on-ledger/02-RESEARCH.md "Code Examples" (the `seedCanonicalAndClose` usage shape, the balance-sum pattern, the snapshot pattern) + the Split-boundary note (B holds 20 BONDX → fills 8 → Split fine; the underfunded variant mints a too-small BONDX so the Split/ensure throws).
  </read_first>
  <action>
    Add to `Tests.daml` a helper `seedCanonicalAndClose` that returns everything the real `Clear` needs. Steps: `Parties{..} <- initialize` (seeds the §4 world). Create a `Round` (`submit operator $ createCmd Round with operator; roundId = "R1"; symbol = "BONDX"; desks = [bankA, bankB, bankC]; openedAt = <a Time, e.g. via getTime>; windowSeconds = 60; status = Open`). Submit the three §4 orders via the seeded `Venue` (query the Venue cid as in `runCanonicalRound`; capture each returned `ContractId Order` — submit as the respective desk). `CloseRound` the round (`submit operator $ exerciseCmd roundCid CloseRound`) and capture the new `ContractId Round` (status now Closed). Query `@Asset operator` to locate: the buyer's (`bankA`) USDCx holding cid (`buyerUsdcCid`), and each seller's BONDX holding cid as `sellerBondCids = [(bankB, bondB), (bankC, bondC)]`. Return a tuple/record carrying `Parties`, the closed `ContractId Round`, `[ContractId Order]`, `buyerUsdcCid`, and `sellerBondCids`. (Locate ContractIds with `query`/`queryContractId` HERE at the Script tier — this is exactly why Option B passes them into `Clear`; RESEARCH Pitfall 5.)

    Add a second helper `seedUnderfundedSellerAndClose` (for `test_atomicity`): same as above but seed a seller with too little BONDX to cover its fill (e.g. mint bankB only 5.0 BONDX instead of 20.0, or pass a `sellerBondCids` pointing at an undersized holding) so that the Split/`ensure quantity >= 0.0` in `Clear` throws. The proposed allocation stays internally-valid §8 (A=10/B=8/C=2) — the failure is the missing asset, not a bad proposal. Keep the §4 ORDER quantities/limits intact so `computeClearing` still returns the canonical result; only the holding is undersized.

    Every Decimal literal carries a decimal point; quantities passed to `Clear`'s `allocations` are `Int`. Reuse `allocateAll`/`initialize` from `Setup` rather than re-allocating parties.
  </action>
  <acceptance_criteria>
    - `cd daml && daml build` EXITS 0 — the helpers compile (they construct a `Round`, submit orders, `CloseRound`, and collect the Option-B cids).
    - `seedCanonicalAndClose` and `seedUnderfundedSellerAndClose` are declared in `Tests.daml` (grep).
    - The helpers locate ContractIds via `query`/`queryContractId` at the Script tier (grep confirms `query @Asset` or `query @Order` present in `Tests.daml`).
    Gate command: `cd daml && daml build; test $? -eq 0 || { echo BUILD_FAILED; exit 1; }; (grep -q 'seedCanonicalAndClose' Umbra/Tests.daml) && (grep -q 'seedUnderfundedSellerAndClose' Umbra/Tests.daml) && (grep -q 'query @Asset' Umbra/Tests.daml) && echo HELPERS_OK`
  </acceptance_criteria>
  <verify>
    <automated>cd daml && daml build; test $? -eq 0 || { echo BUILD_FAILED; exit 1; }; (grep -q 'seedCanonicalAndClose' Umbra/Tests.daml) && (grep -q 'seedUnderfundedSellerAndClose' Umbra/Tests.daml) && echo HELPERS_OK</automated>
  </verify>
  <done>`seedCanonicalAndClose` (canonical §4, Round opened+orders submitted+closed, Option-B cids collected) and `seedUnderfundedSellerAndClose` (a seller short the BONDX) compile and locate the needed ContractIds via Script-tier `query`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_settled_balances + test_atomicity + test_clear_rejects_bad_allocation</name>
  <files>daml/Umbra/Tests.daml</files>
  <read_first>
    - daml/Umbra/Tests.daml (with the helpers from Task 1 + `test_clears_at_100` from Plan 01).
    - daml/Umbra/Auction.daml (post Plan 02): the real `Clear` body + its additive `orderCids`/`buyerUsdcCid`/`sellerBondCids` fields and the recompute-and-assert backstop.
    - spec.md §4 (lines 79–88): the EXACT post-settlement balances (A:10/4000, B:12/1800, C:13/1200) + cash conservation (A −1000, B +800, C +200) + global totals (35 BONDX, 7000 USDCx seeded).
    - spec.md §10 (lines 328–345) + §16: the atomicity + bad-allocation rejection requirements.
    - .planning/phases/02-clear-settle-on-ledger/02-RESEARCH.md "Code Examples" — the three test patterns (`test_settled_balances` balance-sum + conservation; `test_atomicity` `submitMustFail` + before/after snapshot equality; `test_clear_rejects_bad_allocation` two `submitMustFail`s: bad fills AND wrong price).
  </read_first>
  <behavior>
    - `test_settled_balances` (SETL-02): after `Round.Clear` on the §4 fixture → `bal bankA "BONDX" === 10.0`, `bal bankA "USDCx" === 4000.0`, `bal bankB "BONDX" === 12.0`, `bal bankB "USDCx" === 1800.0`, `bal bankC "BONDX" === 13.0`, `bal bankC "USDCx" === 1200.0`; conservation: total BONDX === 35.0 and total USDCx === 7000.0 (unchanged vs the seed).
    - `test_atomicity` (SETL-03): with an underfunded seller, `Round.Clear` is wrapped in `submitMustFail`; the before/after Asset snapshots are IDENTICAL (no balance changed — all-or-nothing).
    - `test_clear_rejects_bad_allocation` (SETL-04/CLEAR-05): `submitMustFail` on `Clear` with an over-stated allocation (e.g. A Buy 12 — exceeds order qty 10 and traded 10); AND a separate `submitMustFail` on `Clear` with the CORRECT fills but a WRONG `clearingPrice = 99.0`. Both rejected by the on-ledger recompute-and-assert.
  </behavior>
  <action>
    Add three `Script ()` tests to `Tests.daml`. DO NOT place fenced settlement code in the action — implement from the named asserts and the RESEARCH code examples.

    `test_settled_balances`: call `seedCanonicalAndClose` → exercise the real `Clear` as operator with `clearingPrice = 100.0`, `allocations = [ Allocation with desk=bankA; side=Buy; filledQty=10, Allocation with desk=bankB; side=Sell; filledQty=8, Allocation with desk=bankC; side=Sell; filledQty=2 ]`, and the collected `orderCids`/`buyerUsdcCid`/`sellerBondCids`. Then `assets <- query @Asset operator`; define `bal owner sym = sum [ a.quantity | (_, a) <- assets, a.owner == owner, a.symbol == sym ]`; assert the six §4 balances above with `===` (Decimal literals with decimal points). Assert conservation: `sum [ a.quantity | (_,a) <- assets, a.symbol == "BONDX" ] === 35.0` and `... "USDCx" ] === 7000.0`. (Per 02-CONTEXT phase_critical_constraint 5 — these are exact.)

    `test_atomicity`: call `seedUnderfundedSellerAndClose` → `before <- query @Asset operator`. Define `snap xs = sort [ (show a.owner, a.symbol, a.quantity) | (_, a) <- xs ]`. Wrap the `Clear` exercise (same internally-valid allocation A=10/B=8/C=2 @100.0) in `submitMustFail operator do exerciseCmd roundCid Clear with ...`. Then `after <- query @Asset operator`; assert `snap after === snap before` (NO balance change — all-or-nothing rollback). (Per 02-CONTEXT phase_critical_constraint 4 + quality_gate: SETL-03 via `submitMustFail` + no-balance-change assertion.)

    `test_clear_rejects_bad_allocation`: call `seedCanonicalAndClose` (funded — so the rejection is by re-verification, NOT by missing assets). First `submitMustFail operator` exercising `Clear` with a BAD allocation (e.g. `[ Allocation bankA Buy 12, Allocation bankB Sell 8, Allocation bankC Sell 4 ]` — A=12 exceeds its order qty and traded volume). Second `submitMustFail operator` exercising `Clear` with the CORRECT fills (A=10/B=8/C=2) but `clearingPrice = 99.0` (wrong p*). Both must fail at the recompute-and-assert. (SETL-04, CLEAR-05.)

    Keep the existing tests untouched. Fills are `Int`; prices/balances are `Decimal` with decimal points; never compare an `Int` against a `Decimal` (RESEARCH Pitfall 3).
  </action>
  <acceptance_criteria>
    - `cd daml && daml test` EXITS 0 (authoritative — a passing run may print summary lines containing "error"/"failed"; gate on exit code ONLY, never grep the log for those words — Phase-1 convention).
    - All three tests are declared and run green: `test_settled_balances`, `test_atomicity`, `test_clear_rejects_bad_allocation` (grep confirms all three names in `Tests.daml`).
    - `test_settled_balances` asserts all six §4 balances (10.0/4000.0, 12.0/1800.0, 13.0/1200.0) AND conservation (35.0 BONDX, 7000.0 USDCx) — grep confirms `4000.0`, `1800.0`, `1200.0`, `35.0`, `7000.0` literals present.
    - `test_atomicity` uses `submitMustFail` and asserts a before/after snapshot equality (no balance change) — grep confirms `submitMustFail` and a `before`/`after` (or `snap`) comparison in the atomicity test.
    - `test_clear_rejects_bad_allocation` has TWO `submitMustFail` exercises of `Clear` (bad fills AND wrong price 99.0).
    - All four Phase-2 tests + both Phase-1 tests are green under one `daml test` run.
    Gate command: `cd daml && daml test; test $? -eq 0 || { echo TESTS_FAILED; exit 1; }; (grep -q 'test_settled_balances' Umbra/Tests.daml) && (grep -q 'test_atomicity' Umbra/Tests.daml) && (grep -q 'test_clear_rejects_bad_allocation' Umbra/Tests.daml) && (grep -q 'submitMustFail' Umbra/Tests.daml) && (grep -q '4000.0' Umbra/Tests.daml) && (grep -q '1800.0' Umbra/Tests.daml) && (grep -q '1200.0' Umbra/Tests.daml) && echo SETTLE_TESTS_OK`
  </acceptance_criteria>
  <verify>
    <automated>cd daml && daml test; test $? -eq 0 || { echo TESTS_FAILED; exit 1; }; (grep -q 'test_settled_balances' Umbra/Tests.daml) && (grep -q 'test_atomicity' Umbra/Tests.daml) && (grep -q 'test_clear_rejects_bad_allocation' Umbra/Tests.daml) && (grep -q 'submitMustFail' Umbra/Tests.daml) && (grep -q '4000.0' Umbra/Tests.daml) && echo SETTLE_TESTS_OK</automated>
  </verify>
  <done>`daml test` exits 0 with `test_settled_balances` (exact §4 balances + conservation — SETL-02), `test_atomicity` (`submitMustFail` + no-balance-change — SETL-03), and `test_clear_rejects_bad_allocation` (bad fills + wrong price both rejected — SETL-04/CLEAR-05) all green, alongside the two Phase-1 tests and `test_clears_at_100`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test proposal → `Round.Clear` | The tests are the (currently trusted) caller, but deliberately feed BAD proposals (`test_clear_rejects_bad_allocation`) and under-funded holdings (`test_atomicity`) to PROVE the ledger backstop rejects/rolls back. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | wrong/malicious allocation (verified) | mitigate | `test_clear_rejects_bad_allocation` proves `Round.Clear` rejects both an over-stated allocation (A=12) and a wrong price (99.0) via the recompute-and-assert — the keystone control is exercised, not just present (CLEAR-05, SETL-04). |
| T-02-04 | Tampering | partial settlement / inconsistent balances | mitigate | `test_atomicity` uses `submitMustFail` + an identical before/after Asset snapshot to prove all-or-nothing rollback (SETL-03). |
| T-02-06 | Tampering | non-conservation of cash/BONDX | mitigate | `test_settled_balances` asserts the six exact §4 balances AND global totals (35.0 BONDX, 7000.0 USDCx) unchanged — conservation verified end-to-end (SETL-02). |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | N/A this plan — no package installs (pure Daml Script; Package Legitimacy Audit N/A per RESEARCH). |
</threat_model>

<verification>
- `cd daml && daml test` exits 0 — all four Phase-2 tests + both Phase-1 tests green.
- `test_settled_balances` asserts the exact §4 balances + conservation; `test_atomicity` uses `submitMustFail` + no-balance-change; `test_clear_rejects_bad_allocation` has two `submitMustFail`s (bad fills + wrong price).
- No watch-mode flags; feedback latency < 60s (per 02-VALIDATION.md).
- Exit code is the gate — never grep the `daml test` log for "error"/"failed" (Phase-1 convention).
</verification>

<success_criteria>
- `test_settled_balances` passes: §4 fixture settles to A:10/4000, B:12/1800, C:13/1200 with cash + assets conserved (SETL-01 behavior + SETL-02).
- `test_atomicity` passes: a failing leg → `Round.Clear` fails and no balances change (SETL-03).
- `test_clear_rejects_bad_allocation` passes: bad fills AND a wrong price are both rejected on-ledger (SETL-04, CLEAR-05).
- `daml test` exits 0 across all six scripts (the four Phase-2 + two Phase-1).
</success_criteria>

<artifacts_this_phase_produces>
This plan produces:
- `seedCanonicalAndClose` and `seedUnderfundedSellerAndClose` helpers in `daml/Umbra/Tests.daml`.
- `test_settled_balances` (SETL-01/SETL-02), `test_atomicity` (SETL-03), `test_clear_rejects_bad_allocation` (SETL-04/CLEAR-05) in `daml/Umbra/Tests.daml`.
Consumes Plan 02-01's `computeClearing` (indirectly, via the `Clear` backstop) and Plan 02-02's real `Round.Clear` body. These three tests + Plan 01's `test_clears_at_100` are the four spec §16 Phase-2 tests — the phase's automated acceptance gate.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-clear-settle-on-ledger/02-03-SUMMARY.md` when done.
</output>
