---
phase: 03-privacy-proof-vertical-slice
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - daml/daml.yaml
  - web/package.json
  - web/index.html
  - web/vite.config.ts
  - web/tsconfig.json
  - web/tsconfig.node.json
  - web/tailwind.config.ts
  - web/postcss.config.js
  - web/src/main.tsx
  - web/src/index.css
  - web/src/App.tsx
  - web/src/config.ts
  - scripts/mint-tokens.mjs
  - package.json
  - .gitignore
autonomous: false
requirements: [PRIV-05, UI-01, UI-03]
user_setup: []
must_haves:
  truths:
    - "daml codegen js produces an installable @daml.js/umbra package with typed Order/Asset/RoundStats/TradeConfirmation/Venue/Round bindings"
    - "node scripts/mint-tokens.mjs reads parties.json and writes web/src/tokens.json with one HS256 dev JWT per identity"
    - "The browser bundle holds ONLY the three desk tokens + the JSON API URL — never the operator token, never the Anthropic key"
    - "cd web && npm run build exits 0 (tsc --noEmit + vite build) on the scaffold"
    - "web/src/tokens.json and web/daml.js/ are gitignored (per-boot ephemeral state)"
  artifacts:
    - path: "scripts/mint-tokens.mjs"
      provides: "Per-party HS256 dev JWT minter (Daml claim shape, ledgerId constant)"
      contains: "https://daml.com/ledger-api"
    - path: "web/tailwind.config.ts"
      provides: "The binding comp theme (paper/ink/lime/red tokens, fonts, redaction backgroundImage)"
      contains: "repeating-linear-gradient"
    - path: "web/vite.config.ts"
      provides: "Dev server :5173 + /v1 proxy to JSON API :7575 (ws true)"
      contains: "7575"
  key_links:
    - from: "scripts/mint-tokens.mjs"
      to: "web/src/tokens.json"
      via: "jwt sign per party from parties.json"
      pattern: "tokens.json"
    - from: "web/tailwind.config.ts backgroundImage.redact"
      to: "Plan 03 redaction columns"
      via: "repeating-linear-gradient(90deg,#0A0A0A 0 5px,#262626 5px 7px)"
      pattern: "repeating-linear-gradient"
---

<objective>
Stand up the build tooling and the empty-but-compiling frontend that Plan 03 fills in: generate the typed `@daml.js/umbra` bindings from the DAR, scaffold `web/` (Vite 5.4 + React 18.3.1 + TypeScript + Tailwind 3.4) with the binding comp's theme tokens and a Vite proxy to the JSON API, and write the per-party HS256 dev-JWT mint script (PRIV-05's wire-level credential).

Purpose: Privacy at the wire (PRIV-05) requires each desk to authenticate with its OWN token. This plan produces those tokens (mint script -> `web/src/tokens.json`) and the typed query/exercise bindings, plus a compiling `web/` shell with the exact Tailwind theme transcribed from the BINDING `Umbra design/` comp.
Output: `@daml.js/umbra` bindings, a `web/` scaffold that passes `npm run build`, `scripts/mint-tokens.mjs`, an extended `.gitignore`. NO React components beyond a placeholder `App` (those are Plan 03). The operator token and Anthropic key NEVER enter the browser bundle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-privacy-proof-vertical-slice/03-CONTEXT.md
@.planning/phases/03-privacy-proof-vertical-slice/03-RESEARCH.md
@.planning/phases/03-privacy-proof-vertical-slice/03-UI-SPEC.md
@DECISIONS.md
@.gitignore
@parties.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify jsonwebtoken package legitimacy before first install</name>
  <files>(none — verification gate, no files modified)</files>
  <what-built>
    Nothing executed yet — a pre-install legitimacy gate. The phase introduces ONE new external npm package, `jsonwebtoken@9.0.3`, for HS256 dev-JWT minting. RESEARCH could not run slopcheck this session, so per the Package Legitimacy Gate this package requires human verification (it is NOT auto-rated [OK]). The `@daml/*` packages and the React/Vite/Tailwind toolchain were pre-cleared in Phase 1 / re-confirmed in RESEARCH.
  </what-built>
  <action>
    PAUSE for human verification before the first `jsonwebtoken` install (see how-to-verify). Do not install until the resume-signal is given. If "zero-dep" is chosen, Task 4 uses Node `node:crypto` and no package is installed.
  </action>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/jsonwebtoken — confirm publisher auth0/okta, tens-of-millions weekly downloads, repo github.com/auth0/node-jsonwebtoken, 9.x line.
    2. Confirm no postinstall: `npm view jsonwebtoken@9.0.3 scripts.postinstall` — expect EMPTY output.
    3. Zero-dep alternative: RESEARCH provides a Node `crypto.createHmac('sha256', secret)` + base64url variant needing NO install. Approve with note "zero-dep crypto path" to use it instead.
  </how-to-verify>
  <verify>
    <human-check>Human confirms jsonwebtoken@9.0.3 legitimacy on npmjs.com (publisher auth0, no postinstall) OR selects the zero-dep crypto path.</human-check>
  </verify>
  <resume-signal>Type "approved" to install jsonwebtoken@9.0.3, or "zero-dep" to use the Node crypto variant, or describe a concern.</resume-signal>
  <done>Human has approved the install path (jsonwebtoken or zero-dep); the supply-chain gate is cleared for Task 4.</done>
</task>

<task type="auto">
  <name>Task 2: daml codegen js -> @daml.js/umbra bindings</name>
  <files>daml/daml.yaml</files>
  <read_first>
    - daml/daml.yaml — lines 36-37 explicitly say "Add [codegen] in Phase 3 when @daml.js/umbra is needed". This is that moment.
    - 03-RESEARCH.md "daml codegen js -> @daml.js/umbra Workflow" — the standalone command, the daml.yaml codegen-block option, and the import-subpath note (Assumption A2: confirm `/lib/Umbra/...` against the generated package.json).
    - DECISIONS.md D1 (SDK 2.10.4 via the ~/bin shim — do NOT run `daml install`).
  </read_first>
  <action>
    Build the DAR then generate bindings. `cd daml && daml build` produces `daml/.daml/dist/umbra-0.1.0.dar`. From the repo root run `daml codegen js daml/.daml/dist/umbra-0.1.0.dar -o web/daml.js` (standalone is simplest; does not slow future `daml start`). This emits `web/daml.js/umbra/` (scope `@daml.js`, package `@daml.js/umbra`) with a package.json declaring a `@daml/types` dep. Inspect `web/daml.js/umbra/package.json` + the `lib/Umbra/` tree and RECORD the exact import subpaths in the SUMMARY (expected: `@daml.js/umbra/lib/Umbra/Auction` for Order/RoundStats/TradeConfirmation/Round, `.../Roles` for Venue/SubmitOrder, `.../Asset` for Asset) so Plan 03 imports them without guessing. You MAY instead add a codegen block to daml.yaml for auto-regen on `daml start` — document it if so. Do NOT change sdk-version, source, init-script, or script-options.
  </action>
  <verify>
    <automated>test -f web/daml.js/umbra/package.json && grep -q '@daml/types' web/daml.js/umbra/package.json && echo CODEGEN_OK</automated>
  </verify>
  <acceptance_criteria>
    - `web/daml.js/umbra/package.json` exists and declares a `@daml/types` dependency.
    - `ls web/daml.js/umbra/lib/Umbra/` shows the generated Auction module (Order/RoundStats/TradeConfirmation/Round live there).
    - The exact import subpaths are recorded in the SUMMARY for Plan 03.
    - daml.yaml sdk-version/source/init-script lines unchanged.
  </acceptance_criteria>
  <done>@daml.js/umbra is generated from umbra-0.1.0.dar; the import subpaths are recorded for Plan 03.</done>
</task>

<task type="auto">
  <name>Task 3: web/ scaffold (Vite 5.4 + React 18.3.1 + Tailwind 3.4) with comp theme + proxy</name>
  <files>web/package.json, web/index.html, web/vite.config.ts, web/tsconfig.json, web/tsconfig.node.json, web/tailwind.config.ts, web/postcss.config.js, web/src/main.tsx, web/src/index.css, web/src/App.tsx, web/src/config.ts, .gitignore</files>
  <read_first>
    - 03-RESEARCH.md "Standard Stack" (pinned versions: react/react-dom 18.3.1, vite 5.4.21, @vitejs/plugin-react 4.7.0, tailwindcss 3.4.19, postcss 8.5.15, autoprefixer 10.5.2, @daml/* 2.10.4), "Installation (frontend)" (the npm installs with --legacy-peer-deps), "Vite dev proxy" (proxy /v1 -> :7575, ws true).
    - 03-UI-SPEC.md "Tailwind config" (lines 66-146) — TRANSCRIBE theme.extend VERBATIM (colors paper/ink/lime/red/redact/buy/sell/flame; fontFamily display/mono/body; fontSize 9..108; letterSpacing; spacing; borderColor DEFAULT #0A0A0A; backgroundImage.redact + redact-wipe; keyframes + animation). Also "Global resets" (lines 31-36) and "Font delivery" (line 29, the Google Fonts link + preconnect).
    - DECISIONS.md D3 — every `web/` install needs `--legacy-peer-deps` (Pitfall 7).
    - 03-RESEARCH.md "Runtime State Inventory" — gitignore `web/src/tokens.json` + `web/daml.js/`.
  </read_first>
  <action>
    Scaffold Vite + React 18 + TypeScript + Tailwind 3.4 in `web/`. Pin the EXACT RESEARCH versions — NOT Tailwind 4, NOT React 19, NOT @vitejs/plugin-react 6.

    web/package.json: name umbra-web, type module; scripts dev=vite, build="tsc --noEmit && vite build", preview="vite preview", tokens="node ../scripts/mint-tokens.mjs". deps: react@18.3.1, react-dom@18.3.1, @daml/react@2.10.4, @daml/ledger@2.10.4, @daml/types@2.10.4, @daml.js/umbra from file:./daml.js/umbra. devDeps: typescript@5.6, vite@5.4, @vitejs/plugin-react@4.7, tailwindcss@3.4, postcss@8, autoprefixer@10, @types/react@18, @types/react-dom@18. Install with `--legacy-peer-deps` (D3): runtime deps first, then `npm install ./daml.js/umbra --legacy-peer-deps`.

    web/vite.config.ts: @vitejs/plugin-react; server.port 5173; server.proxy['/v1'] = { target: 'http://localhost:7575', changeOrigin: true, ws: true } (transcribe RESEARCH proxy).

    web/tsconfig.json + web/tsconfig.node.json: standard Vite-React strict config (strict true, jsx react-jsx, moduleResolution bundler, noEmit true).

    web/tailwind.config.ts: TRANSCRIBE the UI-SPEC theme.extend VERBATIM (content ['./index.html','./src/**/*.{ts,tsx}']). web/postcss.config.js: { plugins: { tailwindcss: {}, autoprefixer: {} } }.

    web/index.html: Google Fonts link + preconnect EXACTLY as UI-SPEC line 29 (Inter + IBM Plex Mono + Space Grotesk weights 400;500;600;700, display=swap); div id root; script type=module src /src/main.tsx.

    web/src/index.css: @tailwind base/components/utilities + the UI-SPEC global resets (html,body margin 0 background #F4F1EA; ::selection background #D6FB3C color #0A0A0A; root font/background).

    web/src/config.ts: export JSON_API_URL = '/' (Vite-proxy path) and DAML_LEDGER_ID = 'sandbox' (single drift constant — Plan 03 verifies it live).

    web/src/main.tsx: React 18 createRoot render(<App/>), import ./index.css.

    web/src/App.tsx: a PLACEHOLDER rendering paper bg + an UMBRA wordmark so build typechecks. Comment `// Plan 03 fills this in` — the real shell + Privacy view are Plan 03.

    Extend .gitignore: add `web/src/tokens.json` and `web/daml.js/` (node_modules already covered).

    If a `web/` install errors ERESOLVE on @daml/react + react@18, you omitted --legacy-peer-deps — re-run with it (D3).
  </action>
  <verify>
    <automated>cd web && npm run build > /tmp/umbra-web-build.txt 2>&1; echo "EXIT=$?"; tail -3 /tmp/umbra-web-build.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` exits 0 (tsc --noEmit clean + vite build produces web/dist/).
    - `grep -q 'repeating-linear-gradient(90deg,#0A0A0A 0 5px,#262626 5px 7px)' web/tailwind.config.ts` — exact redaction motif token.
    - `grep -q "'#F4F1EA'" web/tailwind.config.ts && grep -q "'#D6FB3C'" web/tailwind.config.ts` — paper + lime tokens.
    - `grep -q 7575 web/vite.config.ts && grep -q 'ws: true' web/vite.config.ts` — JSON API proxy wired.
    - `grep -q '"tailwindcss": "3' web/package.json` (NOT 4) and `grep -q '"react": "18' web/package.json` (NOT 19).
    - `grep -q 'web/src/tokens.json' .gitignore && grep -q 'web/daml.js/' .gitignore`.
    - `grep -q 'family=Space+Grotesk' web/index.html` — binding fonts linked.
  </acceptance_criteria>
  <done>web/ compiles (build exit 0) with the exact comp Tailwind theme, the JSON-API proxy, and gitignored generated state; only a placeholder App exists.</done>
</task>

<task type="auto">
  <name>Task 4: scripts/mint-tokens.mjs — per-party HS256 dev JWTs (PRIV-05)</name>
  <files>scripts/mint-tokens.mjs, package.json</files>
  <read_first>
    - 03-RESEARCH.md "Per-Party JWT Minting (PRIV-05)" — the claim shape `{"https://daml.com/ledger-api":{ledgerId, applicationId, actAs:[party], readAs:[party]}}`, HS256, empty/dev secret, the copy-ready mint script, and the zero-dep `node:crypto` variant. Use whichever Task 1 approved.
    - parties.json — at the repo ROOT (`./parties.json`, NOT `daml/parties.json`; the actual export lives at root per Phase-1 script-options). Shape `{ operator, bankA, bankB, bankC }` of `hint::fingerprint` IDs.
    - DECISIONS.md D4 (read parties.json fresh — IDs are per-boot, never hard-code), D5 (--allow-insecure-tokens dev-only), D6 (operator/Anthropic secrets never in the browser).
  </read_first>
  <action>
    Create `scripts/mint-tokens.mjs` (ESM). Read `./parties.json` at the repo root (if absent, print "run `daml start` to export parties.json, then re-run" and exit non-zero — do NOT write empty tokens). For EACH of the four identities mint an HS256 JWT with claim `{"https://daml.com/ledger-api":{ ledgerId: DAML_LEDGER_ID, applicationId: "umbra", actAs:[party], readAs:[party] }}`, where `DAML_LEDGER_ID = process.env.DAML_LEDGER_ID ?? "sandbox"` (single drift constant) and the dev secret is "" (arbitrary under --allow-insecure-tokens; if jsonwebtoken rejects empty, use a fixed dev string and note it). Write `web/src/tokens.json` = `{ operator:{party,token}, bankA:{...}, bankB:{...}, bankC:{...} }`.

    SECURITY (D6): document at the top of the script AND in the SUMMARY that the browser bundle imports ONLY tokens.bankA/bankB/bankC (the center count is read with a desk token — RoundStats observer = desks); the operator token is for CLI/out-of-browser use (the live verify curl, the Phase-4 service) and must NOT be imported into any `web/src` component. tokens.json is gitignored (Task 3) — a per-boot bearer credential.

    If Task 1 approved "zero-dep": sign with `node:crypto` createHmac('sha256', secret) + base64url; add NO npm dependency. If "approved": `npm install jsonwebtoken@9.0.3` at the repo ROOT (NOT in web/'s runtime deps), create a minimal root package.json if none exists (`{ "name":"umbra","private":true,"type":"module","scripts":{"tokens":"node scripts/mint-tokens.mjs"} }`), and `import jwt from "jsonwebtoken"`.
  </action>
  <verify>
    <automated>DAML_LEDGER_ID=sandbox node scripts/mint-tokens.mjs && node -e "const t=require('./web/src/tokens.json');const c=JSON.parse(Buffer.from(t.bankA.token.split('.')[1],'base64url'))['https://daml.com/ledger-api'];if(c.ledgerId!=='sandbox'||c.actAs[0]!==t.bankA.party)throw new Error('bad');console.log('MINT_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/mint-tokens.mjs` writes `web/src/tokens.json` with keys operator/bankA/bankB/bankC, each `{party, token}`.
    - The decoded bankA claim has ledgerId === "sandbox" (or env override), applicationId === "umbra", actAs[0] === tokens.bankA.party (asserted by verify).
    - `grep -q 'https://daml.com/ledger-api' scripts/mint-tokens.mjs` and `grep -q parties.json scripts/mint-tokens.mjs`.
    - The script header documents the operator-token-not-in-browser boundary (D6).
  </acceptance_criteria>
  <done>mint-tokens.mjs reads parties.json and mints per-party HS256 dev JWTs to web/src/tokens.json with the correct Daml claim; the operator-token-not-in-browser boundary is documented (PRIV-05 / D6).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build tooling -> browser bundle | Only desk tokens + JSON_API_URL may cross into `web/src`; the operator token and Anthropic key must NOT. |
| mint script -> web/src/tokens.json | Per-boot bearer-credential file (gitignored); regenerated after every `daml start`. |
| browser -> JSON API :7575 (via Vite proxy) | Each request carries one desk token whose actAs/readAs is the wire-level disclosure filter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Elevation of privilege | operator (broad-authority) token reaching the browser | mitigate | Mint script + SUMMARY document: `web/src` imports ONLY desk tokens; center count uses a desk token (RoundStats observer = desks); no operator-authored exercise in the browser (D6). |
| T-03-07 | Information disclosure | ANTHROPIC_API_KEY in the frontend bundle | accept (out of scope) | Key is solver-only (Phase 5); `web/` imports nothing from `solver/`; `.env` gitignored, `.env.example` placeholders only (D6). |
| T-03-08 | Tampering | Supply-chain: new `jsonwebtoken` install | mitigate | Blocking human-verify checkpoint (Task 1) vs npmjs.com + no-postinstall check; zero-dep `node:crypto` fallback. Not auto-approvable (slopcheck unavailable). |
| T-03-09 | Spoofing | dev JWT (insecure HS256 secret) reused in a deploy | accept (dev-only, documented) | --allow-insecure-tokens + empty/dev secret is sandbox-ONLY (D5); tokens.json gitignored; documented never-for-deploy. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck unavailable -> Task 1 blocking checkpoint before the only new external install; other installs pre-cleared in Phase 1. |
</threat_model>

<verification>
- `cd web && npm run build` exits 0 (scaffold typechecks + bundles).
- `web/daml.js/umbra/package.json` exists with a `@daml/types` dep.
- `node scripts/mint-tokens.mjs` writes a valid `web/src/tokens.json` (bankA claim checks pass).
- `.gitignore` ignores `web/src/tokens.json` + `web/daml.js/`.
- No operator token or Anthropic key is imported under `web/src/` (App is a placeholder this plan).
</verification>

<success_criteria>
- `@daml.js/umbra` typed bindings generated + installable; import subpaths recorded for Plan 03 (PRIV-05/UI-03 wiring foundation).
- `web/` scaffold compiles with the EXACT binding-comp Tailwind theme + the JSON-API proxy (UI-01/UI-03 visual foundation).
- The per-party HS256 mint script produces `web/src/tokens.json` with correct Daml claims; operator-token-not-in-browser boundary established + documented (PRIV-05, D6).
- All generated/per-boot state gitignored.
</success_criteria>

<output>
Create `.planning/phases/03-privacy-proof-vertical-slice/03-02-SUMMARY.md` when done. RECORD the exact `@daml.js/umbra` import subpaths and whether the jsonwebtoken or zero-dep path was used — Plan 03 depends on both.
</output>
