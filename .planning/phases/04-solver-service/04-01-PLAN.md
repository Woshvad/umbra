---
phase: 04-solver-service
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - solver/package.json
  - solver/tsconfig.json
  - solver/vitest.config.ts
  - solver/.env.example
  - solver/.gitignore
  - solver/src/auction.ts
  - solver/src/auction.test.ts
  - solver/src/ledger-smoke.ts
autonomous: true
requirements: [CLEAR-02, CLEAR-03, SOLV-05]
must_haves:
  truths:
    - "The §4 fixture clears at exactly 100.00 in TypeScript with fills A=10 / B=8 / C=2 (C residual 3) — identical to the Daml test_clears_at_100"
    - "Dropping the topPrices max-matched filter makes the §4 fixture clear at 99 — guarded by a dedicated regression test"
    - "A no-cross batch (best buy < best sell) yields matched 0 and pStar 0.0, no clear"
    - "The @daml.js/umbra-0.1.0 generated bindings import cleanly under Node ESM (import smoke passes)"
  artifacts:
    - path: "solver/src/auction.ts"
      provides: "Pure §8 clearing port (computeClearing, choosePStar, rationByPriority) + Side/OrderView/Allocation/ClearingResult types"
      exports: ["computeClearing", "choosePStar", "rationByPriority", "demandAt", "supplyAt", "matchedAt", "candidatePrices", "Side", "OrderView", "Allocation", "ClearingResult"]
      min_lines: 60
    - path: "solver/src/auction.test.ts"
      provides: "≥5 vitest clearing scenarios (§4 canary, same-limit ties, imbalance, no-cross, 99-vs-100 trap)"
      contains: "section-4 fixture"
    - path: "solver/package.json"
      provides: "Node ESM solver package with pinned deps + test/typecheck/dev scripts"
      contains: "\"type\": \"module\""
    - path: "solver/tsconfig.json"
      provides: "Node-ESM TS config (esModuleInterop) for the generated CJS bindings"
      contains: "esModuleInterop"
  key_links:
    - from: "solver/src/auction.test.ts"
      to: "solver/src/auction.ts"
      via: "import { computeClearing }"
      pattern: "import.*computeClearing.*auction"
    - from: "solver/src/ledger-smoke.ts"
      to: "@daml.js/umbra-0.1.0/lib/Umbra/Auction/module"
      via: "import Round/Order bindings"
      pattern: "@daml\\.js/umbra-0.1.0"
---

<objective>
Scaffold the greenfield `solver/` Node 20 + TypeScript ESM package, install the spec-pinned dependencies, de-risk the generated-bindings ESM import (RESEARCH Open Question 1), and port the deterministic §8 clearing algorithm from `daml/Umbra/Clearing.daml` to `solver/src/auction.ts` bit-for-bit — proven by ≥5 vitest scenarios with the §4 fixture as the continuous canary.

Purpose: `auction.ts` is the single off-ledger source of clearing math and the first dependency every later plan (ledger/api) consumes. Getting the §8 port and its tie-break trap exactly right here is the correctness keystone of the whole phase (CLEAR-02, CLEAR-03). The scaffold + import smoke unblock Waves 2-4.
Output: `solver/package.json`, `solver/tsconfig.json`, `solver/vitest.config.ts`, `solver/.env.example`, `solver/.gitignore`, `solver/src/auction.ts`, `solver/src/auction.test.ts`, `solver/src/ledger-smoke.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-solver-service/04-RESEARCH.md
@.planning/phases/04-solver-service/04-PATTERNS.md
@.planning/phases/04-solver-service/04-VALIDATION.md
@daml/Umbra/Clearing.daml
@daml/Umbra/Tests.daml
@web/package.json
@web/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold solver/ package, install pinned deps, prove the bindings import under Node ESM</name>
  <read_first>
    - web/package.json (mirror "type":"module", "private":true, identical @daml/ledger@2.10.4 + @daml/types@2.10.4 pins, and the tsc --noEmit gate)
    - web/tsconfig.json (mirror "strict", "module":"ESNext"; DEVIATE moduleResolution + esModuleInterop for Node — see RESEARCH Pitfall 6)
    - .planning/phases/04-solver-service/04-RESEARCH.md (Standard Stack install block lines 110-120; Pitfall 6 ESM interop; Pitfall 7 token path)
    - .planning/phases/04-solver-service/04-PATTERNS.md (package.json/tsconfig.json section lines 144-150; .env.example section lines 154-156)
  </read_first>
  <files>solver/package.json, solver/tsconfig.json, solver/vitest.config.ts, solver/.env.example, solver/.gitignore, solver/src/ledger-smoke.ts</files>
  <action>
    Create `solver/package.json` with `"type":"module"`, `"private":true`, name `umbra-solver`, version `0.1.0`. Scripts: `dev` = `tsx watch src/index.ts`, `test` = `vitest run`, `typecheck` = `tsc --noEmit`, `smoke` = `tsx src/ledger-smoke.ts`. Dependencies pinned IDENTICAL to the spec-era stack: `express@4.19.x`, `cors@2.8.x`, `zod@3.23.x`, `dotenv@16.x`, `@daml/ledger@2.10.4`, `@daml/types@2.10.4`, and `@daml.js/umbra-0.1.0` via `file:../web/daml.js/umbra-0.1.0`. devDependencies: `typescript@5.6.3` (match web exactly), `vitest@2.x`, `tsx@4.x`, `@types/express@4.17.x`, `@types/cors@2.8.x`, `@types/node@20.x`. Do NOT use registry-latest majors (express 5 / zod 4 / vitest 4 / dotenv 17) — they contradict CLAUDE.md and change APIs.
    Create `solver/tsconfig.json` mirroring web's `strict:true` + `module:"ESNext"`, but DEVIATE for Node: set `moduleResolution:"Bundler"` (fall back to `"Node16"` only if Bundler fails to resolve the generated package), `esModuleInterop:true`, `resolveJsonModule:true`, `skipLibCheck:true`, `noEmit:true`, `target:"ES2022"`, `lib:["ES2022"]`. Drop `jsx` and the DOM libs (no React here). `include:["src"]`.
    Create `solver/vitest.config.ts` minimal: `defineConfig` with `test.environment:'node'` and `test.include:['src/**/*.test.ts']`.
    Create `solver/.env.example` with the §15/D6 keys exactly: `JSON_API_URL=http://localhost:7575`, `SOLVER_PORT=4000`, `ROUND_SECONDS=60`, `ANTHROPIC_API_KEY=` (empty placeholder, unused until Phase 5). Never ship a real `.env`.
    Create `solver/.gitignore` ignoring `node_modules/`, `.env`, `dist/` (the repo-root `.gitignore` already covers `scripts/.operator-token`; do NOT duplicate or relocate the operator token).
    Create `solver/src/ledger-smoke.ts` (RESEARCH Open Question 1 de-risk): a tiny script that imports `Round`, `Order`, `RoundStats` from `@daml.js/umbra-0.1.0/lib/Umbra/Auction/module`, `Asset` from `.../Umbra/Asset/module`, `Side` from `.../Umbra/Clearing/module`, and `Ledger` (default) from `@daml/ledger`, then `console.log` the resolved `Round.templateId` / `Order.templateId` and exit 0. This proves the CJS-bindings-under-Node-ESM resolution BEFORE any client code (Pitfall 6). If a named import fails, fall back to a namespace/default import and record the working form in the SUMMARY.
    Install: `cd solver && npm install` (add `--legacy-peer-deps` only if peer friction surfaces, the established D3 remedy from the React-18 install).
  </action>
  <verify>
    <automated>cd solver && npm install --legacy-peer-deps && npx tsx src/ledger-smoke.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd solver && npm install` (or with `--legacy-peer-deps`) exits 0 and creates `solver/node_modules` with `@daml.js/umbra-0.1.0` present.
    - `npx tsx src/ledger-smoke.ts` exits 0 and prints a non-empty `Round.templateId` containing `:Umbra.Auction:Round` (proves the generated bindings import cleanly under Node ESM).
    - `solver/package.json` contains `"type": "module"`, `@daml/ledger` pinned to `2.10.4`, and NO express 5.x / zod 4.x / vitest 4.x.
    - `solver/.env.example` exists with `JSON_API_URL`, `SOLVER_PORT`, `ROUND_SECONDS`, `ANTHROPIC_API_KEY=` and NO real secret value; `solver/.gitignore` ignores `.env` and `node_modules/`.
    - No `solver/.env` file is committed (only `.env.example`).
  </acceptance_criteria>
  <done>Solver package scaffolded, deps installed, generated bindings proven importable under Node ESM, secrets-by-example only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the ≥5 vitest clearing scenarios (RED) — §4 canary + tie-break trap guard</name>
  <read_first>
    - daml/Umbra/Tests.daml (the test_clears_at_100 §4 fixture: A Buy 10 @101.0, B Sell 8 @99.0 wait — read the actual seeded order values and the asserted p*=100.00 / A=10 B=8 C=2 outcome to mirror exactly)
    - daml/Umbra/Clearing.daml (the algorithm under test: choosePStar topPrices filter lines 87-101; computeClearing sort order lines 125-138; the C-residual = quantity - filledQty note lines 120-124)
    - daml/Umbra/Auction.daml (allocation-equality semantics: sort by (desk, side, filledQty) before comparing — lines 182-185)
    - .planning/phases/04-solver-service/04-RESEARCH.md (the 5 mandatory scenarios + commands, lines 446-452; the TS port reference lines 313-373)
    - .planning/phases/04-solver-service/04-VALIDATION.md (per-task verification map, the vitest commands)
  </read_first>
  <files>solver/src/auction.test.ts</files>
  <behavior>
    - Test "section-4 fixture": the canonical §4 batch clears at clearingPrice===100, with a Buy allocation filledQty 10 for the buyer desk and Sell allocations summing to 10 (8 + 2); assert the C-residual (order.quantity − filledQty) equals 3.
    - Test "lower-price tie-break (99-vs-100 trap)": a batch where max-matched volume is achieved at BOTH 99 and 100, but |demand−supply| is minimized only correctly when the topPrices filter is applied — asserts clearingPrice===100 (goes red at 99 if the max-matched filter is dropped). MANDATORY regression guard.
    - Test "same-limit ties": two equal-limit orders on the long side → pro-rata split with the leftover unit going to the largest order; assert exact integer fills summing to traded, never exceeding any order's quantity.
    - Test "all-or-nothing imbalance": demand ≠ supply at p* → the short side fills fully and matched volume == min(demand, supply).
    - Test "no-cross": best buy limit < best sell limit → matchedAt(p*) === 0 and choosePStar returns 0.0 (verifies the foldl-max-0 empty-list seed); no allocation crosses.
  </behavior>
  <action>
    Author `solver/src/auction.test.ts` importing `computeClearing` (and `choosePStar`/`matchedAt` as needed) from `./auction`. Encode the §4 fixture order values mirrored EXACTLY from `daml/Umbra/Tests.daml` (the same desks, sides, quantities, limits that produce p*=100.00 there) so this test is the TypeScript twin of `test_clears_at_100`. Use string desk identifiers (e.g. `'BankA'`/`'BankB'`/`'BankC'`) since `auction.ts` operates on plain `OrderView` data, not on-ledger `Party`. For allocation comparison, sort both expected and actual by the key `(desk, side, filledQty)` before `toEqual`, mirroring the Daml multiset-equality (Auction.daml lines 183-185). Write all five `it(...)` blocks now even though `auction.ts` does not exist yet — they are EXPECTED to fail at this step (RED). Do NOT use watch mode anywhere (`vitest run`, never bare `vitest`).
    Name the test titles to match the VALIDATION command filters: `"section-4 fixture"`, `"lower-price tie-break"`, `"same-limit ties"`, `"imbalance"`, `"no-cross"`.
  </action>
  <verify>
    <automated>cd solver && npx vitest run src/auction.test.ts; test $? -ne 0 && echo "RED-as-expected (auction.ts not yet implemented)"</automated>
  </verify>
  <acceptance_criteria>
    - `solver/src/auction.test.ts` exists with exactly the five named scenarios above, each an `it`/`test` block with concrete assertions (no `.skip`, no `.todo`).
    - Running `npx vitest run src/auction.test.ts` FAILS at this step (RED) because `./auction` is not yet implemented — this is the expected pre-implementation state.
    - The §4 test asserts `clearingPrice === 100` and Sell fills of 8 and 2 (sum 10) and a C-residual of 3.
    - The tie-break test asserts `clearingPrice === 100` (the 99-vs-100 trap guard).
    - No watch-mode invocation (`vitest run` only).
  </acceptance_criteria>
  <done>Five clearing scenarios authored and failing for the right reason (implementation absent), forming the executable contract for Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port §8 to solver/src/auction.ts (GREEN) — bit-identical to Clearing.daml</name>
  <read_first>
    - daml/Umbra/Clearing.daml (THE source of truth — port 1:1: Side/OrderView/Allocation types lines 31-50; demandAt/supplyAt/matchedAt/candidatePrices lines 58-74; choosePStar topPrices trap-guard lines 87-101; rationByPriority lines 114-118; computeClearing sort-order + allocation emission lines 125-138)
    - .planning/phases/04-solver-service/04-RESEARCH.md (the verified TS port, lines 313-373 — use as the reference implementation; the parity note on emitting an Allocation for every eligible order, line 374)
    - .planning/phases/04-solver-service/04-PATTERNS.md (auction.ts section lines 23-57 — the load-bearing Daml excerpts to mirror)
    - solver/src/auction.test.ts (the five scenarios this must make pass)
  </read_first>
  <files>solver/src/auction.ts</files>
  <action>
    Implement `solver/src/auction.ts` as a PURE module (no I/O, no imports beyond TS) mirroring `Clearing.daml` function-for-function. Export the types `Side` (`'Buy' | 'Sell'`), `OrderView` (`{ desk: string; side: Side; quantity: number; limit: number }`), `Allocation` (`{ desk: string; side: Side; filledQty: number }`), and a `ClearingResult` type (`{ clearingPrice: number; allocations: Allocation[] }`). Export `demandAt`, `supplyAt`, `matchedAt`, `candidatePrices`, `choosePStar`, `rationByPriority`, and `computeClearing`.
    Reproduce the THREE load-bearing invariants EXACTLY: (1) `choosePStar` computes `maxMatched` with a `reduce((m, x) => Math.max(m, x), 0)` empty-list-safe seed, filters candidate prices to the `topPrices` max-matched subset FIRST, THEN sorts that subset by `(abs(demand−supply), price)` ascending and takes the first — dropping the topPrices filter clears §4 at 99 not 100. (2) `computeClearing` sorts eligible buys DESCENDING by limit (most-aggressive first) and eligible sells ASCENDING by limit, then greedily rations via `rationByPriority`. (3) Round the price with `Math.round(pStar * 100) / 100` (mirrors Daml `roundBankers 2`). Emit an `Allocation` for every eligible order (buys with limit≥p*, sells with limit≤p*), including partial fills like C — the consumer computes the residual as `quantity − filledQty`. Keep unit quantities as integer `number`s; do not introduce float drift.
    Do NOT "optimize" or restructure the algorithm — any divergence from `Clearing.daml` is rejected by the on-ledger `Round.Clear` re-verification and breaks the §4 canary.
  </action>
  <verify>
    <automated>cd solver && npx vitest run src/auction.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd solver && npx vitest run src/auction.test.ts` exits 0 — ALL five scenarios pass (GREEN).
    - The "section-4 fixture" test passes with `clearingPrice === 100`, fills A=10 / B=8 / C=2, C residual 3 — identical to Daml `test_clears_at_100`.
    - The "lower-price tie-break" regression passes (the topPrices filter is present; removing it would clear §4 at 99).
    - `npx tsc --noEmit` exits 0 (no type errors).
    - `auction.ts` exports `computeClearing`, `choosePStar`, `rationByPriority`, `Side`, `OrderView`, `Allocation`, `ClearingResult` and imports nothing (pure).
  </acceptance_criteria>
  <done>The §8 clearing algorithm runs in TypeScript bit-identically to Daml; the §4 fixture clears at 100.00 and all five vitest scenarios are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → solver/node_modules | third-party packages installed during scaffold |
| repo working tree → git | `.env` / token files must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | npm install (express/cors/zod/dotenv/vitest/tsx) | mitigate | All packages cleared by the RESEARCH Package Legitimacy Audit ([OK]; vitest [SUS] resolved as heuristic false positive — official `vitest-dev/vitest`). Pin exact spec-era versions; reject registry-latest majors. |
| T-04-02 | Information Disclosure | solver/.env committed with a real key | mitigate | Ship only `solver/.env.example` with empty `ANTHROPIC_API_KEY=`; `solver/.gitignore` ignores `.env`; repo `.gitignore` already covers `scripts/.operator-token`. Acceptance criteria assert no real secret value and no `.env` file. |
| T-04-03 | Tampering | the clearing math itself (a wrong port) | mitigate | The §4 canary + the 99-vs-100 tie-break regression test fail immediately on any divergence; the on-ledger `Round.Clear` re-verification (Phase 2) is the second backstop — the TS number is never trusted unverified. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH `## Package Legitimacy Audit` present and complete; no `[SLOP]` packages; the single `[SUS]` (vitest) is documented as a false positive with source repo `github.com/vitest-dev/vitest`. No blocking-human checkpoint required. |
</threat_model>

<verification>
- `cd solver && npx vitest run` → all five clearing scenarios green.
- `cd solver && npx tsc --noEmit` → clean.
- `cd solver && npx tsx src/ledger-smoke.ts` → exits 0, prints a real `:Umbra.Auction:Round` templateId.
- `git status` shows `solver/.env.example` tracked and no `solver/.env` tracked.
</verification>

<success_criteria>
The §4 fixture clears at exactly 100.00 in TypeScript (A=10 / B=8 / C=2, C residual 3) identical to Daml; the 99-vs-100 tie-break trap is guarded; the generated bindings import cleanly under Node ESM; the package is scaffolded with spec-pinned deps and secrets-by-example only.
</success_criteria>

<output>
Create `.planning/phases/04-solver-service/04-01-SUMMARY.md` when done.

## Artifacts this phase produces (Plan 04-01)
- `solver/src/auction.ts` exports: `computeClearing(orders: OrderView[]): ClearingResult`, `choosePStar(orders: OrderView[]): number`, `rationByPriority(ordered: OrderView[], remaining: number): [OrderView, number][]`, `demandAt`, `supplyAt`, `matchedAt`, `candidatePrices`; types `Side`, `OrderView`, `Allocation`, `ClearingResult`.
- `solver/src/auction.test.ts`: five vitest scenarios (`section-4 fixture`, `lower-price tie-break`, `same-limit ties`, `imbalance`, `no-cross`).
- `solver/src/ledger-smoke.ts`: ESM import-smoke for the generated bindings (proves Round/Order/Asset/Side import under Node).
- New files: `solver/package.json`, `solver/tsconfig.json`, `solver/vitest.config.ts`, `solver/.env.example`, `solver/.gitignore`.
- Env keys introduced (`.env.example`): `JSON_API_URL`, `SOLVER_PORT`, `ROUND_SECONDS`, `ANTHROPIC_API_KEY` (empty, Phase-5).
</output>
