---
phase: 04-solver-service
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - solver/src/api.ts
  - solver/src/api.test.ts
autonomous: true
requirements: [SOLV-03, SOLV-04, CLEAR-02]
must_haves:
  truths:
    - "The solver exposes exactly the spec §11 endpoints on :4000: POST /round, GET /round/:id, POST /round/:id/close, GET /round/:id/solve-preview, POST /round/:id/settle"
    - "GET /round/:id calls refreshStats(id) to recompute+write the live sealed-order count, so the returned sealedOrderCount reflects what the SOLVER maintains (not a stale Phase-3 seed)"
    - "solve-preview returns the deterministic §8 proposal + matchedVolume (= matchedAt(views, pStar)) + supply/demand curve points + rationale:null WITHOUT settling; settle exercises Round.Clear and 409s on double-settle"
    - "CORS is scoped to http://localhost:5173 only; POST /round bodies are zod-validated"
    - "No response body or error envelope ever contains the Operator token, the Anthropic key, or request auth headers"
  artifacts:
    - path: "solver/src/api.ts"
      provides: "Express app factory with the 5 §11 endpoints, cors(:5173), zod validation, secret-safe error envelope, refreshStats on GET, matchedVolume derived from matchedAt"
      exports: ["createApp"]
      min_lines: 80
    - path: "solver/src/api.test.ts"
      provides: "vitest: GET /round/:id calls refreshStats and reflects the recomputed count; solve-preview returns 100.00 + matchedVolume + curve + rationale:null; no response contains a token; double-settle → 409; CORS scoped"
      contains: "refreshStats"
  key_links:
    - from: "solver/src/api.ts"
      to: "solver/src/ledger.ts"
      via: "openRound/readSealedOrders/refreshStats/closeRound/settle"
      pattern: "from './ledger'"
    - from: "solver/src/api.ts (GET /round/:id)"
      to: "solver/src/ledger.ts (refreshStats)"
      via: "refreshStats(id) recompute+write before returning sealedOrderCount"
      pattern: "refreshStats\\("
    - from: "solver/src/api.ts"
      to: "solver/src/auction.ts"
      via: "computeClearing + matchedAt for solve-preview matchedVolume + curve points"
      pattern: "matchedAt"
    - from: "solver/src/api.ts"
      to: "cors"
      via: "cors({ origin: 'http://localhost:5173' })"
      pattern: "localhost:5173"
---

<objective>
Build `solver/src/api.ts` — the Express 4.19 application exposing the spec §11 HTTP surface on :4000 so the browser can drive the demo without ledger-admin rights, and lock the secret boundary (SOLV-04): CORS scoped to the Vite origin, zod-validated request bodies, a structured error envelope that never echoes secrets/headers, and forward-compatible response shapes (nullable `rationale`, `curve` points) so Phase 5/6 drop in without API changes. This plan ALSO closes the SOLV-01 "maintains sealedOrderCount" wire (the BLOCKER): `GET /round/:id` calls `refreshStats(id)` so the returned count is recomputed-and-written by the solver, and specifies `matchedVolume` derivation (= `matchedAt(views, pStar)`) so the field is actually populated, not just declared (the WARNING).

Purpose: SOLV-03 (the 5-endpoint API) and SOLV-04 (the secret boundary) are the consumer-facing contract of this phase. `solve-preview` exposes the deterministic §8 proposal + matched volume + the supply/demand curve the Phase-6 SVG chart needs; `settle` drives the verified `Round.Clear` and guards against double-settle. `GET /round/:id` is the live call site for `refreshStats`, so an opened round's `sealedOrderCount` advances from 0 as sealed orders arrive.
Output: `solver/src/api.ts`, `solver/src/api.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-solver-service/04-RESEARCH.md
@.planning/phases/04-solver-service/04-PATTERNS.md
@scripts/verify-privacy.mjs
@.planning/phases/04-solver-service/04-01-SUMMARY.md
@.planning/phases/04-solver-service/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Express app — the 5 §11 endpoints, CORS, zod, refreshStats wire, matchedVolume, secret-safe error envelope</name>
  <read_first>
    - .planning/phases/04-solver-service/04-RESEARCH.md (Endpoint contracts — the EXACT forward-compatible JSON shapes lines 389-411, INCLUDING matchedVolume; Pattern 2 step 2 "computeClearing → {clearingPrice, allocations}" + the matched-volume note line 222; Pattern 4 forward-compat shapes lines 232-234; the security domain V5/V7/V4 lines 469-487; the curve = {price, demand, supply}[] note line 411)
    - .planning/phases/04-solver-service/04-PATTERNS.md (api.ts section lines 130-134: Express 4.19 + cors(:5173) + zod on POST /round; error envelope { error: { code, message } }; never echo token/key/headers)
    - scripts/verify-privacy.mjs (the in-repo structured-message style precedent that emits NO secrets — mirror the no-secrets-in-output discipline)
    - solver/src/ledger.ts (the client functions to call: openRound, queryRound, readSealedOrders, refreshStats, closeRound, settle — refreshStats is the GET /round/:id count source)
    - solver/src/auction.ts (computeClearing for solve-preview; matchedAt for matchedVolume; demandAt/supplyAt to build the curve points)
  </read_first>
  <files>solver/src/api.ts</files>
  <action>
    Export a `createApp(deps)` factory (dependency-injected ledger functions so the test can stub them) returning a configured Express app. The `deps` object includes `openRound`, `queryRound`, `readSealedOrders`, `refreshStats`, `closeRound`, `settle` (from `ledger.ts`) plus the pure helpers `computeClearing`, `matchedAt`, `demandAt`, `supplyAt`, `candidatePrices` (from `auction.ts`). Mount `express.json()`, `cors({ origin: 'http://localhost:5173' })` (the Vite dev origin ONLY — not `*`). Wire the FIVE endpoints with the exact forward-compatible shapes from RESEARCH lines 389-411:
    - `POST /round` → zod-validate the body (e.g. optional `desks: string[]`, `windowSeconds?: number`), call `openRound(...)`, respond `{ roundId, status:'Open', openedAt, windowSeconds }`.
    - `GET /round/:id` → FIRST call `refreshStats(id)` (the BLOCKER fix — recompute the live sealed-order count and write it via updateStats so the count the solver MAINTAINS is what's returned, advancing off 0 as orders arrive); read the current round; respond `{ roundId, status, sealedOrderCount }` (the count from refreshStats, NOT a cached/seeded value) plus, only after clear/settle, `{ clearingPrice, matchedVolume, curve, rationale: null }`. If `refreshStats` fails (e.g. round not on ledger), surface a clean error envelope — never a secret.
    - `POST /round/:id/close` → call `closeRound(id)`, respond `{ roundId, status:'Closed' }`.
    - `GET /round/:id/solve-preview` → read the round's sealed orders → build `views: OrderView[]`, run `const { clearingPrice, allocations } = computeClearing(views)`, derive `const matchedVolume = matchedAt(views, clearingPrice)` (the traded volume at p* — RESEARCH Pattern 2 step 2 / Endpoint contract; this is what POPULATES the declared `matchedVolume` field — it is NOT on `ClearingResult`, it is computed here from the exported `matchedAt` helper), build `curve` as `candidatePrices(views).map(price => ({ price, demand: demandAt(views, price), supply: supplyAt(views, price) }))`, respond `{ roundId, clearingPrice, matchedVolume, allocations, curve, rationale: null }` — COMPUTE BUT DO NOT SETTLE. The `rationale:null` and `curve` are the additive seams for Phase 5/6.
    - `POST /round/:id/settle` → guard: if the round is already `Cleared`/`Settled`, respond `409` with the error envelope (double-settle protection); else call `settle(id)` and respond `{ roundId, status:'Settled', clearingPrice, matchedVolume, allocations, txConfirmations }` (derive `matchedVolume` the same way from the settle inputs/result).
    Add a single error-handling middleware producing the envelope `{ error: { code, message } }`. SECURITY (SOLV-04 / V7): the envelope and every response MUST NEVER include the Operator token, `ANTHROPIC_API_KEY`, `process.env`, or request `Authorization`/headers. zod validation errors map to a `400` with a sanitized message (field + reason only — never the raw header/env). Do not `console.log` request headers or env.
    Use `express@4.19` semantics (synchronous error forwarding via `next(err)` in async handlers wrapped in try/catch — express 4 does not auto-catch async rejections).
  </action>
  <verify>
    <automated>cd solver && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd solver && npx tsc --noEmit` exits 0 with `api.ts` present, exporting `createApp`.
    - The app registers exactly five routes: `POST /round`, `GET /round/:id`, `POST /round/:id/close`, `GET /round/:id/solve-preview`, `POST /round/:id/settle` (no extra public routes).
    - `GET /round/:id` calls `refreshStats(id)` and returns the recomputed `sealedOrderCount` from it (BLOCKER fix — the count is solver-maintained, not a stale seed; grep shows `refreshStats(` in the GET handler).
    - `solve-preview` derives `matchedVolume` via `matchedAt(views, clearingPrice)` (WARNING fix — the field is populated from the exported helper, not left undefined), calls `computeClearing`, builds `curve` from `candidatePrices`/`demandAt`/`supplyAt`, returns `rationale: null`, and does NOT call `settle`.
    - `cors` is configured with `origin: 'http://localhost:5173'` (literal string match), NOT `*`.
    - `POST /round` validates its body with zod; the error envelope shape is `{ error: { code, message } }`.
    - `POST /round/:id/settle` returns 409 when the round is already Cleared/Settled.
    - Grep `api.ts`: no response/envelope construction references the token variable, `ANTHROPIC_API_KEY`, `process.env` (beyond reading config at startup), or request headers.
  </acceptance_criteria>
  <done>The Express app exposes the 5 §11 endpoints with CORS scoped to :5173, zod-validated POST /round, a secret-safe error envelope, a live refreshStats-backed sealedOrderCount on GET, a matchedAt-derived matchedVolume, and forward-compatible solve-preview/settle shapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API tests — refreshStats wired, solve-preview shape + matchedVolume, secret-never-in-response, double-settle 409, CORS scope</name>
  <read_first>
    - solver/src/api.ts (the createApp factory + the route shapes under test)
    - .planning/phases/04-solver-service/04-RESEARCH.md (endpoint contracts lines 389-411 incl. matchedVolume; the validation map SOLV-03/SOLV-04 rows lines 453; "assert no token in GET /round/:id body" from 04-VALIDATION line 45)
    - .planning/phases/04-solver-service/04-VALIDATION.md (the SOLV-04 secret-never-in-response row lines 45)
    - solver/src/auction.ts (the §4 fixture values to stub readSealedOrders with, so solve-preview asserts clearingPrice 100.00 and matchedVolume 10)
  </read_first>
  <files>solver/src/api.test.ts</files>
  <behavior>
    - Test "GET /round/:id refreshes the sealed-order count": with a stubbed `refreshStats` that returns 3 (recomputed from 3 stubbed sealed orders), `GET /round/:id` responds 200 with `sealedOrderCount === 3` AND the stubbed `refreshStats` was CALLED with the round id — proving the count is solver-maintained (BLOCKER fix), not read from a static seed. Assert `refreshStats` was invoked (e.g. a spy/mock call count ≥ 1).
    - Test "solve-preview returns deterministic §4 clearing + matchedVolume": with stubbed ledger deps returning the §4 sealed orders, `GET /round/:id/solve-preview` responds 200 with `clearingPrice === 100`, `matchedVolume === 10` (traded volume at p* — WARNING fix), a non-empty `allocations` array, a non-empty `curve` of `{price, demand, supply}` points, and `rationale === null`. (CLEAR-02 surfaced through the API.)
    - Test "no secret in any response body": neither `GET /round/:id` nor `solve-preview` response JSON (stringified) contains the stubbed operator token sentinel string or any `ANTHROPIC_API_KEY` value. (SOLV-04.)
    - Test "double-settle → 409": calling `POST /round/:id/settle` when the stubbed round status is already `Settled` responds 409 with the `{ error: { code, message } }` envelope. (T-04-06.)
    - Test "CORS scoped to :5173": a request with `Origin: http://localhost:5173` gets the matching `Access-Control-Allow-Origin`; an `Origin: http://evil.example` does NOT receive `Access-Control-Allow-Origin: *`.
  </behavior>
  <action>
    Author `solver/src/api.test.ts` using vitest. Build the app via `createApp` with STUBBED ledger deps (inject fakes for `openRound`/`queryRound`/`readSealedOrders`/`refreshStats`/`closeRound`/`settle` — do NOT touch a live ledger). Use a `vi.fn()` spy for `refreshStats` returning 3 so the GET test can assert both the returned `sealedOrderCount === 3` and that the spy was called (the BLOCKER assertion). Drive HTTP using Node's built-in `fetch` against an ephemeral `app.listen(0)` server (read the assigned port from the server address) — no new test-only dependency. For the §4 case, stub `readSealedOrders` to return the §4 fixture `OrderView`s so `computeClearing` yields 100.00 and `matchedAt` yields 10. Pass a sentinel operator-token string into the stub closure and assert it never appears in any serialized response. Use `vitest run` only (no watch mode).
  </action>
  <verify>
    <automated>cd solver && npx vitest run src/api.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd solver && npx vitest run src/api.test.ts` exits 0 — all five API tests pass.
    - The GET test asserts `sealedOrderCount === 3` AND that the stubbed `refreshStats` spy was called (the count is solver-maintained, not a static seed).
    - The solve-preview test asserts `clearingPrice === 100`, `matchedVolume === 10`, a non-empty `curve`, and `rationale === null`.
    - The secret test asserts the sentinel token string is absent from `JSON.stringify(responseBody)` for both `GET /round/:id` and `solve-preview`.
    - The double-settle test asserts HTTP 409 with the `{ error: { code, message } }` envelope.
    - The CORS test asserts the allow-origin header is the literal `http://localhost:5173` for that origin and is not `*` for a foreign origin.
    - No live-ledger dependency in the test (ledger deps are stubbed); `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The API surface is proven by automated tests: refreshStats-backed live count, deterministic 100.00 + matchedVolume 10 via solve-preview, secrets never serialized, 409 on double-settle, CORS scoped to the Vite origin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (:5173, desk tokens) → solver API (:4000) | untrusted request bodies / origins cross here |
| solver API responses/logs → browser | secrets must never cross outward |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | Operator token / Anthropic key in a response or log | mitigate | Secret-safe error envelope `{ error: { code, message } }` that never echoes the token/key/`process.env`/request headers; an automated test asserts no sentinel token in any serialized response (SOLV-04 / V7). |
| T-04-08 | Tampering | unvalidated `POST /round` body | mitigate | zod schema on the request body; reject malformed input with a sanitized 400; the on-ledger `Round.Clear` re-verification is the deep backstop (V5). |
| T-04-09 | Spoofing / EoP | a foreign origin driving the demo | mitigate | `cors({ origin: 'http://localhost:5173' })` — the Vite dev origin only, never `*`; asserted by the CORS test (V4). Dev-only posture (no production auth in P4 scope). |
| T-04-06 | Tampering / EoP | double-settle replay via `POST /settle` | mitigate | API returns 409 if the round is already Cleared/Settled, layered on top of `Clear`'s on-ledger lifecycle guard; asserted by the double-settle test. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new installs in this plan (express/cors/zod installed + audited in Plan 04-01). Tests use Node built-in `fetch` (no new test dependency). |
</threat_model>

<verification>
- `cd solver && npx vitest run src/api.test.ts` → all five API tests green (incl. the refreshStats-count and matchedVolume assertions).
- `cd solver && npx vitest run` → full suite (auction + ledger + api) green.
- `cd solver && npx tsc --noEmit` → clean.
- Grep `api.ts`: CORS origin is `http://localhost:5173`; `refreshStats(` is called in the GET /round/:id handler; `matchedAt(` derives matchedVolume; no response references the token/key/headers; five routes registered.
</verification>

<success_criteria>
The solver exposes the five spec §11 endpoints on :4000 with CORS scoped to the Vite origin, zod-validated bodies, a structured secret-safe error envelope, a live `refreshStats`-backed `sealedOrderCount` on GET (solver-maintained, not a stale seed), deterministic solve-preview returning 100.00 + matchedVolume + curve + rationale:null, and a 409 double-settle guard — all proven by automated tests with no live-ledger dependency.
</success_criteria>

<output>
Create `.planning/phases/04-solver-service/04-03-SUMMARY.md` when done.

## Artifacts this phase produces (Plan 04-03)
- `solver/src/api.ts` exports: `createApp(deps)` → configured Express app. Route handlers: `POST /round`, `GET /round/:id` (calls `refreshStats` → live sealedOrderCount), `POST /round/:id/close`, `GET /round/:id/solve-preview` (derives `matchedVolume = matchedAt(views, pStar)`), `POST /round/:id/settle`.
- `solver/src/api.test.ts`: five vitest tests (GET refreshStats-count, solve-preview §4 shape + matchedVolume 10, no-secret-in-response, double-settle 409, CORS scope).
- Response contract (forward-compatible for P5/P6): `solve-preview` → `{ roundId, clearingPrice, matchedVolume, allocations, curve:{price,demand,supply}[], rationale:null }`; `GET /round/:id` returns `refreshStats`-recomputed `sealedOrderCount` and adds `clearingPrice/matchedVolume/curve/rationale` after clear.
- Error envelope: `{ error: { code, message } }` — secrets never serialized.
</output>
