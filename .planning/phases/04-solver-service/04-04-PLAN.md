---
phase: 04-solver-service
plan: 04
type: execute
wave: 4
depends_on: ["04-02", "04-03"]
files_modified:
  - solver/src/index.ts
  - solver/src/clock.ts
  - solver/src/clock.test.ts
autonomous: false
requirements: [SOLV-01, SOLV-02]
must_haves:
  truths:
    - "An in-memory Map<roundId, RoundState> tracks status/openedAt/deadline/timer; the ledger Round.status remains authoritative"
    - "Opening a round starts a ROUND_SECONDS (default 60) timer that auto-advances the round to Closed at expiry; POST /round/:id/close force-closes early and cancels the timer"
    - "On boot the solver loads .env via dotenv, rehydrates state by querying live Rounds, wires the clock into the API deps, and listens on SOLVER_PORT (default 4000)"
    - "The live open→close→solve-preview(100.00)→settle(Settled) flow runs against a running daml start sandbox"
  artifacts:
    - path: "solver/src/clock.ts"
      provides: "In-memory round clock: openRoundClock, forceClose, getState, cancelTimer; ROUND_SECONDS window"
      exports: ["createClock", "RoundState"]
      min_lines: 40
    - path: "solver/src/index.ts"
      provides: "Boot: dotenv config, rehydrate-from-ledger, wire clock+ledger into createApp, listen on SOLVER_PORT"
      min_lines: 25
    - path: "solver/src/clock.test.ts"
      provides: "vitest: timer auto-closes at the window; force-close cancels the timer; state transitions are correct"
      contains: "ROUND_SECONDS"
  key_links:
    - from: "solver/src/index.ts"
      to: "solver/src/api.ts"
      via: "createApp(deps).listen(SOLVER_PORT)"
      pattern: "createApp\\("
    - from: "solver/src/index.ts"
      to: "solver/src/ledger.ts"
      via: "openRound/queryRound for rehydrate + clock callbacks"
      pattern: "from './ledger'"
    - from: "solver/src/clock.ts"
      to: "solver/src/ledger.ts"
      via: "closeRound on timer expiry"
      pattern: "closeRound"
---

<objective>
Build `solver/src/clock.ts` (the in-memory round-lifecycle clock) and `solver/src/index.ts` (dotenv boot + rehydrate + wire + listen), completing the service: a `Map<roundId, RoundState>` enforces the 60s `ROUND_SECONDS` window with a per-round auto-close timer and a force-close affordance, and `index.ts` boots the Express app from Plan 04-03 with the ledger client from Plan 04-02 injected, listening on `SOLVER_PORT`.

Purpose: SOLV-01 (open a Round, maintain the count, enforce the 60s window, close) and SOLV-02 (on close read orders → compute/verify → exercise Clear) are completed by wiring the clock to the ledger client and the API. The map is clock/cache state only — the ledger `Round.status` stays authoritative, so the solver rehydrates from a live `query(Round)` on boot and can drive the Phase-3 seeded round for the canonical demo.
Output: `solver/src/clock.ts`, `solver/src/index.ts`, `solver/src/clock.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-solver-service/04-RESEARCH.md
@.planning/phases/04-solver-service/04-PATTERNS.md
@.planning/phases/04-solver-service/04-02-SUMMARY.md
@.planning/phases/04-solver-service/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: In-memory round clock (clock.ts) + timer tests (clock.test.ts)</name>
  <read_first>
    - .planning/phases/04-solver-service/04-RESEARCH.md (Pattern 3 in-memory round clock lines 227-230; the "map is cache/clock only, ledger Round.status authoritative; rehydrate on boot" note; Runtime State Inventory lines 299-309)
    - .planning/phases/04-solver-service/04-PATTERNS.md (index.ts/clock section lines 138-140: Map<roundId, RoundState> = { status, openedAt, deadline, timer, cachedProposal? }; setTimeout(ROUND_SECONDS*1000) auto-close)
    - solver/src/ledger.ts (openRound/closeRound — the clock calls closeRound on timer expiry; queryRound for rehydrate)
  </read_first>
  <files>solver/src/clock.ts, solver/src/clock.test.ts</files>
  <action>
    Create `solver/src/clock.ts` exporting a `createClock(deps)` factory (inject `closeRound` so the test can stub it) and a `RoundState` type `{ roundId: string; status: 'Open'|'Closed'|'Cleared'|'Settled'; openedAt: number; deadline: number; timer?: NodeJS.Timeout; cachedProposal?: unknown }`. The clock holds a private `Map<string, RoundState>`. Methods: `openRoundClock(roundId, windowSeconds)` → record state `status:'Open'`, `openedAt:Date.now()`, `deadline:openedAt + windowSeconds*1000`, and start `setTimeout(() => forceClose(roundId), windowSeconds*1000)` storing the handle as `timer`. `forceClose(roundId)` → `clearTimeout` the timer, call `deps.closeRound(roundId)`, set `status:'Closed'`; idempotent (no-op if already Closed/Cleared/Settled). `getState(roundId)` → the RoundState or undefined. `setStatus(roundId, status)` → update after settle/clear. `rehydrate(rounds)` → seed the map from an array of live `{ roundId, status, openedAt, windowSeconds }` (so boot can recover the seeded round). The Map is cache/clock state ONLY — never the authority; the ledger `Round.status` wins.
    Create `solver/src/clock.test.ts` (vitest) using fake timers (`vi.useFakeTimers()`): test that opening a round with a small window and advancing time past the window calls the stubbed `closeRound` exactly once and transitions state to `Closed`; test that `forceClose` BEFORE the window cancels the timer (advancing time afterward does NOT call `closeRound` a second time); test that `forceClose` is idempotent. Use a stubbed `closeRound` (no live ledger). `vitest run` only.
  </action>
  <verify>
    <automated>cd solver && npx vitest run src/clock.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd solver && npx vitest run src/clock.test.ts` exits 0 — timer-auto-close, force-close-cancels-timer, and idempotency tests pass (using `vi.useFakeTimers`).
    - `clock.ts` exports `createClock` and the `RoundState` type; the window is derived from a `windowSeconds` argument (driven by `ROUND_SECONDS` at the call site).
    - The auto-close path calls the injected `closeRound` exactly once; `forceClose` clears the timer so the timer callback cannot also fire.
    - `npx tsc --noEmit` clean.
    - The clock holds no Operator token and no secret (it deals only in roundId/status/timestamps).
  </acceptance_criteria>
  <done>The in-memory clock enforces the ROUND_SECONDS window with auto-close + force-close, proven by fake-timer tests; the ledger status stays authoritative.</done>
</task>

<task type="auto">
  <name>Task 2: Boot index.ts — dotenv, rehydrate-from-ledger, wire clock+ledger into the API, listen</name>
  <read_first>
    - solver/src/api.ts (the createApp(deps) factory — what deps it expects)
    - solver/src/ledger.ts (openRound/queryRound/readSealedOrders/updateStats/closeRound/settle/operatorParty — the functions to inject)
    - solver/src/clock.ts (createClock + rehydrate)
    - .planning/phases/04-solver-service/04-RESEARCH.md (Pattern 3 rehydrate-on-boot lines 227-230; the .env keys JSON_API_URL/SOLVER_PORT/ROUND_SECONDS lines 184; secret hygiene lines 426-430)
    - solver/.env.example (the env keys to load via dotenv)
  </read_first>
  <files>solver/src/index.ts</files>
  <action>
    Create `solver/src/index.ts`: import and call `dotenv.config()` FIRST (so `JSON_API_URL`/`SOLVER_PORT`/`ROUND_SECONDS` are available before the ledger client constructs its `Ledger`). Read `SOLVER_PORT` (default 4000) and `ROUND_SECONDS` (default 60). Construct the clock via `createClock({ closeRound })` from `./ledger`. On boot, REHYDRATE: `query(Round)` (via a `queryAllRounds`/`queryRound` helper from `ledger.ts`) and seed the clock map so the solver recognizes the Phase-3 seeded round (`R1`) and any live rounds — the ledger status is authoritative. Assemble the API deps object (the ledger functions + clock methods + `ROUND_SECONDS` so `POST /round` starts the timer and `POST /:id/close` force-closes), call `createApp(deps)`, and `app.listen(SOLVER_PORT)` logging only the bound port and `operatorParty` (NEVER the token, NEVER the full env). Wire it so `POST /round` calls `openRound` then `clock.openRoundClock(roundId, ROUND_SECONDS)`, and `POST /round/:id/close` calls `clock.forceClose(id)` (which calls `closeRound`).
    Keep boot log lines secret-free: log `Solver listening on :<port> as <operatorParty>` — do not log the token, `ANTHROPIC_API_KEY`, or `process.env`.
  </action>
  <verify>
    <automated>cd solver && npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `cd solver && npx tsc --noEmit` exits 0 with `index.ts` present.
    - `cd solver && npx vitest run` exits 0 — the FULL suite (auction + api + clock) is green.
    - `index.ts` calls `dotenv.config()` before constructing the ledger client; reads `SOLVER_PORT` (default 4000) and `ROUND_SECONDS` (default 60).
    - `index.ts` rehydrates clock state from a live `query(Round)` on boot and wires `openRound`+`openRoundClock` into `POST /round` and `forceClose` into `POST /round/:id/close`.
    - Boot logging contains no token, no `ANTHROPIC_API_KEY`, no `process.env` dump (grep `index.ts`).
  </acceptance_criteria>
  <done>index.ts boots the full service: dotenv config, rehydrate-from-ledger, clock+ledger wired into the Express app, listening on SOLVER_PORT — with secret-free logging.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live end-to-end round lifecycle against daml start (SOLV-01/02/03 live gate)</name>
  <files>(no files modified — live verification only)</files>
  <what-built>
    The full solver service: `auction.ts` (§8), `ledger.ts` (Operator client + Option-B Clear), `api.ts` (5 endpoints), `clock.ts` (60s window), `index.ts` (boot). This checkpoint verifies the live wire-up against a running sandbox — the one path that cannot be automated in unit tests because it needs `daml start` (:7575) and exercises the real `Round.Clear` DvP. Everything Claude can automate (the §8 math, the API shapes, the secret hygiene, the clock timers) is already proven green by vitest in Plans 04-01/03/04.
  </what-built>
  <action>
    Boot `daml start` (:7575, which seeds round `R1` + 3 §4 orders via `seedOpenRound`), mint tokens if needed (`node scripts/mint-tokens.mjs`), run `cd solver && npm run dev`, then drive the seeded round `R1` through the five endpoints (GET status → close → solve-preview → settle → re-settle) per the how-to-verify steps and confirm the secret boundary. This is a MANUAL live gate because it needs a running sandbox and exercises the real on-ledger `Round.Clear` DvP; no automation is possible without :7575 up. All other behavior (the §8 math, API shapes, secret hygiene, clock timers) is already covered by vitest in Plans 04-01/03/04.
  </action>
  <how-to-verify>
    1. In one terminal: `cd daml && daml start` (boots the sandbox + JSON API on :7575 and runs `seedOpenRound`, which seeds the Open round `R1` + the 3 §4 orders). Wait for "Press 'r' ... " / the JSON API ready line.
    2. Mint tokens if needed: `node scripts/mint-tokens.mjs` (writes `scripts/.operator-token`).
    3. In a second terminal: `cd solver && cp .env.example .env` (leave `ANTHROPIC_API_KEY` empty), then `npm run dev`. Expect a log line like `Solver listening on :4000 as operator::…` with NO token printed.
    4. Drive the canonical demo against the seeded round `R1` (use curl or the browser):
       - `curl http://localhost:4000/round/R1` → shows `status` and `sealedOrderCount: 3`.
       - `curl -X POST http://localhost:4000/round/R1/close` → `{ status: "Closed" }`.
       - `curl http://localhost:4000/round/R1/solve-preview` → `clearingPrice: 100` (the §4 money number), a non-empty `curve`, and `rationale: null`.
       - `curl -X POST http://localhost:4000/round/R1/settle` → `{ status: "Settled", clearingPrice: 100, … }`.
       - `curl -X POST http://localhost:4000/round/R1/settle` AGAIN → HTTP 409 (double-settle guard).
       - `curl http://localhost:4000/round/R1` → now shows `clearingPrice: 100` + curve + `rationale: null`.
    5. Confirm the secret boundary: inspect every response body above — none contains the Operator token or any `ANTHROPIC_API_KEY` value; the dev-tools Network tab (if using the browser) shows no Authorization header echoed back.
    6. (Optional) `curl -X POST http://localhost:4000/round` with `{}` → opens a FRESH round and returns a new `roundId`, status `Open`.
  </how-to-verify>
  <verify>
    <automated>MISSING — live sandbox gate; verified manually per how-to-verify (the §8 math, API shapes, secret hygiene, and clock timers are already covered by vitest in Plans 04-01/03/04)</automated>
    <human-check>solve-preview returns clearingPrice 100.00 + non-empty curve + rationale:null; settle reaches Settled; a second settle returns 409; no response body or boot log contains the Operator token or ANTHROPIC_API_KEY</human-check>
  </verify>
  <acceptance_criteria>
    - `daml start` is running and the solver logs `Solver listening on :4000 as operator::…` with NO token in the log line.
    - `GET /round/R1` shows `sealedOrderCount: 3`; `POST /round/R1/close` → Closed.
    - `GET /round/R1/solve-preview` → `clearingPrice: 100`, a non-empty `curve`, `rationale: null`, and does NOT settle.
    - `POST /round/R1/settle` → `status: "Settled"`, `clearingPrice: 100`; a second `POST …/settle` → HTTP 409.
    - No response body inspected contains the Operator token or any `ANTHROPIC_API_KEY` value.
  </acceptance_criteria>
  <done>The live §4 round drives end-to-end through solve-preview (100.00) to atomic Settled, the double-settle 409 guard holds, and no secret leaks into any response or log.</done>
  <resume-signal>Type "approved" once solve-preview shows 100.00 and settle reaches Settled with the 409 double-settle guard and no secret in any response — or describe what failed (e.g. a stale Round cid, an Int/Decimal decode error, a missing Asset cid) so it can be fixed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-memory clock map → ledger Round.status | the map is cache only; the ledger is authoritative |
| boot logging / env → stdout | secrets must never be logged |
| solver process (Operator) → JSON API :7575 | the live settlement path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | token / Anthropic key in boot logs | mitigate | Boot logs only `:<port>` + `operatorParty`; never the token, `ANTHROPIC_API_KEY`, or `process.env`; acceptance criteria grep `index.ts`. `.env` is gitignored (Plan 04-01). |
| T-04-10 | Tampering | clock map treated as the source of truth (drift from ledger) | mitigate | The map is documented and used as cache/clock ONLY; boot rehydrates from a live `query(Round)`; the ledger `Round.status` is authoritative and `Clear`'s on-ledger guard is the real lifecycle gate. |
| T-04-06 | Tampering / EoP | double-settle | mitigate | The live checkpoint explicitly re-runs settle and confirms 409; layered on the on-ledger `Clear` guard. |
| T-04-11 | Denial of Service | a leaked/never-cleared timer | mitigate | `forceClose` clears the timer; `openRoundClock` stores one handle per round; idempotency test guards double-fire. |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new installs in this plan (dotenv installed + audited in Plan 04-01). |
</threat_model>

<verification>
- `cd solver && npx vitest run` → full suite (auction + api + clock) green.
- `cd solver && npx tsc --noEmit` → clean.
- LIVE (Task 3 human gate): `daml start` + `npm run dev`, then drive R1 open→close→solve-preview(100.00)→settle(Settled)→settle(409); confirm no secret in any response.
</verification>

<success_criteria>
The solver service is complete and live-verified: the in-memory clock enforces the ROUND_SECONDS window with auto-close + force-close, boot rehydrates from the ledger and wires the clock+ledger into the Express app on SOLVER_PORT, and the canonical §4 round drives end-to-end through solve-preview (100.00) to atomic Settled — with the Operator token and Anthropic key never exposed to the browser or logs.
</success_criteria>

<output>
Create `.planning/phases/04-solver-service/04-04-SUMMARY.md` when done.

## Artifacts this phase produces (Plan 04-04)
- `solver/src/clock.ts` exports: `createClock(deps)` → `{ openRoundClock, forceClose, getState, setStatus, rehydrate }`; type `RoundState`.
- `solver/src/index.ts`: boot entrypoint — `dotenv.config()`, rehydrate from `query(Round)`, `createApp(deps).listen(SOLVER_PORT)`; secret-free logging (`:<port>` + operatorParty).
- `solver/src/clock.test.ts`: fake-timer tests (auto-close at window, force-close cancels timer, idempotency).
- Env consumed: `SOLVER_PORT` (default 4000), `ROUND_SECONDS` (default 60), `JSON_API_URL` (via ledger.ts).
</output>
