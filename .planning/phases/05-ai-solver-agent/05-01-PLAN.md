---
phase: 05-ai-solver-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - solver/package.json
  - solver/.env.example
  - solver/src/agent.ts
  - solver/src/agent.test.ts
autonomous: true
requirements: [AGENT-01, AGENT-02]
user_setup:
  - service: anthropic
    why: "Live Claude rationale on the agreement path (optional — the demo runs keyless; the §4 fixture clears at 100.00 with or without a key)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console → Settings → API Keys (server-side only; read ONLY by solver/agent.ts, never the frontend, never committed)"
must_haves:
  truths:
    - "On round close the agent ingests the sealed batch and PROPOSES a clearing (price + allocation + rationale) by calling Claude (claude-haiku-4-5, temperature 0) via client.messages.parse with output_config.format = zodOutputFormat(proposalSchema), reading message.parsed_output (AGENT-01)"
    - "The deterministic computeClearing(views) numbers are ALWAYS authoritative — the model's clearingPrice/allocations are never used unverified; the model only ever sets rationale + the agent block, and only on an exact match (AGENT-02)"
    - "Agreement (2dp price equal AND allocation-set equal by desk|side→filledQty) → verified:true, source:'claude', rationale from the model; the §4 fixture is the agreement fixture (100.00, A=10/B=8/C=2)"
    - "Disagreement / malformed-or-null parsed_output / SDK error|timeout / NO ANTHROPIC_API_KEY → deterministic fallback (verified:false, source:'deterministic-fallback', generated neutral rationale); proposeClearing NEVER throws"
    - "ANTHROPIC_API_KEY is read exactly once inside agent.ts, module-private, never exported/returned/logged; the §4 clears at 100.00 whether or not Claude is reachable (graceful keyless degradation is a designed, tested path)"
  artifacts:
    - path: "solver/src/agent.ts"
      provides: "createAgent({client?, computeClearing, matchedAt}) DI factory → { proposeClearing(views): Promise<AgentResult> }; proposalSchema (zod); SYSTEM_PROMPT (§8 rules verbatim); priceEqual/allocationsEqual predicates; neutralRationale generator; module-private ANTHROPIC_API_KEY read"
      exports: ["createAgent", "AgentResult", "proposalSchema", "SYSTEM_PROMPT"]
      min_lines: 90
    - path: "solver/src/agent.test.ts"
      provides: "vitest with a DI-injected fake Anthropic client (no network, no key): agreement → verified:true + rationale used; disagreement → gate rejects, §4 still 100.00, source fallback; unavailable (parse throws / no client) → deterministic + neutral rationale, no crash; malformed parsed_output → safeParse fails → fallback; sentinel-key never in any response/log"
      contains: "deterministic-fallback"
    - path: "solver/package.json"
      provides: "@anthropic-ai/sdk@0.106.0 pinned in dependencies (the ONE new dep this phase; zod/vitest/dotenv already present)"
      contains: "@anthropic-ai/sdk"
  key_links:
    - from: "solver/src/agent.ts"
      to: "@anthropic-ai/sdk"
      via: "default import Anthropic + named subpath import zodOutputFormat from '@anthropic-ai/sdk/helpers/zod'; client.messages.parse(...)"
      pattern: "messages\\.parse"
    - from: "solver/src/agent.ts"
      to: "solver/src/auction.ts"
      via: "computeClearing/matchedAt injected via createAgent — the deterministic oracle the proposal is verified against (FROZEN, never modified)"
      pattern: "computeClearing"
    - from: "solver/src/agent.ts"
      to: "process.env.ANTHROPIC_API_KEY"
      via: "module-private read once; if absent/empty do NOT construct a client (keyless degradation)"
      pattern: "ANTHROPIC_API_KEY"
    - from: "solver/src/agent.test.ts"
      to: "solver/src/agent.ts"
      via: "createAgent with a fake client returning a parsed_output; the three mocked-SDK paths + sentinel-key sweep"
      pattern: "createAgent\\("
---

<objective>
Build `solver/src/agent.ts` — the AI Solver Agent: a thin, DI-friendly adapter that, when a round closes, asks Claude to PROPOSE a uniform clearing (price + allocation + a 2–3 sentence rationale), then VERIFIES that proposal against the frozen deterministic §8 core (`auction.ts`) and never trusts it. The deterministic `computeClearing` numbers ALWAYS flow to the response and to settlement; the model only ever colors `rationale` + an additive `agent:{verified,source}` block, and only on an exact match (2dp price + allocation-set equality by desk|side→filledQty). This plan also installs the ONE new pinned dependency (`@anthropic-ai/sdk@0.106.0`) and writes the mocked-SDK tests (no live network, no real key in CI) covering agreement / disagreement-rejected / unavailable / malformed.

Purpose: AGENT-01 (Claude proposes a structured-JSON clearing at temperature 0) and AGENT-02 (the verify-don't-trust equality gate — the correctness guarantee) are the thesis of this phase. The agent is STRICTLY additive and OFF the critical settlement path: `settle` and the on-ledger `Round.Clear` always submit the deterministic allocation, so a wrong / malicious / unavailable model can never produce an unfair clear. The §4 money shot MUST clear at exactly 100.00 whether or not Claude is reachable — graceful keyless degradation is a first-class designed, tested behavior.
Output: `solver/src/agent.ts`, `solver/src/agent.test.ts`, `solver/package.json` (+`@anthropic-ai/sdk@0.106.0`), `solver/.env.example` (comment-only update).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-solver-agent/05-RESEARCH.md
@.planning/phases/05-ai-solver-agent/05-PATTERNS.md
@.planning/phases/05-ai-solver-agent/05-CONTEXT.md
@solver/src/auction.ts
@solver/src/ledger.ts
@solver/src/api.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @anthropic-ai/sdk@0.106.0 (the one new pinned dep) + ESM import smoke</name>
  <read_first>
    - .planning/phases/05-ai-solver-agent/05-RESEARCH.md (Package Legitimacy Audit lines 117-128 — @anthropic-ai/sdk is the official, spec-mandated, publisher-namespaced SDK, version 0.106.0 confirmed on the registry, clean tarball, no postinstall, helper files present; Installation line 108-112; Pitfall 1 ESM helper-path resolution lines 242-246; Environment Availability lines 567-577)
    - .planning/phases/05-ai-solver-agent/05-PATTERNS.md (package.json section lines 198-207 — add "@anthropic-ai/sdk": "0.106.0" to the dependencies block alphabetically; "type":"module" already set; ./helpers/* subpath exports resolve under the solver moduleResolution; .env.example section lines 211-216 — comment-only update)
    - solver/package.json (the dependencies block to extend; zod@3.23.8 / vitest@2.1.9 / dotenv@16.6.1 already present — no other install)
    - solver/.env.example (ANTHROPIC_API_KEY= already at L15 with the server-side-only comment)
  </read_first>
  <files>solver/package.json, solver/.env.example</files>
  <action>
    Run `npm install @anthropic-ai/sdk@0.106.0` in `solver/` (the package legitimacy is established in RESEARCH: it is the spec §6/§9-mandated official `@anthropic-ai/*` publisher-namespaced SDK, exact version 0.106.0 confirmed on the npm registry with a clean tarball and no `postinstall` — NOT a name discovered via search). This adds the single new pinned dependency for the entire phase; do NOT add any other package and do NOT use `npx --yes`. Verify the `dependencies` block in `solver/package.json` now lists `"@anthropic-ai/sdk": "0.106.0"` (the exact pin, alphabetically ordered alongside the existing `@daml.js`/`@daml/ledger`/`@daml/types`/`cors`/`dotenv`/`express`/`zod` entries). Confirm the ESM helper subpath resolves under the solver's `"type":"module"` + `moduleResolution:"Bundler"` tsconfig by checking that `node_modules/@anthropic-ai/sdk/helpers/zod` exists (the `exports` map maps `./helpers/*` → `helpers/*.mjs`/`helpers/*.js`; this is the path `agent.ts` imports `zodOutputFormat` from). Update the `solver/.env.example` comment for `ANTHROPIC_API_KEY` from "PRESENT-BUT-UNUSED in Phase 4" to note it is now consumed by `agent.ts` (server-side only, never the frontend, never committed — spec §15). Do not change the empty value or add any new env var (the key is already plumbed via `index.ts` `dotenv.config()`).
  </action>
  <verify>
    <automated>cd solver && node -e "require('fs').accessSync('node_modules/@anthropic-ai/sdk/helpers/zod'); const p=require('./package.json'); if(p.dependencies['@anthropic-ai/sdk']!=='0.106.0'){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `solver/package.json` `dependencies` contains exactly `"@anthropic-ai/sdk": "0.106.0"` (the exact pin; no caret/tilde range).
    - `node_modules/@anthropic-ai/sdk/helpers/zod` resolves (the `zodOutputFormat` subpath `agent.ts` will import).
    - No other new package was added to `dependencies` or `devDependencies`; `zod`/`vitest`/`dotenv` are unchanged.
    - `solver/.env.example` `ANTHROPIC_API_KEY=` remains EMPTY (no real key committed); only the comment is updated to note `agent.ts` consumes it.
    - The verify command exits 0 (helper subpath present AND the exact 0.106.0 pin recorded).
  </acceptance_criteria>
  <done>`@anthropic-ai/sdk@0.106.0` is installed and pinned in `solver/package.json`, the ESM helper subpath resolves, and `.env.example` documents the now-consumed (still-empty) `ANTHROPIC_API_KEY`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: agent.test.ts — the three mocked-SDK paths (agreement / disagreement / unavailable) + malformed + sentinel-key sweep (Wave 0 failing tests)</name>
  <read_first>
    - .planning/phases/05-ai-solver-agent/05-RESEARCH.md (Code Example 2 the verify-don't-trust gate lines 354-418 — the AgentResult shape, priceEqual 2dp, allocationsEqual by desk|side; Code Example 4 mocking the SDK in vitest lines 441-493 — fakeClientReturning + the three it() blocks + the sentinel assertion; Test Map lines 591-601; Wave 0 Gaps lines 608-612)
    - .planning/phases/05-ai-solver-agent/05-PATTERNS.md (agent.test.ts section lines 137-181 — reuse api.test.ts SECTION4_VIEWS + DI-fake client; the ledger.test.ts sentinel-secret sweep L179-183; the three required paths)
    - solver/src/api.test.ts (SECTION4_VIEWS at L36-40 — reuse the EXACT §4 fixture; the SENTINEL_TOKEN + JSON.stringify(...).not.toContain(...) discipline at L47-49/L174-175 to mirror for the API key)
    - solver/src/ledger.test.ts (the log+return sentinel sweep at L170-183 — spy console.log AND console.error, assert the sentinel appears in NO captured log call and NO returned value)
    - solver/src/auction.ts (computeClearing/matchedAt — the REAL §8 helpers injected into createAgent so the §4 fixture clears at 100.00/matched 10; Allocation/OrderView/Side types and the buys-then-sells emission order the allocationsEqual set-compare must be order-insensitive about)
  </read_first>
  <files>solver/src/agent.test.ts</files>
  <behavior>
    - Define `SECTION4_VIEWS` identical to api.test.ts L36-40 (BankA Buy 10@101, BankB Sell 8@99, BankC Sell 5@100) and a `SENTINEL_KEY` string (e.g. 'SENTINEL-ANTHROPIC-KEY-do-not-leak-…') standing in for ANTHROPIC_API_KEY. Build the agent via `createAgent({ client, computeClearing, matchedAt })` injecting the REAL §8 helpers and a fake client per scenario (no network, no real key).
    - Test "agreement: verified true, uses model rationale": the fake client's `messages.parse` resolves `{ parsed_output: { clearingPrice: 100, allocations: [BankA Buy 10, BankB Sell 8, BankC Sell 2], rationale: 'Cleared at 100.00 …' } }`. Expect `proposeClearing(SECTION4_VIEWS)` → `verified===true`, `source==='claude'`, `clearingPrice===100`, `matchedVolume===10`, and `rationale` is the MODEL's string. Critically, assert the returned `allocations` are the DETERMINISTIC ones (numbers from `computeClearing`, never from the model's raw object).
    - Test "agreement is order-insensitive": same as agreement but the fake returns allocations in a different order (sells before buys). Still `verified===true` (allocationsEqual compares as a set keyed by desk|side, not by index — RESEARCH Pitfall 5).
    - Test "disagreement: deterministic wins, source fallback": the fake returns `clearingPrice: 99` (or a wrong allocation). Expect `verified===false`, `source==='deterministic-fallback'`, `clearingPrice===100` (the §4 canary HOLDS — the gate rejected the model), and `rationale` is the GENERATED neutral string (not 'bogus').
    - Test "malformed: safeParse fails → fallback": the fake returns a parsed_output missing `clearingPrice` (or `side:'Hold'`, or `parsed_output: null`). Expect `source==='deterministic-fallback'`, `clearingPrice===100`, no throw.
    - Test "unavailable: SDK throws → fallback, no crash, no leak": the fake client's `parse` rejects with `new Error('network')`. Expect `proposeClearing` RESOLVES (never rejects) with `source==='deterministic-fallback'`, `clearingPrice===100`, and `JSON.stringify(result)` does NOT contain `SENTINEL_KEY`. Spy `console.log`+`console.error`; assert no captured log call contains `SENTINEL_KEY`.
    - Test "no-client (keyless) path": `createAgent({ computeClearing, matchedAt })` with NO client → `proposeClearing` short-circuits to `source==='deterministic-fallback'`, `clearingPrice===100`, no network call.
  </behavior>
  <action>
    Author `solver/src/agent.test.ts` with vitest. Import the REAL `computeClearing`, `matchedAt`, and the `OrderView`/`Allocation` types from `./auction.js`; import `createAgent` (and `AgentResult` type) from `./agent.js`. Define a `fakeClientReturning(parsedOutput)` helper shaped `{ messages: { parse: vi.fn(async () => ({ parsed_output: parsedOutput })) } }` (mirrors RESEARCH Example 4 — the DI fake is preferred over `vi.mock` for no module-hoisting). For the throw path use `{ messages: { parse: vi.fn(async () => { throw new Error('network') }) } }`. Inject the REAL §8 helpers so every assertion measures against the genuine deterministic §4 result (100.00 / matched 10 / A=10,B=8,C=2). For the sentinel sweep, mirror ledger.test.ts L170-183: `vi.spyOn(console,'log')` + `vi.spyOn(console,'error')`, run the unavailable path, then assert `SENTINEL_KEY` appears in NO `logSpy.mock.calls`/`errSpy.mock.calls` and not in `JSON.stringify(result)`; restore the spies. Use `vitest run` only (no watch). These tests are written FIRST (Wave 0) and define the `agent.ts` contract — they fail until Task 3 implements the module.
  </action>
  <verify>
    <automated>cd solver && npx vitest run src/agent.test.ts; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `solver/src/agent.test.ts` exists and imports `createAgent`/`AgentResult` from `./agent.js` and the REAL `computeClearing`/`matchedAt` from `./auction.js`.
    - It covers all required paths: agreement (verified:true, model rationale, DETERMINISTIC numbers), order-insensitive agreement, disagreement (source:'deterministic-fallback', clearingPrice 100), malformed (safeParse fail → fallback), unavailable (parse throws → fallback, no throw, no leak), keyless (no client → fallback).
    - The sentinel-key sweep asserts `SENTINEL_KEY` is absent from `JSON.stringify(result)` AND from every captured `console.log`/`console.error` call.
    - BEFORE Task 3 (no `agent.ts` yet): `npx vitest run src/agent.test.ts` FAILS (module-not-found / unimplemented) — proving the tests are real (the verify command asserts a non-zero exit at this point).
  </acceptance_criteria>
  <done>The mocked-SDK test contract for the agent exists and currently fails (no `agent.ts`): agreement uses the model rationale with deterministic numbers; disagreement/malformed/unavailable/keyless all collapse to the deterministic §4 result (100.00); the API-key sentinel never appears in any response or log.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: agent.ts — createAgent DI factory, structured-output Claude call, verify-don't-trust gate, module-private ANTHROPIC_API_KEY (turn the tests green)</name>
  <read_first>
    - .planning/phases/05-ai-solver-agent/05-RESEARCH.md (Code Example 1 the structured-output call lines 280-310 + the forced-tool-use FALLBACK lines 312-352; Code Example 2 the gate lines 354-418; Recommended Module Shape lines 186-201; Pattern 1 keyless-safe client construction lines 203-214; Anti-Patterns lines 220-226; Pitfalls 2/3/4/5 lines 248-271)
    - .planning/phases/05-ai-solver-agent/05-PATTERNS.md (agent.ts section lines 24-99 — Pattern A DI factory from api.ts createApp; Pattern B module-private secret from ledger.ts L82-95; Pattern C zod safeParse from api.ts L128-134; Pattern D verify against auction.ts; secret-safe error handling)
    - solver/src/ledger.ts (L82-95 — the module-private `_operatorToken` discipline: read once, export only the public id, construct the client once with the secret closed over, never log/return it — apply identically to ANTHROPIC_API_KEY)
    - solver/src/api.ts (createApp(deps) factory shape at L118 to mirror as createAgent; the existing zod safeParse-then-branch at L128-134)
    - solver/src/auction.ts (computeClearing L104 / matchedAt L54 / Allocation L30 / OrderView L22 — the injected oracle; note computeClearing already rounds Math.round(p*100)/100 so compare on the 2dp integer, never raw float ===)
    - solver/src/agent.test.ts (the contract this implementation must satisfy — the exact AgentResult fields and the three paths)
  </read_first>
  <files>solver/src/agent.ts</files>
  <action>
    Implement `solver/src/agent.ts`. Define and export `proposalSchema` (zod): `{ clearingPrice: z.number(), allocations: z.array(z.object({ desk: z.string(), side: z.enum(['Buy','Sell']), filledQty: z.number().int().nonnegative() })), rationale: z.string() }`. Define and export the `SYSTEM_PROMPT` const string stating the §8 rules VERBATIM plus the role framing ("you PROPOSE; the service VERIFIES; your numbers are never used unless they match exactly; temperature 0; output ONLY the required JSON") — this is the SINGLE source of truth that PROMPT.md (Plan 05-02) quotes; note in a comment that the two must stay in sync (RESEARCH Pitfall 6). Define a `buildBatchMessage(views)` helper producing the user-message JSON `{ buys:[{desk,quantity,limit}], sells:[{desk,quantity,limit}] }` from the OrderView batch. Define pure predicates `priceEqual(a,b)` = `Math.round(a*100)===Math.round(b*100)` (2dp, float-safe — RESEARCH Pitfall 4) and `allocationsEqual(a,b)` comparing as a SET keyed by `desk|side` → `filledQty` (order-insensitive — RESEARCH Pitfall 5; equal length AND every key maps to the same filledQty). Define `neutralRationale(det, matchedVolume)` generating a plain 2–3 sentence deterministic-fallback string (no model, no secret).

    Read `process.env.ANTHROPIC_API_KEY?.trim()` exactly ONCE at module scope into a module-private `const` (mirror ledger.ts L82-95): if absent/empty do NOT construct a client; if present construct `new Anthropic({ apiKey })` once. NEVER export, return, or log the key. Export `interface AgentResult { clearingPrice: number; allocations: Allocation[]; matchedVolume: number; rationale: string; verified: boolean; source: 'claude' | 'deterministic-fallback' }`.

    Export the DI factory `createAgent({ client?, computeClearing, matchedAt })` (mirrors api.ts `createApp(deps)` L118) returning `{ proposeClearing }`. The optional injected `client` overrides the module-private one (this is what the test injects and what `index.ts` passes at boot). `proposeClearing(views: OrderView[]): Promise<AgentResult>`:
    1. ALWAYS recompute the authoritative deterministic result first: `const det = computeClearing(views)` and `const matchedVolume = matchedAt(views, det.clearingPrice)`. Define `fallback()` returning `{ ...det, matchedVolume, rationale: neutralRationale(det, matchedVolume), verified: false, source: 'deterministic-fallback' }`.
    2. If no resolved client → `return fallback()` (keyless degradation).
    3. Else inside a try/catch: call `client.messages.parse({ model: 'claude-haiku-4-5', max_tokens: 1024, temperature: 0, system: SYSTEM_PROMPT, messages: [{ role: 'user', content: buildBatchMessage(views) }], output_config: { format: zodOutputFormat(proposalSchema) } })` (import `Anthropic` default + `zodOutputFormat` from `@anthropic-ai/sdk/helpers/zod`). Read `message.parsed_output`; `proposalSchema.safeParse(...)` it — on `!success` `return fallback()` (malformed/null). On success, gate: `priceEqual(c.clearingPrice, det.clearingPrice) && allocationsEqual(c.allocations as Allocation[], det.allocations)`. On mismatch → `return fallback()`. On match → `return { ...det, matchedVolume, rationale: c.rationale, verified: true, source: 'claude' }` — the NUMBERS come from `det`, never from `c`; the model contributes ONLY `rationale`.
    4. The `catch` (SDK error / timeout / network) MUST `return fallback()` — NEVER `throw` (a Claude outage must not 500 `solve-preview`). Log ONLY a fixed secret-free string (e.g. `'[agent] Claude unavailable — falling back to deterministic clearing'`) plus at most `err instanceof Error ? err.name : 'unknown'`; NEVER log `err.message`/the raw object/the key (RESEARCH Pitfall 4 / Anti-Patterns).

    Document the forced-tool-use FALLBACK (RESEARCH Code Example 1 fallback: a single `strict:true` `propose_clearing` tool + `tool_choice`, read JSON from the `tool_use` block, same `proposalSchema.safeParse`) in a comment as the locked alternative if GA structured outputs are disabled for the account/region — the equality gate and downstream handling are identical. Do NOT add a beta header. Do NOT modify `auction.ts` (FROZEN) or any settlement path.
  </action>
  <verify>
    <automated>cd solver && npx vitest run src/agent.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd solver && npx vitest run src/agent.test.ts` exits 0 — all agent paths green (agreement, order-insensitive, disagreement, malformed, unavailable, keyless, sentinel-key sweep).
    - `cd solver && npx tsc --noEmit` is clean with `agent.ts` exporting `createAgent`, `AgentResult`, `proposalSchema`, `SYSTEM_PROMPT`.
    - The Claude call uses `client.messages.parse` with `model: 'claude-haiku-4-5'`, `temperature: 0`, `max_tokens` set (≥1024), `output_config.format: zodOutputFormat(proposalSchema)`, and reads `message.parsed_output` (grep confirms `messages.parse` + `parsed_output`; no beta header).
    - The agreement-path RETURN uses `det`'s `clearingPrice`/`allocations` (deterministic numbers) and only the model's `rationale` (the test asserting deterministic allocations on agreement passes).
    - `priceEqual` compares on `Math.round(*100)` (no raw float `===` on price); `allocationsEqual` is a desk|side-keyed set compare (order-insensitive test passes).
    - `proposeClearing` NEVER throws on SDK error / no key — every failure path returns the deterministic fallback (`source:'deterministic-fallback'`, `clearingPrice===100` on the §4 fixture).
    - `ANTHROPIC_API_KEY` is read once at module scope, module-private, NOT exported/returned/logged — grep shows it nowhere in any return value or `console.*` argument; the catch logs a fixed string + at most `err.name`.
    - `auction.ts` is byte-unchanged (grep `git diff --stat` shows no modification to `solver/src/auction.ts`).
  </acceptance_criteria>
  <done>`agent.ts` exposes `createAgent({client?, computeClearing, matchedAt})` → `proposeClearing` that calls Claude (haiku-4-5, temp 0, structured output), enforces the 2dp-price + allocation-set equality gate, returns deterministic numbers always (model only colors the rationale on an exact match), degrades gracefully on any failure / missing key, and keeps `ANTHROPIC_API_KEY` module-private — all mocked-SDK tests green and `tsc` clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent.ts → Anthropic API (server-side) | the ONLY place `ANTHROPIC_API_KEY` is read; untrusted MODEL OUTPUT crosses back inward here |
| model output → verify-don't-trust gate | the model's clearingPrice/allocations are untrusted input until they pass the equality gate |
| agent.ts internals → any response / log / error envelope | `ANTHROPIC_API_KEY` and SDK error context must never cross outward |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `ANTHROPIC_API_KEY` leaking into a response / log / error envelope / git | mitigate | Key read exactly once inside `agent.ts`, module-private (mirrors `ledger.ts` L82-95 Operator-token discipline), never exported/returned/logged; the catch logs a FIXED secret-free string + at most `err.name`. A sentinel-key test asserts the key appears in NO `JSON.stringify(result)` and NO captured `console.log`/`console.error` call (spec §15 / SOLV-04 / ASVS V14, V7). `.env.example` stays empty. |
| T-05-02 | Tampering | LLM returns a wrong / hallucinated / adversarial clearing (price or allocation) | mitigate | The verify-don't-trust equality gate (AGENT-02): zod `safeParse` the shape, recompute the deterministic §8 result, accept ONLY on `priceEqual` (2dp) AND `allocationsEqual` (set by desk|side); any mismatch/malformed → deterministic fallback. The model is structurally off the settlement path; on-ledger `Round.Clear` re-verifies §8 as the deep backstop. The §4 canary (100.00) holds on every non-agreement path (ASVS V5). |
| T-05-03 | Denial of Service | Claude outage / timeout / network error causing a demo-time 500 of the solve flow | mitigate | `proposeClearing` catches ALL SDK errors and returns the deterministic fallback — it NEVER throws into the API; keyless degradation is a designed, tested path. The §4 fixture clears at 100.00 with Claude down or absent. |
| T-05-04 | Tampering | prompt-injection via order data influencing the model | mitigate | Order data is numeric (qty/limit) + service-controlled desk ids; even a manipulated rationale is presentational only — the settled NUMBERS come from the deterministic core, never the model. |
| T-05-SC | Tampering | npm install of `@anthropic-ai/sdk` (supply chain) | mitigate | Single spec-mandated, publisher-namespaced (`@anthropic-ai/*`) official SDK, exact pin `0.106.0` confirmed on the npm registry with a clean tarball + no `postinstall` (RESEARCH Package Legitimacy Audit). slopcheck was sandbox-denied → legitimacy independently established to a HIGH bar; not a search-discovered name. No `npx --yes`; no other package added. |
</threat_model>

<verification>
- `cd solver && npx vitest run src/agent.test.ts` → all agent paths green (agreement / order-insensitive / disagreement / malformed / unavailable / keyless + sentinel-key sweep).
- `cd solver && npx vitest run` → full suite (auction + ledger + api + agent) green — the 21 P4 tests stay green.
- `cd solver && npx tsc --noEmit` → clean.
- Grep `agent.ts`: `messages.parse` + `output_config` + `zodOutputFormat` + `parsed_output` present; `model: 'claude-haiku-4-5'` + `temperature: 0`; no beta header; `ANTHROPIC_API_KEY` read once, never in any return/log; the agreement return spreads `det` (deterministic numbers) and takes only `rationale` from the model.
- Grep: `solver/src/auction.ts` is unmodified.
</verification>

<success_criteria>
The AI Solver Agent exists as a thin DI adapter: on close it asks Claude (haiku-4-5, temperature 0, structured JSON) to PROPOSE a clearing, verifies it against the frozen deterministic §8 core, and uses the model's numbers NEVER — only its rationale, and only on an exact 2dp-price + allocation-set match. Every non-agreement path (disagreement / malformed / SDK error / no key) collapses to the deterministic §4 result (100.00), `proposeClearing` never throws, and `ANTHROPIC_API_KEY` is module-private and never leaked — all proven by mocked-SDK tests with no live network or real key, and `tsc` clean.
</success_criteria>

<output>
Create `.planning/phases/05-ai-solver-agent/05-01-SUMMARY.md` when done.

## Artifacts this phase produces (Plan 05-01)
- `solver/src/agent.ts` exports:
  - `createAgent({ client?, computeClearing, matchedAt })` → `{ proposeClearing(views: OrderView[]): Promise<AgentResult> }` (the DI factory; injected `client` overrides the module-private one).
  - `interface AgentResult { clearingPrice: number; allocations: Allocation[]; matchedVolume: number; rationale: string; verified: boolean; source: 'claude' | 'deterministic-fallback' }`.
  - `proposalSchema` (zod): `{ clearingPrice: number, allocations: [{ desk, side: 'Buy'|'Sell', filledQty }], rationale: string }` — drives both `zodOutputFormat()` and the verify-side `.safeParse()`.
  - `SYSTEM_PROMPT` (const string): the §8 rules verbatim + role framing — the SINGLE source PROMPT.md (Plan 05-02) mirrors.
- `solver/src/agent.test.ts`: the mocked-SDK contract (agreement / order-insensitive / disagreement / malformed / unavailable / keyless + sentinel-key sweep).
- `solver/package.json`: `@anthropic-ai/sdk@0.106.0` added to `dependencies` (the one new pinned dep).
- `solver/.env.example`: `ANTHROPIC_API_KEY=` comment updated to note `agent.ts` consumes it (value stays empty).
- Env keys consumed: `ANTHROPIC_API_KEY` (server-side, read ONLY in `agent.ts`, module-private).
- Downstream seam for Plan 05-02: `AppDeps` will gain `proposeClearing: (views: OrderView[]) => Promise<AgentResult>`; `api.ts` `solve-preview`/`GET` replace `rationale:null` with `agent.rationale` + an additive `agent:{verified,source}` block.
</output>
