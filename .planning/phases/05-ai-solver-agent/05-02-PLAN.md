---
phase: 05-ai-solver-agent
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - solver/src/api.ts
  - solver/src/api.test.ts
  - solver/src/index.ts
  - solver/PROMPT.md
autonomous: true
requirements: [AGENT-03, AGENT-04]
must_haves:
  truths:
    - "GET /round/:id/solve-preview returns the deterministic clearingPrice/matchedVolume/allocations/curve UNCHANGED plus the now-populated rationale (was null in P4) and an additive agent:{verified,source} block; calling proposeClearing never throws (AGENT-03)"
    - "GET /round/:id terminal-status branch gets the same treatment — body.rationale is the agent rationale (was null) plus the agent block; P4 consumers that read clearingPrice/matchedVolume/allocations/curve are unaffected (backward-compatible)"
    - "POST /round/:id/settle is UNCHANGED in authority — it submits the verified deterministic allocation; the rationale is presentational only (the AI stays off the settlement path)"
    - "ANTHROPIC_API_KEY (and any Anthropic SDK error context) never appears in any solve-preview / GET response or the error envelope — proven by a sentinel-key assertion extending the existing api.test secret sweep"
    - "solver/PROMPT.md documents the prompt contract: the system prompt verbatim (mirrored from agent.ts SYSTEM_PROMPT), the user-message batch JSON shape, and the required response JSON {clearingPrice, allocations:[{desk,side,filledQty}], rationale}, and states explicitly the model's numbers are never used unverified (AGENT-04)"
  artifacts:
    - path: "solver/src/api.ts"
      provides: "AppDeps gains proposeClearing; solve-preview + GET terminal branch emit agent.rationale + agent:{verified,source} (deterministic numbers unchanged); settle unchanged"
      contains: "proposeClearing"
      min_lines: 90
    - path: "solver/src/api.test.ts"
      provides: "extended: solve-preview/GET assert populated rationale + agent block + P4 fields unchanged; the existing secret sweep extended with an ANTHROPIC_API_KEY sentinel"
      contains: "agent"
    - path: "solver/src/index.ts"
      provides: "buildDeps wires the real createAgent's proposeClearing into AppDeps (real client constructed once at boot, keyless-safe)"
      contains: "proposeClearing"
    - path: "solver/PROMPT.md"
      provides: "the AGENT-04 prompt contract doc — system prompt (mirrored from SYSTEM_PROMPT), batch JSON shape, required response JSON, the verify-don't-trust statement"
      contains: "never used unverified"
  key_links:
    - from: "solver/src/api.ts (solve-preview + GET terminal)"
      to: "deps.proposeClearing (agent.ts)"
      via: "await deps.proposeClearing(views) → rationale + agent:{verified,source}; deterministic numbers from computeClearing unchanged"
      pattern: "proposeClearing\\("
    - from: "solver/src/index.ts (buildDeps)"
      to: "solver/src/agent.ts (createAgent)"
      via: "construct the real agent once at boot, thread proposeClearing into AppDeps"
      pattern: "createAgent\\("
    - from: "solver/PROMPT.md"
      to: "solver/src/agent.ts (SYSTEM_PROMPT)"
      via: "PROMPT.md quotes the SYSTEM_PROMPT const verbatim — single source of truth, kept in sync"
      pattern: "VERIFIES"
---

<objective>
Wire the Phase-5 agent into the existing API and document the prompt contract. Two surgical, backward-compatible edits to `solver/src/api.ts` at the seams P4 deliberately left as `rationale:null` (`solve-preview` and the `GET /round/:id` terminal-status branch): call `deps.proposeClearing(views)` and emit the deterministic numbers UNCHANGED plus the now-populated `rationale` and a small additive `agent:{verified,source}` block. Add `proposeClearing` to `AppDeps` and thread the real `createAgent`-built agent through `index.ts` `buildDeps`. Extend `solver/src/api.test.ts` to assert the populated rationale + agent block (with P4 fields unchanged) and to extend the existing secret sweep with an `ANTHROPIC_API_KEY` sentinel. Write `solver/PROMPT.md` documenting the prompt contract (system prompt verbatim, batch JSON, required response JSON) — the AGENT-04 deliverable.

Purpose: AGENT-03 (the 2–3 sentence rationale surfaced through the API for the Phase-6 Solver Agent panel) and AGENT-04 (the documented prompt contract) complete the phase. `solve-preview` is the "watch the agent think" data source; the wiring is additive so P4 consumers are unaffected and `settle` keeps its deterministic-only authority (the AI never touches the settlement path). PROMPT.md mirrors the `SYSTEM_PROMPT` const so the documented rules and the rules the model actually sees never diverge.
Output: `solver/src/api.ts` (edited), `solver/src/api.test.ts` (extended), `solver/src/index.ts` (wiring), `solver/PROMPT.md` (new).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-solver-agent/05-RESEARCH.md
@.planning/phases/05-ai-solver-agent/05-PATTERNS.md
@.planning/phases/05-ai-solver-agent/05-01-SUMMARY.md
@solver/src/api.ts
@solver/src/api.test.ts
@solver/src/index.ts
@solver/src/agent.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire proposeClearing into api.ts (solve-preview + GET terminal) + index.ts buildDeps; extend api.test.ts (rationale + agent block + ANTHROPIC_API_KEY sentinel)</name>
  <read_first>
    - .planning/phases/05-ai-solver-agent/05-RESEARCH.md (Code Example 3 wiring into api.ts solve-preview lines 420-439 — the EXACT backward-compatible response: deterministic numbers unchanged + rationale: agent.rationale + agent:{verified,source}; the "GET /round/:id terminal branch gets the same treatment" note line 439; AGENT-03 row lines 71)
    - .planning/phases/05-ai-solver-agent/05-PATTERNS.md (api.ts MODIFIED section lines 102-133 — Edit 1 solve-preview L202-209 replace rationale:null; Edit 2 GET /round/:id terminal branch L167-176 replace body.rationale=null; add to AppDeps L54-68 one field proposeClearing; index.ts buildDeps L66-91 wires the real agent; preserve the secret-safe error envelope)
    - solver/src/api.ts (the EXACT seams: solve-preview handler L191-211 with rationale:null at L208; GET /round/:id terminal branch L167-176 with body.rationale=null at L175; AppDeps L54-68; buildCurve L108-116; the secret-safe error middleware L243-255 — do NOT regress it)
    - solver/src/api.test.ts (SECTION4_VIEWS L36-40; makeDeps L52-75 — add a proposeClearing default stub; the SENTINEL_TOKEN sweep at L47-49/L154-175 to extend with an ANTHROPIC_API_KEY sentinel; the listen()/readJson() harness L79-89)
    - solver/src/index.ts (buildDeps L66-91 + BuildDepsArgs; the live boot main() L94-189 — construct the real agent via createAgent and pass proposeClearing into buildDeps; the secret-free boot log L185-188; the LedgerPort/RoundView projections L131-160)
    - solver/src/agent.ts (createAgent + AgentResult + proposeClearing — the dep being wired; the module-private client it constructs at boot when a key is present)
  </read_first>
  <files>solver/src/api.ts, solver/src/index.ts, solver/src/api.test.ts</files>
  <behavior>
    - Test "solve-preview surfaces the agent rationale + block (verified)": with `makeDeps` overriding `proposeClearing` to return `{ clearingPrice:100, allocations:[…§4…], matchedVolume:10, rationale:'Cleared at 100.00 …', verified:true, source:'claude' }`, `GET /round/:id/solve-preview` responds 200 with `clearingPrice===100`, `matchedVolume===10`, a non-empty `curve`, `rationale` === the agent string (NOT null), and `agent.verified===true` / `agent.source==='claude'`.
    - Test "solve-preview deterministic-fallback shape": with `proposeClearing` returning `source:'deterministic-fallback', verified:false` and a neutral rationale, `solve-preview` still responds `clearingPrice===100` (P4 fields unchanged) with the neutral `rationale` (non-null) and `agent.source==='deterministic-fallback'`.
    - Test "GET /round/:id terminal branch surfaces rationale": with a stubbed round status `'Settled'` and the §4 sealed orders + a `proposeClearing` stub, `GET /round/:id` body has `rationale` non-null + an `agent` block, while `clearingPrice/matchedVolume/allocations/curve` are the deterministic values (P4 unchanged).
    - Test "no ANTHROPIC_API_KEY in any response (sentinel)": extend the existing secret sweep — with a `proposeClearing` stub that closes over an `ANTHROPIC_API_KEY` sentinel string, neither `solve-preview` nor `GET /round/:id` response JSON (stringified) contains the sentinel (alongside the existing operator-token sentinel assertion).
    - Test "backward-compat: P4 fields intact": `solve-preview` still returns `roundId`, `clearingPrice`, `matchedVolume`, `allocations`, `curve` with the SAME values as before the wiring (the agent block + non-null rationale are purely additive).
  </behavior>
  <action>
    EDIT `solver/src/api.ts`:
    1. Add ONE field to `AppDeps` (L54-68): `proposeClearing: (views: OrderView[]) => Promise<AgentResult>` (import `AgentResult` from `./agent.js`). Place it alongside the existing injected helpers.
    2. solve-preview handler (L191-211): after building `views`, `const agent = await deps.proposeClearing(views)` (it NEVER throws). Replace the `res.json({...})` body so `clearingPrice`/`matchedVolume`/`allocations`/`curve` come from the DETERMINISTIC path UNCHANGED (keep using `computeClearing`/`matchedAt`/`buildCurve` exactly as today — do NOT take numbers from the agent), set `rationale: agent.rationale` (replacing `rationale: null` at L208), and add `agent: { verified: agent.verified, source: agent.source }`. (You MAY read the deterministic numbers from `agent.clearingPrice`/`agent.matchedVolume`/`agent.allocations` since the agent always returns the deterministic numbers — but the canonical, unambiguous choice is to keep the existing `computeClearing(views)`/`matchedAt(views, clearingPrice)`/`buildCurve(deps, views)` lines and only ADD rationale+agent, so the diff is minimal and the P4 numbers are provably unchanged.)
    3. GET /round/:id terminal branch (L167-176): inside `if (TERMINAL_STATUSES.has(round.status))`, after the deterministic `body.clearingPrice/matchedVolume/allocations/curve` lines, `const agent = await deps.proposeClearing(views)`; replace `body.rationale = null` (L175) with `body.rationale = agent.rationale` and add `body.agent = { verified: agent.verified, source: agent.source }`.
    4. settle handler (L213-241): UNCHANGED — it submits the deterministic allocation; the AI is off this path. Do NOT call proposeClearing here.
    5. The secret-safe error middleware (L243-255) is UNCHANGED — keep it; agent errors never reach it because `proposeClearing` handles its own failures (Plan 05-01).

    EDIT `solver/src/index.ts`:
    - In `main()`, after the dynamic `auction` import, construct the real agent ONCE: `const { createAgent } = await import('./agent.js')`; `const agent = createAgent({ computeClearing: auction.computeClearing, matchedAt: auction.matchedAt })` (no `client` passed — `agent.ts` constructs the real Anthropic client itself from the module-private key, or runs keyless). Thread `agent.proposeClearing` into the deps. Either add `proposeClearing` to `BuildDepsArgs`/`buildDeps` (preferred — mirrors the existing `math` wiring) so `buildDeps` returns it on `AppDeps`, or pass it through `buildDeps`'s result. Keep the entrypoint guard + secret-free boot log UNCHANGED. Ensure `index.test.ts` still type-checks (if `buildDeps`'s signature gains a field, supply a stub `proposeClearing` in that test's args).

    EXTEND `solver/src/api.test.ts`:
    - Add a default `proposeClearing` stub to `makeDeps` (e.g. returns the deterministic §4 result with a neutral rationale + `source:'deterministic-fallback'`) so existing tests keep passing.
    - Add the behaviors above. Reuse the existing `listen()`/`readJson()` harness and `SECTION4_VIEWS`. Define an `ANTHROPIC_API_KEY` sentinel string and assert it never appears in `JSON.stringify(...)` of the solve-preview and GET responses, alongside the existing `SENTINEL_TOKEN` operator sweep. `vitest run` only.
  </action>
  <verify>
    <automated>cd solver && npx vitest run src/api.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `cd solver && npx vitest run src/api.test.ts` exits 0 — the new rationale/agent-block + sentinel tests pass and all pre-existing P4 api tests stay green.
    - `solve-preview` response now has `rationale` (non-null, the agent string) + `agent: { verified, source }`; `clearingPrice/matchedVolume/allocations/curve` are byte-for-byte the deterministic P4 values (backward-compat test passes).
    - `GET /round/:id` terminal-status branch sets `body.rationale` (non-null) + `body.agent`; `body.rationale = null` is gone (grep shows no `rationale: null` / `body.rationale = null` remaining in api.ts).
    - `POST /round/:id/settle` is unchanged — it does NOT call `proposeClearing` (grep: `proposeClearing` appears in the solve-preview + GET handlers only, never in settle).
    - The secret sweep asserts BOTH the operator-token sentinel AND an `ANTHROPIC_API_KEY` sentinel are absent from `JSON.stringify(...)` of solve-preview and GET responses.
    - `index.ts` constructs the real agent via `createAgent({ computeClearing, matchedAt })` (no `client` arg) and threads `proposeClearing` into the deps; `npx tsc --noEmit` is clean (incl. index.test.ts).
    - `cd solver && npx vitest run` (full suite) is green — the 21 P4 tests + Plan 05-01 agent tests + the new api tests.
  </acceptance_criteria>
  <done>`api.ts` `solve-preview` and the `GET /round/:id` terminal branch surface the agent's `rationale` + an additive `agent:{verified,source}` block while the deterministic numbers stay unchanged; `settle` is untouched; `index.ts` wires the real keyless-safe agent; the API-key sentinel never appears in any response — all proven by extended api tests with the full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: solver/PROMPT.md — the AGENT-04 prompt contract (system prompt verbatim, batch JSON, required response JSON, verify-don't-trust statement)</name>
  <read_first>
    - .planning/phases/05-ai-solver-agent/05-RESEARCH.md (PROMPT.md Contract lines 495-525 — the copy-ready three blocks: the verbatim system prompt = §8 rules + role framing, the user-message batch JSON shape, the required response JSON shape, and the explicit "never used unverified" statement)
    - .planning/phases/05-ai-solver-agent/05-PATTERNS.md (PROMPT.md section lines 185-194 — no code analog; copy the three blocks from RESEARCH; the system prompt MUST be the SAME text as agent.ts SYSTEM_PROMPT, kept in sync — RESEARCH Pitfall 6; mirror the auction.ts header-comment "must stay in sync" convention)
    - solver/src/agent.ts (the SYSTEM_PROMPT const — PROMPT.md must quote it VERBATIM; the proposalSchema fields — the required response JSON shape must match { clearingPrice, allocations:[{desk,side,filledQty}], rationale })
    - spec.md §9 lines 315-324 (the AI design contract this doc fulfills — the prompt-contract requirement: "system prompt states the auction rules verbatim; user message provides the batch as JSON; require a JSON response {clearingPrice, allocations, rationale}; temperature 0; never let the model's number be used unverified")
  </read_first>
  <files>solver/PROMPT.md</files>
  <action>
    Author `solver/PROMPT.md` documenting the three contract blocks (RESEARCH lines 495-525):
    1. SYSTEM PROMPT — quote the `agent.ts` `SYSTEM_PROMPT` const VERBATIM (the §8 candidate-prices / demand-supply-matched / two-level tie-break / allocation rules + the role framing "you PROPOSE; the service VERIFIES; your numbers are never used unless they match exactly; temperature 0; output ONLY the required JSON"). Add a note: "This block is mirrored from `solver/src/agent.ts` `SYSTEM_PROMPT` — keep the two in sync" (RESEARCH Pitfall 6; the const is the single source of truth).
    2. USER MESSAGE (batch JSON shape) — `{ "buys": [{ "desk", "quantity", "limit" }], "sells": [{ "desk", "quantity", "limit" }] }`, with the §4 fixture as the worked example (A Buy 10@101 / B Sell 8@99 / C Sell 5@100).
    3. REQUIRED RESPONSE JSON — `{ "clearingPrice": <number, 2dp>, "allocations": [{ "desk", "side": "Buy"|"Sell", "filledQty": <int> }], "rationale": <2–3 sentence plain string> }`, with the §4 worked answer (100.00, A=10/B=8/C=2) and the example rationale tone from spec §9 / CONTEXT.md.
    State EXPLICITLY: "The model's clearingPrice and allocations are never used unverified — the service recomputes the deterministic §8 result (`auction.ts`) and only the rationale is taken from the model when the proposal matches exactly (2dp price + allocation set); on any mismatch / malformed output / outage / missing key the deterministic result settles with a generated neutral rationale, and the on-ledger `Round.Clear` re-verifies §8 as the final backstop." Document the structured-output mechanism (`output_config.format` via `zodOutputFormat`, GA, no beta header) and the forced-tool-use fallback as the locked alternative. Keep it a doc only — no code change.
  </action>
  <verify>
    <automated>cd solver && grep -q "never used unverified" PROMPT.md && grep -q "clearingPrice" PROMPT.md && grep -q "rationale" PROMPT.md && grep -q "VERIFIES" PROMPT.md</automated>
  </verify>
  <acceptance_criteria>
    - `solver/PROMPT.md` exists and contains all three blocks: the verbatim system prompt (the §8 rules + role framing, matching `agent.ts` `SYSTEM_PROMPT`), the user-message batch JSON shape, and the required response JSON shape.
    - It states explicitly the model's clearingPrice/allocations are "never used unverified" and names the deterministic §8 recompute + on-ledger `Round.Clear` re-verification as the guarantee.
    - The required response JSON matches `proposalSchema`: `{ clearingPrice, allocations:[{desk, side, filledQty}], rationale }`.
    - It carries the "mirrored from `agent.ts` SYSTEM_PROMPT — keep in sync" note and documents the `output_config.format` structured-output mechanism (no beta header) + the forced-tool-use fallback.
    - The §4 worked example (100.00, A=10/B=8/C=2) appears as the canonical fixture.
    - The verify grep finds `never used unverified`, `clearingPrice`, `rationale`, and `VERIFIES`.
  </acceptance_criteria>
  <done>`solver/PROMPT.md` documents the AGENT-04 prompt contract — the verbatim system prompt (mirrored from the code), the batch JSON shape, the required response JSON, and the explicit verify-don't-trust statement with the §4 worked example.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solver API (:4000) → browser (:5173) | `ANTHROPIC_API_KEY` / SDK error context must never cross outward in any response or envelope |
| deps.proposeClearing (agent.ts) → api.ts response | only the verified `rationale` + the `agent:{verified,source}` block cross; the settled NUMBERS come from the deterministic core |
| index.ts boot → agent construction | the real Anthropic client is constructed inside agent.ts from the module-private key; index.ts never reads the key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `ANTHROPIC_API_KEY` / Anthropic SDK error context leaking into a solve-preview / GET response or the error envelope | mitigate | Wiring only surfaces `agent.rationale` + `{verified,source}` (no key, no SDK error object); `index.ts` never reads the key (the agent constructs its own client from the module-private value — Plan 05-01). The existing secret-safe error middleware (api.ts L243-255) is preserved; the api.test secret sweep is EXTENDED with an `ANTHROPIC_API_KEY` sentinel asserting absence from every solve-preview/GET response (spec §15 / SOLV-04 / ASVS V7, V14). |
| T-05-02 | Tampering | the AI's numbers being surfaced/settled through the API | mitigate | `solve-preview`/`GET` emit the DETERMINISTIC `computeClearing`/`matchedAt`/`buildCurve` numbers unchanged (the agent's numbers are identical-or-rejected by Plan 05-01's gate); `settle` is UNCHANGED and never calls `proposeClearing` — the AI is provably off the settlement path; on-ledger `Round.Clear` re-verifies §8 (ASVS V5). A backward-compat test asserts the P4 numeric fields are unchanged. |
| T-05-03 | Denial of Service | a Claude outage 500-ing solve-preview / GET | mitigate | `deps.proposeClearing` never throws (Plan 05-01); the handlers `await` it without a try/catch needed for the agent path; the demo still serves 100.00 + a neutral rationale keyless. |
| T-05-04 | Repudiation / Tampering | PROMPT.md drifting from the actual `SYSTEM_PROMPT` the model sees | mitigate | PROMPT.md quotes the `agent.ts` `SYSTEM_PROMPT` const verbatim with a "keep in sync" note (the const is the single source of truth — RESEARCH Pitfall 6); the documented response shape matches `proposalSchema`. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new installs in this plan (`@anthropic-ai/sdk` installed + audited in Plan 05-01). Tests use the existing Node built-in `fetch` harness — no new test dependency. |
</threat_model>

<verification>
- `cd solver && npx vitest run src/api.test.ts` → the new rationale/agent-block + sentinel tests green; all P4 api tests still green.
- `cd solver && npx vitest run` → full suite (auction + ledger + api + agent + index) green.
- `cd solver && npx tsc --noEmit` → clean (api.ts, index.ts, api.test.ts).
- Grep `api.ts`: `proposeClearing(` appears in solve-preview + GET terminal handlers only (never in settle); no `rationale: null` / `body.rationale = null` remains; the secret-safe error middleware is intact.
- Grep `index.ts`: `createAgent(` constructs the agent with `{ computeClearing, matchedAt }` (no `client` arg); the secret-free boot log is unchanged.
- `solver/PROMPT.md` contains the three blocks + the "never used unverified" statement + the "mirrored from SYSTEM_PROMPT" note.
</verification>

<success_criteria>
The agent's 2–3 sentence rationale is surfaced through `solve-preview` and the `GET /round/:id` terminal branch as a populated `rationale` + an additive `agent:{verified,source}` block, with the deterministic clearing numbers unchanged and `settle` keeping its deterministic-only authority (the AI off the settlement path); the real keyless-safe agent is wired at boot; `ANTHROPIC_API_KEY` never appears in any response; and `solver/PROMPT.md` documents the full prompt contract (verbatim system prompt, batch JSON, required response JSON, verify-don't-trust) — all proven by extended api tests with the full suite green and `tsc` clean.
</success_criteria>

<output>
Create `.planning/phases/05-ai-solver-agent/05-02-SUMMARY.md` when done.

## Artifacts this phase produces (Plan 05-02)
- `solver/src/api.ts` (edited): `AppDeps` gains `proposeClearing: (views: OrderView[]) => Promise<AgentResult>`; `solve-preview` and the `GET /round/:id` terminal branch emit `rationale: agent.rationale` (was null) + `agent: { verified, source }` while the deterministic `clearingPrice`/`matchedVolume`/`allocations`/`curve` stay unchanged; `settle` UNCHANGED (deterministic-only, no `proposeClearing`).
- `solver/src/index.ts` (edited): `buildDeps`/`main()` construct the real agent once via `createAgent({ computeClearing, matchedAt })` (keyless-safe) and thread `proposeClearing` into the deps.
- `solver/src/api.test.ts` (extended): solve-preview/GET assert populated `rationale` + `agent` block + P4 fields unchanged; the secret sweep is extended with an `ANTHROPIC_API_KEY` sentinel.
- `solver/PROMPT.md` (new): the AGENT-04 prompt contract — the verbatim system prompt (mirrored from `agent.ts SYSTEM_PROMPT`), the user-message batch JSON shape, the required response JSON `{ clearingPrice, allocations:[{desk,side,filledQty}], rationale }`, and the explicit "model's numbers are never used unverified" statement with the §4 worked example.
- Response contract (P5, backward-compatible): `solve-preview` → `{ roundId, clearingPrice, matchedVolume, allocations, curve, rationale, agent:{verified,source} }`; `GET /round/:id` (terminal) adds `rationale` + `agent` alongside the existing deterministic fields. P4 consumers reading `clearingPrice/matchedVolume/allocations/curve` are unaffected.
</output>
