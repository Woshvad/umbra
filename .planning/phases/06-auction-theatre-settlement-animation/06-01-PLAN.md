---
phase: 06-auction-theatre-settlement-animation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/solver.ts
  - web/src/lib/curve.ts
  - web/src/lib/balance.ts
  - web/src/lib/solverParse.ts
  - web/src/lib/curve.test.ts
  - web/src/lib/balance.test.ts
  - web/src/lib/solverParse.test.ts
  - web/vitest.config.ts
  - web/package.json
  - web/tailwind.config.ts
  - web/src/components/Nav.tsx
  - web/src/App.tsx
  - web/src/views/DeskView.tsx
  - web/src/views/TheatreView.tsx
  - web/src/views/AgentView.tsx
  - web/src/views/SettlementView.tsx
autonomous: true
requirements: [UI-02, UI-04, UI-05, UI-06]
must_haves:
  truths:
    - "web/src/solver.ts exposes createRound/getRound/closeRound/solvePreview/settle against VITE_SOLVER_URL (default http://localhost:4000)"
    - "A network failure to :4000 surfaces as SolverError with code 'OFFLINE' (graceful solver-offline guard) and never leaks the operator token"
    - "Pure lib helpers (curve->SVG path, balance lerp, solver payload parse + badge map) pass §4-value unit tests: clearingPrice===100, matchedVolume===10, crossing at (296,160), lerp endpoints A:10/4000·B:12/1800·C:13/1200"
    - "balance.test.ts asserts deskBalancesFromAllocations(<canonical §4 allocations fixture>, <before balances>) yields the §4 FINALS A:10/4000·B:12/1800·C:13/1200 (the full allocations→balances derivation, not just raw lerp endpoints)"
    - "Nav Screen union is 'privacy'|'desk'|'theatre'|'agent'|'settlement' with all 5 tabs enabled"
    - "App routes all 5 views, lifts roundId/phase/preview/offline state, threads activeDesk to DeskView; cd web && npm run build is green"
    - "tailwind.config.ts adds umbraLeg keyframe + the large fontSize literals (54/56/44/40/30b/34/84/120/22/18/15) AND the missing animation aliases umbra-draw/umbra-pulse/umbra-caret (+umbra-leg) without altering any existing binding value"
    - "No operator token and no operator @daml/react context exist anywhere in web/src"
  artifacts:
    - path: "web/src/solver.ts"
      provides: ":4000 fetch client + response types + SolverError offline guard"
      exports: ["createRound", "getRound", "closeRound", "solvePreview", "settle", "SolverError", "SOLVER_BASE_URL"]
    - path: "web/src/lib/curve.ts"
      provides: "curve->SVG coordinate mapping for the crossing chart"
    - path: "web/src/lib/balance.ts"
      provides: "lerp + per-desk before/after balance derivation"
    - path: "web/src/lib/solverParse.ts"
      provides: "agent.source->badge label map + §4 payload assertions helpers"
    - path: "web/vitest.config.ts"
      provides: "node-environment vitest config (mirrors solver/vitest.config.ts)"
    - path: "web/tailwind.config.ts"
      provides: "umbraLeg keyframe + large fontSize literals + the umbra-draw/umbra-pulse/umbra-caret/umbra-leg animation aliases that Plans 03/04 consume"
      contains: "umbra-draw"
    - path: "web/src/components/Nav.tsx"
      provides: "5-tab Screen union, all enabled"
    - path: "web/src/App.tsx"
      provides: "routing + lifted solver round state + offline flag"
  key_links:
    - from: "web/src/solver.ts"
      to: "http://localhost:4000"
      via: "native fetch to VITE_SOLVER_URL"
      pattern: "fetch\\(`\\$\\{SOLVER_BASE_URL\\}"
    - from: "web/src/App.tsx"
      to: "web/src/solver.ts"
      via: "lifted round/phase/preview state shared across operator views"
      pattern: "solver"
    - from: "web/tailwind.config.ts animation block"
      to: "Plan 03 animate-umbra-draw / animate-umbra-pulse, Plan 04 animate-umbra-caret"
      via: "tailwind animation aliases consumed by className"
      pattern: "umbra-draw|umbra-pulse|umbra-caret"
---

<objective>
Build the Wave-0 foundation that every Phase-6 view consumes: the `web/src/solver.ts` :4000 client (with the solver-offline guard), the pure `web/src/lib/*` helpers (curve→SVG, balance lerp, solver-payload parse + agent badge map) with §4-value unit tests, the vitest runner for `web/`, the additive `tailwind.config.ts` keyframe + fontSize literals + the missing animation aliases (umbra-draw/umbra-pulse/umbra-caret/umbra-leg that Plans 03/04 consume), the widened `Nav` union, and the `App` routing + lifted solver round state. Also create the four NEW view files as compiling section-marker stubs so `App` routes them and `npm run build` stays green; Plans 02–04 replace each stub with the full view.

Purpose: This is the contract + scaffold layer. Defining `solver.ts` response types, the lib helpers, the tailwind motion aliases, and the routing/state shape FIRST means the view plans build against fixed interfaces (and live motion classes) instead of exploring the codebase.
Output: `web/src/solver.ts`, `web/src/lib/{curve,balance,solverParse}.ts` + tests, `web/vitest.config.ts`, updated `web/package.json` (vitest devDep + test script), extended `web/tailwind.config.ts`, `web/src/components/Nav.tsx`, `web/src/App.tsx`, and four stub views.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-auction-theatre-settlement-animation/06-UI-SPEC.md
@.planning/phases/06-auction-theatre-settlement-animation/06-RESEARCH.md
@.planning/phases/06-auction-theatre-settlement-animation/06-PATTERNS.md
@web/src/App.tsx
@web/src/components/Nav.tsx
@web/src/desks.ts
@web/src/config.ts
@web/tailwind.config.ts
@solver/src/api.ts
@solver/src/auction.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: solver.ts client + lib helpers + vitest setup with §4-value tests</name>
  <files>web/src/solver.ts, web/src/lib/curve.ts, web/src/lib/balance.ts, web/src/lib/solverParse.ts, web/src/lib/curve.test.ts, web/src/lib/balance.test.ts, web/src/lib/solverParse.test.ts, web/vitest.config.ts, web/package.json</files>
  <read_first>
    - .planning/phases/06-auction-theatre-settlement-animation/06-RESEARCH.md (Pattern 1 lines 184-262 = the full solver.ts client to transcribe; Pattern 6 lines 376-390 = curve mapping; Pattern 7 lines 392-424 = lerp; Validation Architecture lines 586-623 = the test map)
    - .planning/phases/06-auction-theatre-settlement-animation/06-PATTERNS.md (web/src/solver.ts section lines 40-56 = verified Allocation/agent/CurvePoint shapes; lib section lines 164-173)
    - .planning/phases/06-auction-theatre-settlement-animation/06-UI-SPEC.md ("SVG Crossing Chart — Exact Geometry" lines 236-245 = the binding marker (296,160) and axis frame the curve mapping must reproduce)
    - solver/src/api.ts (the frozen response shapes solver.ts type-mirrors: solve-preview, GET /round/:id terminal fields, settle txConfirmations, the {error:{code,message}} envelope)
    - solver/src/auction.ts (Allocation = {desk:string; side:'Buy'|'Sell'; filledQty:number} — VERIFIED lines 30-34; CurvePoint via buildCurve = {price,demand,supply})
    - solver/vitest.config.ts (mirror exactly), solver/src/auction.test.ts (describe/it/expect + SECTION4 fixture style)
    - web/src/config.ts (the single-drift-const + comment convention for SOLVER_BASE_URL)
  </read_first>
  <behavior>
    - solverParse: parseSolvePreview maps a §4 solve-preview payload → clearingPrice===100, matchedVolume===10; badgeLabel('claude')==='VERIFIED · CLAUDE', badgeLabel('deterministic-fallback')==='VERIFIED · DETERMINISTIC'
    - solver client call(): a fetch network reject throws SolverError with status 0 and code 'OFFLINE' and message 'SOLVER OFFLINE — START THE SERVICE ON :4000'; a non-ok JSON {error:{code,message}} response throws SolverError carrying that code+message (never the raw headers/url beyond the base host)
    - curve: mapping the §4 curve points onto the viewBox 0 0 480 360 frame reproduces the crossing at the binding marker (296,160) for p*=100, q=10 (within ±1px rounding); sx/sy stay within axis bounds x∈[48,460], y∈[20,320]
    - balance: lerp(x,y,0)===x, lerp(x,y,1)===y; deskBalancesFromAllocations(<§4 allocations fixture: BLUEROCK Buy +10 / MERIDIAN Sell −8 / HALWARD Sell −2 at price 100>, <before balances>) yields the §4 FINALS BLUEROCK 10/4000, MERIDIAN 12/1800, HALWARD 13/1200 (NOT just raw lerp endpoints — the full allocations→balances derivation is asserted on the canonical fixture)
  </behavior>
  <action>
    Install the test runner: `cd web && npm i -D vitest@2.1.9 --legacy-peer-deps` (match solver's 2.1.9; `--legacy-peer-deps` because web already uses it for @daml/react React-18). Add `web/vitest.config.ts` transcribed from solver/vitest.config.ts (defineConfig from 'vitest/config'; test.environment 'node'; test.include ['src/**/*.test.ts']). Add a `"test": "vitest run"` script to web/package.json.

    Create `web/src/solver.ts` by transcribing RESEARCH Pattern 1 verbatim. Export `SOLVER_BASE_URL = import.meta.env.VITE_SOLVER_URL ?? 'http://localhost:4000'` (single drift const + comment, mirroring web/src/config.ts). Define the response types from the frozen api.ts shapes: CurvePoint {price,demand,supply:number}; Allocation {desk:string; side:'Buy'|'Sell'; filledQty:number} (per auction.ts lines 30-34); AgentMeta {verified:boolean; source:'claude'|'deterministic-fallback'}; RoundResponse (roundId,status,sealedOrderCount + optional clearingPrice/matchedVolume/allocations/curve/rationale/agent); SolvePreviewResponse (all-present clearingPrice/matchedVolume/allocations/curve/rationale/agent); SettleResponse (roundId,status:'Settled',clearingPrice,matchedVolume,allocations,txConfirmations). Implement the `class SolverError extends Error` carrying status+code, the `call<T>` helper that wraps fetch in try/catch → `new SolverError(0,'OFFLINE','SOLVER OFFLINE — START THE SERVICE ON :4000')` on network reject and reads `{error:{code,message}}` on non-ok, and the five endpoint functions createRound/getRound/closeRound/solvePreview/settle. Do NOT import any @daml/react context here; do NOT reference any operator token.

    Create `web/src/lib/solverParse.ts` exporting `badgeLabel(source)` (the Pitfall-5 map) and a `parseSolvePreview(json)` that returns the typed SolvePreviewResponse. Create `web/src/lib/curve.ts` exporting `sx(q,qMax)`, `sy(p,pMin,pMax)`, and a `crossingPoint(curve, pStar, qStar)` that maps to screen coords on the binding viewBox 0 0 480 360 frame (axis x∈[48,460], y∈[20,320]) reproducing (296,160) for the §4 fixture; export the binding axis constants. Create `web/src/lib/balance.ts` exporting `lerp(x,y,t)` and `deskBalancesFromAllocations(allocations, before)` deriving each desk's after-balance from the Allocation array (Allocation = {desk, side, filledQty}; signed BONDX delta = +filledQty for Buy, −filledQty for Sell; signed USDCx delta = ∓filledQty·clearingPrice) — §4 at 100.00: BLUEROCK +10 BONDX/−1000 USDCx → 10/4000, MERIDIAN −8/+800 → 12/1800, HALWARD −2/+200 → 13/1200.

    Create `web/src/lib/curve.test.ts`, `balance.test.ts`, `solverParse.test.ts` with a shared §4 fixture (the canonical solve-preview payload: clearingPrice 100, matchedVolume 10, allocations BLUEROCK Buy +10 / MERIDIAN Sell −8 / HALWARD Sell −2, curve points reproducing the crossing). Assert every behavior in <behavior>. In `balance.test.ts`, assert BOTH the raw `lerp` endpoints AND `deskBalancesFromAllocations(<§4 allocations fixture>, <before balances>)` yielding the §4 FINALS A:10/4000 · B:12/1800 · C:13/1200 (so the allocations→balances derivation is covered, not only the lerp primitive). Style: `describe/it/expect` mirroring solver/src/auction.test.ts. The §4 numbers are the binding correctness anchor — assert them exactly, never fabricate drift values.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/curve.test.ts src/lib/balance.test.ts src/lib/solverParse.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npx vitest run` passes all three lib test files (curve crossing at (296,160), p*=100, q=10; lerp endpoints = §4 balances; badge map + parse → 100/10).
    - `balance.test.ts` asserts `deskBalancesFromAllocations(<canonical §4 allocations fixture>, <before balances>)` yields the §4 FINALS A:10/4000 · B:12/1800 · C:13/1200 (the full allocations→balances derivation — not just `lerp` endpoints). `grep -n "deskBalancesFromAllocations" web/src/lib/balance.test.ts` matches.
    - `cd web && npm run build` is green (tsc --noEmit + vite build) with the new solver.ts + lib modules.
    - `grep -rn "operator" web/src/solver.ts web/src/lib/` returns no operator token or operator @daml/react context (only benign mentions, if any, in comments — assert no `import` of a ledger context and no token literal).
    - solver.ts exports createRound, getRound, closeRound, solvePreview, settle, SolverError, SOLVER_BASE_URL; an OFFLINE SolverError is thrown on a network reject.
  </acceptance_criteria>
  <done>solver.ts client + the three pure lib helpers exist, are unit-tested green on the §4 values (incl. the crossing-chart marker, the lerp endpoints, AND the allocations→balances derivation to the §4 finals), vitest is wired in web/, and the build is green. No operator token in web/src.</done>
</task>

<task type="auto">
  <name>Task 2: tailwind additions (keyframe + fontSize + animation aliases) + Nav 5-tab union + App routing & lifted solver state + 4 compiling view stubs</name>
  <files>web/tailwind.config.ts, web/src/components/Nav.tsx, web/src/App.tsx, web/src/views/DeskView.tsx, web/src/views/TheatreView.tsx, web/src/views/AgentView.tsx, web/src/views/SettlementView.tsx</files>
  <read_first>
    - web/tailwind.config.ts (the existing keyframes lines 62-71 + animation lines 72-78 + fontSize block lines 25-36 — ADD only, never alter binding values; note the animation block currently aliases ONLY umbra-wipe/slam/stamp/rise/fade — the draw/pulse/caret keyframes exist with NO alias)
    - web/src/components/Nav.tsx (widen Screen union line 6; enable the 4 disabled tabs lines 8-14; active rule lines 25-27 unchanged)
    - web/src/App.tsx (keep RoundStateProbe lines 18-38; extend the screen switch lines 72-74; lift roundId/phase/preview/offline; thread activeDesk to DeskView per RESEARCH Pattern 8)
    - web/src/views/PrivacyView.tsx (the <main> section-marker frame to copy into each stub: padding 30px 48px {64|48|72}px → mono num + Inter .16em label → 1px bg-ink rule → display headline)
    - .planning/phases/06-auction-theatre-settlement-animation/06-UI-SPEC.md ("Named Keyframes" lines 249-266 = umbraLeg def + the full motion contract incl. umbra-draw/pulse/caret usage; Typography table lines 76-101 = the fontSize literals; Shell lines 139-145 = Nav/App extension contract; per-view headlines for the stubs)
    - .planning/phases/06-auction-theatre-settlement-animation/06-PATTERNS.md (tailwind section lines 194-205; App lines 177-181; Nav lines 183-191)
    - .planning/phases/06-auction-theatre-settlement-animation/06-RESEARCH.md (Pattern 8 lines 427-431 = lifted App state + offline guard; Open Question 2 = default roundId 'R1'; Pattern 7 lines 425 = umbraLeg keyframe addition)
  </read_first>
  <action>
    Extend `web/tailwind.config.ts` additively (do NOT touch any existing value — binding comp): add the `umbraLeg` keyframe `{ from: { strokeDashoffset: 'var(--len)' }, to: { strokeDashoffset: '0' } }` alongside the existing keyframes, and add the missing large fontSize literals from the UI-SPEC Typography table — `'54':['54px','.96']`, `'56':['56px','.98']`, `'44':['44px','1']`, `'40':['40px','1']`, `'30b':['30px','1']` (note: '30' wordmark already exists at line 33 — add the new theatre/reveal sizes under distinct keys so the existing '30' stays byte-unchanged; use `'34'`, `'84'`, `'120':['120px','.9']`, `'22'`, `'18'`, `'15'`). Keep every existing umbra* keyframe as-is (umbraSlam/umbraStamp/umbraDraw/umbraCaret/umbraPulse/umbraRise/umbraWipe keyframes are all present).

    CRITICAL — also extend the `animation` block with the THREE missing aliases that Plans 03/04 consume but which currently have only keyframes and NO animation alias (without these, `animate-umbra-draw` / `animate-umbra-pulse` / `animate-umbra-caret` are dead no-ops — Tailwind generates the utility class only when an `animation` entry exists): add `'umbra-draw': 'umbraDraw 1s ease forwards'`, `'umbra-pulse': 'umbraPulse 1s infinite'`, `'umbra-caret': 'umbraCaret .9s steps(1) infinite'`. Also add `'umbra-leg': 'umbraLeg .8s linear forwards'` to pair with the new umbraLeg keyframe (used IF an SVG-stroke leg path is chosen in Plan 04; harmless otherwise). Keep the existing animation aliases (umbra-wipe/slam/stamp/rise/fade) byte-unchanged. Plan 01 is the SOLE owner of `web/tailwind.config.ts` — Plans 03/04 only consume these aliases via className, they never edit this file.

    Edit `web/src/components/Nav.tsx`: widen `export type Screen = 'privacy' | 'desk' | 'theatre' | 'agent' | 'settlement'`; set the four TABS entries `screen:'desk'|'theatre'|'agent'|'settlement'` with `enabled:true`. Leave the active-tab style (red 3px top / ink fg) untouched.

    Create four compiling stub views — `web/src/views/DeskView.tsx`, `TheatreView.tsx`, `AgentView.tsx`, `SettlementView.tsx`. Each is the PrivacyView `<main>` frame (section marker `0N · {label}` + 1px ink rule + the binding 54px/56px display headline from the UI-SPEC: Desk "ORDERS IN THE DARK", Theatre "ONE PRICE. / NO LEAKS.", Agent "THE AGENT CLEARS THE BOOK", Settlement "ONE TRANSACTION. ALL OR NOTHING.") plus a single Inter 14px opacity-.65 placeholder paragraph. DeskView accepts `{ activeDesk }`. TheatreView/AgentView/SettlementView accept `{ roundId, phase, preview, offline, ... }` props matching the App-lifted state shape (define minimal prop types now; Plans 02–04 flesh out the bodies). These stubs exist ONLY so App routing compiles and the build stays green; Plans 02/03/04 OVERWRITE their own stub.

    Edit `web/src/App.tsx`: import the four views; widen the `screen` switch to render each (`screen==='desk' && <DeskView activeDesk={activeDesk} />`, etc.). Lift solver state per RESEARCH Pattern 8: `roundId` (default `'R1'` — the seeded round, Open Question 2 RESOLVED: Theatre drives the seeded R1), a theatre `phase` ('open'|'running'|'solving'|'cleared'|'settling'|'settled'), a `preview` (SolvePreviewResponse | null), and an `offline` boolean (set when a solver call throws SolverError.code==='OFFLINE'); pass the relevant slice to Theatre/Agent/Settlement so they share one round. Keep the existing RoundStateProbe (ctxA) feeding StatusIndicator unchanged. Do NOT add any operator @daml/react context or operator token.
  </action>
  <verify>
    <automated>cd web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` green (tsc --noEmit + vite build) with all 5 views routed.
    - `grep -n "umbraLeg" web/tailwind.config.ts` and `grep -n "'120'" web/tailwind.config.ts` both match (keyframe + hero numeral fontSize added).
    - The animation block contains the three new aliases: `grep -n "umbra-draw\|umbra-pulse\|umbra-caret" web/tailwind.config.ts` matches all three inside the `animation:` block (so `animate-umbra-draw`/`-pulse`/`-caret` are live utility classes for Plans 03/04, not dead no-ops).
    - Nav Screen union has all 5 members and all 5 TABS are `enabled:true` (`grep -c "enabled: true" web/src/components/Nav.tsx` returns 5).
    - App.tsx routes 'desk'/'theatre'/'agent'/'settlement' and declares lifted roundId/phase/preview/offline state.
    - `grep -rni "operator.*token\|createLedgerContext.*operator" web/src/App.tsx web/src/components/Nav.tsx` finds no operator token / operator context (operator plane is :4000 only).
    - Existing tailwind binding values (umbraSlam easing, the '78' headline, the '108' count, the '30' wordmark, the umbra-wipe/slam/stamp/rise/fade aliases) are byte-unchanged.
  </acceptance_criteria>
  <done>tailwind has umbraLeg + the large fontSize literals + the umbra-draw/umbra-pulse/umbra-caret/umbra-leg animation aliases (no existing binding value altered), Nav is a 5-tab enabled union, App routes all 5 views with lifted solver round/phase/preview/offline state, four stub views compile, and the build is green. No operator token anywhere in the shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → solver :4000 | Untrusted browser drives operator-authority actions via REST; the solver holds the operator token, not the browser |
| browser → JSON API :7575 | Per-party desk reads/exercises carry only that desk's own HS256 token (structural privacy) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure / Elevation | web/src/solver.ts, web/src/App.tsx, web/src/components/Nav.tsx | mitigate | Operator token NEVER imported/stored in web/; all operator actions go through :4000. Acceptance criteria grep asserts no operator token / no operator @daml/react context in the touched files. |
| T-06-02 | Information Disclosure | web/src/solver.ts SolverError | mitigate | The client surfaces only the solver's structured `{code,message}` and a fixed OFFLINE message; it never logs raw headers or the attempted URL beyond the benign base host. |
| T-06-03 | Denial of Service (graceful) | App offline flag | mitigate | A :4000 network reject → SolverError 'OFFLINE' → operator views show the offline caption; the Privacy (desk-plane) money shot still renders (no crash). |
| T-06-SC | Tampering | npm install (vitest@2.1.9 dev-only) | accept | Already used by solver/ @2.1.9 (industry-standard, RESEARCH legitimacy audit Approved); dev-dependency only, not in the shipped bundle. |
</threat_model>

<verification>
- `cd web && npm run build` green.
- `cd web && npx vitest run` green (the three §4 lib tests, incl. the allocations→balances §4-finals assertion).
- tailwind animation block exposes umbra-draw/umbra-pulse/umbra-caret (the classes Plans 03/04 use are live).
- No operator token / operator context in web/src (grep gate, comments filtered).
</verification>

<success_criteria>
solver.ts + lib helpers + vitest exist and pass the §4-value tests (incl. deskBalancesFromAllocations → §4 finals); tailwind has umbraLeg + the large fontSize literals + the umbra-draw/umbra-pulse/umbra-caret/umbra-leg animation aliases; Nav is a 5-tab enabled union; App routes all 5 views with lifted solver state; four stub views compile; build is green; no operator token in web/src.
</success_criteria>

<output>
Create `.planning/phases/06-auction-theatre-settlement-animation/06-01-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 01)

- **NEW** `web/src/solver.ts` — exports: `SOLVER_BASE_URL`, `SolverError`, `createRound`, `getRound`, `closeRound`, `solvePreview`, `settle`; types `CurvePoint`, `Allocation`, `AgentMeta`, `RoundResponse`, `SolvePreviewResponse`, `SettleResponse`, `ApiErrorBody`.
- **NEW** `web/src/lib/curve.ts` (sx/sy/crossingPoint + axis consts), `web/src/lib/balance.ts` (lerp + deskBalancesFromAllocations), `web/src/lib/solverParse.ts` (badgeLabel + parseSolvePreview) + their `.test.ts` siblings (balance.test.ts asserts the allocations→balances §4 finals, not just lerp endpoints).
- **NEW** `web/vitest.config.ts`; **MODIFIED** `web/package.json` (vitest devDep + `test` script).
- **MODIFIED** `web/tailwind.config.ts` — adds `umbraLeg` keyframe + fontSize literals 54/56/44/40/30b/34/84/120/22/18/15 + the `animation` aliases **`umbra-draw`** (`umbraDraw 1s ease forwards`), **`umbra-pulse`** (`umbraPulse 1s infinite`), **`umbra-caret`** (`umbraCaret .9s steps(1) infinite`), **`umbra-leg`** (`umbraLeg .8s linear forwards`) — these make the `animate-umbra-draw`/`-pulse`/`-caret` classes used by Plans 03/04 live (they were keyframes-only before).
- **MODIFIED** `web/src/components/Nav.tsx` — Screen union members `privacy|desk|theatre|agent|settlement`, all tabs enabled.
- **MODIFIED** `web/src/App.tsx` — routes all 5 views; lifts `roundId|phase|preview|offline`; threads `activeDesk`.
- **NEW stubs (overwritten by Plans 02–04)** `web/src/views/{DeskView,TheatreView,AgentView,SettlementView}.tsx`.
</output>
