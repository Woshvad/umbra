---
phase: 06-auction-theatre-settlement-animation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - web/src/views/DeskView.tsx
  - web/src/components/OrderTicket.tsx
  - web/src/components/HoldingsPanel.tsx
  - web/src/components/FillCard.tsx
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "The active desk's DeskView shows an order ticket (Side toggle Buy/Sell, integer Quantity, decimal Limit) that submits Venue.SubmitOrder via that desk's own useLedger and disables after submit (one order per round)"
    - "A 'load demo order' affordance pre-fills the §4 values per desk: BlueRock Buy 10@101 · Meridian Sell 8@99 · Halward Sell 5@100"
    - "The desk's own sealed order + status and its live BONDX/USDCx holdings render from that desk's own per-party queries (:7575) — never another desk's data"
    - "After settlement the desk's own TradeConfirmation renders in a YOUR FILL card (filledQty / clearingPrice 100.00 / cashMoved), sourced from that desk's own contract"
    - "Order ticket Int/Numeric args are passed to Venue.SubmitOrder as STRINGS (Pitfall 1)"
    - "Submit and reads use only the active desk's per-party ctx; no operator token / operator context appears in DeskView or its components"
  artifacts:
    - path: "web/src/views/DeskView.tsx"
      provides: "02 Desk view — mounts the active desk's ctx.DamlLedger and composes the ticket + holdings + fill"
      min_lines: 40
    - path: "web/src/components/OrderTicket.tsx"
      provides: "Side toggle + qty/limit inputs + SEAL ORDER + one-per-round lock + load-demo prefill + seal-wipe overlay"
    - path: "web/src/components/HoldingsPanel.tsx"
      provides: "live BONDX/USDCx from the desk's own Assets (sum by symbol)"
    - path: "web/src/components/FillCard.tsx"
      provides: "post-settlement YOUR FILL trio from the desk's own TradeConfirmation"
  key_links:
    - from: "web/src/components/OrderTicket.tsx"
      to: "Venue.SubmitOrder"
      via: "ctx.useLedger().exercise with Int/Numeric as strings"
      pattern: "Venue\\.SubmitOrder"
    - from: "web/src/components/HoldingsPanel.tsx"
      to: "Asset"
      via: "ctx.useStreamQueries(Asset) summed by symbol"
      pattern: "useStreamQueries\\(Asset\\)"
    - from: "web/src/components/FillCard.tsx"
      to: "TradeConfirmation"
      via: "ctx.useStreamQueries(TradeConfirmation) own contract only"
      pattern: "useStreamQueries\\(TradeConfirmation\\)"
---

<objective>
Build the 02 Desk view (UI-02): the per-party order ticket, live holdings, and post-settlement YOUR FILL card, all driven by the active desk's OWN credentials on the JSON API (:7575). Overwrites the Plan-01 DeskView stub.

Purpose: This is the desk-side half of the full money shot — each desk submits blind and, after the batch clears, sees only its own fill. Privacy is structural (per-party ctx), exactly as Phase 3 proved.
Output: `web/src/views/DeskView.tsx` + `web/src/components/{OrderTicket,HoldingsPanel,FillCard}.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-auction-theatre-settlement-animation/06-UI-SPEC.md
@.planning/phases/06-auction-theatre-settlement-animation/06-PATTERNS.md
@web/src/components/DeskColumn.tsx
@web/src/views/PrivacyView.tsx
@web/src/ledgerContexts.ts
@web/src/desks.ts
@web/src/components/OrderRow.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OrderTicket + DeskView frame + per-party mount + load-demo prefill + one-per-round lock</name>
  <files>web/src/views/DeskView.tsx, web/src/components/OrderTicket.tsx</files>
  <read_first>
    - web/src/components/DeskColumn.tsx (the EXACT analog: imports lines 17-25 — note Side from Umbra/Clearing, Venue from Umbra/Roles; submit path lines 60-81 — venue query + Venue.SubmitOrder with Int/Decimal as STRINGS + ticketLocked one-per-round; provider mount lines 212-227)
    - web/src/views/PrivacyView.tsx (the <main> section-marker frame lines 22-48 to copy: padding 30px 48px 64px → mono "02" + Inter .16em "Desk · {firm}" → 1px bg-ink rule → 54px display headline "ORDERS IN THE DARK" margin 26px 0 36px)
    - web/src/ledgerContexts.ts (ctxFor mapping lines 40-44; Ctx/DeskKey types)
    - web/src/desks.ts (tokens, httpBaseUrl, wsBaseUrl, DESKS code/role per desk)
    - .planning/phases/06-auction-theatre-settlement-animation/06-UI-SPEC.md ("02 — DESK VIEW" lines 148-170: the 2-col grid minmax(0,420px) 1fr; Order Ticket layout — Side toggle BUY #2B3AF2 / SELL #FF3D9A active fills, 44px mono borderless inputs with 2px ink underline, "Limit Price (max|min)" label, SEAL ORDER ink/paper button, SEALED + RE-OPEN ghost, load-demo affordance, umbraWipe seal overlay)
    - .planning/phases/06-auction-theatre-settlement-animation/06-PATTERNS.md (OrderTicket section lines 85-109)
  </read_first>
  <behavior>
    - The SubmitOrder arg builder produces STRING-typed quantity and limit: quantity===String(qty) (e.g. '10'), limit===Number(limit).toFixed(1) (e.g. '101.0'); side is Side.Buy | Side.Sell
    - Load-demo prefills the active desk's §4 values: bankA → {side:Buy, qty:10, limit:101}, bankB → {side:Sell, qty:8, limit:99}, bankC → {side:Sell, qty:5, limit:100}
    - ticketLocked is true once an Order exists in the desk's own stream OR after a successful submit; SEAL ORDER is disabled while locked
  </behavior>
  <action>
    Create `web/src/components/OrderTicket.tsx`. Import `Venue` from `@daml.js/umbra-0.1.0/lib/Umbra/Roles/module` and `Side` from `@daml.js/umbra-0.1.0/lib/Umbra/Clearing/module` (Side lives in Clearing — Pitfall in DeskColumn line 23). Props: `{ ctx, deskKey, order }` where `order` is the desk's existing Order payload (or undefined). Render the UI-SPEC Order Ticket: a "Order Ticket" section label; the Side toggle (1px ink box, two equal buttons with border-right between; active BUY bg #2B3AF2/#fff, active SELL bg #FF3D9A/#fff, inactive transparent/rgba(10,10,10,.45), mono 13px/700 .08em); a Quantity input (Inter 10px label + "BONDX" hint opacity .4, borderless input border-bottom 2px ink, IBM Plex Mono 44px/600 tabular, integer parse); a Limit input (label "Limit Price (max)" when BUY / "(min)" when SELL + "USDCx / unit" hint, same 44px input, decimal .toFixed(2) display). Local state for side/qty/limit. A ghost mono "load demo order" link that pre-fills the active desk's §4 values (A Buy 10@101 · B Sell 8@99 · C Sell 5@100 — derive from deskKey). SEAL ORDER button (full-width, bg ink/paper, padding 18px, mono 14px/700 .18em) → on click, query the Venue cid (ledger.query(Venue)), exercise `Venue.SubmitOrder` with `{ desk: tokens[deskKey].party, roundId: 'R1', side, quantity: String(qty), limit: Number(limit).toFixed(1) }` (Int/Numeric as STRINGS — Pitfall 1). After submit, lock. `ticketLocked = !!order || submitted`: render the SEALED state (1px ink box, bg-redact stripe swatch 34×16, "SEALED", a RE-OPEN ghost button re-enabling the ticket) and disable the inputs. Add the seal-wipe overlay: an absolute redaction-stripe panel `inset:60px 36px 0 0` with `className="animate-umbra-wipe"` on seal; gate on prefers-reduced-motion (matchMedia '(prefers-reduced-motion: reduce)') → skip the overlay, lock instantly.

    Create `web/src/views/DeskView.tsx` (overwrite the Plan-01 stub). Props `{ activeDesk }`. Copy the PrivacyView <main> frame: `<main style={{position:'relative', padding:'30px 48px 64px', overflow:'hidden'}}>` → section marker mono 13 "02" + Inter 11 uppercase .16em `Desk · {DESKS firm code}` → `<div className="bg-ink" style={{height:'1px', margin:'12px 0 0'}}/>` → `<h1 className="font-display font-bold" style={{fontSize:'54px', lineHeight:.96, letterSpacing:'-.02em', margin:'26px 0 36px'}}>ORDERS IN THE DARK</h1>`. Then a 2-col grid `gridTemplateColumns:'minmax(0,420px) 1fr', gap:0, borderTop:'1px solid #0A0A0A'`: left cell = the OrderTicket (padding 28px 36px 30px 0, border-right 1px ink), right cell = HoldingsPanel + FillCard (Task 2). Mount the body inside the active desk's `ctx.DamlLedger` provider exactly like DeskColumn lines 212-227 (`<ctx.DamlLedger token={token} party={party} httpBaseUrl={httpBaseUrl} wsBaseUrl={wsBaseUrl}>`), selecting `ctx` via `ctxFor[activeDesk]` and `token/party` via `tokens[activeDesk]`. Read the desk's own Order via `ctx.useStreamQueries(Order)` inside the provider and pass `order` to OrderTicket.
  </action>
  <verify>
    <automated>cd web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` green.
    - `grep -n "Venue.SubmitOrder" web/src/components/OrderTicket.tsx` matches; `grep -n "String(qty)\|toFixed(1)" web/src/components/OrderTicket.tsx` confirms Int/Numeric serialized as STRINGS.
    - `grep -n "Umbra/Clearing/module" web/src/components/OrderTicket.tsx` confirms Side imported from Clearing (NOT Auction).
    - DeskView headline literal is exactly "ORDERS IN THE DARK" (54px); the load-demo prefill uses the §4 per-desk values (A 10@101, B 8@99, C 5@100).
    - `grep -rni "operator" web/src/views/DeskView.tsx web/src/components/OrderTicket.tsx` finds no operator token / operator context (desk plane only).
  </acceptance_criteria>
  <done>OrderTicket submits Venue.SubmitOrder via the active desk's own ledger with Int/Numeric as strings, locks one-per-round, and offers the §4 load-demo prefill + seal-wipe; DeskView mounts the active desk's ctx and renders the frame + ticket; build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HoldingsPanel + FillCard (live holdings + post-settlement YOUR FILL)</name>
  <files>web/src/components/HoldingsPanel.tsx, web/src/components/FillCard.tsx, web/src/views/DeskView.tsx</files>
  <read_first>
    - web/src/components/DeskColumn.tsx (holdings sum lines 51-56 — filter by symbol BONDX/USDCx, reduce Number(quantity); own-contract read pattern; OrderRow usage)
    - web/src/components/OrderRow.tsx (the label/value/color/tabular row to reuse for the fill trio)
    - .planning/phases/06-auction-theatre-settlement-animation/06-UI-SPEC.md ("Right — Holdings + Fill" lines 165-170: Holdings 2-col grid 40px mono tabular + "→ {after} after settle" sub-line; YOUR FILL card 1px ink box animate-umbra-rise, ink header "YOUR FILL"/"Visible only to you", 3-col grid Filled/Clearing Price/Cash Moved at 30px mono, sign colors #2B3AF2 + / #FF3D9A −; empty state copy)
    - .planning/phases/06-auction-theatre-settlement-animation/06-RESEARCH.md (Code Examples lines 504-513 = reading TradeConfirmation own fill; Pitfall 2 = Daml numbers are STRINGS → Number(...))
    - .planning/phases/06-auction-theatre-settlement-animation/06-PATTERNS.md (HoldingsPanel/FillCard section lines 113-131)
  </read_first>
  <behavior>
    - HoldingsPanel sums the desk's own Assets by symbol: bond = Σ Number(quantity) where symbol==='BONDX', cash = Σ where symbol==='USDCx'
    - FillCard reads the desk's own TradeConfirmation: filledQty = Number(tc.filledQty), clearingPrice = Number(tc.clearingPrice) (100.00), cashMoved = Number(tc.cashMoved); fill sign color = #2B3AF2 when filledQty>0 (buy) else #FF3D9A (sell)
    - When no TradeConfirmation exists, FillCard renders the empty-state paragraph (no crash)
  </behavior>
  <action>
    Create `web/src/components/HoldingsPanel.tsx`. Props `{ ctx }` (rendered inside the desk's provider). Query `ctx.useStreamQueries(Asset)` (import Asset from `@daml.js/umbra-0.1.0/lib/Umbra/Asset/module`). Sum BONDX and USDCx via the DeskColumn lines 51-56 pattern (`Number(c.payload.quantity)`). Render the UI-SPEC Holdings block: a "Holdings" section label → a 2-col grid (1px ink top+bottom, mid border-right) showing BONDX (IBM Plex Mono 40px/600 tabular) and USDCx (40px). When a fill exists (passed from FillCard or a shared TradeConfirmation read), show the 12px mono sub-line "→ {after} after settle" (BONDX color = fill sign, USDCx opacity .6).

    Create `web/src/components/FillCard.tsx`. Props `{ ctx, deskKey }`. Query `ctx.useStreamQueries(TradeConfirmation)` (import TradeConfirmation from `@daml.js/umbra-0.1.0/lib/Umbra/Auction/module`); take `confirms.contracts[0]?.payload` (own confirmation only — privacy). Derive `filledQty/clearingPrice/cashMoved` via `Number(...)` (Daml numbers are STRINGS — Pitfall 2). When present (`hasFill`), render the YOUR FILL card per UI-SPEC: 1px ink box with `className="animate-umbra-rise"`, an ink header bar (paper text) "YOUR FILL" + "Visible only to you", a 3-col grid (ink borders) of Filled (30px mono, color #2B3AF2 if + else #FF3D9A, with a "{n} of {total} BONDX" note on partial — C is −2 of 5), Clearing Price (30px → "100.00"), Cash Moved (30px → signed). Reuse `OrderRow` styling for the value rows where it fits. When no fill, render the empty-state Inter 13px/1.6 opacity-.65 paragraph (verbatim comp copy from the UI-SPEC Copywriting table "Desk empty state").

    Wire both into DeskView's right cell (padding 28px 0 0 40px): HoldingsPanel then FillCard, both inside the active desk's provider.
  </action>
  <verify>
    <automated>cd web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` green.
    - `grep -n "useStreamQueries(Asset)" web/src/components/HoldingsPanel.tsx` and `grep -n "useStreamQueries(TradeConfirmation)" web/src/components/FillCard.tsx` both match.
    - `grep -n "Number(" web/src/components/HoldingsPanel.tsx web/src/components/FillCard.tsx` confirms Daml numbers wrapped (Pitfall 2); FillCard renders "100.00" for clearingPrice and sign-colors filledQty (#2B3AF2 / #FF3D9A).
    - FillCard has a no-fill empty state (renders the comp paragraph, no crash when contracts[0] is undefined).
    - `grep -rni "operator" web/src/components/HoldingsPanel.tsx web/src/components/FillCard.tsx` finds no operator token / operator context.
  </acceptance_criteria>
  <done>HoldingsPanel sums the desk's own BONDX/USDCx live; FillCard renders the post-settlement YOUR FILL trio (100.00 clearing, sign-colored fill, signed cash) from the desk's own TradeConfirmation with a graceful empty state; both wired into DeskView; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → JSON API :7575 | DeskView reads/exercises carry only the active desk's own HS256 token — structural per-party privacy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure / Elevation | web/src/views/DeskView.tsx, OrderTicket/HoldingsPanel/FillCard | mitigate | No operator token / operator @daml/react context in the Desk plane; all reads/exercises use the active desk's own ctx. Acceptance grep asserts no operator token in the touched files. |
| T-06-04 | Information Disclosure | DeskView per-party reads | mitigate | Reuse the Phase-3 createLedgerContext per-party providers (ctxFor[activeDesk]); the rival desk's contracts genuinely never stream into this view (no render-time filter to bypass). |
| T-06-05 | Tampering | Venue.SubmitOrder args | mitigate | The on-ledger Round.Clear re-verifies §8; browser input is UX only. Int/Numeric passed as STRINGS so the codegen decode is correct (Pitfall 1). |
</threat_model>

<verification>
- `cd web && npm run build` green.
- DeskView renders "ORDERS IN THE DARK", the ticket submits via the desk's own ledger (strings), the load-demo prefill matches §4, holdings sum the desk's own Assets, FillCard shows 100.00 from the own TradeConfirmation.
- No operator token / operator context in DeskView or its components.
</verification>

<success_criteria>
The 02 Desk view shows the order ticket (one-per-round lock + §4 load-demo), the desk's own sealed order/status + live BONDX/USDCx holdings, and — post-settlement — the desk's own TradeConfirmation in the YOUR FILL card, all on the per-party plane. Build green.
</success_criteria>

<output>
Create `.planning/phases/06-auction-theatre-settlement-animation/06-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 02)

- **NEW** `web/src/views/DeskView.tsx` (02; overwrites the Plan-01 stub) — props `{ activeDesk }`; mounts `ctxFor[activeDesk].DamlLedger` and composes the ticket + holdings + fill.
- **NEW** `web/src/components/OrderTicket.tsx` — Side toggle, qty/limit inputs, SEAL ORDER → Venue.SubmitOrder (strings), one-per-round lock, RE-OPEN, §4 load-demo prefill, umbraWipe seal overlay.
- **NEW** `web/src/components/HoldingsPanel.tsx` — live BONDX/USDCx from the desk's own Assets + "→ after settle" sub-line.
- **NEW** `web/src/components/FillCard.tsx` — post-settlement YOUR FILL trio (filledQty / 100.00 / cashMoved) from the desk's own TradeConfirmation + empty state.
- Consumes from Plan 01: the DeskView stub it overwrites; the Nav 'desk' route; the tailwind animate-umbra-wipe / animate-umbra-rise classes.
