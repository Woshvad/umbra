---
phase: 06-auction-theatre-settlement-animation
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - web/src/views/TheatreView.tsx
  - web/src/components/CountdownRing.tsx
  - web/src/components/CrossingChart.tsx
  - web/src/components/PriceReveal.tsx
autonomous: true
requirements: [UI-04, UI-05]
must_haves:
  truths:
    - "The Theatre running state shows a 60s countdown ring (CIRC=753.98, r=120, ring+numeral red at seconds<=10, auto-fires Close&Solve at 0) + live sealedOrderCount from the solver GET /round/:id"
    - "Close & Solve = POST /round/:id/close then GET /round/:id/solve-preview; an inline SOLVER-AGENT-00 COMPUTING beat is held for the real round-trip, then the uniform clearing price reveals as a hero"
    - "The price reveal renders 100.00 on the lime #D6FB3C slab (120px mono, red skew sliver) with animate-umbra-slam — the money-shot wow beat; robust to a keyless solver (price always 100.00, rationale present via deterministic fallback)"
    - "A hand-rolled SVG crossing chart (no chart lib) renders the step demand (down) + step supply (up) curves from the solve-preview curve points and marks p*=100, q=10 at the binding crossing, with the '▮ MATCHED 10 @ 100.00' legend"
    - "If the solver is offline the Theatre shows the 'SOLVER OFFLINE — START THE SERVICE ON :4000' caption (no crash); Privacy still renders"
    - "All operator actions go through web/src/solver.ts (:4000); no operator token / operator @daml/react context exists in the Theatre view or its components"
  artifacts:
    - path: "web/src/views/TheatreView.tsx"
      provides: "03 Theatre — dark inverted surface; running (ring + count + CTA) and solved (chart + reveal) states; drives close/solve-preview via solver.ts"
      min_lines: 50
    - path: "web/src/components/CountdownRing.tsx"
      provides: "hand-rolled 280x280 SVG ring, 1s setInterval 60->0, red<=10, auto-fire at 0"
    - path: "web/src/components/CrossingChart.tsx"
      provides: "hand-rolled supply/demand crossing SVG from curve points, p* marker"
    - path: "web/src/components/PriceReveal.tsx"
      provides: "lime 100.00 hero slab with umbra-slam + red skew sliver + sub-stats"
  key_links:
    - from: "web/src/views/TheatreView.tsx"
      to: "web/src/solver.ts"
      via: "closeRound then solvePreview on Close & Solve"
      pattern: "solvePreview|closeRound"
    - from: "web/src/components/CrossingChart.tsx"
      to: "solve-preview curve"
      via: "lib/curve mapping onto the binding viewBox"
      pattern: "curve"
    - from: "web/src/components/PriceReveal.tsx"
      to: "preview.clearingPrice"
      via: "100.00 lime slab"
      pattern: "clearingPrice"
---

<objective>
Build the 03 Auction Theatre (UI-04) and the hand-rolled supply/demand crossing chart (UI-05) on the operator plane (:4000): the 60s countdown ring + live sealedOrderCount, the "Close & Solve" → solve-preview reveal of the lime $100.00 hero (the first wow beat), the inline SOLVER-AGENT-00 COMPUTING beat, and the binding-geometry crossing chart marking p*=100, q=10. Overwrites the Plan-01 TheatreView stub. Dark inverted surface.

Purpose: This is the reveal half of the money shot — the operator closes the window and the uniform clearing price slams onto the lime slab. The crossing chart makes the mechanism legible.
Output: `web/src/views/TheatreView.tsx` + `web/src/components/{CountdownRing,CrossingChart,PriceReveal}.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-auction-theatre-settlement-animation/06-UI-SPEC.md
@.planning/phases/06-auction-theatre-settlement-animation/06-RESEARCH.md
@.planning/phases/06-auction-theatre-settlement-animation/06-PATTERNS.md
@web/src/views/PrivacyView.tsx
@web/src/components/VenueSpine.tsx
@web/src/solver.ts
@web/src/lib/curve.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CountdownRing + TheatreView running state (ring + sealedOrderCount + Close & Solve wiring)</name>
  <files>web/src/components/CountdownRing.tsx, web/src/views/TheatreView.tsx</files>
  <read_first>
    - .planning/phases/06-auction-theatre-settlement-animation/06-UI-SPEC.md ("03 — AUCTION THEATRE" lines 172-192: dark stage #0A0A0A/#F4F1EA, State A running flex row gap 72px; CountdownRing geometry line 179 — 280x280, viewBox rotated -90deg, track r120 stroke #F4F1EA opacity .14, progress stroke-dasharray 753.98, offset 753.98*(1-s/60), red at s<=10, 84px center numeral; right column captions + "ONE PRICE. / NO LEAKS." 56px headline + sealedOrderCount 44px + START 60s WINDOW (paper/ink) / CLOSE & SOLVE (red #E2231A/paper) CTAs)
    - .planning/phases/06-auction-theatre-settlement-animation/06-RESEARCH.md (Pattern 4 lines 323-351 = countdown setInterval + 753.98*(1-s/60) + auto-fire; Pattern 5 lines 353-374 = closeAndSolve = closeRound→solvePreview→umbraSlam; Pattern 8 = lifted App state; Pitfall 6 = timer cleanup on unmount; Pitfall 4 = use solve-preview NOT GET for the reveal; Pitfall 3 = offline guard)
    - .planning/phases/06-auction-theatre-settlement-animation/06-PATTERNS.md (TheatreView + CountdownRing lines 134-141)
    - web/src/views/PrivacyView.tsx (the <main> section-marker frame to reuse — padding 30px 48px 48px for Theatre)
    - web/src/solver.ts (getRound, closeRound, solvePreview, SolverError — Plan 01)
  </read_first>
  <action>
    Create `web/src/components/CountdownRing.tsx` (hand-rolled SVG, no in-repo analog — transcribe RESEARCH Pattern 4). Props `{ seconds }`. Constant `CIRC = 753.98` (2π·120). Render a 280×280 box, SVG `viewBox="0 0 280 280"` rotated -90deg: a track circle `cx140 cy140 r120 stroke #F4F1EA strokeOpacity .14 strokeWidth 2`; a progress circle same geometry with `strokeDasharray={CIRC} strokeDashoffset={CIRC*(1-seconds/60)}` and `style={{ transition:'stroke-dashoffset 1s linear, stroke .3s ease' }}`; ring + numeral color `#F4F1EA` normally, `#E2231A` when `seconds<=10`. Center overlay: zero-padded 2-digit seconds in IBM Plex Mono 84px/600 tabular + "Seconds to close" caption (Inter 10px .22em opacity .6).

    Create `web/src/views/TheatreView.tsx` (overwrite the Plan-01 stub). Props from the App-lifted state `{ roundId, phase, setPhase, preview, setPreview, offline, setOffline }` (match the Plan-01 App shape; the local phase machine is open|running|solving|cleared|settling|settled). Render the PrivacyView <main> frame with `padding:'30px 48px 48px'` → section marker "03 · Auction Theatre" + 1px ink rule → the dark stage `<div style={{ background:'#0A0A0A', color:'#F4F1EA', padding:'48px 56px 56px', position:'relative', overflow:'hidden', minHeight:'560px' }}>`.

    Running state (phase open|running): flex row gap 72px — the CountdownRing (Task 1) + a right column (caption mono 13px .16em opacity .6 "WINDOW READY" / "WINDOW OPEN — ORDERS LOCKED & HIDDEN"; headline "ONE PRICE. / NO LEAKS." Space Grotesk 56px/.98 -.02em two lines; the live `sealedOrderCount` 44px mono 700 tabular + "Sealed orders in the book" caption; the CTA — phase open shows START 60s WINDOW paper/ink, phase running shows CLOSE & SOLVE red #E2231A/paper, both mono 13px/700 .16em padding 16px 30px).

    Wire the clock + actions (RESEARCH Patterns 4+5): `startWindow()` sets phase 'running', seconds 60, and a 1s `setInterval` decrementing seconds; at 0 it clears the interval and fires `closeAndSolve()`. `closeAndSolve()` sets phase 'solving', then `await closeRound(roundId)`, `await solvePreview(roundId)` → setPreview + phase 'cleared'; on `SolverError.code==='OFFLINE'` setOffline(true). Fetch `sealedOrderCount` via `getRound(roundId)` (the UI-SPEC requires reading 03 sealed orders for the §4 demo). Clean up the interval on unmount (`useEffect(() => () => clearInterval(ref.current), [])` — Pitfall 6). When `offline`, render the "SOLVER OFFLINE — START THE SERVICE ON :4000" mono caption instead of the action UI (no crash).
  </action>
  <verify>
    <automated>cd web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` green.
    - `grep -n "753.98" web/src/components/CountdownRing.tsx` matches; the ring goes red at seconds<=10 and auto-fires Close&Solve at 0.
    - `grep -n "closeRound\|solvePreview" web/src/views/TheatreView.tsx` confirms Close&Solve = close then solve-preview (NOT GET for the reveal — Pitfall 4).
    - `grep -n "OFFLINE\|SOLVER OFFLINE" web/src/views/TheatreView.tsx` confirms the graceful offline caption.
    - Theatre headline literal "ONE PRICE." present; the interval is cleared on unmount (`grep -n "clearInterval" web/src/views/TheatreView.tsx`).
    - `grep -rni "operator.*token\|createLedgerContext" web/src/views/TheatreView.tsx web/src/components/CountdownRing.tsx` finds no operator token / operator context (operator plane = :4000 only).
  </acceptance_criteria>
  <done>CountdownRing renders the binding 280×280 ring (753.98, red<=10, auto-fire); TheatreView's dark running stage shows the ring + live sealedOrderCount + START/CLOSE&SOLVE CTAs, drives close→solve-preview via solver.ts, handles offline gracefully, and cleans up its timer; build green.</done>
</task>

<task type="auto">
  <name>Task 2: CrossingChart (UI-05) + PriceReveal (the wow beat) + Theatre solved state</name>
  <files>web/src/components/CrossingChart.tsx, web/src/components/PriceReveal.tsx, web/src/views/TheatreView.tsx</files>
  <read_first>
    - .planning/phases/06-auction-theatre-settlement-animation/06-UI-SPEC.md ("SVG Crossing Chart — Exact Geometry" lines 236-245 = BINDING: viewBox 0 0 480 360; axes; matched-region rect x48 y160 w248 h160 fill #D6FB3C .16; supply polyline 48,260 196,260 196,160 358,160 358,90 stroke-dasharray 640 animate-umbra-draw; demand polyline 48,110 296,110 296,320 dashed; p* rule + dropline + circle cx296 cy160 r5 #E2231A; "100.00"/"q=10"/"p*" annotations. "03 — State B SOLVING/CLEARED" lines 182-192 = chart left + reveal pane right; PriceReveal slab lines 186-189)
    - .planning/phases/06-auction-theatre-settlement-animation/06-RESEARCH.md (Pattern 6 lines 376-390 = curve→coords mapping reproducing (296,160); Code Examples lines 516-528 = the exact reveal slab markup; Pitfall 4 = solve-preview source)
    - .planning/phases/06-auction-theatre-settlement-animation/06-PATTERNS.md (CrossingChart / PriceReveal lines 134-141; CrossingChart "no analog" note lines 230-234)
    - web/src/lib/curve.ts (sx/sy/crossingPoint + axis consts from Plan 01)
    - web/src/components/VenueSpine.tsx (the closest big-numeral block analog for PriceReveal)
  </read_first>
  <action>
    Create `web/src/components/CrossingChart.tsx` (hand-rolled SVG, no chart lib — spec §6/§12.4). Props `{ curve, clearingPrice, matchedVolume }`. Render the binding `viewBox="0 0 480 360"` frame from the UI-SPEC geometry: axes (vertical x1 48 y1 20 x2 48 y2 320; horizontal x1 48 y1 320 x2 460 y2 320; stroke #F4F1EA opacity .5); the matched-region rect (x48 y160 w248 h160 fill #D6FB3C fillOpacity .16); the supply step polyline + the demand step polyline derived from the `curve` points via the `web/src/lib/curve.ts` mapping (sx/sy) so the live paths land on the binding coordinate frame; the supply curve draws on via `className="animate-umbra-draw"` (strokeDasharray 640); the red p* rule + dropline + `circle cx296 cy160 r5 fill #E2231A` marker positioned by the curve mapping at p*=clearingPrice, q=matchedVolume; the "100.00" / "q=10" / "p*" text annotations + axis caps. The §4 fixture MUST reproduce the crossing at the binding marker (296,160) — the Plan-01 curve.test.ts already asserts this mapping; render with it. Add the "▮ MATCHED 10 @ 100.00" legend (lime swatch + mono 11px) and the "SUPPLY × DEMAND" label.

    Create `web/src/components/PriceReveal.tsx`. Props `{ clearingPrice, matchedVolume }`. Render the UI-SPEC reveal pane (RESEARCH Code Examples 516-528): a "CLEARS AT" label (Space Grotesk 30px/700 .04em); the hero slab `display:inline-block; background:#D6FB3C; color:#0A0A0A; padding:10px 26px 14px; position:relative` with `className="animate-umbra-slam"`, the numeral `{clearingPrice.toFixed(2)}` (→ "100.00") in IBM Plex Mono 120px/.9 -.03em 700 tabular, and the red skew sliver `position:absolute; right:-1; top:-1; bottom:-1; width:5; background:#E2231A; transform:skewX(-12deg)`; two sub-stats (gap 40px, margin-t 26px): "Matched Volume → {matchedVolume} units" + "Uniform Price → 1 for all" (34px mono); the CTA VIEW SETTLEMENT → (paper/ink mono 13px/700 .14em) that routes to screen 'settlement'.

    Extend `web/src/views/TheatreView.tsx` solved state (phase solving|cleared|settling|settled): a 2-col grid `minmax(0,520px) 1fr; gap:56px; align-items:center` — left = CrossingChart (with the "SUPPLY × DEMAND" label + legend), right = the reveal pane. When phase 'solving', render the inline COMPUTING beat: a flame #FF6A1A 12×12 square `className="animate-umbra-pulse"` + "SOLVER-AGENT-00 COMPUTING…" (mono 18px .12em). When phase 'cleared' (preview present), render PriceReveal with `preview.clearingPrice` / `preview.matchedVolume` and CrossingChart with `preview.curve`. Reveal is robust to a keyless solver (clearingPrice always 100.00; rationale present). Ensure `animate-umbra-pulse` exists (add to tailwind animation block if only the keyframe is present — coordinate with Plan-01 additions; the umbraPulse keyframe already exists).
  </action>
  <verify>
    <automated>cd web && npm run build && cd web && npx vitest run src/lib/curve.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` green; `npx vitest run src/lib/curve.test.ts` green (the §4 crossing maps to (296,160)).
    - `grep -n "viewBox=\"0 0 480 360\"" web/src/components/CrossingChart.tsx` and `grep -n "cx.*296\|296" web/src/components/CrossingChart.tsx` confirm the binding frame + p* marker.
    - `grep -n "animate-umbra-slam" web/src/components/PriceReveal.tsx` and `grep -n "120px\|#D6FB3C" web/src/components/PriceReveal.tsx` confirm the slam + lime 120px hero slab; `clearingPrice.toFixed(2)` renders "100.00".
    - `grep -n "SOLVER-AGENT-00 COMPUTING" web/src/views/TheatreView.tsx` confirms the inline computing beat.
    - No chart library imported (`grep -rni "recharts\|d3\|chart.js" web/src/components/CrossingChart.tsx` returns nothing).
    - `grep -rni "operator" web/src/components/CrossingChart.tsx web/src/components/PriceReveal.tsx` finds no operator token / operator context.
  </acceptance_criteria>
  <done>CrossingChart renders the binding-geometry supply/demand crossing from live curve points marking p*=100/q=10 (no chart lib); PriceReveal slams the lime 100.00 hero with the red sliver; the Theatre solved state shows COMPUTING → chart + reveal; build + curve test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → solver :4000 | Theatre drives operator-authority close/solve via REST; the solver holds the operator token, not the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure / Elevation | web/src/views/TheatreView.tsx, CountdownRing/CrossingChart/PriceReveal | mitigate | No operator token / operator @daml/react context in the Theatre; all operator actions via web/src/solver.ts → :4000. Acceptance grep asserts no operator token in the touched files. |
| T-06-03 | Denial of Service (graceful) | TheatreView offline path | mitigate | A :4000 reject → SolverError 'OFFLINE' → "SOLVER OFFLINE" caption; no crash; Privacy still renders. |
| T-06-06 | Tampering | displayed clearing price | mitigate | The reveal displays the solver's returned clearingPrice; the on-ledger Round.Clear is the source of truth (verify-don't-trust). The §4 curve.test asserts the chart maps to the binding crossing (catches UI drift). |
</threat_model>

<verification>
- `cd web && npm run build` green; `npx vitest run src/lib/curve.test.ts` green.
- Theatre running shows the ring + live sealedOrderCount; Close&Solve reveals the lime 100.00 slam; the crossing chart marks p*=100/q=10; offline caption on a :4000 reject.
- No operator token / operator context in the Theatre view or its components.
</verification>

<success_criteria>
The 03 Theatre shows the 60s countdown ring + live sealedOrderCount and a Close & Solve action that triggers solve-preview and reveals the uniform $100.00 as the hero wow beat; a hand-rolled SVG crossing chart marks p* where matched volume (10) is maximized. Build green.
</success_criteria>

<output>
Create `.planning/phases/06-auction-theatre-settlement-animation/06-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 03)

- **NEW** `web/src/views/TheatreView.tsx` (03; overwrites the Plan-01 stub) — dark inverted surface; running (ring + count + START/CLOSE&SOLVE) and solved (COMPUTING → chart + reveal) states; drives close/solve-preview via solver.ts; offline-safe.
- **NEW** `web/src/components/CountdownRing.tsx` — 280×280 hand-rolled SVG ring, 1s setInterval 60→0, red≤10, auto-fire at 0.
- **NEW** `web/src/components/CrossingChart.tsx` — hand-rolled supply/demand crossing SVG from live curve points (lib/curve mapping), p*=100/q=10 marker, matched-region rect, legend. **Reused by Plan 04 (AgentView).**
- **NEW** `web/src/components/PriceReveal.tsx` — lime 100.00 hero slab (umbra-slam + red skew sliver) + sub-stats + VIEW SETTLEMENT CTA.
- Consumes from Plan 01: the TheatreView stub it overwrites; solver.ts (closeRound/solvePreview/getRound/SolverError); lib/curve; tailwind animate-umbra-slam/draw/pulse + the 56/84/120/34 fontSize literals.
