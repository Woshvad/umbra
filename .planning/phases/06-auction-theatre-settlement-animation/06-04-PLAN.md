---
phase: 06-auction-theatre-settlement-animation
plan: 04
type: execute
wave: 3
depends_on: ["06-01", "06-03"]
files_modified:
  - web/src/views/AgentView.tsx
  - web/src/components/AgentProposal.tsx
  - web/src/components/AgentRationale.tsx
  - web/src/views/SettlementView.tsx
  - web/src/components/DvpLegs.tsx
  - web/src/components/AtomicStamp.tsx
  - web/src/components/BalanceTable.tsx
autonomous: true
requirements: [UI-06]
must_haves:
  truths:
    - "The 04 Agent view shows the SOLVER-AGENT-00 proposal (Clearing Price 100.00, Matched 10, per-desk fill rows +10/−8/−2) + the NEW agent badge VERIFIED · CLAUDE | VERIFIED · DETERMINISTIC mapped from agent.source"
    - "The agent rationale renders typewriter-revealed on the ink panel with a flame caret (animate-umbra-caret, the Plan-01-provided alias), from the solver rationale (Claude or deterministic fallback); reduced-motion renders it instantly"
    - "Only the rank-1 real SOLVER-AGENT-00 row renders (competing AGENT-01/02 are stretch §19); the table markup is kept but no fabricated rows"
    - "The 05 Settlement view renders the DvP legs (A↔B 8@100, A↔C 2@100) as paired asset+cash arrows with a single '1 TRANSACTION · ATOMIC' stamp"
    - "Settle atomically (POST /round/:id/settle) plays the atomic animation: ALL legs draw on SIMULTANEOUSLY from a single rAF settleProgress (~800ms) and the before→after balances lerp in lockstep to the §4 finals A:10/4000 · B:12/1800 · C:13/1200; reduced-motion → instant"
    - "The aggregate balances/legs are derived from solver allocations via lib/balance.deskBalancesFromAllocations, with the Allocation field names verified against solver/src/auction.ts before wiring (Allocation = {desk, side:'Buy'|'Sell', filledQty})"
    - "Operator/aggregate balances come from solver.ts allocations (:4000); a desk's own row may use its own TradeConfirmation; no operator token / operator context in any of these files"
  artifacts:
    - path: "web/src/views/AgentView.tsx"
      provides: "04 Agent — proposal + rationale; reuses CrossingChart-free proposal/rationale panels"
      min_lines: 40
    - path: "web/src/components/AgentProposal.tsx"
      provides: "SOLVER-AGENT-00 proposal list + the verified/source badge"
    - path: "web/src/components/AgentRationale.tsx"
      provides: "typewriter rationale on ink panel + flame caret + rank-1 competing-agents row"
    - path: "web/src/views/SettlementView.tsx"
      provides: "05 Settlement — DvP legs + balances; drives settle via solver.ts"
      min_lines: 50
    - path: "web/src/components/DvpLegs.tsx"
      provides: "paired asset(ink)+cash(red) arrows, simultaneous single-rAF draw-on"
    - path: "web/src/components/AtomicStamp.tsx"
      provides: "the '1 TRANSACTION · ATOMIC' umbra-stamp badge"
    - path: "web/src/components/BalanceTable.tsx"
      provides: "before→after balance grid, lerp in lockstep with settleProgress"
  key_links:
    - from: "web/src/components/AgentProposal.tsx"
      to: "agent.source"
      via: "badgeLabel from lib/solverParse"
      pattern: "badgeLabel|source"
    - from: "web/src/views/SettlementView.tsx"
      to: "web/src/solver.ts"
      via: "settle(roundId) on Settle atomically"
      pattern: "settle\\("
    - from: "web/src/components/BalanceTable.tsx"
      to: "lib/balance lerp"
      via: "settleProgress lerp before→after"
      pattern: "lerp"
---

<objective>
Build the 04 Solver Agent view (proposal + typewriter rationale + the verified/source badge) and the 05 Settlement view (UI-06: DvP legs + before/after balances + the one-transaction stamp) on the operator plane (:4000). The settlement is the SECOND wow beat — all legs draw on SIMULTANEOUSLY from a single rAF clock while balances lerp to the §4 finals. Overwrites the Plan-01 AgentView + SettlementView stubs.

Purpose: This closes the money shot — after the reveal, "Settle atomically" snaps every leg at once (atomic = simultaneous, never sequenced) and the balances land on the canonical §4 numbers; the agent view narrates the clearing and shows the verify-don't-trust provenance badge.
Output: `web/src/views/{AgentView,SettlementView}.tsx` + `web/src/components/{AgentProposal,AgentRationale,DvpLegs,AtomicStamp,BalanceTable}.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-auction-theatre-settlement-animation/06-UI-SPEC.md
@.planning/phases/06-auction-theatre-settlement-animation/06-RESEARCH.md
@.planning/phases/06-auction-theatre-settlement-animation/06-PATTERNS.md
@web/src/views/PrivacyView.tsx
@web/src/components/DeskColumn.tsx
@web/src/components/OrderRow.tsx
@web/src/solver.ts
@web/src/lib/balance.ts
@web/src/lib/solverParse.ts
@solver/src/auction.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AgentView — AgentProposal (badge) + AgentRationale (typewriter)</name>
  <files>web/src/views/AgentView.tsx, web/src/components/AgentProposal.tsx, web/src/components/AgentRationale.tsx</files>
  <read_first>
    - .planning/phases/06-auction-theatre-settlement-animation/06-UI-SPEC.md ("04 — SOLVER AGENT" lines 196-210: headline "THE AGENT CLEARS THE BOOK" 54px; Left AgentProposal — flame dot + SOLVER-AGENT-00 + SELECTED tag + the NEW agent badge same tag styling ink text; proposal list Clearing Price 100.00 / Matched 10 / per-desk fill rows colored #2B3AF2/#FF3D9A/rgba(10,10,10,.4); Right AgentRationale — ink panel typewriter mono 15px/1.7 + flame caret umbra-caret ~26ms/char; rank-1-only competing-agents row, keep markup; empty state copy)
    - .planning/phases/06-auction-theatre-settlement-animation/06-RESEARCH.md (Pattern 5 startType lines 353-374; Pitfall 5 = agent.source badge map; Pitfall 6 = clear typewriter interval on unmount; Pitfall 7 = reduced-motion → instant text)
    - .planning/phases/06-auction-theatre-settlement-animation/06-PATTERNS.md (AgentView/AgentProposal/AgentRationale lines 144-151)
    - web/src/components/DeskColumn.tsx (the SELECTED-style tag lines 98-103; OrderRow list usage for the fill rows)
    - web/src/lib/solverParse.ts (badgeLabel — Plan 01)
    - web/tailwind.config.ts (the animate-umbra-caret alias — ADDED by Plan 01; this plan only CONSUMES it via className, it does NOT edit tailwind.config.ts)
    - web/src/views/PrivacyView.tsx (frame + the paragraph block for the rationale panel shell + empty state)
  </read_first>
  <action>
    Create `web/src/components/AgentProposal.tsx`. Props `{ preview }` (the SolvePreviewResponse | null from App-lifted state). Header: a flame #FF6A1A 11px dot + "SOLVER-AGENT-00" (mono 15px/600 .08em) + a `SELECTED` tag (mono 9px .12em, 1px ink border, padding 3px 7px — reuse DeskColumn lines 98-103) + the NEW agent badge using the SAME tag styling with ink text: `badgeLabel(preview.agent.source)` from lib/solverParse → "VERIFIED · CLAUDE" | "VERIFIED · DETERMINISTIC" (never invent a new color — use ink). Proposal list (1px ink top, rows border-bottom rgba(10,10,10,.16)): Clearing Price → `{preview.clearingPrice.toFixed(2)}` (22px mono) → "100.00"; Matched Volume → `{preview.matchedVolume} units` (22px); per-desk fill rows from `preview.allocations` (reuse OrderRow): code (mono 13px .06em) + signed qty colored #2B3AF2 (Buy +) / #FF3D9A (Sell −) / rgba(10,10,10,.4) (0) — §4: BLUEROCK +10 · MERIDIAN −8 · HALWARD −2.

    Create `web/src/components/AgentRationale.tsx`. Props `{ rationale }` (string | null). A "Rationale" label → an ink panel `background:#0A0A0A; color:#F4F1EA; padding:22px 24px; min-height:120px` with the rationale text typewriter-revealed (mono 15px/1.7) + a flame #FF6A1A 9×18 caret `className="animate-umbra-caret"` (the animate-umbra-caret alias is provided by Plan 01's tailwind animation block). Typewriter ≈ 26ms/char via setInterval slicing text[0..i]; if `matchMedia('(prefers-reduced-motion: reduce)').matches` render the full text instantly (Pitfall 7). Clear the interval on unmount (Pitfall 6). Below: a "Competing Agents · ranked by matched volume" label + a table rendering ONLY the rank-1 real SOLVER-AGENT-00 row (flame dot, bg rgba(255,106,26,.08), price 100.00, vol 10 u); keep the `grid 14px 1fr auto auto` table markup for stretch §19 rows but do NOT fabricate AGENT-01/02.

    Create `web/src/views/AgentView.tsx` (overwrite the Plan-01 stub). Props `{ preview, offline }`. PrivacyView <main> frame `padding:'30px 48px 64px'` → marker "04 · Solver Agent" + rule → headline "THE AGENT CLEARS THE BOOK" (54px). When `preview` present: 2-col grid `minmax(0,560px) 1fr; gap:56px; border-top:1px ink` — left AgentProposal, right AgentRationale (rationale = preview.rationale). Empty state (no preview): the Inter 14px/1.6 opacity-.65 "No proposal yet…" paragraph (verbatim UI-SPEC Copywriting). When `offline`: the "SOLVER OFFLINE — START THE SERVICE ON :4000" caption.
  </action>
  <verify>
    <automated>cd web && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` green.
    - `grep -n "badgeLabel\|VERIFIED" web/src/components/AgentProposal.tsx` confirms the agent badge maps agent.source (claude → "VERIFIED · CLAUDE", deterministic-fallback → "VERIFIED · DETERMINISTIC").
    - AgentProposal renders Clearing Price "100.00" + Matched "10 units" + the §4 fill rows (+10 / −8 / −2 colored).
    - `grep -n "animate-umbra-caret\|prefers-reduced-motion" web/src/components/AgentRationale.tsx` confirms the typewriter caret (consuming the Plan-01 alias) + reduced-motion gate; the interval is cleared on unmount.
    - Only one competing-agents data row (`grep -c "SOLVER-AGENT-00" web/src/components/AgentRationale.tsx` ≥1 and NO "SOLVER-AGENT-01" literal).
    - `grep -rni "operator" web/src/views/AgentView.tsx web/src/components/AgentProposal.tsx web/src/components/AgentRationale.tsx` finds no operator token / operator context.
  </acceptance_criteria>
  <done>AgentView shows the SOLVER-AGENT-00 proposal (100.00 / 10 / §4 fills) + the verified/source badge and the typewriter rationale (animate-umbra-caret, reduced-motion-safe, single rank-1 row); build green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SettlementView — DvpLegs + BalanceTable + AtomicStamp (the simultaneous-settle wow beat)</name>
  <files>web/src/views/SettlementView.tsx, web/src/components/DvpLegs.tsx, web/src/components/AtomicStamp.tsx, web/src/components/BalanceTable.tsx</files>
  <read_first>
    - .planning/phases/06-auction-theatre-settlement-animation/06-UI-SPEC.md ("05 — SETTLEMENT" lines 214-232: headline "ONE TRANSACTION. ALL OR NOTHING." 54px; Left DvpLegs — "Delivery vs Payment · 2 legs"; each leg grid 120px 1fr 120px; asset arrow → ink track + CSS-triangle arrowhead + "{qty} BONDX →" chip; cash arrow ← red track + label "← {cash} USDCx"; §4 leg1 MERIDIAN→BLUEROCK 8 BONDX / 800 USDCx, leg2 HALWARD→BLUEROCK 2 BONDX / 200 USDCx; the SIMULTANEOUS draw-on via single settleProgress; AtomicStamp "1 TRANSACTION · ATOMIC" umbra-stamp; SETTLE ATOMICALLY ink/paper CTA → POST settle; Right BalanceTable width 360 — before→after lerp, §4 finals A:10/4000 B:12/1800 C:13/1200)
    - .planning/phases/06-auction-theatre-settlement-animation/06-RESEARCH.md (Pattern 7 lines 392-424 = the SINGLE rAF settleProgress 0→1 over ~800ms driving ALL legs + lerp; Anti-pattern "never sequence legs"; Pitfall 6 = cancelAnimationFrame on unmount; Pitfall 7 = reduced-motion dur=0; Open Question 1 + Assumption A4 = confirm Allocation shape from auction.ts before wiring the aggregate BalanceTable/DvpLegs)
    - .planning/phases/06-auction-theatre-settlement-animation/06-PATTERNS.md (SettlementView/DvpLegs/AtomicStamp/BalanceTable lines 154-160)
    - solver/src/auction.ts (the Allocation type — lines 30-34: `Allocation = { desk: string; side: 'Buy' | 'Sell'; filledQty: number }`. VERIFY these exact field names in the file before wiring deskBalancesFromAllocations + the DvP legs, so the §4-finals derivation decodes the right fields — RESEARCH Open Question 1 / Assumption A4)
    - web/src/lib/balance.ts (lerp + deskBalancesFromAllocations — Plan 01); web/src/lib/balance.test.ts (the §4-finals assertion already proven, incl. the allocations→balances derivation)
    - web/src/solver.ts (settle, SolverError + the Allocation type mirror — Plan 01)
    - web/src/components/DeskColumn.tsx (holdings numerals / markup shell)
  </read_first>
  <behavior>
    - A single settleProgress (0→1) drives every leg's draw-on AND every balance lerp; at t=0 all legs are undrawn and balances show before; at t=1 all legs are fully drawn and balances show the §4 finals (BLUEROCK 10/4000, MERIDIAN 12/1800, HALWARD 13/1200) — legs are never sequenced
    - prefers-reduced-motion → dur=0 → instant (settleProgress jumps to 1)
    - The rAF is cancelled on unmount (no leaked clock)
  </behavior>
  <action>
    Before wiring the aggregate balances/legs, OPEN `solver/src/auction.ts` and confirm the `Allocation` field names (expected `{ desk: string; side: 'Buy' | 'Sell'; filledQty: number }`, lines 30-34). Confirm `web/src/solver.ts`'s mirrored `Allocation` type (from Plan 01) matches it, and that `lib/balance.deskBalancesFromAllocations` reads `desk`/`side`/`filledQty` (signed BONDX = +filledQty for Buy, −filledQty for Sell; signed USDCx = ∓filledQty·clearingPrice). This guards the §4-finals derivation against decoding the wrong fields (RESEARCH Open Question 1 / Assumption A4). The per-desk own row remains fully sourced via TradeConfirmation regardless.

    Create `web/src/components/DvpLegs.tsx`. Props `{ legs, settleProgress }` where each leg = { seller, buyer, qty, cash }. Render the UI-SPEC legs: container border-top 1px ink; each leg padding 24px 0 border-bottom rgba(10,10,10,.18), grid `120px 1fr 120px; gap:18px`. Asset arrow (→, ink): seller code (15px mono 600 right-aligned) · centered track height 2px background #0A0A0A with a right-pointing CSS-triangle arrowhead (border-left 9px solid #0A0A0A) + a centered chip "{qty} BONDX →" (13px mono on paper) · buyer code (15px). Cash arrow (←, red, margin-t 14px): track height 2px background #E2231A, LEFT arrowhead (border-right 9px solid #E2231A), label "← {cash} USDCx" red 13px. Drive the draw-on by `settleProgress`: scale each track's width `${settleProgress*100}%` (the comp's HTML-div approach — RESEARCH A3) — ALL legs share the one settleProgress so they snap together (never sequenced). §4 legs: leg1 MERIDIAN→BLUEROCK 8 BONDX / 800 USDCx, leg2 HALWARD→BLUEROCK 2 BONDX / 200 USDCx (derive from preview.allocations where possible).

    Create `web/src/components/AtomicStamp.tsx`. Props `{ show }`. When show, render the "1 TRANSACTION · ATOMIC" stamp `position:absolute; top/left 50%; transform:translate(-50%,-50%) rotate(-4deg); className="animate-umbra-stamp"`, box border 3px solid #E2231A, color #E2231A, padding 12px 26px, mono 22px/700 .1em, background rgba(244,241,234,.82).

    Create `web/src/components/BalanceTable.tsx`. Props `{ before, after, settleProgress }` (before/after = per-desk {code, bondx, usdcx}). Width 360px. "Balances · before → after" label → table (border-top 1px ink): header "Desk / BONDX / USDCx"; per-desk row code (mono 12px/600) + "Now" sub-label + BONDX/USDCx numerals (18px mono tabular right-aligned) computed as `lerp(before, after, settleProgress)` from lib/balance — at t=1 the §4 finals BLUEROCK 10/4000 · MERIDIAN 12/1800 · HALWARD 13/1200.

    Create `web/src/views/SettlementView.tsx` (overwrite the Plan-01 stub). Props `{ roundId, preview, phase, setPhase, offline, setOffline }`. PrivacyView <main> frame `position:relative; padding:'30px 48px 72px'; overflow:hidden` → marker "05 · Settlement Ledger" + rule → headline "ONE TRANSACTION. ALL OR NOTHING." (54px). 2-col grid `minmax(0,1fr) 360px; gap:56px; align-items:start` — left DvpLegs + AtomicStamp + the CTA, right BalanceTable. Derive `legs` and `before/after` balances from `preview.allocations` via lib/balance (`deskBalancesFromAllocations`, reading the auction.ts-verified Allocation fields); per-desk own rows may use the desk's own TradeConfirmation but the aggregate comes from allocations (:4000). Wire `settleAtomically()` (RESEARCH Pattern 7 VERBATIM): guard if already settling/settled; `await settle(roundId)` (catch SolverError 'OFFLINE' → setOffline); setPhase 'settling'; `dur = prefersReducedMotion() ? 0 : 800`; a SINGLE `requestAnimationFrame` loop advancing `settleProgress` 0→1 over `dur`; after `dur+60` set phase 'settled' + show the AtomicStamp + settleProgress 1. ALL legs + BalanceTable read the one `settleProgress` (simultaneity). Cancel the rAF on unmount (Pitfall 6). Empty state (no preview): the "Nothing to settle yet…" paragraph. Offline → the SOLVER OFFLINE caption.
  </action>
  <verify>
    <automated>cd web && npm run build && cd web && npx vitest run src/lib/balance.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd web && npm run build` green; `npx vitest run src/lib/balance.test.ts` green (the allocations→balances derivation yields the §4 finals A:10/4000 · B:12/1800 · C:13/1200, and the lerp endpoints hold).
    - `grep -n "requestAnimationFrame\|settleProgress" web/src/views/SettlementView.tsx` confirms a single rAF clock; `grep -c "requestAnimationFrame" web/src/views/SettlementView.tsx` shows ONE loop (legs are not individually animated).
    - `grep -n "settle(" web/src/views/SettlementView.tsx` confirms POST /settle via solver.ts; offline-safe; rAF cancelled on unmount (`grep -n "cancelAnimationFrame" web/src/views/SettlementView.tsx`).
    - DvpLegs renders the §4 legs (MERIDIAN→BLUEROCK 8 BONDX/800 USDCx, HALWARD→BLUEROCK 2 BONDX/200 USDCx); AtomicStamp shows "1 TRANSACTION · ATOMIC" with `animate-umbra-stamp`.
    - BalanceTable numerals lerp from before to the §4 finals via deskBalancesFromAllocations (Allocation fields confirmed against solver/src/auction.ts); reduced-motion → instant (dur=0).
    - `grep -rni "operator" web/src/views/SettlementView.tsx web/src/components/DvpLegs.tsx web/src/components/AtomicStamp.tsx web/src/components/BalanceTable.tsx` finds no operator token / operator context.
  </acceptance_criteria>
  <done>SettlementView renders the §4 DvP legs + the one-transaction stamp and plays the SIMULTANEOUS atomic settle (single rAF settleProgress drives all legs + lerping balances to A:10/4000·B:12/1800·C:13/1200, the aggregate derived from solver allocations with the Allocation shape verified against auction.ts), driving POST /settle via solver.ts; offline-safe; rAF cleaned up; build + balance test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → solver :4000 | Agent/Settlement drive operator-authority settle + read aggregate via REST; the solver holds the operator token, not the browser |
| browser → JSON API :7575 | A desk's own settlement row (TradeConfirmation) uses only that desk's own token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure / Elevation | AgentView, SettlementView + their components | mitigate | No operator token / operator @daml/react context; aggregate via web/src/solver.ts → :4000, desk-own rows via the desk ctx only. Acceptance grep asserts no operator token in the touched files. |
| T-06-06 | Tampering | displayed settled balances | mitigate | Balances/legs display the solver's allocations; the on-ledger Round.Clear is the atomic source of truth (verify-don't-trust). The §4 balance.test asserts the allocations→balances finals (catches UI drift), and the Allocation field shape is confirmed against auction.ts before wiring. |
| T-06-07 | Denial of Service (graceful) | SettlementView offline path | mitigate | A :4000 reject → SolverError 'OFFLINE' → caption; no crash; Privacy still renders. |
</threat_model>

<verification>
- `cd web && npm run build` green; `npx vitest run src/lib/balance.test.ts` green.
- Agent view shows 100.00/10/§4 fills + the verified/source badge + typewriter rationale (animate-umbra-caret); Settlement plays the simultaneous atomic settle to the §4 finals with the one-transaction stamp, the aggregate derived from solver allocations (Allocation shape verified against auction.ts).
- No operator token / operator context in any Plan-04 file.
</verification>

<success_criteria>
The 04 Agent view renders the proposal + rationale + verify-don't-trust badge; the 05 Settlement view plays the atomic-settlement animation (all legs simultaneous), shows before/after balances at the §4 finals (derived from solver allocations with the verified Allocation shape), renders the DvP legs (A↔B 8@100, A↔C 2@100), and a single one-transaction badge. Build green.
</success_criteria>

<output>
Create `.planning/phases/06-auction-theatre-settlement-animation/06-04-SUMMARY.md` when done.
</output>

## Artifacts this phase produces (Plan 04)

- **NEW** `web/src/views/AgentView.tsx` (04; overwrites the Plan-01 stub) — proposal + rationale + verified/source badge; offline-safe.
- **NEW** `web/src/components/AgentProposal.tsx` — SOLVER-AGENT-00 list (100.00 / 10 / §4 fills) + the badgeLabel(agent.source) tag.
- **NEW** `web/src/components/AgentRationale.tsx` — typewriter rationale (animate-umbra-caret, reduced-motion-safe) + rank-1-only competing-agents row.
- **NEW** `web/src/views/SettlementView.tsx` (05; overwrites the Plan-01 stub) — DvP legs + balances; single-rAF simultaneous settle; drives POST /settle; offline-safe; aggregate balances derived from solver allocations with the Allocation shape verified against solver/src/auction.ts.
- **NEW** `web/src/components/DvpLegs.tsx` — paired asset(ink)+cash(red) arrows, simultaneous settleProgress draw-on.
- **NEW** `web/src/components/AtomicStamp.tsx` — "1 TRANSACTION · ATOMIC" umbra-stamp.
- **NEW** `web/src/components/BalanceTable.tsx` — before→after lerp to the §4 finals.
- Consumes from Plan 01: lib/balance (lerp + deskBalancesFromAllocations), lib/solverParse (badgeLabel), solver.ts (settle/SolverError + the Allocation type mirror), tailwind animation aliases **animate-umbra-caret / animate-umbra-stamp** + the 22/18/15 fontSize literals. (This plan does NOT edit tailwind.config.ts — Plan 01 is the sole owner; the caret alias is now provided there.) Consumes from Plan 03: shares the operator-plane / lifted-state conventions (and the App round/phase/preview slice).
</output>
