---
phase: 07-polish-demo-acceptance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/PriceReveal.tsx
  - web/src/views/TheatreView.tsx
  - web/src/components/CrossingChart.tsx
  - web/src/components/AgentRationale.tsx
  - web/src/views/AgentView.tsx
autonomous: true
requirements: [UI-07]
must_haves:
  truths:
    - "The reveal money-shot frame matches the comp: CLEARS AT sits 4px above the lime slab; unit words render at 13px; the VIEW SETTLEMENT CTA uses comp padding/margin."
    - "The Theatre running frame matches the comp: ONE PRICE./NO LEAKS. has an 18px bottom gap; the sealed-count is an inline baseline row (count + caption) with the comp caption typography."
    - "The CrossingChart p* and q= annotations both render at fill-opacity .7 per the comp."
    - "The AgentRationale rank-1 row reads the live preview (clearingPrice/matchedVolume), so it can never silently disagree with the proposal column on a non-§4 round."
    - "web build stays green and the 11 §4-value lib tests stay green after every change."
  artifacts:
    - path: "web/src/components/PriceReveal.tsx"
      provides: "Comp-accurate reveal margins, unit-word size, CTA padding/margin"
    - path: "web/src/views/TheatreView.tsx"
      provides: "Comp-accurate headline margin + inline-baseline sealed-count row"
    - path: "web/src/components/CrossingChart.tsx"
      provides: "p* / q= annotations at fill-opacity .7"
    - path: "web/src/components/AgentRationale.tsx"
      provides: "Rank-1 competing-agents row bound to live preview"
    - path: "web/src/views/AgentView.tsx"
      provides: "Threads preview into AgentRationale"
  key_links:
    - from: "web/src/views/AgentView.tsx"
      to: "web/src/components/AgentRationale.tsx"
      via: "preview prop"
      pattern: "AgentRationale[^>]*preview"
    - from: "web/src/components/AgentRationale.tsx"
      to: "preview.clearingPrice / preview.matchedVolume"
      via: "rank-1 row numerals"
      pattern: "preview\\.(clearingPrice|matchedVolume)"
---

<objective>
Close the precise px/opacity/binding drifts the Phase-6 UI audit (`06-UI-REVIEW.md`,
scored 22/24) found between the implementation and the binding comp, taking the frontend to
**100% comp fidelity (UI-07)**. Apply the exact fixes enumerated in `07-UI-SPEC.md` (the delta
contract), then run a five-view comp sweep and fix any further real drift found.

Purpose: The two pitch screenshots (07-03) ARE the deliverable that wins; every still frame must
match the comp pixel-for-pixel. This plan is the polish pass that makes that true. It also closes
the one non-cosmetic defect (AgentRationale hardcodes §4 numbers), removing a credibility hole in
the differentiator view.

Output: Comp-accurate PriceReveal / TheatreView / CrossingChart, AgentRationale bound to the live
`preview`, and a verified five-view sweep — all with a green `web build` and green `lib` tests.

CONSTRAINT (per `07-CONTEXT.md` / `07-UI-SPEC.md`): NO logic, data-plane, animation, or copy
changes. The two wow beats (lime `umbra-slam` reveal, single-rAF simultaneous settle) and
reduced-motion gating are already correct (P6 22/24) — touch ONLY the static px / typography /
binding. Where a Phase-6 spec literal deviates from the comp, the comp wins (the governing rule).
Keep §4 values exact everywhere (100.00 / matched 10 / fills A=10·B=8·C=2 / balances
A:10/4000 · B:12/1800 · C:13/1200).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-demo-acceptance/07-UI-SPEC.md
@.planning/phases/06-auction-theatre-settlement-animation/06-UI-REVIEW.md

# Ground-truth comp (read the cited line numbers, not the whole file)
@Umbra design/Umbra.dc.html

# The files this plan edits (read before editing — match each file's own inline-style convention)
@web/src/components/PriceReveal.tsx
@web/src/views/TheatreView.tsx
@web/src/components/CrossingChart.tsx
@web/src/components/AgentRationale.tsx
@web/src/views/AgentView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: PriceReveal + CrossingChart comp-px fixes</name>
  <files>web/src/components/PriceReveal.tsx, web/src/components/CrossingChart.tsx</files>
  <action>
Apply the exact comp-px fixes from `07-UI-SPEC.md` (UI-07). Do NOT touch the lime slab, the
`animate-umbra-slam` hero, the red sliver, or any data binding — only the cited literals.

PriceReveal.tsx (comp lines 325-341):
- The "CLEARS AT" label (currently `marginBottom: '14px'`, ~line 21) → `marginBottom: '4px'` (comp 326).
- Both sub-stat unit words — the "units" span (~line 67) and the "for all" span (~line 78) — currently
  `style={{ opacity: 0.5, fontSize: '18px' }}` → `fontSize: '13px'` and `opacity: 0.6` (comp 334/338).
- The VIEW SETTLEMENT CTA: the wrapper `marginTop` (currently `34px`, ~line 84) → `30px`, and the
  button `padding` (currently `16px 30px`, ~line 92) → `15px 28px` (comp 341). This is one of the two
  audit "spec-vs-comp" flags; per the governing rule the comp wins.

CrossingChart.tsx (comp lines 313-314):
- The `p*` annotation `<text>` (~line 114) currently renders at full opacity (`fill="#F4F1EA"` with no
  fill-opacity) → add `fillOpacity={0.7}` (comp 314).
- Confirm the `q={matchedVolume}` annotation `<text>` (~line 111) already carries `fillOpacity={0.7}`
  (comp 313) — it does today; leave it. Do NOT change the axis-cap texts (QTY → / ↑ PRICE) which are
  intentionally `fillOpacity={0.5}`.

No other changes in either file.
  </action>
  <verify>
  <automated>cd web && npm run build && npx vitest run src/lib</automated>
  </verify>
  <done>
`cd web && npm run build` is green (tsc + vite) and `cd web && npx vitest run src/lib` is green
(11 §4-value tests untouched). PriceReveal "CLEARS AT" marginBottom is `4px`; both unit words are
`fontSize:'13px'` / `opacity:0.6`; the CTA wrapper marginTop is `30px` and button padding is
`15px 28px`. CrossingChart `p*` `<text>` has `fillOpacity={0.7}`; the `q=` annotation remains
`fillOpacity={0.7}`. No logic/animation/copy change in the diff.
  </done>
</task>

<task type="auto">
  <name>Task 2: TheatreView headline margin + inline-baseline sealed-count row</name>
  <files>web/src/views/TheatreView.tsx</files>
  <action>
Restructure the RunningStage sealed-count block to the comp's inline baseline row and fix the headline
bottom margin, per `07-UI-SPEC.md` (comp lines 273-288). This is layout/typography only — do NOT change
the `sealedOrderCount` data read, the clock, the CTAs' handlers, or any copy.

In `RunningStage` (~lines 160-228):
- The "ONE PRICE. / NO LEAKS." `<h2>` (~line 174) currently `margin: '14px 0 0'` → `margin: '14px 0 18px'`
  (comp 275). This restores the bottom gap before the sealed-count block.
- The sealed-count block (~lines 181-191) is currently a `marginTop:34px` wrapper with the 44px count
  stacked ABOVE the caption. Replace it with the comp's inline baseline row: a single flex container
  `style={{ display: 'flex', alignItems: 'baseline', gap: '14px', marginBottom: '30px' }}` (no marginTop
  wrapper — the gap above now comes from the headline's 18px bottom). Inside it keep the 44px count
  (`font-mono text-44 font-bold tabular-nums`, lineHeight 1) and, beside it, the caption.
- The "Sealed orders in the book" caption (~lines 185-190) currently `font-body text-10` `.16em`
  `opacity 0.6` (stacked, with `marginTop:6px`) → comp values: Inter `fontSize: '12px'`, `letterSpacing:
  '.2em'`, `opacity: 0.7`, no marginTop (it now sits on the baseline beside the count). Keep `font-body`
  (Inter) + `uppercase`; drop `text-10` in favor of an inline `fontSize:'12px'` (the comp value isn't a
  tailwind token) to match the file's inline-style convention; remove the `marginTop:'6px'`.
- The CTA row wrapper (~line 193) currently `style={{ marginTop: '34px' }}` → `style={{ display: 'flex',
  gap: '14px' }}` (comp 280). The 30px gap above the CTA now comes from the sealed block's
  `margin-bottom:30px`. The two buttons (Start 60s Window / Close & Solve) and their handlers/copy are
  UNCHANGED.

No change to the dark-stage wrapper, CountdownRing, SolvedStage, or OfflineCaption.
  </action>
  <verify>
  <automated>cd web && npm run build && npx vitest run src/lib</automated>
  </verify>
  <done>
`cd web && npm run build` green; `cd web && npx vitest run src/lib` green. The headline `<h2>` margin
is `14px 0 18px`; the sealed-count is an inline baseline flex row (`align-items:baseline; gap:14px;
margin-bottom:30px`) with the 44px count and the caption at Inter `12px / .2em / opacity .7`; the CTA
row is `display:flex; gap:14px` (no marginTop wrapper). `sealedOrderCount` data read, clock, CTA
handlers, and all copy unchanged in the diff.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bind AgentRationale rank-1 row to live preview + five-view comp sweep</name>
  <files>web/src/components/AgentRationale.tsx, web/src/views/AgentView.tsx</files>
  <action>
Part A — the one non-cosmetic fix (audit Top-3 #1, `AgentRationale.tsx:124-125`):
- `AgentView.tsx` already holds the lifted `preview` (`OperatorViewState`). Thread it into the rationale
  component: change the render to `<AgentRationale rationale={preview.rationale} preview={preview} />`
  (this branch only runs when `preview` is truthy, ~line 51).
- In `AgentRationale.tsx`, add `preview` to `Props`. Use the SolvePreviewResponse type already imported
  by the operator plane (mirror how `AgentProposal` / the operator state type the lifted preview — import
  the same `SolvePreviewResponse`/preview type, do not invent a new shape). Make the prop optional/nullable
  so the component never crashes if it is absent.
- Bind the rank-1 competing-agents row numerals (currently hardcoded `100.00` and `10 u`, lines 124-125):
  the price `<span>` → `{(preview?.clearingPrice ?? 100).toFixed(2)}`; the volume `<span>` →
  `{preview?.matchedVolume ?? 10} u`. The deterministic §4 fallback (100.00 / 10) preserves the current
  behavior when preview is null and keeps the panel honest on the §4 fixture. Do NOT change the typewriter
  rationale block, the flame caret, the reduced-motion gating, or any copy/spacing.

Part B — five-view comp sweep (UI-07 covers all five numbered views, not just the three audited in P6).
Re-verify each view's STILL FRAME against the binding comp (`Umbra design/Umbra.dc.html`) and fix only
REAL drift found, at the same standard (exact token/px/letter-spacing/opacity), matching each file's
existing inline-style convention:
- 01 Privacy (`PrivacyView.tsx` + DeskColumn / SealedRail / RedactionBar): re-check the redaction motif,
  the 3-up column rhythm, and the center venue-count panel vs the comp.
- 02 Desk (`DeskView.tsx` + OrderTicket / HoldingsPanel / FillCard): re-check the order ticket, holdings,
  and post-settle fill card vs the comp.
- 03/04/05 (Theatre/Agent/Settlement): the targeted fixes above land here; spot-check the rest stays
  on-comp.
- CountdownRing "Seconds to close" label: confirm Inter `10px; .22em; opacity .6; margin-top 6px`
  (comp 270); fix if drifted.
If a view is already on-comp, make NO change to it (do not gold-plate). Record in the SUMMARY which views
were swept and whether any additional drift was found+fixed. Keep §4 values exact and on-comp everywhere.
  </action>
  <verify>
  <automated>cd web && npm run build && npx vitest run src/lib</automated>
  </verify>
  <done>
`cd web && npm run build` green; `cd web && npx vitest run src/lib` green. `AgentView` passes `preview`
into `AgentRationale`; the rank-1 row numerals read `preview.clearingPrice.toFixed(2)` /
`preview.matchedVolume` with a §4 (100.00 / 10) fallback and never crash when preview is null. The
five-view sweep is complete with each view re-verified against the comp; any real drift found is fixed
to the comp values and the SUMMARY lists the sweep result. No animation/copy/logic/data-plane change in
the diff.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → JSON API (:7575) | desk-token plane; UI-only edits must not change which token a component holds |
| browser → solver (:4000) | operator plane via `solver.ts`; this plan touches presentation, not the data read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information disclosure | AgentRationale preview binding | mitigate | `preview` is the already-lifted operator-plane SolvePreviewResponse (no new fetch, no token) — bind to the existing prop only; never introduce a desk/operator token into a presentation component |
| T-07-02 | Tampering | §4 fixture values in UI | accept | Fallbacks stay exactly 100.00 / 10; the `lib` §4-value tests + `web build` guard against numeral drift |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO packages (UI-only edits to existing files); no supply-chain surface introduced |
</threat_model>

<verification>
- `cd web && npm run build` green (tsc typecheck + vite bundle) after EVERY task.
- `cd web && npx vitest run src/lib` green (the 11 §4-value pure-helper tests — none of these px/binding
  fixes touch the pure helpers, so they must remain untouched and green).
- Each cited comp value present in the diff (the specific px/opacity/binding change for the task).
- The AgentRationale binding flag from `06-UI-REVIEW.md` is cleared (rank-1 row reads `preview`).
- Visual fidelity (UI-07 → target 24/24) is confirmed by the 07-03 acceptance screenshots + a `gsd-ui-review`
  re-audit; this plan's automated gate is the green build + green lib tests + the diff check.
</verification>

<success_criteria>
- All three `07-UI-SPEC.md` targeted fixes applied at the comp px/opacity/binding values.
- AgentRationale rank-1 row bound to the live `preview` (never disagrees with the proposal column;
  never crashes when preview is null).
- Five-view comp sweep complete; any real drift fixed to the comp; §4 values exact and on-comp.
- `web build` + `lib` tests green; no logic/data-plane/animation/copy change.
</success_criteria>

<output>
Create `.planning/phases/07-polish-demo-acceptance/07-01-SUMMARY.md` when done (list the exact px/binding
changes per file and the five-view sweep result).
</output>
