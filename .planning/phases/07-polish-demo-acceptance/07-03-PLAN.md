---
phase: 07-polish-demo-acceptance
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - docs/01-privacy-3up.png
  - docs/02-atomic-settlement.png
  - docs/DEMO.md
autonomous: true
execution: orchestrator-run
requirements: [DEMO-02, DEMO-03, DEMO-04]
must_haves:
  truths:
    - "All six Daml Script tests pass (test_clears_at_100, test_settled_balances, test_atomicity, test_privacy_orders, test_privacy_confirmations, test_clear_rejects_bad_allocation) and the output is captured."
    - "The live E2E acceptance flow runs: solve-preview returns clearingPrice==100.00 + matchedVolume==10 + a rationale; settle returns Settled; each desk's /v1/query sees only its own Order/TradeConfirmation; the aggregate legs are A↔B 8@100 + A↔C 2@100; balances finalize A:10/4000·B:12/1800·C:13/1200; a second settle returns 409."
    - "docs/01-privacy-3up.png (3-up blindness) and docs/02-atomic-settlement.png (atomic settle) are captured from the polished live UI."
    - "docs/DEMO.md is a 3-minute demo script (setup → click-path → talking points → §4 numbers)."
  artifacts:
    - path: "docs/01-privacy-3up.png"
      provides: "The 3-up blindness pitch screenshot"
    - path: "docs/02-atomic-settlement.png"
      provides: "The atomic-settlement pitch screenshot"
    - path: "docs/DEMO.md"
      provides: "3-minute demo script"
  key_links:
    - from: "POST /round/:id/close → GET /round/:id/solve-preview"
      to: "clearingPrice==100.00 / matchedVolume==10 / rationale present"
      via: "solver :4000 the same path the UI drives"
      pattern: "clearingPrice.*100"
    - from: "POST /round/:id/settle (twice)"
      to: "Settled then 409"
      via: "double-settle guard"
      pattern: "409|ALREADY_SETTLED"
---

<objective>
Prove Umbra works end-to-end (DEMO-02/03/04): run the six Daml Script tests, drive the LIVE E2E
acceptance flow against the booted stack, and capture the two pitch screenshots + the 3-minute demo
script.

Purpose: This is the de-risking + acceptance pass — the live run is the authoritative proof of the money
shot (privacy → clear at $100.00 → atomic DvP), and the two screenshots ARE the deliverable that wins.

Output: captured `daml test` output (6/6), the live API/ledger assertion results, `docs/01-privacy-3up.png`,
`docs/02-atomic-settlement.png`, and `docs/DEMO.md`.

⚠ **ORCHESTRATOR-RUN — DO NOT DELEGATE TO A SUBAGENT.** The parent agent executes this plan DIRECTLY.
The live ledger + long-running background processes (`daml start` :7575 / solver :4000 / Vite :5173) and
the browser-preview screenshot tooling cannot be held inside a gsd-executor subagent. Boot the stack at
the top level, drive the assertions yourself, and capture the screenshots yourself.

CONSTRAINT (per `07-CONTEXT.md`): the Daml / solver / web logic + data planes are FROZEN. The ONLY
in-scope code change is fixing a GENUINE wiring bug if the live E2E surfaces one (that is what acceptance
is for) — and if so, fix the minimal real bug, re-run, and record it. Keep §4 values exact everywhere.
Privacy invariants intact (desk tokens only in `web/src/tokens.json`; operator token only in
`scripts/.operator-token`; Anthropic key only in `solver/`). No Claude git attribution. The two
screenshots show the §4 fixture only — no secrets in frame.
</objective>

<execution_context>
Run by the orchestrator at the top level (NOT via execute-plan.md subagent dispatch).
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-polish-demo-acceptance/07-RESEARCH.md
@.planning/phases/07-polish-demo-acceptance/07-CONTEXT.md

# Anchors for the assertions + the boot sequence
@scripts/mint-tokens.mjs
@scripts/verify-privacy.mjs
@solver/src/api.ts
@spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DEMO-02 — run the six Daml Script tests, capture 6/6 pass</name>
  <files>(no committed file — capture output into the SUMMARY)</files>
  <action>
Run `cd daml && daml test`. This is self-contained (does NOT need `daml start` running). Assert all six
named acceptance scripts pass:
- `test_clears_at_100` — §4 clears at p*=100.00, fills A=10/B=8/C=2
- `test_settled_balances` — finals A:10/4000 · B:12/1800 · C:13/1200, conservation
- `test_atomicity` — a failing leg → whole `Round.Clear` fails, no balance change
- `test_privacy_orders` — BankA's query returns only BankA's Order (zero B/C)
- `test_privacy_confirmations` — a `TradeConfirmation` is observed only by its desk
- `test_clear_rejects_bad_allocation` — bad allocation rejected
(The suite also runs the two P1 tests `test_setup_seeds` / `test_asset_split_merge`; those should pass
too, but the six above are the DEMO-02 gate.) Capture the pass/fail line for each into the SUMMARY. The
Daml layer is frozen — this is a confirmation gate; if any test FAILS, treat it as a genuine regression,
stop, and report it (do not paper over it).
  </action>
  <verify>
  <automated>cd daml && daml test 2>&1 | tee /tmp/umbra-damltest.log; grep -Eq 'test_clears_at_100' /tmp/umbra-damltest.log</automated>
  </verify>
  <done>
`cd daml && daml test` completes; all six named scripts (test_clears_at_100, test_settled_balances,
test_atomicity, test_privacy_orders, test_privacy_confirmations, test_clear_rejects_bad_allocation) report
pass; the per-test result is captured in the SUMMARY. No failures (or, if any, reported as a regression
with the failing assertion).
  </done>
</task>

<task type="auto">
  <name>Task 2: DEMO-03 — boot the live stack and drive the E2E acceptance assertions</name>
  <files>(no committed file — capture assertion results into the SUMMARY)</files>
  <action>
Boot the full stack at the TOP LEVEL and drive the acceptance flow live (07-RESEARCH §C). Background the
long-running processes; capture each assertion result.

Boot sequence (hard ordering):
1. `cd daml && daml start` in the background; WAIT for the JSON API on :7575 to answer and for
   `daml/parties.json` to be written (seedOpenRound seeds the §4 Open round `R1` + RoundStats{count=3} +
   3 sealed orders).
2. `node scripts/mint-tokens.mjs` — writes desk tokens → `web/src/tokens.json`, operator token →
   `scripts/.operator-token`. (REQUIRES parties.json.)
3. `cd solver && npm run dev` in the background; WAIT for :4000.
4. `cd web && npm run dev` in the background; :5173 (needed for Task 3 screenshots).

API/ledger assertions (curl/fetch — the AUTHORITATIVE proof, the same path the UI drives). The seeded
round id is `"R1"`. Prefer the FRESH-SUBMIT path to also prove "three desks submit blind": `POST :4000/round`
to open a fresh round, then submit the three §4 orders per-party via the JSON API (BankA Buy 10 @101,
BankB Sell 8 @99, BankC Sell 5 @100) as each desk using its OWN token — this exercises `Venue.SubmitOrder`.
If the fresh round's ids do not cooperate, fall back to driving the SEEDED round `R1` straight through
close→solve→settle (the minimal honest money-shot proof). Either path, assert:
- `GET :4000/round/<id>` → `sealedOrderCount` reflects the book (3 for the §4 batch).
- Per-party `/v1/query` on :7575 with EACH desk token → each desk sees ONLY its own `Order` (zero rivals)
  — mirror `scripts/verify-privacy.mjs` (you may just run `node scripts/verify-privacy.mjs` for the seeded
  round, which asserts BankA sees only its own Order with no B/C leak).
- `POST :4000/round/<id>/close` → status Closed.
- `GET :4000/round/<id>/solve-preview` → assert `clearingPrice === 100.00`, `matchedVolume === 10`,
  `rationale` present (non-null), and `agent:{verified, source}` present.
- `POST :4000/round/<id>/settle` → assert `status === "Settled"`.
- Re-query per-party → each desk sees ONLY its own `TradeConfirmation`; the aggregate (operator/ledger
  view) shows legs A↔B 8@100 + A↔C 2@100; balances finalize A:10/4000 · B:12/1800 · C:13/1200.
- `POST :4000/round/<id>/settle` AGAIN → assert 409 (`ALREADY_SETTLED`, the double-settle guard).

If a GENUINE wiring bug surfaces (in scope per CONTEXT), fix the minimal real cause in the live layer,
re-run the affected assertions, and record the bug + fix in the SUMMARY. Capture every assertion's actual
value (not just pass/fail). Keep the stack UP for Task 3 (screenshots), then tear down the background
processes when done.
  </action>
  <verify>
  <automated>node scripts/verify-privacy.mjs 2>&1 | tee /tmp/umbra-privacy.log; grep -q 'PASS (PRIV-05)' /tmp/umbra-privacy.log</automated>
  </verify>
  <done>
The live stack booted (:7575 / :4000 / :5173). solve-preview returned clearingPrice 100.00 + matchedVolume
10 + a present rationale + agent provenance; settle returned Settled; each desk's `/v1/query` saw only its
own Order and only its own TradeConfirmation (verify-privacy PASS); the aggregate legs were A↔B 8@100 +
A↔C 2@100; balances finalized A:10/4000·B:12/1800·C:13/1200; the second settle returned 409. All actual
values are captured in the SUMMARY. Any genuine wiring bug found is fixed + re-run + recorded.
  </done>
</task>

<task type="auto">
  <name>Task 3: DEMO-04 — capture the two pitch screenshots + write docs/DEMO.md</name>
  <files>docs/01-privacy-3up.png, docs/02-atomic-settlement.png, docs/DEMO.md</files>
  <action>
With the stack from Task 2 still UP and the POLISHED UI (07-01) live, capture the two pitch screenshots
into a new `docs/` directory, then write the 3-minute demo script.

Screenshots (use the `Claude_Preview` MCP — `preview_start` + `preview_screenshot` — the sanctioned
preview path; try it FIRST):
- `docs/01-privacy-3up.png` — the 01 Privacy view's 3-up blindness (three desk columns, each on its own
  desk token, redaction motif on the "other" columns, the center venue-count panel).
- `docs/02-atomic-settlement.png` — the 05 Settlement view after the atomic settle (the DvP legs A↔B 8@100
  + A↔C 2@100, before/after balances at the §4 finals, the "1 TRANSACTION · ATOMIC" stamp).

KNOWN ENVIRONMENT RISK (07-RESEARCH / Phases 1–3): the Privacy view holds a persistent `@daml/react`
JSON-API **WebSocket**; headless PNG capture that waits for network-idle TIMED OUT there (the app
rendered fine; DOM + computed-style verified). Mitigation order: (1) `Claude_Preview` MCP first (it may
capture without waiting for net-idle); (2) the Theatre/Settlement views use the :4000 fetch plane (NO WS)
and should capture cleanly — capture `02-atomic-settlement.png` cleanly regardless; (3) if the WS-heavy
Privacy 3-up capture is environmentally BLOCKED, capture it via the best available route + document the
block precisely with DOM/computed-style evidence and a best-effort frame. NEVER fake a screenshot —
honest reporting. The frame must show the §4 fixture only (no tokens/secrets visible).

`docs/DEMO.md` — a 3-minute demo script (analog: `spec.md` §17/§18 narrative + the CONTEXT money-shot
description). Structure:
- The one-line setup (what the audience is about to see).
- The click-path: Privacy (3 desks blind to each other) → Desk submits the §4 order → Theatre "Close &
  Solve" reveals **100.00** with the verified rationale → Settlement "Settle atomically" → each desk sees
  only its own fill, the operator sees the aggregate.
- The talking points: sealed-bid privacy (enforced at the wire, not in render) → AI solver +
  verify-don't-trust (the deterministic §8 core + on-ledger `Round.Clear` re-verify are authoritative) →
  atomic DvP (one Canton transaction, all-or-nothing).
- The §4 numbers to call out: clears at **$100.00**, matched **10**, fills A=10·B=8·C=2, legs A↔B 8@100 +
  A↔C 2@100, finals A:10/4000 · B:12/1800 · C:13/1200.

Commit `docs/` (the two PNGs + DEMO.md) — author/committer `woshvad <woshvad@gmail.com>`, zero Claude
attribution. (`docs/` content is the §4 fixture only; no secret in frame.)
  </action>
  <verify>
  <automated>test -f docs/DEMO.md && grep -q '100.00' docs/DEMO.md && ls docs/*.png 2>/dev/null | head</automated>
  </verify>
  <done>
`docs/01-privacy-3up.png` + `docs/02-atomic-settlement.png` are captured from the polished live UI (or, if
the WS-heavy Privacy capture is environmentally blocked, captured via the best route + documented precisely
with evidence — never faked). `docs/DEMO.md` is a 3-minute script with the setup line, the click-path, the
talking points, and the §4 numbers (incl. 100.00). No secret in any frame. Committed with no Claude
attribution.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → JSON API (:7575) | desk-token plane; the live run must prove per-party isolation at the wire |
| browser → solver (:4000) | operator plane; the solver holds the operator token + Anthropic key, never the browser |
| screenshots → docs/ (committed) | a captured frame must not leak any token/secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information disclosure | per-party /v1/query | mitigate | The live assertion IS the privacy proof — each desk token returns only its own Order/TradeConfirmation; verify-privacy.mjs fails the run if any rival row leaks |
| T-07-06 | Information disclosure | committed docs/ screenshots | mitigate | Frames show the §4 fixture UI only; no token/key/parties.json visible; verify no secret in frame before commit |
| T-07-07 | Tampering | acceptance numbers | mitigate | Assert clearingPrice==100.00 / matched==10 / legs 8@100+2@100 / finals §4 / 409 — the deterministic core + on-ledger re-verify are authoritative; a mismatch fails acceptance |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO new packages (it runs existing scripts/services + the preview MCP); no new supply-chain surface |
</threat_model>

<verification>
- DEMO-02: `cd daml && daml test` → 6/6 named scripts pass (captured).
- DEMO-03: the live API/ledger assertions hold — clearingPrice 100.00, matchedVolume 10, Settled, per-desk
  Order + TradeConfirmation privacy (verify-privacy PASS), aggregate legs A↔B 8@100 + A↔C 2@100, balances
  A:10/4000·B:12/1800·C:13/1200, double-settle 409. Actual values captured.
- DEMO-04: `docs/01-privacy-3up.png` + `docs/02-atomic-settlement.png` present (or the Privacy capture
  documented as environment-blocked with evidence + best-effort frame); `docs/DEMO.md` present with the
  3-minute script and the §4 numbers.
- Privacy invariants intact; no secret in any committed frame; no Claude git attribution.
</verification>

<success_criteria>
- 6/6 Daml Script tests pass (DEMO-02).
- The live E2E acceptance flow runs and every §4 assertion holds, including the 409 double-settle guard
  (DEMO-03).
- The two pitch screenshots are in `docs/` (or the WS-blocked Privacy capture is honestly documented) and
  `docs/DEMO.md` (3-minute script) is written (DEMO-04).
</success_criteria>

<output>
Create `.planning/phases/07-polish-demo-acceptance/07-03-SUMMARY.md` when done — capture the 6/6 daml test
result, every live assertion's actual value, the screenshot-capture outcome (incl. any documented
WS-block + evidence), and any genuine wiring bug found+fixed.
</output>
