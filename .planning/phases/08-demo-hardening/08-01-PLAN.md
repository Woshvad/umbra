---
phase: 08-demo-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/solver.ts
  - web/src/views/AgentView.tsx
  - web/src/views/SettlementView.tsx
  - web/src/views/TheatreView.tsx
  - web/src/components/AgentProposal.tsx
  - solver/src/index.ts
  - .github/workflows/ci.yml
  - .gitignore
  - web/.env.example
autonomous: true
requirements: [TRUST-01]
must_haves:
  truths:
    - "Every solver-plane offline caption and request URL in web/src displays the LIVE configured port (:4100), never the stale :4000 literal"
    - "A CI workflow runs the §8 golden vitest suite on every push/PR touching solver/ or daml/ and is green on the $100.00 / A=10·B=8·C=2 case"
    - "solver/proofs/ is gitignored so no per-round proof bundle (nor the API key) can ever be committed"
    - "A fresh clone has web/.env.example documenting VITE_SOLVER_URL=http://localhost:4100"
    - "The §4 fixture still clears at exactly $100.00 (no solver behavior changed — captions/config only)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "TRUST-01 CI golden-eval gate (solver vitest always + daml test job)"
      contains: "vitest"
    - path: "web/.env.example"
      provides: "documented live solver port for a fresh clone"
      contains: "VITE_SOLVER_URL"
    - path: "web/src/solver.ts"
      provides: "port-derived OFFLINE caption + :4100 fallback (single source of truth)"
  key_links:
    - from: "web/src/solver.ts (SOLVER_BASE_URL → derived port caption)"
      to: "web/src/views/AgentView.tsx + web/src/views/SettlementView.tsx + web/src/views/TheatreView.tsx"
      via: "imported OFFLINE_CAPTION constant"
      pattern: "OFFLINE_CAPTION"
---

<objective>
Lay the clean Wave-1 base every later Phase-8 wave lands on: kill the shipped `:4000`→`:4100` port-drift bug (a lie on screen — the solver runs on :4100), stand up the TRUST-01 CI golden-eval gate (none exists yet), gitignore the new `solver/proofs/` decision-bundle dir, and document the live solver port for a fresh clone.

Purpose: The port literals currently render `:4000` in offline captions and the fetch fallback even though the live solver binds `:4100` (RESEARCH § Runtime State Inventory). Later WOW surfaces show request URLs and offline states — those must be truthful. The stale literal appears in web/src across FIVE files (13 occurrences): the functional fallback + four RENDERED on-screen offline captions (including the WOW-02 Theatre/operator surface) + comment sites — ALL must be zero for the port-drift gate to pass. TRUST-01 requires a NEW `.github/workflows/ci.yml` running the §8 fixtures as a regression gate.
Output: Truthful ports across `web/src`, a green two-job CI workflow, `solver/proofs/` gitignored, `web/.env.example`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-demo-hardening/08-RESEARCH.md
@.planning/phases/08-demo-hardening/08-VALIDATION.md
@.planning/phases/08-demo-hardening/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the :4000→:4100 port drift across all web/src by reading the configured port (no second literal)</name>
  <files>web/src/solver.ts, web/src/views/AgentView.tsx, web/src/views/SettlementView.tsx, web/src/views/TheatreView.tsx, web/src/components/AgentProposal.tsx, solver/src/index.ts</files>
  <read_first>
    - web/src/solver.ts (SOLVER_BASE_URL fallback at L16; the OFFLINE caption in call() at L101; stale port comment L1)
    - web/src/views/AgentView.tsx (rendered offline caption L39; stale port comment L2)
    - web/src/views/SettlementView.tsx (rendered offline caption L154; stale port comments L2/L14/L69)
    - web/src/views/TheatreView.tsx (rendered OfflineCaption() component L286 — the WOW-02 Theatre/operator demo surface; stale port comments L2/L14)
    - web/src/components/AgentProposal.tsx (stale port comment L13)
    - solver/src/index.ts (DEFAULT_SOLVER_PORT = 4000 at L26; stale ":4000 bind" comment)
    - 08-RESEARCH.md § "Runtime State Inventory" (the stale :4000 literal sites across web/src) and § "State of the Art" (solver moved :4000→:4100)
    - 08-UI-SPEC.md Reconciliation Note 2 + the Copywriting "Offline caption (all solver-plane surfaces)" row (must reflect the live {live port}, do not hard-code a second port)
  </read_first>
  <action>
    In web/src/solver.ts: change the SOLVER_BASE_URL fallback at L16 from 'http://localhost:4000' to 'http://localhost:4100' — this URL default is the SINGLE source of truth for the port (VITE_SOLVER_URL still overrides). Derive the port ONCE from SOLVER_BASE_URL — add an exported `solverPort` (parse `new URL(SOLVER_BASE_URL).port`, default '4100' inside a try/catch) and an exported `OFFLINE_CAPTION` = the template `SOLVER OFFLINE — START THE SERVICE ON :${solverPort}` (UI-SPEC Copywriting "Offline caption" row — {live port}). Replace the hard-coded ':4000' string thrown by call()'s catch at L101 (SolverError OFFLINE) with OFFLINE_CAPTION. Do NOT introduce a second ':4100' string literal in ANY caption — every caption must READ the derived port (UI-SPEC Reconciliation Note 2 / RESEARCH Anti-Pattern "hard-coding a second port literal"). Scrub the stale ':4000' comment at L1.
    In web/src/views/AgentView.tsx (rendered caption L39), web/src/views/SettlementView.tsx (rendered caption L154), and web/src/views/TheatreView.tsx (rendered OfflineCaption() at L286 — the WOW-02 operator/Theatre surface): import OFFLINE_CAPTION from '../solver' and render it in place of each verbatim 'SOLVER OFFLINE — START THE SERVICE ON :4000' paragraph. Do not otherwise change the shipped layout, headline, or grid on any of the three views.
    Scrub the remaining stale ':4000' port comments so the web grep gate is clean: solver.ts L1, AgentView L2, SettlementView L2/L14/L69, TheatreView L2/L14, and AgentProposal.tsx L13 — update each ':4000' reference to ':4100'. Leave unrelated data literals untouched (e.g. SettlementView's '10/4000' order-size values are '/4000', not ':4000' — the gate matches ':4000' only, so do not disturb them).
    In solver/src/index.ts: change DEFAULT_SOLVER_PORT from 4000 to 4100 (SOLVER_PORT env still overrides). Update the stale ":4000 bind" comment to :4100. Change NOTHING else in index.ts (buildDeps/main untouched — later waves extend it).
    Leave the §8 math, ledger I/O, and settle path byte-unchanged — this task is captions + config + comment scrubs only.
  </action>
  <acceptance_criteria>
    - `grep -rn ':4000' web/src` returns NOTHING (exit non-zero) — the port-drift lint gate (08-VALIDATION.md row "no :4000 literal remains in web/src") is repo-wide over web/src (all 5 files: solver.ts, AgentView, SettlementView, TheatreView, AgentProposal)
    - All four rendered offline captions (solver.ts L101, AgentView L39, SettlementView L154, TheatreView L286) render the imported OFFLINE_CAPTION — none contains a literal port digit
    - `cd web && npm run build` passes (tsc --noEmit + vite build) with the imported OFFLINE_CAPTION
    - `grep -n '4100' solver/src/index.ts` shows DEFAULT_SOLVER_PORT = 4100
    - The OFFLINE caption in web/src/solver.ts is produced from the derived `solverPort`, not a duplicated port string
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run build && ! grep -rn ':4000' src</automated>
  </verify>
  <done>Every `:4000` in web/src is gone (grep gate clean): the functional fallback (solver.ts L16) defaults to :4100 and is the single port source, the four rendered offline captions (solver.ts L101, AgentView L39, SettlementView L154, TheatreView L286) render the derived OFFLINE_CAPTION, the stale port comments (solver.ts L1, AgentView L2, SettlementView L2/L14/L69, TheatreView L2/L14, AgentProposal L13) are scrubbed, and solver/src/index.ts DEFAULT_SOLVER_PORT = 4100; web builds.</done>
</task>

<task type="auto">
  <name>Task 2: TRUST-01 CI golden-eval workflow + gitignore solver/proofs/ + web/.env.example</name>
  <files>.github/workflows/ci.yml, .gitignore, web/.env.example</files>
  <read_first>
    - 08-RESEARCH.md § "Validation Architecture" (framework vitest 2.1.9 · commands) + § "Open Questions" Q1 (two-job CI: golden always, daml test SDK-install job) + § "Environment Availability" (Daml SDK not on CI — split job)
    - 08-PATTERNS.md § ".github/workflows/ci.yml (NEW) — NO in-repo analog" (golden job `cd solver && npm ci && npm test`; daml job installs SDK then `daml build && daml test`; triggers on solver/ or daml/ changes)
    - package.json (root scripts: `solver:test` → cd solver && npm test; `test` → cd daml && daml test) and solver/package.json (`test` → vitest run)
    - .gitignore (verify solver/proofs/ is NOT currently ignored) and solver/.env presence note in RESEARCH § Runtime State Inventory
  </read_first>
  <action>
    Create .github/workflows/ci.yml with `on: push` and `on: pull_request` filtered to `paths: ['solver/**', 'daml/**', '.github/workflows/ci.yml']`. Two jobs:
    (1) `golden` — runs on ubuntu-latest ALWAYS: checkout, setup-node@v4 (node 20), `cd solver && npm ci && npm test` (the vitest golden regression gate — the §4 fixture clears $100.00, the ≥5 §8 scenarios, and the verify-don't-trust equality gate per 08-VALIDATION.md). This is the TRUST-01 core gate.
    (2) `daml` — installs the Daml 3.4 SDK via the official get.daml.com script, then `cd daml && daml build && daml test` (protects the on-ledger §8 recompute incl. test_clear_rejects_bad_allocation). Gate this job to run on push to the default branch (it is heavy — RESEARCH Open Q1 / A4); keep `golden` on every push and PR.
    Add `solver/proofs/` to .gitignore under a new "Phase 8 — decision proof bundles (ephemeral per-round; NEVER commit; must not carry ANTHROPIC_API_KEY)" comment (RESEARCH Pitfall 6 / Runtime State Inventory — it is NOT currently ignored).
    Create web/.env.example with a documented `VITE_SOLVER_URL=http://localhost:4100` line + a one-line comment naming the live solver port (RESEARCH Runtime State Inventory — web/.env.example does not exist yet). Do NOT put any secret in it.
    NEVER add Claude/Anthropic as a commit contributor (the commit helper enforces author = woshvad).
  </action>
  <acceptance_criteria>
    - `cd solver && npm test` (the exact command the golden job runs) is green locally — §4 clears $100.00, all §8 scenarios pass
    - `git check-ignore -v solver/proofs/anything.json` reports .gitignore (the dir is now ignored)
    - `test -f web/.env.example && grep -q 'VITE_SOLVER_URL=http://localhost:4100' web/.env.example`
    - `.github/workflows/ci.yml` parses as valid YAML and has both `golden` and `daml` jobs with the solver/**·daml/** path filter
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npm test && cd .. && git check-ignore solver/proofs/x.json && test -f web/.env.example</automated>
  </verify>
  <done>CI workflow exists with the golden vitest gate (green) + a separate daml-test job; solver/proofs/ is gitignored; web/.env.example documents :4100.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → git history | secrets / ephemeral proof bundles must never be committed |
| browser → solver (:4100) | offline captions/URLs shown to a judge must be truthful, not the stale :4000 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-CFG | Information Disclosure | port captions in web/src (solver.ts, AgentView, SettlementView, TheatreView, AgentProposal) | mitigate | derive the displayed port from SOLVER_BASE_URL once; a wrong on-screen port (esp. on the WOW-02 Theatre surface) undermines the WOW-01/WOW-02 wire-evidence credibility |
| T-08-01-SEC | Information Disclosure | solver/proofs/ + web/.env.example | mitigate | gitignore solver/proofs/ (never commit a bundle/key, RESEARCH Pitfall 6); .env.example carries only the non-secret VITE_SOLVER_URL |
| T-08-01-CI | Tampering | §8 clearing math regression | mitigate | TRUST-01 golden vitest runs on every push/PR touching solver/ or daml/ — the $100.00 case is the regression gate |
| T-08-01-SC | Tampering | npm ci in CI | accept | no NEW packages this phase (RESEARCH Package Legitimacy Audit = N/A); CI uses the committed, pinned lockfile |
</threat_model>

## Artifacts this phase produces

| Symbol / File | Kind | Notes |
|---------------|------|-------|
| `web/src/solver.ts` `solverPort` | export (const) | port parsed once from SOLVER_BASE_URL |
| `web/src/solver.ts` `OFFLINE_CAPTION` | export (const) | port-derived offline caption reused by AgentView + SettlementView + TheatreView |
| `web/src/views/TheatreView.tsx` OfflineCaption | edit | renders imported OFFLINE_CAPTION (was stale :4000) — WOW-02 operator surface |
| `web/src/components/AgentProposal.tsx` | edit | stale :4000 comment scrubbed to :4100 |
| `.github/workflows/ci.yml` | file (NEW) | TRUST-01 two-job CI (golden vitest always + daml test) |
| `web/.env.example` | file (NEW) | documents VITE_SOLVER_URL=http://localhost:4100 |
| `.gitignore` `solver/proofs/` entry | rule | gitignores the TRUST-03 decision-bundle dir |

<verification>
- `cd web && npm run build` passes and `grep -rn ':4000' web/src` is empty (all 5 files clean).
- `cd solver && npm test` (the CI golden command) is green — §4 clears $100.00, A=10/B=8/C=2.
- `solver/proofs/` is honored by `git check-ignore`.
</verification>

<success_criteria>
No `:4000` literal remains in `web/src` (solver.ts, AgentView, SettlementView, TheatreView, AgentProposal all clean); offline captions read the live :4100 via the single derived OFFLINE_CAPTION; a TRUST-01 CI workflow runs the §8 golden vitest on push/PR (green on $100.00); `solver/proofs/` is gitignored; `web/.env.example` documents the live port. Solver clearing behavior is byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/08-demo-hardening/08-01-SUMMARY.md` when done.
</output>
