---
phase: 08-demo-hardening
plan: 03
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - solver/src/agent.ts
  - solver/src/agent.test.ts
  - solver/src/api.ts
  - solver/src/api.test.ts
  - solver/src/index.ts
autonomous: true
requirements: [TRUST-02, WOW-03]
must_haves:
  truths:
    - "With the Anthropic path disabled (no key / SDK error / malformed / disagreement / TIMEOUT) a round still clears deterministically at exactly $100.00 — the degradation ladder is formalized and locked by tests"
    - "proposeClearing imposes a deadline (Promise.race) so a slow/hanging Claude call never stalls the round — timeout is treated as the deterministic fallback"
    - "A desk's plain-English text is parsed SERVER-SIDE via Claude structured output into a zod-validated {side, qty, limit}; malformed input returns 422; the parse never auto-submits"
    - "The Anthropic key never appears in any /parse-order response (secret-sweep extended to the new endpoint)"
  artifacts:
    - path: "solver/src/agent.ts"
      provides: "parseOrder() + a timeout-guarded proposeClearing (TRUST-02 ladder)"
      exports: ["parseOrder"]
    - path: "solver/src/api.ts"
      provides: "POST /parse-order route on createApp(deps)"
      contains: "/parse-order"
  key_links:
    - from: "solver/src/api.ts POST /parse-order"
      to: "deps.parseOrder (agent.ts, server-side key)"
      via: "AppDeps.parseOrder wired once in index.ts buildDeps"
      pattern: "parseOrder"
---

<objective>
Harden the AI-trust core and add server-side natural-language order parsing. TRUST-02: formalize the graceful-degradation ladder in `agent.ts` and CLOSE the one real gap — `proposeClearing` has no deadline, so add a `Promise.race` timeout and treat a timeout exactly like every other failure (deterministic §4 fallback at $100.00). WOW-03 (backend): add `parseOrder(text)` mirroring the proven `messages.parse` + `jsonSchemaOutputFormat` + zod shape, exposed as `POST /parse-order`, so a desk can type "buy up to 10 under 101" and get a validated `{side, qty, limit}` back — server-side only, key never in the browser, never auto-submitted.

Purpose: The demo must never stall on a slow key (Pitfall 2), and the round must clear $100.00 with the AI fully disabled (TRUST-02). The NL assist is the front door for WOW-03; its output only prefills the ticket.
Output: A timeout-guarded, ladder-tested `proposeClearing`; `parseOrder` + `POST /parse-order`; extended agent/api tests incl. the ANTHROPIC_API_KEY secret sweep.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-demo-hardening/08-RESEARCH.md
@.planning/phases/08-demo-hardening/08-PATTERNS.md
@.planning/phases/08-demo-hardening/08-VALIDATION.md
@solver/src/agent.ts
@solver/src/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TRUST-02 — timeout-guard proposeClearing + formalize the degradation ladder (agent.ts)</name>
  <files>solver/src/agent.ts, solver/src/agent.test.ts</files>
  <read_first>
    - solver/src/agent.ts (proposeClearing L159-219 — the deterministic `fallback()` L163-170; the keyless short-circuit L173; malformed L192; disagreement L199; SDK-error catch L210-219; the module-private `_apiKey`/`_client` L136-137)
    - solver/src/agent.test.ts (the existing agreement/disagreement/unavailable/keyless mocked-SDK cases + the sentinel-key sweep)
    - 08-RESEARCH.md Pitfall 2 (no deadline today — wrap messages.parse in Promise.race([call, timeout(ms)]); a timeout is the deterministic fallback) + § Phase Requirements TRUST-02
    - 08-VALIDATION.md rows: "keyless → $100.00", "SDK error / malformed / disagreement → $100.00", "timeout → $100.00 (deadline via Promise.race)"
  </read_first>
  <behavior>
    - A resolved client whose messages.parse never settles within the deadline → proposeClearing resolves to the deterministic fallback (clearingPrice 100.00 on §4, verified:false, source:'deterministic-fallback') and NEVER throws.
    - The ladder is exhaustive: keyless, malformed parsed_output, zod-invalid, price/allocation disagreement, thrown SDK error, AND timeout ALL map to the SAME deterministic §4 result ($100.00, A=10/B=8/C=2) — assert each branch in a table-style test.
    - The agreement path (mock returns the exact deterministic numbers + a rationale) still yields verified:true, source:'claude', rationale from the model.
    - The timeout is configurable but defaults to a sane demo deadline; a fast successful call is unaffected.
  </behavior>
  <action>
    In agent.ts, add a module-level timeout helper (a Promise that rejects/resolves-to-fallback after AGENT_TIMEOUT_MS, e.g. read from process.env.AGENT_TIMEOUT_MS with a default) and wrap the `client.messages.parse(...)` call at L181 in `Promise.race([parseCall, timeout])`. A timeout must land in the SAME fallback path as the existing catch (do NOT throw). Add a short doc comment naming the formalized ladder: keyless → malformed → zod-invalid → disagreement → SDK-error → TIMEOUT, all → deterministic §4 fallback. Keep the module-private key discipline (never a param/return/log; catch logs only a fixed string + err.name). Do NOT change the equality gate, the deterministic numbers, or the settle-path callers.
    Extend agent.test.ts with: (a) a timeout case — inject a client whose parse returns a never-resolving Promise (with fake timers or a short AGENT_TIMEOUT_MS) and assert proposeClearing resolves to the §4 deterministic result ($100.00) verified:false; (b) a ladder table test iterating the six failure modes and asserting each yields the identical deterministic $100.00 result.
  </action>
  <acceptance_criteria>
    - `cd solver && npx vitest run src/agent.test.ts` green, including the new timeout + ladder-table cases
    - a never-resolving parse yields clearingPrice 100.00, verified:false, source:'deterministic-fallback' (no throw, no hang)
    - the deterministic numbers/equality gate and the agreement path are unchanged (existing tests still pass)
    - `cd solver && npm test` (full suite) green — auction.ts / api.ts behavior byte-unchanged
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/agent.test.ts</automated>
  </verify>
  <done>proposeClearing has a deadline; the six-mode degradation ladder is formalized and every mode (incl. timeout) provably clears $100.00.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WOW-03 — parseOrder() in agent.ts (structured output, jsonSchemaOutputFormat + zod)</name>
  <files>solver/src/agent.ts, solver/src/agent.test.ts</files>
  <read_first>
    - solver/src/agent.ts (proposeClearing L181-192 — messages.parse + `jsonSchemaOutputFormat(proposalJsonSchema)` + `proposalSchema.safeParse(message.parsed_output)`; the import at L29; the DEVIATION note L20-26)
    - 08-RESEARCH.md § "Code Examples — WOW-03" (the exact orderSchema/orderJsonSchema + keyless short-circuit → null) + Pitfall 1 (use jsonSchemaOutputFormat, NEVER zodOutputFormat — zod is pinned v3; zodOutputFormat hard-imports zod/v4)
    - 08-PATTERNS.md § "solver/src/agent.ts — WOW-03 parseOrder" (mirror proposeClearing exactly)
  </read_first>
  <behavior>
    - parseOrder("buy up to 10 under 101") → { side:'Buy', qty:10, limit:101 } (zod-validated int qty>0, number limit>0).
    - parseOrder on gibberish / a non-order → null (zod safeParse fails or the model can't produce a valid order).
    - Keyless (no client) → null (the UI will show the error state; the key never leaves the server).
    - The parse NEVER throws into the caller; NEVER logs/returns the key.
  </behavior>
  <action>
    In agent.ts export `parseOrder(text: string): Promise<{side:'Buy'|'Sell'; qty:number; limit:number} | null>`. Author a hand-written JSON-Schema literal `orderJsonSchema` ({ side: enum[Buy,Sell], qty: integer, limit: number }, additionalProperties:false, required all three) and a zod `orderSchema` mirroring it. Reuse the module-private `_client` (or the createAgent-injected client) — if absent, return null. Call `client.messages.parse` with model 'claude-haiku-4-5', temperature 0, max_tokens 256, a system instruction to extract a single sealed limit order (map "under/at most" → Buy limit, "at least/over" on a sell → Sell limit), `output_config.format = jsonSchemaOutputFormat(orderJsonSchema)`, then `orderSchema.safeParse(message.parsed_output)` and return `parsed.success ? parsed.data : null`. Cap input length upstream (the endpoint zod caps ≤280). Keep the key module-private (RESEARCH Anti-Pattern: no key near the browser). Add a test extending agent.test.ts with a mocked client returning a valid order (→ typed object) and one returning a malformed payload (→ null) and a keyless case (→ null).
  </action>
  <acceptance_criteria>
    - `cd solver && npx vitest run src/agent.test.ts` green with the parseOrder cases (valid → typed, malformed → null, keyless → null)
    - parseOrder uses `jsonSchemaOutputFormat` (NOT zodOutputFormat) — `grep -n zodOutputFormat solver/src/agent.ts` returns nothing
    - `cd solver && npx tsc --noEmit` clean
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/agent.test.ts</automated>
  </verify>
  <done>agent.ts parses plain English into a zod-validated {side,qty,limit} via structured output, degrades to null keyless/malformed, and keeps the key server-only.</done>
</task>

<task type="auto">
  <name>Task 3: WOW-03 — POST /parse-order endpoint + AppDeps wiring + secret-sweep test</name>
  <files>solver/src/api.ts, solver/src/api.test.ts, solver/src/index.ts</files>
  <read_first>
    - solver/src/api.ts (createApp(deps) + AppDeps L65-88; the `wrap` helper L120-124; the ApiError L93-102 + secret-safe error middleware L310-317; the zod .strict() body pattern L107-113)
    - solver/src/index.ts (buildDeps L70-98 — how proposeClearing is threaded through; main() constructs the agent at L117-120)
    - solver/src/api.test.ts (makeDeps overrides L72+, SECTION4 fixtures, SENTINEL_TOKEN L50 + SENTINEL_API_KEY L54 sweep, app.listen(0) + node fetch)
    - 08-RESEARCH.md § "Pattern 1: Additive endpoint on the DI factory" (the /parse-order example) + § Phase Requirements WOW-03
    - 08-UI-SPEC.md § "WOW-03" (server parses; never auto-submits; key server-only)
  </read_first>
  <action>
    In api.ts add `parseOrder: (text: string) => Promise<{side:'Buy'|'Sell'; qty:number; limit:number} | null>` to the AppDeps interface. Register `POST /parse-order` inside createApp using the `wrap` pattern: validate the body with a zod `.strict()` schema `{ text: z.string().min(1).max(280) }` (cap length — ASVS V5 / prompt-injection mitigation); call `deps.parseOrder(text)`; if it returns null throw `new ApiError(422, 'PARSE_FAILED', "couldn't parse that order")`; else `res.json(order)`. Author every ApiError message secret-free.
    In index.ts, thread `parseOrder: agent.parseOrder` (or a wrapper over the boot agent) through buildDeps/BuildDepsArgs so the real server-side key backs it (mirror how proposeClearing is wired). Do NOT change the /settle handler, solve-preview, or the GET terminal branch.
    Extend api.test.ts: a `/parse-order` case with a mocked `deps.parseOrder` returning {side:'Buy',qty:10,limit:101} → 200 + that body; a null-returning mock → 422 envelope; a malformed body ({} or text too long) → 400. Extend the sentinel sweep so `/parse-order` responses NEVER contain SENTINEL_API_KEY or SENTINEL_TOKEN (a parseOrder stub closes over the sentinel key the way agent.ts holds the real one).
  </action>
  <acceptance_criteria>
    - `cd solver && npx vitest run src/api.test.ts` green: /parse-order returns the parsed order (200), 422 on null, 400 on a malformed/oversized body
    - the sentinel API key / operator token NEVER appear in any /parse-order response body (extended secret sweep)
    - `cd solver && npm test` full suite green; `cd solver && npx tsc --noEmit` clean
    - `grep -n '/parse-order' solver/src/api.ts` present; the /settle handler is byte-unchanged
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/api.test.ts</automated>
  </verify>
  <done>POST /parse-order validates+caps input, returns the zod-validated order (422 on failure), is wired to the server-side agent, and leaks no secret.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → solver (:4100) | desk posts plain English; solver holds the Anthropic key server-side |
| solver → Anthropic API | the only place the key is used; must never be echoed to a response/log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-KEY | Information Disclosure | ANTHROPIC_API_KEY via /parse-order response/log | mitigate | key module-private in agent.ts; secret-safe `{error:{code,message}}` envelope; sentinel sweep extended to /parse-order (RESEARCH Pitfall 6) |
| T-08-03-INJ | Tampering | prompt-injection in NL order text | mitigate | output is zod-validated `{side,qty,limit}` and only PREFILLS the ticket (WOW-03 web); input capped ≤280; never auto-submitted (a hostile order is still a normal sealed order re-verified at Clear) |
| T-08-03-STALL | Denial of Service | slow/hanging Claude call | mitigate | Promise.race deadline in proposeClearing → deterministic §4 fallback; the round never stalls (Pitfall 2 / TRUST-02) |
| T-08-03-CORS | Spoofing | cross-origin abuse of /parse-order | mitigate | existing `cors({ origin: 'http://localhost:5173' })` (never `*`) covers the new route |
</threat_model>

## Artifacts this phase produces

| Symbol / File | Kind | Notes |
|---------------|------|-------|
| `solver/src/agent.ts` `parseOrder` | export (function) | server-side NL → {side,qty,limit} |
| `solver/src/agent.ts` AGENT_TIMEOUT_MS + Promise.race | logic | TRUST-02 deadline in proposeClearing |
| `POST /parse-order` | endpoint | zod-capped body → deps.parseOrder → 422 on fail |
| `AppDeps.parseOrder` | interface field | wired in index.ts buildDeps |

<verification>
- `cd solver && npm test` full suite green; `cd solver && npx tsc --noEmit` clean.
- Every TRUST-02 ladder branch (keyless/malformed/zod/disagreement/SDK-error/timeout) clears $100.00.
- /parse-order returns the parsed order or 422; no secret in any response.
</verification>

<success_criteria>
With the AI disabled by any failure mode (including a new timeout) a round still clears $100.00; a desk's plain English parses server-side into a validated `{side,qty,limit}` via `POST /parse-order`; the key never reaches the browser or any response. The settle path and deterministic numbers are byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/08-demo-hardening/08-03-SUMMARY.md` when done.
</output>
