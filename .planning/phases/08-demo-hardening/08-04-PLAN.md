---
phase: 08-demo-hardening
plan: 04
type: execute
wave: 3
depends_on: [08-03]
files_modified:
  - solver/src/agent.ts
  - solver/src/brief.ts
  - solver/src/ledger.ts
  - solver/src/ledger.test.ts
  - solver/src/api.ts
  - solver/src/api.test.ts
  - solver/src/index.ts
autonomous: true
requirements: [WOW-04, WOW-02]
must_haves:
  truths:
    - "The solver streams its clearing rationale token-by-token as Server-Sent Events; on any stream error it emits a single fixed deterministic fallback frame that never contains the key or prompt"
    - "The solver composes a shareable post-round natural-language brief (clearing price, matched volume, per-desk outcomes, rationale) server-side"
    - "A dedicated tamper seam submits a deliberately WRONG allocation to the on-ledger Round.Clear; the recompute-and-assert backstop rejects it with the VERBATIM ledger error, changing nothing on-ledger (atomic)"
    - "The real deterministic /settle path stays BYTE-UNCHANGED and the §4 fixture still clears $100.00"
  artifacts:
    - path: "solver/src/ledger.ts"
      provides: "tamperClear(roundId, mode) attempting a rejected Round.Clear"
      exports: ["tamperClear"]
    - path: "solver/src/brief.ts"
      provides: "composeBrief() post-round NL summary"
    - path: "solver/src/api.ts"
      provides: "GET /round/:id/rationale-stream (SSE) + brief exposure + POST /round/:id/tamper-clear"
      contains: "rationale-stream"
  key_links:
    - from: "solver/src/api.ts POST /round/:id/tamper-clear"
      to: "deps.tamperClear (ledger.ts) → on-ledger Round.Clear assertMsg"
      via: "AppDeps.tamperClear wired in index.ts; verbatim reject surfaced through submitAndWait throw"
      pattern: "tamper-clear|tamperClear"
    - from: "solver/src/api.ts GET /round/:id/rationale-stream"
      to: "deps.streamRationale (agent.ts messages.stream)"
      via: "text/event-stream data: frames; onError → single fallback frame"
      pattern: "text/event-stream"
---

<objective>
Two backend seams. WOW-04: add `streamRationale` (Anthropic SDK `messages.stream().on('text')`) proxied as a `GET /round/:id/rationale-stream` SSE endpoint, plus a `composeBrief` post-round summary exposed for the shareable Round Brief. WOW-02: add a DEDICATED `tamperClear(roundId, mode)` in `ledger.ts` that gathers the SAME contract ids as the real `settle` but submits a WRONG `clearingPrice` (`wrong-price`) or over-filled `allocations` (`overfill`); the on-ledger `Round.Clear` recompute-and-assert rejects it verbatim, exposed via `POST /round/:id/tamper-clear`. The real `/settle` path is left byte-unchanged.

Purpose: WOW-04 makes the AI's reasoning legible live and shareable; WOW-02 makes "the ledger, not the AI, is the backstop" visceral by showing the ACTUAL on-ledger rejection then a correct clear at $100.00.
Output: `streamRationale` + `composeBrief` + SSE/brief endpoints; `tamperClear` + `/tamper-clear`; extended ledger/api tests with the verbatim rejection strings.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-demo-hardening/08-RESEARCH.md
@.planning/phases/08-demo-hardening/08-PATTERNS.md
@.planning/phases/08-demo-hardening/08-VALIDATION.md
@solver/src/ledger.ts
@solver/src/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WOW-02 — tamperClear(roundId, mode) in ledger.ts + verbatim-reject tests</name>
  <files>solver/src/ledger.ts, solver/src/ledger.test.ts</files>
  <read_first>
    - solver/src/ledger.ts (settle L298-364 — the exact contract gathering: orderCids, buyerUsdcCid, sellerBondCids tuple `{_1,_2}`, the re-query of the Round cid L347, the exerciseChoice('Umbra.Auction:Round', cid, 'Clear', {...}) L349-355; the submitAndWait throw `submit HTTP ${status}: ${body}` L111-114; the Int/Decimal wire note L18-22)
    - daml/Umbra/Auction.daml lines 166-198 (the recompute-and-assert body; the three verbatim assertMsg strings; atomic rollback)
    - 08-RESEARCH.md § "Pattern 3: Tamper-then-correct" (VERBATIM strings: `clearingPrice does not match recomputed §8 p*` ← wrong-price; `allocations do not match recomputed §8` and `fills not conserved (Σbuy /= Σsell)` ← overfill) + Pitfall 3 (perturb VALUES not TYPES so the §8 assert fires, not a decoder error)
    - 08-VALIDATION.md WOW-02 rows (stubbed submitAndWait returning the assertMsg body)
  </read_first>
  <behavior>
    - tamperClear(roundId, 'wrong-price'): gathers the correct §8 result, submits `clearingPrice - 1` (still a valid Decimal) with correct allocations → the ledger throws; tamperClear resolves `{ rejected:true, error }` where `error` carries the verbatim `clearingPrice does not match recomputed §8 p*`.
    - tamperClear(roundId, 'overfill'): submits Buy-side filledQty + 2 → the ledger throws with `allocations do not match recomputed §8` or `fills not conserved (Σbuy /= Σsell)`.
    - tamperClear NEVER throws (it catches the submit error) and NEVER settles — it only ATTEMPTS the exercise; a rejected atomic Round.Clear changes nothing on-ledger.
    - the error string is secret-free (only the ledger body slice, never the token).
  </behavior>
  <action>
    In ledger.ts export `tamperClear(roundId, mode: 'wrong-price'|'overfill'): Promise<{rejected:true; error:string}>`. Copy settle()'s exact contract gathering (orderCids/buyerUsdcCid/sellerBondCids and the Round cid re-query) — DO NOT refactor or touch settle() itself (it must stay byte-unchanged, hard constraint). Compute the CORRECT §8 clearing via the imported computeClearing, then perturb ONLY numeric VALUES: `badPrice = mode==='wrong-price' ? clearingPrice - 1 : clearingPrice`; `badAllocs = mode==='overfill' ? allocations.map(a => a.side==='Buy' ? {...a, filledQty: a.filledQty + 2} : a) : allocations`. Call exerciseChoice('Umbra.Auction:Round', round.contractId, 'Clear', { clearingPrice: badPrice, allocations: badAllocs, orderCids, buyerUsdcCid, sellerBondCids }) inside a try; on success return an `UNEXPECTED: ledger accepted a tampered clear` sentinel (should never happen); in catch return `{ rejected:true, error: e instanceof Error ? e.message : 'rejected' }`. Keep Int/Decimal wire types intact (Pitfall 3).
    Extend/create ledger.test.ts cases with a stubbed `submitAndWait`/fetch that returns a non-200 whose body carries each verbatim assertMsg; assert tamperClear surfaces the verbatim string for both modes and never throws.
  </action>
  <acceptance_criteria>
    - `cd solver && npx vitest run src/ledger.test.ts` green: wrong-price → verbatim `clearingPrice does not match recomputed §8 p*`; overfill → `fills not conserved (Σbuy /= Σsell)` / `allocations do not match recomputed §8`
    - tamperClear resolves (never throws) and its error string contains no operator token
    - `grep -n 'export const settle' solver/src/ledger.ts` shows settle() body byte-unchanged (diff limited to the new tamperClear + its imports)
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/ledger.test.ts</automated>
  </verify>
  <done>tamperClear attempts a wrong Round.Clear and surfaces the verbatim ledger rejection for both modes without settling; settle() is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WOW-04 — streamRationale (agent.ts) + composeBrief (brief.ts) + SSE/brief endpoints</name>
  <files>solver/src/agent.ts, solver/src/brief.ts, solver/src/api.ts, solver/src/api.test.ts, solver/src/index.ts</files>
  <read_first>
    - solver/src/agent.ts (SYSTEM_PROMPT L77-82, buildBatchMessage L88-96, the module-private client, neutralRationale L114-117)
    - solver/src/api.ts (createApp routes; the `wrap` helper — NOTE: the SSE route must NOT use wrap(); it is a streaming response; the settled-rationale string builder L225-227 as the brief analog; AppDeps L65-88)
    - solver/src/index.ts (buildDeps + the boot agent at L117-120; adapters L147-176)
    - 08-RESEARCH.md § "Pattern 2: SSE proxy of Anthropic streaming" (Content-Type text/event-stream, res.write(`data: ...\n\n`), event: done sentinel; onError emits ONLY a fixed fallback frame, NEVER err.message/prompt) + § "Code Examples — WOW-04" (client.messages.stream(...).on('text', onDelta) then await stream.finalMessage(); onDone) + Pitfall 5 (EventSource needs no auth header; disable buffering) + Pitfall 6 (SSE onError must not leak the key)
    - 08-PATTERNS.md § "solver/src/brief.ts" (compose from api.ts:225-227 + neutralRationale)
  </read_first>
  <behavior>
    - streamRationale(views, {onDelta, onDone, onError}): forwards each Claude text delta via onDelta; on completion calls onDone; on any error (or keyless) calls onError EXACTLY ONCE with a fixed deterministic fallback string — never the key/prompt/err.message.
    - The SSE endpoint emits `data: <json-string delta>\n\n` frames then `event: done\ndata: {}\n\n`; on error it writes ONE fallback `data:` frame (the deterministic single-shot rationale) then ends — the browser typewriter still has text (graceful fallback).
    - composeBrief(clearingPrice, matchedVolume, allocations, rationale): returns a 2-4 sentence NL summary naming the $100.00 clear, matched volume, per-desk aggregate outcomes, and the rationale — secret-free, deterministic-safe.
  </behavior>
  <action>
    In agent.ts export `streamRationale(views, handlers)` using `client.messages.stream({ model:'claude-haiku-4-5', temperature:0, max_tokens:512, system: SYSTEM_PROMPT, messages:[{role:'user', content: buildBatchMessage(views)}] }).on('text', handlers.onDelta)`, `await stream.finalMessage()` then `handlers.onDone()`; wrap in a try/catch mirroring proposeClearing's catch (log only a fixed string + err.name) → call `handlers.onError()`; keyless (no client) → call `handlers.onError()` immediately. The onError CONTRACT: it never receives err.message.
    Create solver/src/brief.ts exporting `composeBrief(...)` (pure; mirror the settled-rationale string builder + neutralRationale wording).
    In api.ts: add `streamRationale` + `composeBrief` to AppDeps. Register `GET /round/:id/rationale-stream` WITHOUT wrap(): set headers Content-Type text/event-stream, Cache-Control no-cache, Connection keep-alive, flushHeaders(); read the round's sealed views (deps.readSealedOrders) and call deps.streamRationale forwarding onDelta→`res.write('data: '+JSON.stringify(delta)+'\n\n')`, onDone→write the done sentinel + res.end(), onError→write ONE deterministic fallback `data:` frame (from composeBrief/neutralRationale) then res.end(). Expose the brief where the settled result is surfaced (add `brief: composeBrief(...)` to the GET /round/:id terminal-settled body and/or the settle response — additive; do not remove existing fields). Wire both new deps through index.ts buildDeps (streamRationale from the boot agent; composeBrief imported).
    Extend api.test.ts: a mocked streamRationale that emits two deltas then onDone → assert the SSE body contains both `data:` frames + the done sentinel; a mocked onError path → assert exactly one fallback frame and NO SENTINEL_API_KEY anywhere in the streamed bytes; a brief presence + secret-sweep assertion on the settled body.
  </action>
  <acceptance_criteria>
    - `cd solver && npx vitest run src/api.test.ts` green: SSE emits the deltas + done sentinel; the error path emits a single fallback frame; brief present on the settled body
    - no SENTINEL_API_KEY / operator token appears in the streamed SSE bytes or the brief (extended sweep)
    - the SSE route does NOT use wrap() and sets Content-Type text/event-stream
    - `cd solver && npm test` full suite green; `cd solver && npx tsc --noEmit` clean; solve-preview/GET/settle deterministic numbers unchanged
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/api.test.ts</automated>
  </verify>
  <done>The solver streams rationale deltas as SSE with a secret-free single-frame fallback, composes a shareable brief, and both are wired through the DI factory without disturbing the deterministic paths.</done>
</task>

<task type="auto">
  <name>Task 3: WOW-02 — POST /round/:id/tamper-clear endpoint + wiring + secret-sweep</name>
  <files>solver/src/api.ts, solver/src/api.test.ts, solver/src/index.ts</files>
  <read_first>
    - solver/src/api.ts (the `wrap` + ApiError + zod .strict() body patterns; AppDeps; secret-safe error middleware L310-317; the UNCHANGED /settle handler L273-303 — do not touch)
    - solver/src/index.ts (buildDeps wiring; settle adapter — tamperClear is wired here as a new dep pointing at ledger.tamperClear)
    - 08-RESEARCH.md § "Open Questions" Q3 (dedicated POST /round/:id/tamper-clear with {mode} keeps /settle byte-unchanged) + § Anti-Patterns ("Touching /settle for WOW-02")
    - 08-UI-SPEC.md § "WOW-02" (tamper modes Wrong price · Over-fill; the verbatim reject is the credibility)
  </read_first>
  <action>
    In api.ts add `tamperClear: (roundId: string, mode: 'wrong-price'|'overfill') => Promise<{rejected:true; error:string}>` to AppDeps. Register `POST /round/:id/tamper-clear` via wrap(): validate a zod `.strict()` body `{ mode: z.enum(['wrong-price','overfill']) }`; call `deps.tamperClear(id, mode)`; `res.json({ rejected: result.rejected, error: result.error })` (the verbatim ledger rejection is the payload — do NOT summarize it, but it is already a secret-free ledger body slice). Leave POST /round/:id/settle byte-unchanged.
    In index.ts thread `tamperClear: ledger.tamperClear` through buildDeps/BuildDepsArgs (mirror settle wiring).
    Extend api.test.ts: a mocked deps.tamperClear returning `{rejected:true, error:'... clearingPrice does not match recomputed §8 p* ...'}` → assert 200 + the verbatim error surfaces in the response; the overfill mode returns the conservation string; assert the operator token / API key never appear in the /tamper-clear response (secret sweep). Also assert a bad `mode` body → 400.
  </action>
  <acceptance_criteria>
    - `cd solver && npx vitest run src/api.test.ts` green: /tamper-clear surfaces the verbatim rejection for both modes; bad mode → 400
    - the /settle handler is byte-unchanged (`grep` diff limited to the new /tamper-clear route + AppDeps field)
    - no secret appears in any /tamper-clear response (extended sweep)
    - `cd solver && npm test` full suite green
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/api.test.ts</automated>
  </verify>
  <done>POST /round/:id/tamper-clear returns the verbatim on-ledger rejection for both tamper modes; /settle is untouched; no secret leaks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → solver (:4100) | triggers the tamper attempt + consumes the SSE rationale; holds no operator/AI credential |
| solver → Canton Round.Clear | the on-ledger recompute-and-assert is the actual rejection authority (verify-don't-trust) |
| solver → Anthropic (stream) | key server-side; a stream error must not leak the key/prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-TAMPER | Tampering / Elevation | tampered clear accidentally settling | mitigate | Round.Clear is atomic recompute-and-assert; a mismatch rolls back (Auction.daml 166-198); tamperClear only ATTEMPTS, is a DEDICATED endpoint, never /settle (hard constraint) |
| T-08-04-SSEKEY | Information Disclosure | SSE onError leaking the key/prompt | mitigate | onError emits ONLY a fixed deterministic fallback frame; never err.message/the prompt (Pitfall 5/6); catch logs only err.name |
| T-08-04-BRIEF | Information Disclosure | brief text carrying a secret | mitigate | composeBrief is pure over numbers + the rationale; secret sweep asserts absence |
| T-08-04-DECODE | Tampering | wrong-shape tamper args firing a decoder error not the §8 assert | mitigate | perturb VALUES not TYPES; badPrice stays a valid Decimal (Pitfall 3) so the demo shows the §8 assert verbatim |
| T-08-04-CORS | Spoofing | cross-origin abuse of the new routes | mitigate | existing cors(:5173) (never `*`) covers /tamper-clear + /rationale-stream |
</threat_model>

## Artifacts this phase produces

| Symbol / File | Kind | Notes |
|---------------|------|-------|
| `solver/src/ledger.ts` `tamperClear` | export (function) | attempts a rejected Round.Clear (2 modes) |
| `solver/src/brief.ts` `composeBrief` | export (function, NEW file) | post-round NL summary |
| `solver/src/agent.ts` `streamRationale` | export (function) | messages.stream → onDelta/onDone/onError |
| `GET /round/:id/rationale-stream` | endpoint (SSE) | data: frames + done; single fallback frame on error |
| `POST /round/:id/tamper-clear` | endpoint | verbatim ledger rejection payload |
| `AppDeps.tamperClear` / `.streamRationale` / `.composeBrief` | interface fields | wired in index.ts |
| `brief` field on settled body | additive response field | consumed by WOW-04 RoundBrief + WOW-05 proof-pack |

<verification>
- `cd solver && npm test` full suite green; `cd solver && npx tsc --noEmit` clean.
- tamperClear surfaces both verbatim rejections; SSE streams deltas with a secret-free fallback frame.
- settle()/`/settle` byte-unchanged; §4 still clears $100.00.
</verification>

<success_criteria>
The solver streams its rationale live (SSE) with a graceful single-frame fallback, composes a shareable brief, and a dedicated tamper seam produces the VERBATIM on-ledger rejection for both modes while the real settle path stays byte-unchanged and still clears $100.00. No key/token leaks in any response or stream.
</success_criteria>

<output>
Create `.planning/phases/08-demo-hardening/08-04-SUMMARY.md` when done.
</output>
