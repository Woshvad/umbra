---
phase: 08-demo-hardening
plan: 06
type: execute
wave: 4
depends_on: [08-01, 08-03, 08-04]
files_modified:
  - web/src/solver.ts
  - web/src/components/BreakTheAiPanel.tsx
  - web/src/views/AgentView.tsx
  - web/src/components/OrderTicket.tsx
autonomous: true
requirements: [WOW-02, WOW-03]
must_haves:
  truths:
    - "An operator-plane 'Break the AI' control forces a wrong clear, shows the VERBATIM on-ledger rejection, then runs the correct clear which settles at $100.00 — the before/after contrast is on screen"
    - "A desk types plain English, PARSE → calls the solver /parse-order, and the returned {side,qty,limit} PREFILLS the existing ticket; SEAL ORDER stays the single mandatory confirmation (never auto-submit)"
    - "Both surfaces call the solver via web/src/solver.ts (:4100) — no operator/Anthropic credential in the browser"
    - "The new panels reuse existing brand tokens/keyframes and the ink evidence surface — no new tokens introduced"
  artifacts:
    - path: "web/src/components/BreakTheAiPanel.tsx"
      provides: "WOW-02 tamper control + before/after verbatim-reject → correct-clear panel on AgentView"
      min_lines: 50
    - path: "web/src/solver.ts"
      provides: "tamperClear() + parseOrder() typed client methods"
      exports: ["tamperClear", "parseOrder"]
  key_links:
    - from: "web/src/components/BreakTheAiPanel.tsx"
      to: "web/src/solver.ts tamperClear + settle"
      via: "POST /round/:id/tamper-clear then the correct clear"
      pattern: "tamperClear"
    - from: "web/src/components/OrderTicket.tsx (NL sub-block)"
      to: "web/src/solver.ts parseOrder"
      via: "POST /parse-order → prefill side/qty/limit"
      pattern: "parseOrder"
---

<objective>
Ship the two operator-/desk-plane UI surfaces for the break-the-AI and NL-order features, plus the web client methods that back them. WOW-02: a `BreakTheAiPanel` on the Agent view (04) with a tamper-mode toggle (Wrong price · Over-fill) and a `FORCE A WRONG CLEAR` red-ghost control that shows the VERBATIM on-ledger rejection, then a `RUN CORRECT CLEAR` that settles at $100.00 (the before/after contrast). WOW-03: an NL sub-block at the top of the Order Ticket where plain English + `PARSE →` calls the solver and PREFILLS the existing structured ticket (SEAL ORDER stays the single confirm — never auto-submit).

Purpose: These make "the ledger is the backstop" and "Claude drafts, the desk confirms" tangible, on the shipped views, without disturbing their layout or the one-order-per-round lock.
Output: `tamperClear`/`parseOrder` in web/src/solver.ts, `BreakTheAiPanel` mounted on AgentView, the NL sub-block in OrderTicket.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-demo-hardening/08-UI-SPEC.md
@.planning/phases/08-demo-hardening/08-PATTERNS.md
@.planning/phases/08-demo-hardening/08-RESEARCH.md
@web/src/solver.ts
@web/src/components/OrderTicket.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Web client methods — tamperClear() + parseOrder() in web/src/solver.ts</name>
  <files>web/src/solver.ts</files>
  <read_first>
    - web/src/solver.ts (the `call<T>()` wrapper L91-109; the five endpoint fns L112-129; the SolverError offline guard; the OFFLINE_CAPTION added by 08-01)
    - solver/src/api.ts (the POST /parse-order response {side,qty,limit} and POST /round/:id/tamper-clear response {rejected, error} shapes from 08-03/08-04)
    - 08-PATTERNS.md § "web/src/solver.ts (client) — EXTEND" (new typed methods copy the call<T> shape)
  </read_first>
  <action>
    Add two typed methods mirroring the call<T> pattern: `parseOrder(text: string): Promise<{side:'Buy'|'Sell'; qty:number; limit:number}>` → POST '/parse-order' with `{text}` (a 422 surfaces as SolverError the UI maps to the parse-error state); `tamperClear(id: string, mode: 'wrong-price'|'overfill'): Promise<{rejected:boolean; error:string}>` → POST `/round/${id}/tamper-clear` with `{mode}`. Add the matching response types. Do NOT add any port literal; reuse SOLVER_BASE_URL. Keep the browser free of operator/Anthropic credentials.
  </action>
  <acceptance_criteria>
    - `cd web && npm run build` passes with the two new typed methods
    - `grep -rn ':4000' web/src` still returns nothing
    - web/src/solver.ts contains no operator token / Anthropic key literal
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run build && ! grep -rn ':4000' src</automated>
  </verify>
  <done>web/src/solver.ts exposes typed parseOrder() + tamperClear() over the :4100 solver plane with no credential in the bundle.</done>
</task>

<task type="auto">
  <name>Task 2: WOW-02 — BreakTheAiPanel + mount on AgentView</name>
  <files>web/src/components/BreakTheAiPanel.tsx, web/src/views/AgentView.tsx</files>
  <read_first>
    - web/src/views/AgentView.tsx (the operator-plane view; the OFFLINE_CAPTION path from 08-01; the proposal/rationale grid — mount the panel BELOW it)
    - web/src/components/AgentRationale.tsx (the ink evidence surface treatment #0A0A0A/#F4F1EA/pre-wrap/padding 22px 24px; prefersReducedMotion L19-25)
    - web/src/solver.ts (tamperClear + the existing settle() for the correct clear; SolverError OFFLINE)
    - 08-UI-SPEC.md § "WOW-02 — Break the AI" (panel header sub-label + `DEMO · ADVERSARIAL` red-bordered tag; tamper-mode segmented toggle Wrong price · Over-fill; `FORCE A WRONG CLEAR` red 1px-border ghost button; two stacked result rows: TAMPERED CLEAR → red-square `REJECTED BY LEDGER` + verbatim error on ink surface, then CORRECT CLEAR → inline lime `100.00` 34px sub-reveal + red-square `VERIFIED · SETTLED @ 100.00`; verdict "The ledger — not the AI — is the backstop."; states idle/attempting/rejected/corrected) + Copywriting rows + Color (reuse #E2231A red + lime #D6FB3C for the correct clear ONLY; no new token) + Motion (umbra-slam sub-reveal, umbra-pulse status; reduced-motion instant)
    - 08-RESEARCH.md § "Pattern 3" (verbatim strings are the credibility — render them, never summarize)
  </read_first>
  <action>
    Create web/src/components/BreakTheAiPanel.tsx: takes the current roundId (from the lifted operator state) + optional offline flag. Renders the sub-label "Break the AI" + a `DEMO · ADVERSARIAL` red-bordered mono tag; a segmented tamper-mode toggle (Wrong price · Over-fill); the red-ghost `FORCE A WRONG CLEAR` control that calls `tamperClear(roundId, mode)` and renders the VERBATIM `error` string on the ink evidence surface under a red-square `REJECTED BY LEDGER` row; then a `RUN CORRECT CLEAR` control that calls the existing `settle(roundId)` and shows the inline lime `100.00` 34px sub-reveal (umbra-slam, reduced-motion instant) + a red-square `VERIFIED · SETTLED @ 100.00` row; the verdict line. No confirm dialog (the tamper is harmless/atomic — UI-SPEC). On SolverError OFFLINE, show the graceful caption. Reuse prefersReducedMotion + existing keyframes; introduce NO new token/keyframe. Never render an operator token.
    Mount `<BreakTheAiPanel roundId={...} offline={offline} />` in AgentView.tsx BELOW the shipped proposal/rationale grid; do not alter the shipped grid, headline, or the AgentProposal/AgentRationale composition.
  </action>
  <acceptance_criteria>
    - `cd web && npm run build` passes with the panel mounted
    - `grep -rn ':4000' web/src` returns nothing; BreakTheAiPanel has no operator-token literal
    - the verbatim `error` string renders on the ink surface (not a styled badge); the correct-clear uses lime #D6FB3C only for the 100.00 sub-reveal
    - MANUAL (phase gate): live — FORCE A WRONG CLEAR shows the verbatim `clearingPrice does not match recomputed §8 p*` (or the conservation string), then RUN CORRECT CLEAR settles at 100.00
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run build && ! grep -rn ':4000' src</automated>
    <human-check>Live round: toggle Wrong price → FORCE A WRONG CLEAR → observe the verbatim on-ledger rejection; toggle Over-fill → the conservation reject; RUN CORRECT CLEAR → settles at 100.00.</human-check>
  </verify>
  <done>BreakTheAiPanel shows the verbatim on-ledger rejection then the correct $100.00 clear on the Agent view, reusing existing tokens/motifs; web builds; no credential leaks.</done>
</task>

<task type="auto">
  <name>Task 3: WOW-03 — Natural-language sub-block in OrderTicket (prefill, never auto-submit)</name>
  <files>web/src/components/OrderTicket.tsx</files>
  <read_first>
    - web/src/components/OrderTicket.tsx (the shipped ticket: side toggle, 44px qty/limit inputs, the one-per-round lock `ticketLocked` L58, loadDemo L63-68, onSeal L70+, prefersReducedMotion L36; the Side import from @daml.js)
    - web/src/solver.ts (parseOrder + SolverError)
    - 08-UI-SPEC.md § "WOW-03" (a new NL sub-block at the TOP of the ticket, above the side toggle: sub-label "Natural Language · Describe your order"; single-line Inter 14px input, borderless with a 1px ink bottom rule, placeholder `e.g. buy up to 10 under 101`; `PARSE →` ghost-mono CTA; on success PREFILL the side toggle + qty + limit and show the mono note `PROPOSED BY CLAUDE — REVIEW & SEAL`; states idle/parsing/parsed/error; error copy verbatim) + Copywriting rows + Color (buy #2B3AF2 / sell #FF3D9A side indicator only) + the never-auto-submit rule
    - 08-RESEARCH.md § Anti-Patterns ("Auto-submitting the parsed order") — the shipped SEAL ORDER stays the single confirmation
  </read_first>
  <action>
    In OrderTicket.tsx add an NL sub-block ABOVE the existing side toggle: the sub-label, a controlled single-line input (Inter 14px, 1px ink underline, placeholder from UI-SPEC), and a `PARSE →` ghost-mono CTA. On PARSE, call `parseOrder(text)`; on success set the existing `side`/`qty`/`limit` state (mapping {side} to the Side enum, qty→String, limit→toFixed) so the shipped 44px inputs PREFILL, and show the `PROPOSED BY CLAUDE — REVIEW & SEAL` mono note; on SolverError/422 show the verbatim WOW-03 error copy. Do NOT auto-call onSeal — the shipped SEAL ORDER remains the single confirmation and the one-order-per-round lock (`ticketLocked`) is untouched. States: idle · parsing (`PARSING…`, disabled, umbra-pulse) · parsed (fields filled + note) · error. Reuse prefersReducedMotion; NO new token/keyframe. The Anthropic key never reaches the browser (parseOrder hits the solver).
  </action>
  <acceptance_criteria>
    - `cd web && npm run build` passes with the NL sub-block
    - `grep -rn ':4000' web/src` returns nothing
    - OrderTicket still gates submit behind SEAL ORDER (no auto-submit path added); the `ticketLocked` one-per-round logic is unchanged
    - the error/prompt copy matches 08-UI-SPEC.md Copywriting verbatim
    - MANUAL (phase gate): typing "buy up to 10 under 101" + PARSE prefills Buy/10/101 and requires SEAL ORDER to submit
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run build && ! grep -rn ':4000' src</automated>
    <human-check>Type "sell 8 at 99" → PARSE → the ticket prefills Sell/8/99 with the PROPOSED BY CLAUDE note; SEAL ORDER still required.</human-check>
  </verify>
  <done>The Order Ticket accepts plain English, prefills the structured fields via the solver, and preserves SEAL ORDER as the single confirmation + the one-per-round lock; web builds; no key in the browser.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → solver (:4100) | tamper trigger + NL parse; the browser holds no operator/Anthropic credential |
| desk plane → Venue.SubmitOrder | the NL parse only prefills; the desk's own token still authorizes the actual submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-OPTOK | Elevation of Privilege | operator token in the browser | mitigate | BreakTheAiPanel/OrderTicket call web/src/solver.ts (the sole operator proxy); no operator token/context imported (threat T-06-01 precedent) |
| T-08-06-INJ | Tampering | prompt-injected NL order | mitigate | parseOrder output only PREFILLS; SEAL ORDER stays the single confirm; a hostile order is a normal sealed order re-verified at Clear (never auto-submit) |
| T-08-06-TAMPER | Tampering | the tamper control | mitigate | it only ATTEMPTS an atomic Round.Clear (server-side, rejected → nothing changes); DEMO · ADVERSARIAL tag + verbatim reject are the safety signal (no irreversible write added) |
| T-08-06-KEY | Information Disclosure | Anthropic key near the browser | mitigate | parseOrder hits the solver; no key literal in web/src (RESEARCH Anti-Pattern) |
</threat_model>

## Artifacts this phase produces

| Symbol / File | Kind | Notes |
|---------------|------|-------|
| `web/src/solver.ts` `parseOrder` / `tamperClear` | exports (functions) | typed :4100 client methods |
| `web/src/components/BreakTheAiPanel.tsx` | component (NEW) | WOW-02 before/after tamper panel |
| OrderTicket NL sub-block | in-place UI | WOW-03 prefill, never auto-submit |
| AgentView mount of `<BreakTheAiPanel/>` | wiring | below the shipped grid |

<verification>
- `cd web && npm run build` passes; `grep -rn ':4000' web/src` empty; no operator token/Anthropic key in web/src.
- Live (phase gate): tamper shows the verbatim reject then a $100.00 correct clear; NL parse prefills the ticket, SEAL ORDER still required.
</verification>

<success_criteria>
A demo control forces a wrong clear and shows the verbatim on-ledger rejection, then the correct clear settles at $100.00; a desk enters plain English and Claude's structured output prefills the ticket for confirmation (never auto-submitted). Both go through the solver plane; no operator/Anthropic credential is in the browser; existing tokens/motifs are reused.
</success_criteria>

<output>
Create `.planning/phases/08-demo-hardening/08-06-SUMMARY.md` when done.
</output>
