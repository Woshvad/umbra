---
phase: 08-demo-hardening
plan: 07
type: execute
wave: 5
depends_on: [08-01, 08-05, 08-06]
files_modified:
  - web/src/solver.ts
  - web/src/lib/solverUrls.test.ts
  - web/src/components/AgentRationale.tsx
  - web/src/components/RoundBrief.tsx
  - web/src/components/ProofPackButton.tsx
  - web/src/views/SettlementView.tsx
autonomous: true
requirements: [WOW-04, WOW-05]
must_haves:
  truths:
    - "The shipped AgentRationale panel appends the solver's rationale live from an SSE token stream (flame caret riding the insertion point); if the stream is unavailable it falls back to the shipped single-shot 26ms/char typewriter — identical appearance"
    - "A post-settle Round Brief block shows the shareable NL summary with COPY BRIEF + DOWNLOAD BRIEF ↓; it appears only when the round is settled"
    - "One click on DOWNLOAD PROOF-PACK ↓ downloads the on-brand proof-pack PDF (four bundles) post-settle"
    - "No new tokens/keyframes are introduced; the browser holds no operator/Anthropic credential; no :4000 literal is added"
  artifacts:
    - path: "web/src/components/RoundBrief.tsx"
      provides: "WOW-04 shareable brief block (copy/download) on the Settlement view"
    - path: "web/src/components/ProofPackButton.tsx"
      provides: "WOW-05 one-click proof-pack download"
    - path: "web/src/solver.ts"
      provides: "rationaleStreamUrl() + getBrief()/proofPackUrl() client helpers"
      exports: ["rationaleStreamUrl", "proofPackUrl"]
  key_links:
    - from: "web/src/components/AgentRationale.tsx"
      to: "solver /round/:id/rationale-stream (SSE)"
      via: "EventSource(rationaleStreamUrl(id)); fallback to single-shot typewriter"
      pattern: "EventSource|rationale-stream"
    - from: "web/src/components/ProofPackButton.tsx"
      to: "solver /round/:id/proof-pack.pdf"
      via: "proofPackUrl(id) download trigger"
      pattern: "proof-pack"
---

<objective>
Ship the last two Phase-8 UI surfaces. WOW-04: swap the shipped `AgentRationale` data source to a live SSE token stream (`GET /round/:id/rationale-stream`) so the rationale types out as the solver streams it, with a graceful fallback to the shipped single-shot typewriter; plus a post-settle `RoundBrief` block (shareable NL summary + COPY/DOWNLOAD). WOW-05: a post-settle `ProofPackButton` that one-click downloads the on-brand proof-pack PDF (`GET /round/:id/proof-pack.pdf`). Add the backing web client helpers.

Purpose: The live-streamed rationale makes the AI's reasoning legible in real time; the brief + proof-pack make the round shareable/auditable. All reuse the shipped ink panel, tokens, and motion contract.
Output: `rationaleStreamUrl`/`getBrief`/`proofPackUrl` in web/src/solver.ts, AgentRationale SSE source, RoundBrief + ProofPackButton on SettlementView.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-demo-hardening/08-UI-SPEC.md
@.planning/phases/08-demo-hardening/08-PATTERNS.md
@.planning/phases/08-demo-hardening/08-RESEARCH.md
@web/src/components/AgentRationale.tsx
@web/src/solver.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Web client helpers — rationaleStreamUrl() + getBrief() + proofPackUrl() (+ pure test)</name>
  <files>web/src/solver.ts, web/src/lib/solverUrls.test.ts</files>
  <read_first>
    - web/src/solver.ts (the call<T> wrapper; SOLVER_BASE_URL + solverPort/OFFLINE_CAPTION from 08-01; the endpoint fns; the settled RoundResponse type — the `brief` field added by 08-04)
    - solver/src/api.ts (the GET /round/:id/rationale-stream SSE + the settled `brief` field + GET /round/:id/proof-pack.pdf shapes from 08-04/08-05)
    - 08-RESEARCH.md Pitfall 5 (EventSource needs no auth header — operator plane; use `EventSource(`${SOLVER_BASE_URL}/round/${id}/rationale-stream`)`) + § Supporting stack (Blob/createObjectURL for downloads)
    - an existing web/src/lib/*.test.ts for the pure vitest style
  </read_first>
  <action>
    In web/src/solver.ts add: `rationaleStreamUrl(id: string): string` returning `${SOLVER_BASE_URL}/round/${id}/rationale-stream` (a plain URL — EventSource consumes it, no auth header, Pitfall 5); `proofPackUrl(id: string): string` returning `${SOLVER_BASE_URL}/round/${id}/proof-pack.pdf`; and `getBrief(id: string)` reading the settled `brief` (reuse getRound and return its `brief`, or a dedicated call if the backend exposed one). Do NOT add any port literal (reuse SOLVER_BASE_URL). Add a tiny pure test web/src/lib/solverUrls.test.ts asserting both URL builders produce `${SOLVER_BASE_URL}/round/<id>/…` and carry no `:4000`.
  </action>
  <acceptance_criteria>
    - `cd web && npx vitest run src/lib/solverUrls.test.ts` green (URL builders correct, no :4000)
    - `cd web && npm run build` passes; `grep -rn ':4000' web/src` returns nothing
    - the helpers reference SOLVER_BASE_URL (no duplicated port literal); no credential in the bundle
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx vitest run src/lib/solverUrls.test.ts</automated>
  </verify>
  <done>web/src/solver.ts exposes rationaleStreamUrl/proofPackUrl/getBrief built from SOLVER_BASE_URL, unit-tested, no :4000, no credential.</done>
</task>

<task type="auto">
  <name>Task 2: WOW-04 — AgentRationale live SSE source with single-shot fallback</name>
  <files>web/src/components/AgentRationale.tsx</files>
  <read_first>
    - web/src/components/AgentRationale.tsx (the ink panel L68-92; the useEffect typewriter L32-56; CHAR_MS=26; prefersReducedMotion L19-25; interval cleared on unmount L55; the flame caret L81-91)
    - web/src/solver.ts (rationaleStreamUrl from Task 1)
    - 08-UI-SPEC.md Reconciliation Note 5 + § "WOW-04 live stream" (reuse the shipped ink panel + flame caret UNCHANGED; only the SOURCE becomes an SSE token stream that appends live; reduced-motion → append per chunk without the per-char interval; graceful fallback: if the stream is unavailable, fall back to the shipped single-shot 26ms/char typewriter — identical appearance) + Motion Contract (no new keyframes)
    - 08-RESEARCH.md Pitfall 5 (EventSource; disable buffering; single-shot fallback so the demo never stalls) + Pitfall 7 (reduced-motion guard)
  </read_first>
  <action>
    Extend AgentRationale to accept an optional live-source prop (e.g. an `roundId` or `streamId` that enables SSE). When enabled AND `typeof EventSource !== 'undefined'`, open `new EventSource(rationaleStreamUrl(id))`, append each `data:` delta into the SAME ink panel (the flame caret riding the live insertion point), close on the `done` event; on `error` (or when the source is disabled/absent) FALL BACK to the shipped single-shot `rationale` typewriter — identical appearance (UI-SPEC note 5). Under prefers-reduced-motion, append per chunk without the per-char interval. Clear/close the EventSource on unmount (mirror the shipped interval cleanup). Keep the existing `rationale`/`preview` props and behavior intact so callers without the live source are unchanged. Introduce NO new token/keyframe.
  </action>
  <acceptance_criteria>
    - `cd web && npm run build` passes; the shipped single-shot behavior still works when no live source is provided
    - the EventSource is closed on unmount; reduced-motion path appends without the per-char interval
    - `grep -rn ':4000' web/src` returns nothing; the panel uses the existing ink surface + flame caret (no new keyframe)
    - MANUAL (phase gate, 08-VALIDATION.md Manual-Only): trigger a solve → the rationale types out live from the SSE; kill the stream → the single-shot fallback still renders text
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run build && ! grep -rn ':4000' src</automated>
    <human-check>Trigger a solve; observe live SSE token append with the flame caret; stop the solver mid-stream → single-shot fallback renders the rationale.</human-check>
  </verify>
  <done>AgentRationale appends the rationale live via SSE with a graceful single-shot fallback, reusing the shipped ink panel/caret and honoring reduced-motion; the source cleans up on unmount.</done>
</task>

<task type="auto">
  <name>Task 3: WOW-04 RoundBrief + WOW-05 ProofPackButton on SettlementView (post-settle)</name>
  <files>web/src/components/RoundBrief.tsx, web/src/components/ProofPackButton.tsx, web/src/views/SettlementView.tsx</files>
  <read_first>
    - web/src/views/SettlementView.tsx (the settled state `settled`/`phase === 'settled'` gating; the CTA row grammar; the OFFLINE_CAPTION path from 08-01; the DvP legs + atomic stamp composition — mount the new blocks BELOW the CTA / SETTLED confirmation)
    - web/src/components/AgentRationale.tsx (the ink panel treatment for RoundBrief prose) + web/src/solver.ts (getBrief + proofPackUrl from Task 1)
    - 08-UI-SPEC.md § "WOW-04 Shareable Round Brief" (a 1px-ink bordered block below the Settlement CTA; sub-label "Round Brief · shareable"; Inter 14px/1.6 prose; ghost-mono COPY BRIEF + DOWNLOAD BRIEF ↓; appears only when settled) + § "WOW-05 — Download Proof-Pack" (ink-bordered ghost `DOWNLOAD PROOF-PACK ↓`, mono 13px/700, padding 15px 28px, distinct from the primary SETTLE ATOMICALLY, never red/lime; states ready/preparing/done/error) + Copywriting rows (COPY BRIEF · DOWNLOAD BRIEF ↓ · DOWNLOAD PROOF-PACK ↓ · PREPARING PROOF-PACK… · the error copy) + Color (ink/paper only for these CTAs)
    - 08-RESEARCH.md § Supporting stack (Blob + URL.createObjectURL for the brief download; the proof-pack via an <a download> to proofPackUrl)
  </read_first>
  <action>
    Create web/src/components/RoundBrief.tsx: given the settled brief text (from getBrief / the settled RoundResponse `brief`), render the "Round Brief · shareable" bordered block (Inter prose on the ink/paper grammar) with COPY BRIEF (navigator.clipboard) and DOWNLOAD BRIEF ↓ (Blob + createObjectURL, .txt). Create web/src/components/ProofPackButton.tsx: the ink-bordered ghost `DOWNLOAD PROOF-PACK ↓` that navigates/anchors to `proofPackUrl(roundId)` (Content-Disposition: attachment streams the PDF); states ready → preparing (`PREPARING PROOF-PACK…`, disabled) → done; on failure show the verbatim WOW-05 error copy. Mount BOTH in SettlementView BELOW the CTA/SETTLED confirmation, gated on `settled`/`phase === 'settled'` (hidden pre-settle). Reuse existing tokens/motifs; NO new token/keyframe; the CTAs are ink/paper (never red/lime). No operator token in the browser.
  </action>
  <acceptance_criteria>
    - `cd web && npm run build` passes with both blocks mounted and gated on settled
    - `grep -rn ':4000' web/src` returns nothing; the CTAs are ink/paper (no lime/red on these controls); no operator token literal
    - the copy (COPY BRIEF · DOWNLOAD BRIEF ↓ · DOWNLOAD PROOF-PACK ↓ · PREPARING PROOF-PACK… · error) matches 08-UI-SPEC.md verbatim
    - MANUAL (phase gate): post-settle, the Round Brief shows the shareable summary (copy/download work) and DOWNLOAD PROOF-PACK ↓ downloads the on-brand PDF
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run build && ! grep -rn ':4000' src</automated>
    <human-check>Settle a round; confirm the Round Brief appears with working COPY/DOWNLOAD and that DOWNLOAD PROOF-PACK ↓ downloads an on-brand PDF; both hidden pre-settle.</human-check>
  </verify>
  <done>RoundBrief + ProofPackButton appear only post-settle, offer copy/download + a one-click on-brand proof-pack PDF, reuse the ink/paper grammar, and add no new token; web builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → solver SSE (:4100) | consumes the rationale token stream; no auth header (operator plane) |
| browser → solver proof-pack (:4100) | downloads the PDF; holds no server credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-SSEKEY | Information Disclosure | SSE stream contents rendered in the UI | mitigate | the solver onError emits only a fixed fallback frame (08-04); the UI renders deltas as-is, never a key/prompt; the browser adds no auth header |
| T-08-07-BRIEF | Information Disclosure | brief/proof-pack download | mitigate | brief + proof-pack are composed server-side from numbers only (08-04/08-05 secret sweeps); the download is a plain fetch/anchor with no credential |
| T-08-07-STALL | Denial of Service | SSE unavailable stalls the panel | mitigate | graceful fallback to the shipped single-shot typewriter (UI-SPEC note 5); the demo never stalls |
| T-08-07-OPTOK | Elevation of Privilege | operator token in the browser | mitigate | all calls go through web/src/solver.ts; no operator token/context imported |
</threat_model>

## Artifacts this phase produces

| Symbol / File | Kind | Notes |
|---------------|------|-------|
| `web/src/solver.ts` `rationaleStreamUrl` / `proofPackUrl` / `getBrief` | exports (functions) | SSE + download URL helpers |
| `web/src/components/AgentRationale.tsx` SSE source | extension | live append + single-shot fallback |
| `web/src/components/RoundBrief.tsx` | component (NEW) | shareable brief (copy/download) |
| `web/src/components/ProofPackButton.tsx` | component (NEW) | one-click proof-pack PDF download |
| SettlementView mounts (RoundBrief + ProofPackButton) | wiring | gated on settled |

<verification>
- `cd web && npx vitest run src/lib/solverUrls.test.ts` green; `cd web && npm run build` passes; `grep -rn ':4000' web/src` empty.
- Live (phase gate): rationale streams live with single-shot fallback; the brief copies/downloads; the proof-pack downloads on-brand.
</verification>

<success_criteria>
The solver's rationale streams live into the shipped panel (with a graceful single-shot fallback), a post-settle Round Brief is copyable/downloadable, and one click downloads the on-brand proof-pack PDF. No new tokens/keyframes, no :4000 literal, and no operator/Anthropic credential in the browser.
</success_criteria>

<output>
Create `.planning/phases/08-demo-hardening/08-07-SUMMARY.md` when done.
</output>
