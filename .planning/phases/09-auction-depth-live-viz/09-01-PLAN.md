---
phase: 09-auction-depth-live-viz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - daml/Umbra/Clearing.daml
  - daml/Umbra/Auction.daml
  - daml/Umbra/Roles.daml
  - daml/Umbra/Setup.daml
  - daml/Umbra/Tests.daml
  - solver/src/auction.ts
  - solver/src/ledger.ts
  - web/daml.js
  - web/src/components/OrderTicket.tsx
  - RULEBOOK.md
autonomous: true
requirements: [AUCT-01, AUCT-02]
must_haves:
  truths:
    - "The §4 fixture still clears at exactly $100.00 with fills A=10 / B=8 / C=2 (pure reduction — no behavior change)"
    - "Order, OrderView, Venue.SubmitOrder carry orderType/minQty/firmIf; §4 orders are plain Limit"
    - "Clearing.daml and auction.ts still produce byte-identical results (golden parity intact)"
    - "web/daml.js regenerated + committed; web builds against the new Order/SubmitOrder shape"
  artifacts:
    - path: "daml/Umbra/Clearing.daml"
      provides: "OrderType type + appended OrderView fields"
      contains: "data OrderType"
    - path: "daml/Umbra/Auction.daml"
      provides: "Order template appended fields + relaxed ensure + threaded Round.Clear OrderView build"
      contains: "orderType"
    - path: "solver/src/auction.ts"
      provides: "TS OrderType type + optional OrderView fields (no math change)"
      contains: "OrderType"
    - path: "RULEBOOK.md"
      provides: "Sovereign-grade clearing rulebook skeleton (AUCT-02 doc anchor)"
      contains: "topPrices"
  key_links:
    - from: "web/src/components/OrderTicket.tsx"
      to: "Venue.SubmitOrder"
      via: "ledger.exercise with appended orderType/minQty/firmIf"
      pattern: "SubmitOrder"
    - from: "solver/src/ledger.ts"
      to: "OrderView"
      via: "readSealedOrders maps orderType/minQty/firmIf from the v2 wire"
      pattern: "orderType"
---

<objective>
Land the ADDITIVE order-model foundation for Phase 9 with ZERO observable behavior change: extend the frozen `Order` template, the pure `OrderView`, `Venue.SubmitOrder`, and their TS mirror with an `orderType` discriminator (defaulting to `Limit`) plus optional `minQty`/`firmIf` params, updating every construction site to the `Limit` default, regenerating the frontend bindings, and creating the `RULEBOOK.md` skeleton (AUCT-02 doc anchor).

Purpose: This is the foundational wave-1 plan the whole phase rides on (phase critical path #2). The §4 canary MUST stay green as a PURE REDUCTION before any new-order-type math (09-02/09-03) or UI (09-06) is built. No new clearing rule is added here — the new fields are inert until wave 2.
Output: Extended data model across both planes + regenerated `web/daml.js` + `RULEBOOK.md` skeleton, with the §4 fixture still clearing $100.00 / A=10 / B=8 / C=2 and Daml⇄TS golden parity intact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-auction-depth-live-viz/09-CONTEXT.md
@.planning/phases/09-auction-depth-live-viz/09-RESEARCH.md
@.planning/phases/09-auction-depth-live-viz/09-PATTERNS.md
@daml/Umbra/Clearing.daml
@daml/Umbra/Auction.daml
@daml/Umbra/Roles.daml
@solver/src/auction.ts
@solver/src/ledger.ts
</context>

<artifacts_produced>
New symbols this plan creates (drift-verification excludes these as newly-created):
- Daml: `data OrderType = Limit | Noncompetitive | AllOrNone | Conditional` (in `Clearing.daml`, re-exported by `Auction`); `OrderView` fields `orderType : OrderType`, `minQty : Optional Int`, `firmIf : Optional Decimal`; `Order` fields `orderType`/`minQty`/`firmIf`; `Venue.SubmitOrder` args `orderType`/`minQty`/`firmIf`.
- TS: `export type OrderType`; `OrderView` optional fields `orderType?`/`minQty?`/`firmIf?` (in `solver/src/auction.ts`).
- New file: `RULEBOOK.md` (repo root).
- Regenerated artifact: `web/daml.js` bindings (committed).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Additive Daml order model (Clearing + Auction + Roles + seeds/tests), zero behavior change</name>
  <files>daml/Umbra/Clearing.daml, daml/Umbra/Auction.daml, daml/Umbra/Roles.daml, daml/Umbra/Setup.daml, daml/Umbra/Tests.daml</files>
  <read_first>
    - daml/Umbra/Clearing.daml (OrderView lines 45-50; demandAt/supplyAt/choosePStar/rationByPriority/computeClearing — DO NOT modify these here)
    - daml/Umbra/Auction.daml (Order template lines 59-70; Round.Clear OrderView build lines 176-181; module export list lines 10-14)
    - daml/Umbra/Roles.daml (Venue.SubmitOrder lines 18-30)
    - 09-PATTERNS.md sections "daml/Umbra/Clearing.daml", "daml/Umbra/Auction.daml", "daml/Umbra/Roles.daml", "Setup.daml + Tests.daml" (exact edit sites + Pitfall 5 grep list)
    - 09-RESEARCH.md "Order-Type Encoding (Daml 3.4)" (effective-limit approach, A1) + "Pattern 3: Additive Optional-at-end fields"
  </read_first>
  <action>
    Add `data OrderType = Limit | Noncompetitive | AllOrNone | Conditional deriving (Eq, Show)` to `Clearing.daml` beside `OrderView` (~line 50) and append three fields to `OrderView`: `orderType : OrderType`, `minQty : Optional Int`, `firmIf : Optional Decimal`. KEEP `limit : Decimal` (effective-limit approach per RESEARCH A1 — do NOT make it Optional). Do NOT touch `demandAt`/`supplyAt`/`choosePStar`/`rationByPriority`/`computeClearing` — behavior must be byte-identical (the new fields are inert until 09-02). Re-export `OrderType(..)` from `Umbra.Auction`'s explicit export list (beside `Side(..)`/`Allocation(..)`).
    In `Auction.daml` `Order` (lines 59-70): append the same three fields; relax `ensure` to `quantity > 0 && (orderType == Noncompetitive || limit > 0.0) && optional True (\m -> m >= 1 && m <= quantity) minQty && optional True (> 0.0) firmIf`. In the `Round.Clear` OrderView build (lines 176-181) thread `orderType = o.orderType; minQty = o.minQty; firmIf = o.firmIf` into each `OrderView with …`. The recompute-and-assert shape (lines 182-192) stays UNCHANGED.
    In `Roles.daml` `Venue.SubmitOrder` (lines 18-30): append args `orderType : OrderType`, `minQty : Optional Int`, `firmIf : Optional Decimal`; thread them into `create Order`.
    Update EVERY construction site to the Limit default (Pitfall 5 — Daml has no field defaults): `Setup.daml` SubmitOrder calls at lines 85/88/91 (runCanonicalRound) and 129/132/135 (seedOpenRound); `Tests.daml` `OrderView with` literals at lines 101-103 and SubmitOrder calls at 179/182/185 and 349/352/355 — each gains `orderType = Limit; minQty = None; firmIf = None`. The §4 assertion literals ($100.00 / A=10 / B=8 / C=2) stay unchanged. Grep every `OrderView with` / `Order with` / `SubmitOrder` site to confirm none is missed.
  </action>
  <acceptance_criteria>
    - `bash -lc "export PATH=/c/Users/woshv/bin:$PATH && cd daml && daml build"` compiles clean.
    - `bash -lc "export PATH=/c/Users/woshv/bin:$PATH && cd daml && daml test"` — ALL existing tests green: `test_clears_at_100` ($100.00, A=10/B=8/C=2 canary), `test_settled_balances`, `test_atomicity`, `test_clear_rejects_bad_allocation`, `test_privacy_orders`, `test_privacy_confirmations`.
    - `demandAt`/`supplyAt`/`choosePStar`/`rationByPriority`/`computeClearing` bodies are UNCHANGED (git diff shows only field additions + construction-site edits, no predicate/math edits).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "export PATH=/c/Users/woshv/bin:$PATH && cd daml && daml test"</automated>
  </verify>
  <done>The §4 canary + all existing Daml tests pass; the order model carries orderType/minQty/firmIf additively; no clearing math changed.</done>
</task>

<task type="auto">
  <name>Task 2: TS mirror + solver read-mapping + regenerate bindings + wire OrderTicket exercise (Limit default)</name>
  <files>solver/src/auction.ts, solver/src/ledger.ts, web/daml.js, web/src/components/OrderTicket.tsx</files>
  <read_first>
    - solver/src/auction.ts (OrderView interface lines 22-27; Side type line 18 — mirror the new type here, DO NOT change the math)
    - solver/src/ledger.ts (readSealedOrders lines 224-237 — the view mapping + Number()/string decode pattern)
    - web/src/components/OrderTicket.tsx (onSeal SubmitOrder exercise lines 100-129; import paths lines ~16-18)
    - 09-PATTERNS.md sections "solver/src/auction.ts", "solver/src/ledger.ts", "web/src/components/OrderTicket.tsx" (Pitfall 4 stale-bindings warning)
    - 09-RESEARCH.md "Runtime State Inventory" (regenerate + commit web/daml.js — commit 6e4bade invariant)
  </read_first>
  <action>
    In `solver/src/auction.ts`: add `export type OrderType = 'Limit' | 'Noncompetitive' | 'AllOrNone' | 'Conditional'` and append OPTIONAL fields to the `OrderView` interface — `orderType?: OrderType`, `minQty?: number`, `firmIf?: number`. Making them optional keeps the existing §4 test literals (which omit them) valid; the pure functions stay UNCHANGED (behavior identical — `orderType` defaults semantically to `Limit`). Do NOT edit `demandAt`/`supplyAt`/`choosePStar`/`rationByPriority`/`computeClearing`.
    In `solver/src/ledger.ts` `readSealedOrders` (lines 224-237): map the three new fields into the returned `view` — `orderType: (c.createArgument.orderType ?? 'Limit') as OrderType`, `minQty: c.createArgument.minQty != null ? Number(c.createArgument.minQty) : undefined`, `firmIf: c.createArgument.firmIf != null ? Number(c.createArgument.firmIf) : undefined` (Optional decode; numeric fields arrive as strings on the v2 wire → `Number()`).
    Regenerate the frontend bindings from the rebuilt DAR (`daml codegen js` → `web/daml.js`) and COMMIT the regenerated output in this change (fresh-clone-builds invariant, commit 6e4bade). Confirm the exact dar path/flags against the repo's existing codegen invocation.
    In `web/src/components/OrderTicket.tsx` onSeal (lines 113-119): append `orderType: 'Limit'`, `minQty: null`, `firmIf: null` to the `Venue.SubmitOrder` exercise payload so the regenerated (now-required) choice args are satisfied. `loadDemo` stays a plain Limit. Do NOT build the order-type selector UI here — that is plan 09-06.
  </action>
  <acceptance_criteria>
    - `cd solver && npx vitest run auction.test.ts` — the §4 scenario (clears 100.00, A=10/B=8/C=2) + the 99-vs-100 trap scenario green (parity intact).
    - `cd solver && npx tsc --noEmit` clean.
    - `cd web && npm run build` succeeds (web compiles against the regenerated bindings; the SubmitOrder exercise carries the new fields).
    - `web/daml.js` regenerated files are staged/committed (not stale) — the generated Order/SubmitOrder type includes orderType/minQty/firmIf.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run auction.test.ts</automated>
  </verify>
  <done>auction.ts mirrors the new fields (no math change), ledger.ts decodes them, bindings are regenerated + committed, and the desk ticket submits a plain-Limit order end-to-end.</done>
</task>

<task type="auto">
  <name>Task 3: RULEBOOK.md skeleton (AUCT-02 doc anchor, cites both planes)</name>
  <files>RULEBOOK.md</files>
  <read_first>
    - daml/Umbra/Clearing.daml (choosePStar lines 87-101 topPrices trap guard; rationByPriority lines 115-119; computeClearing 126-139)
    - solver/src/auction.ts (choosePStar lines 64-79; rationByPriority 87-99; computeClearing 105-122)
    - 09-RESEARCH.md "RULEBOOK.md (AUCT-02)" + "Deterministic Algorithms" (bp formula, AON scaling caveat)
    - 09-PATTERNS.md "No Analog Found" (RULEBOOK register model = DECISIONS.md / spec §8 tone)
  </read_first>
  <action>
    Create `RULEBOOK.md` at the repo root — a sovereign-grade written doc that states the clearing objective applied IN ORDER: (1) maximize matched volume; (2) among the max-matched candidate prices, minimize |demand − supply|; (3) lower price. Document the LOAD-BEARING `topPrices` trap guard (filter to the max-matched subset BEFORE the imbalance/price rank — applying imbalance across ALL prices wrongly clears §4 at 99), citing `Clearing.daml::choosePStar` (lines 87-101) AND `auction.ts::choosePStar` (lines 64-79). Document the greedy price-priority rationing rule citing `rationByPriority` in both planes, and pin the bp formula `improvementVsLimitBp = round((|limit − p*| / p*) * 10000)`. Add PLACEHOLDER sections titled "Noncompetitive", "AllOrNone / MAQ", and "Conditional (auto-firming)" — each noting it will be filled by plans 09-02/09-03 and each MUST cite the exact function/symbol in BOTH `Clearing.daml` and `auction.ts` so the doc and code cannot drift. Include the §4 worked example ($100.00, A=10/B=8/C=2) and the AON exponential-scaling caveat (bounded powerset over the tiny demo book; not for large books — deferred).
    Do NOT put fenced implementation code in the doc beyond symbol names/signatures; the golden-eval suite is the enforcement mechanism (state this).
  </action>
  <acceptance_criteria>
    - `RULEBOOK.md` exists at repo root.
    - `grep -c "Clearing.daml" RULEBOOK.md` ≥ 1 AND `grep -c "auction.ts" RULEBOOK.md` ≥ 1 (cites both planes).
    - `grep -i "topPrices" RULEBOOK.md` matches (trap guard documented); the objective ordering (max matched → min imbalance → lower price) and the §4 example ($100.00) are present.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "test -f RULEBOOK.md && grep -q 'Clearing.daml' RULEBOOK.md && grep -q 'auction.ts' RULEBOOK.md && grep -qi 'topPrices' RULEBOOK.md && echo RULEBOOK_OK"</automated>
  </verify>
  <done>RULEBOOK.md documents the objective + trap guard + bp formula + §4 example, cites both code planes, and reserves the three per-type sections for 09-02/09-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desk browser → JSON Ledger API v2 (:3975) | Desk submits a sealed Order as its OWN party token (never the operator token); the appended fields cross here |
| solver (Operator) → Round.Clear | The on-ledger recompute-and-assert backstop; unchanged this plan (fields are inert) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | Order `ensure` guards | mitigate | Relaxed `ensure` still rejects malformed params on-ledger (`minQty` in [1,quantity]; `firmIf > 0`); a malformed order cannot be created |
| T-09-01-02 | Information disclosure | Order privacy (signatory operator+desk, NO observer) | mitigate | No observer added; the appended fields do not widen visibility — privacy stays structural (PRIV-01..04 tests stay green) |
| T-09-01-03 | Tampering | stale `web/daml.js` bindings | mitigate | Regenerate + commit bindings in the same change (Pitfall 4); web build gate fails if the new fields are dropped |
| T-09-01-SC | Tampering | package installs | accept | NO package installs this phase (RESEARCH Package Legitimacy Audit is empty by design) — supply-chain gate N/A |
</threat_model>

<verification>
- `bash -lc "export PATH=/c/Users/woshv/bin:$PATH && cd daml && daml test"` — full Daml suite green (§4 canary is the continuous guard).
- `cd solver && npx vitest run auction.test.ts` — §4 + trap scenarios green (Daml⇄TS parity intact).
- `cd web && npm run build` — web compiles against regenerated bindings.
- git diff confirms the clearing math bodies are unchanged (pure-reduction invariant).
</verification>

<success_criteria>
- The order model (Daml + TS) carries `orderType`/`minQty`/`firmIf` additively; §4 orders are plain `Limit`.
- The §4 fixture still clears at exactly $100.00 with fills A=10 / B=8 / C=2 (pure reduction).
- `web/daml.js` regenerated + committed; the desk ticket submits a plain-Limit order end-to-end.
- `RULEBOOK.md` skeleton exists, documents the objective + trap guard, and cites both code planes.
</success_criteria>

<output>
Create `.planning/phases/09-auction-depth-live-viz/09-01-SUMMARY.md` when done.
</output>
