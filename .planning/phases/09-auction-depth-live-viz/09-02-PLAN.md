---
phase: 09-auction-depth-live-viz
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - daml/Umbra/Clearing.daml
  - solver/src/auction.ts
  - daml/Umbra/Tests.daml
  - solver/src/auction.test.ts
  - RULEBOOK.md
autonomous: true
requirements: [AUCT-01, AUCT-02]
must_haves:
  truths:
    - "The §4 fixture clears at exactly $100.00 with fills A=10 / B=8 / C=2 after the two-pass refactor + noncompetitive rule"
    - "The 99-vs-100 tie-break trap still clears at 100 (topPrices guard preserved)"
    - "A noncompetitive order fills at the clearing price with top priority, capped at its qty, and adds no candidate price"
    - "Clearing.daml and auction.ts produce byte-identical (price, allocations) on every fixture (golden parity)"
  artifacts:
    - path: "daml/Umbra/Clearing.daml"
      provides: "coreClear extraction + two-pass computeClearing wrapper + noncompetitive predicate/priority"
      contains: "coreClear"
    - path: "solver/src/auction.ts"
      provides: "byte-identical TS mirror of coreClear + two-pass + noncompetitive"
      contains: "coreClear"
    - path: "daml/Umbra/Tests.daml"
      provides: "test_noncomp_* golden fixture"
      contains: "test_noncomp"
    - path: "solver/src/auction.test.ts"
      provides: "noncomp golden scenario mirroring the Daml fixture"
      contains: "noncomp"
  key_links:
    - from: "daml/Umbra/Clearing.daml::computeClearing"
      to: "coreClear"
      via: "two-pass wrapper calls coreClear"
      pattern: "coreClear"
    - from: "solver/src/auction.ts::computeClearing"
      to: "coreClear"
      via: "identical two-pass wrapper (parity)"
      pattern: "coreClear"
---

<objective>
Begin the deterministic clearing rulebook (AUCT-02) with two lockstep, golden-first changes landed as ONE plan (Daml + TS together, never split): (1) a behavior-preserving refactor of `computeClearing` into an extracted `coreClear` plus a two-pass wrapper scaffold, and (2) the NONCOMPETITIVE order type — "fill at clear", top priority, any price, capped at qty. Both halves ship in `Clearing.daml` AND `solver/src/auction.ts` in the same commit, gated by the §4 canary, the 99-vs-100 trap regression, and Daml⇄TS golden parity.

Purpose: The two-pass scaffold is the seam conditional auto-firming (09-03) rides on; noncompetitive is the first new order type. Landing them behavior-first (refactor reduces to today) then additively (noncomp) keeps the §4 canary green after every step (phase critical path #1, #2).
Output: `coreClear` + two-pass `computeClearing` in both planes, the noncompetitive rule mirrored byte-identically, matching golden fixtures in both planes, and the RULEBOOK "Noncompetitive" section filled.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-auction-depth-live-viz/09-CONTEXT.md
@.planning/phases/09-auction-depth-live-viz/09-RESEARCH.md
@.planning/phases/09-auction-depth-live-viz/09-PATTERNS.md
@.planning/phases/09-auction-depth-live-viz/09-01-SUMMARY.md
@daml/Umbra/Clearing.daml
@solver/src/auction.ts
</context>

<artifacts_produced>
New symbols this plan creates:
- Daml: `coreClear : [OrderView] -> (Decimal, [Allocation])` (extracted from today's `computeClearing`); `computeClearing` becomes a two-pass wrapper (partition firm vs conditional); an `effLimit`/noncompetitive predicate in `demandAt`/`supplyAt`; a noncompetitive-first sort key in the rationing sort. In `Clearing.daml`.
- TS: identical `coreClear`, two-pass `computeClearing`, `effLimit`/noncomp predicate, `[isNoncomp?0:1, -limit]` sort key. In `solver/src/auction.ts`.
- Tests: `test_noncomp_*` (Tests.daml) + a `noncomp` scenario (auction.test.ts).
- Doc: RULEBOOK.md "Noncompetitive" section (filled).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Two-pass refactor — coreClear + computeClearing wrapper (behavior-preserving, both planes)</name>
  <files>daml/Umbra/Clearing.daml, solver/src/auction.ts, daml/Umbra/Tests.daml, solver/src/auction.test.ts, RULEBOOK.md</files>
  <read_first>
    - daml/Umbra/Clearing.daml (computeClearing lines 126-139; choosePStar 87-101; rationByPriority 115-119)
    - solver/src/auction.ts (computeClearing lines 105-122; choosePStar 64-79; rationByPriority 87-99)
    - solver/src/auction.test.ts (scenario 1 §4 lines 34-61; scenario 5 the 99-vs-100 trap lines 63-70+)
    - 09-RESEARCH.md "Conditional auto-firming — single deterministic TWO-PASS" (the coreClear/two-pass shape + §4 reduction)
    - 09-PATTERNS.md "computeClearing — becomes a TWO-PASS wrapper over an extracted coreClear"
  </read_first>
  <behavior>
    - §4 fixture (A Buy 10@101, B Sell 8@99, C Sell 5@100) → price 100.00, fills A=10/B=8/C=2 (unchanged).
    - 99-vs-100 trap (Buy 10@100, Sell 10@99) → clears at 100 (topPrices guard preserved).
    - With NO conditional orders, `computeClearing` == a single `coreClear` call (pure reduction — identical output to pre-refactor).
  </behavior>
  <action>
    Extract today's `computeClearing` body (candidate-price select via `choosePStar` + buys DESC / sells ASC sort + `rationByPriority`) into a new top-level pure function `coreClear : [OrderView] -> (Decimal, [Allocation])` in `Clearing.daml`. Redefine `computeClearing` as a TWO-PASS wrapper: partition `views` into `firm = [ v | orderType v /= Conditional ]` and `conditional = [ v | orderType v == Conditional ]`; PASS 1 computes a provisional clear over `firm` (`(provP, _) = coreClear firm`); PASS 2 — for now `conditional` yields no firming logic yet (leave a documented hook: `firmed = []` placeholder is FORBIDDEN — instead compute `firmed = conditional` filtered by a `qualifies` stub that in THIS plan is a total pass-through so no behavior changes, OR simply `final = firm ++ conditional` when `conditional` is empty in every current fixture). Since no fixture has conditional orders yet, `final = firm ++ conditional = views` → `coreClear final` == today. Document that PASS-2 firming lands in 09-03. Mirror the SAME structure function-for-function in `solver/src/auction.ts` (`coreClear`, two-pass `computeClearing`) — identical partition + call order.
    Add/confirm a behavior-preserving parity fixture is exercised: the existing §4 + trap tests must pass unchanged in BOTH planes (no new fixture required for the refactor; the refactor is invisible).
    Update RULEBOOK.md: document that `computeClearing` is a two-pass wrapper over `coreClear`, citing both `Clearing.daml::coreClear`/`computeClearing` and `auction.ts::coreClear`/`computeClearing`, and that the §4/no-conditional case reduces to one `coreClear`.
  </action>
  <acceptance_criteria>
    - `bash -lc "export PATH=/c/Users/woshv/bin:$PATH && cd daml && daml test"` — `test_clears_at_100` ($100.00, A=10/B=8/C=2) + all existing tests green.
    - `cd solver && npx vitest run auction.test.ts` — §4 scenario (100.00) + the 99-vs-100 trap scenario green.
    - git diff shows `computeClearing` now delegates to `coreClear` in BOTH planes with identical partition/call structure; no fixture expected-number changed.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "export PATH=/c/Users/woshv/bin:$PATH && cd daml && daml test" && cd solver && npx vitest run auction.test.ts</automated>
  </verify>
  <done>coreClear + two-pass computeClearing exist byte-identically in both planes; §4 + trap still green as a pure reduction; the conditional firming hook is documented for 09-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Noncompetitive order type (fill-at-clear, top priority, any price) — Daml + TS lockstep + golden fixtures</name>
  <files>daml/Umbra/Clearing.daml, solver/src/auction.ts, daml/Umbra/Tests.daml, solver/src/auction.test.ts, RULEBOOK.md</files>
  <read_first>
    - daml/Umbra/Clearing.daml (demandAt/supplyAt lines 59-66; computeClearing buys/sells sort lines 132-133; rationByPriority 115-119)
    - solver/src/auction.ts (demandAt/supplyAt lines 46-51; buys/sells sort lines 108-113)
    - daml/Umbra/Tests.daml (test_clears_at_100 lines 91-110 — clone as the fixture template)
    - 09-RESEARCH.md "Noncompetitive — top-priority, any-price" (effLimit, sort key, Pitfall 6 Bool→0/1) + "Pitfall 3" (single-buyer settle — keep noncomp on the SELL side)
    - 09-PATTERNS.md "rationByPriority — top-level recursion" (noncomp-first sort key + TS Bool→0/1)
  </read_first>
  <behavior>
    - A noncompetitive SELL contributes to supply at ANY price (OR'd into supplyAt), adds NO candidate price, and is allocated with TOP priority (before competitive sells), capped at its qty.
    - §4 (all Limit) is unaffected: noncomp predicate is inert when no order is Noncompetitive → still 100.00 / A=10/B=8/C=2.
    - The noncomp golden fixture produces IDENTICAL (price, allocations) in Daml and TS.
  </behavior>
  <action>
    Implement the noncompetitive rule identically in `Clearing.daml` and `auction.ts`:
    - `demandAt`/`supplyAt`: OR the noncompetitive predicate into the willingness test — a buy contributes to demand at price p when `orderType == Noncompetitive || limit >= p`; a sell contributes to supply when `orderType == Noncompetitive || limit <= p` (effective-limit: noncomp is willing at any price). Noncomp orders add NO candidate price (`candidatePrices` unchanged — it enumerates limits; noncomp has no meaningful limit).
    - Rationing priority: extend the eligible-buys/sells sort key so noncompetitive fills FIRST. In Daml use `sortOn (\o -> (o.orderType /= Noncompetitive, negate effLimit))` (Bool `False < True` sorts noncomp first). In TS map the boolean to `0/1`: sort by `[isNoncomp ? 0 : 1, -effLimit]` lexicographically (Pitfall 6). `effLimit` = the order's limit for competitive orders and the any-price sentinel for noncomp (buys treat noncomp as most-aggressive, sells likewise). Keep the two planes bit-identical.
    - Add `test_noncomp_*` in `Tests.daml` (clone `test_clears_at_100`): a book with a noncompetitive SELL alongside a limit buy + limit sell (keep it single-buyer / noncomp on sell side so the settle-path demo stays valid — Pitfall 3), asserting the exact clearing price + per-desk fills. Add the mirroring `noncomp` scenario in `auction.test.ts` with the SAME expected numbers.
    - Fill the RULEBOOK "Noncompetitive" section: any-price contribution, top-priority allocation capped at qty (US-Treasury-style), the Bool→0/1 parity note, and the single-buyer settle caveat — citing `demandAt`/`supplyAt`/the sort key in BOTH planes.
  </action>
  <acceptance_criteria>
    - `bash -lc "export PATH=/c/Users/woshv/bin:$PATH && cd daml && daml test"` — `test_clears_at_100` (§4 canary) + `test_noncomp_*` green.
    - `cd solver && npx vitest run auction.test.ts` — §4 scenario + the 99-vs-100 trap scenario + the new `noncomp` scenario green, with identical expected numbers to the Daml fixture (parity).
    - `cd solver && npx tsc --noEmit` clean.
    - RULEBOOK "Noncompetitive" section is filled and cites both planes' symbols.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "export PATH=/c/Users/woshv/bin:$PATH && cd daml && daml test" && cd solver && npx vitest run auction.test.ts</automated>
  </verify>
  <done>Noncompetitive clears identically in Daml + TS, the §4 canary + trap stay green, the golden fixtures match across planes, and the RULEBOOK section is authoritative.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solver clearing proposal → Round.Clear | The on-ledger recompute-and-assert re-verifies EVERY new rule via `computeClearing` — a solver-only rule that is not mirrored on-ledger cannot settle |
| Clearing.daml ⇄ auction.ts | The golden parity contract; a divergence simultaneously breaks settlement (recompute mismatch) and the golden tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | new clearing rule present in TS but not Daml (or divergent) | mitigate | Land Daml+TS in ONE commit; golden fixtures with identical expected numbers in both planes are the tripwire (Pitfall 2); `Round.Clear` recompute-and-assert covers noncomp for free |
| T-09-02-02 | Tampering | dropping the topPrices trap guard while generalizing choosePStar/coreClear | mitigate | The 99-vs-100 regression scenario (auction.test.ts) + §4 canary fail if the guard is bypassed |
| T-09-02-03 | Tampering | boolean sort-key parity (Daml Bool vs TS) | mitigate | Map the Daml Bool key to `0/1` in TS; a golden fixture that flips ordering would fail (Pitfall 6) |
| T-09-02-04 | Denial of service | noncompetitive BUY creating a second funded buyer on the settle path | accept/mitigate | Keep noncomp demo/fixtures on the SELL side (single-funded-buyer invariant unchanged, Pitfall 3); clearing/preview may show richer books |
</threat_model>

<verification>
- Full `daml test` + `cd solver && npx vitest run` green after the plan (both planes).
- §4 = $100.00 / A=10 / B=8 / C=2 AND the 99-vs-100 trap = 100 (canary + regression).
- The noncomp golden fixture yields identical numbers in Daml and TS (parity).
- git diff confirms Daml and TS clearing changes are in the same commit (no split).
</verification>

<success_criteria>
- `computeClearing` is a two-pass wrapper over `coreClear` in both planes; §4 reduces to one `coreClear`.
- Noncompetitive orders fill at clear with top priority, capped at qty, adding no candidate price — identical in Daml and TS.
- §4 canary + 99-vs-100 trap green; noncomp golden parity green.
- RULEBOOK "Noncompetitive" section is filled and cites both planes.
</success_criteria>

<output>
Create `.planning/phases/09-auction-depth-live-viz/09-02-SUMMARY.md` when done.
</output>
