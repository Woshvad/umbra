---
phase: 09-auction-depth-live-viz
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - daml/Umbra/Clearing.daml
  - solver/src/auction.ts
  - daml/Umbra/Tests.daml
  - solver/src/auction.test.ts
  - RULEBOOK.md
autonomous: true
requirements: [AUCT-01, AUCT-02]
must_haves:
  truths:
    - "An AllOrNone/MAQ order fills only if its integer fill ≥ minQty at the chosen price, else it is excluded (all-or-none = minQty==quantity)"
    - "A conditional order firms deterministically iff its firmIf band passes vs the PROVISIONAL p* (buy: p*≤firmIf, sell: p*≥firmIf); it is a single two-pass rule, NOT a fixpoint"
    - "The §4 fixture still clears at exactly $100.00 with fills A=10 / B=8 / C=2 after all four order types exist"
    - "Clearing.daml and auction.ts produce byte-identical results on the FULL golden suite (parity)"
  artifacts:
    - path: "daml/Umbra/Clearing.daml"
      provides: "bounded AON/MAQ subset enumeration (top-level recursive) + conditional two-pass qualifies"
      contains: "qualifies"
    - path: "solver/src/auction.ts"
      provides: "byte-identical TS mirror of AON/MAQ enumeration + conditional qualifies"
      contains: "qualifies"
    - path: "daml/Umbra/Tests.daml"
      provides: "test_maq_* / test_aon_* / test_conditional_* golden fixtures"
      contains: "test_conditional"
    - path: "solver/src/auction.test.ts"
      provides: "maq / aon / conditional golden scenarios mirroring the Daml fixtures"
      contains: "conditional"
  key_links:
    - from: "daml/Umbra/Clearing.daml::computeClearing (two-pass)"
      to: "qualifies"
      via: "PASS 2 firms conditionals whose firmIf passes vs provisional p*"
      pattern: "qualifies"
    - from: "solver/src/auction.ts::computeClearing (two-pass)"
      to: "qualifies"
      via: "identical PASS-2 firming (parity)"
      pattern: "qualifies"
---

<objective>
Complete the deterministic order-type rulebook (AUCT-01/AUCT-02) by adding the two remaining types in lockstep (Daml + TS in the same commit each): (1) AllOrNone / MAQ via BOUNDED candidate-price + subset enumeration with a ≥minQty inclusion test, and (2) CONDITIONAL auto-firming via the single two-pass rule (firm the conditionals whose `firmIf` band passes vs the PROVISIONAL p*, then final-clear). Close the plan with the FULL golden parity gate — every fixture green in both planes and the §4 canary + 99-vs-100 trap intact.

Purpose: These are the hardest determinism problems in the phase. The AON enumeration must be a top-level recursive helper (Daml-LF forbids local recursion) and the conditional rule must be exactly two passes (NOT a fixpoint) so `Round.Clear`'s recompute matches the solver. After this plan all four order types exist and the §4 fixture is still the money-shot number.
Output: AON/MAQ enumeration + conditional two-pass mirrored byte-identically in both planes, all new golden fixtures, the finalized `ensure` guards, and the completed RULEBOOK sections.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-auction-depth-live-viz/09-CONTEXT.md
@.planning/phases/09-auction-depth-live-viz/09-RESEARCH.md
@.planning/phases/09-auction-depth-live-viz/09-PATTERNS.md
@.planning/phases/09-auction-depth-live-viz/09-02-SUMMARY.md
@daml/Umbra/Clearing.daml
@solver/src/auction.ts
@RULEBOOK.md
</context>

<artifacts_produced>
New symbols this plan creates:
- Daml (`Clearing.daml`): a top-level recursive powerset/subset-enumeration helper over the AON/MAQ order set; a per-`(price, subset)` inclusion test (fill ≥ minQty); the `qualifies : Side -> Decimal -> Optional Decimal -> Bool` conditional predicate used in the two-pass PASS 2.
- TS (`auction.ts`): identical enumeration helper, inclusion test, and `qualifies`.
- Tests: `test_maq_*`, `test_aon_*`, `test_conditional_*` (Tests.daml) + `maq`/`aon`/`conditional` scenarios (auction.test.ts).
- Doc: RULEBOOK "AllOrNone / MAQ" + "Conditional (auto-firming)" sections (filled).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AllOrNone / MAQ — bounded subset enumeration + ≥minQty inclusion (Daml + TS lockstep + golden fixtures)</name>
  <files>daml/Umbra/Clearing.daml, solver/src/auction.ts, daml/Umbra/Tests.daml, solver/src/auction.test.ts, RULEBOOK.md</files>
  <read_first>
    - daml/Umbra/Clearing.daml (coreClear + choosePStar 87-101; rationByPriority 115-119 — the top-level-recursion pattern)
    - solver/src/auction.ts (coreClear + choosePStar 64-79; rationByPriority 87-99)
    - daml/Umbra/Tests.daml (test_clears_at_100 91-110 as the fixture template)
    - 09-RESEARCH.md "AllOrNone / MAQ — bounded candidate-price + subset enumeration" (2^k powerset, inclusion test, all-or-none = minQty==quantity, §4 reduction, scaling caveat) + "Pitfall 5" (top-level recursion, no field defaults)
    - 09-PATTERNS.md "AON/MAQ: bounded powerset ... top-level recursive helper"
  </read_first>
  <behavior>
    - A MAQ order with minQty=m is INCLUDED at candidate price p only if its resulting integer fill ≥ m; otherwise it is excluded from the book at that (p, subset).
    - All-or-none is the special case minQty == quantity (either full fill or excluded) — no separate code path.
    - Among feasible (p, subset) the SAME tie-break applies: max matched → min |demand−supply| → lower price → deterministic subset tiebreak (e.g. most-included / lexicographically-smallest excluded set).
    - §4 (no AON/MAQ orders) → the AON set is empty → the only subset is ∅ → reduces to today's single coreClear → still 100.00 / A=10/B=8/C=2.
    - The MAQ + AON golden fixtures produce identical (price, allocations) in Daml and TS.
  </behavior>
  <action>
    Extend `coreClear` in BOTH `Clearing.daml` and `auction.ts` to handle AON/MAQ via bounded enumeration: identify the AON/MAQ order subset (`minQty` present); enumerate its subsets with a TOP-LEVEL recursive powerset helper (Daml-LF forbids local recursion — model it on the existing top-level `rationByPriority`). For each candidate price p and each subset S, clear treating S-members as fully participating and non-S AON/MAQ members as excluded; a `(p, S)` is FEASIBLE only if every s∈S gets an integer fill ≥ its `minQty`. Rank feasible `(p, S)` by the existing tie-break (max matched → min imbalance → lower price) plus a documented deterministic subset tiebreak. Mirror the enumeration + inclusion test + tiebreak function-for-function in TS. Confirm the enumeration is empty-safe (no AON orders → single ∅ subset → today's behavior) — reuse the `foldl max 0` / `.reduce(Math.max,0)` empty-safe max pattern.
    Finalize the `ensure` guard support noted in 09-01 (`minQty` in [1, quantity]) — verify a malformed MAQ order cannot be created (the Daml `ensure` already guards it; add a solver-side belt-and-suspenders check only if the solver constructs OrderViews from untrusted input).
    Add `test_maq_*` and `test_aon_*` in `Tests.daml` (clone `test_clears_at_100`): a book where a MAQ order's fill would fall BELOW its minQty (excluded) and one where it meets minQty (included), plus an all-or-none case. Add mirroring `maq`/`aon` scenarios in `auction.test.ts` with identical expected numbers.
    Fill the RULEBOOK "AllOrNone / MAQ" section: bounded enumeration + inclusion test + subset tiebreak + all-or-none special case + the exponential scaling caveat, citing the enumeration helper + inclusion test in BOTH planes.
  </action>
  <acceptance_criteria>
    - `bash -lc "export PATH=/c/Users/woshv/bin:$PATH && cd daml && daml test"` — `test_clears_at_100` (§4) + `test_maq_*` + `test_aon_*` green.
    - `cd solver && npx vitest run auction.test.ts` — §4 + trap + noncomp + `maq` + `aon` scenarios green with numbers identical to the Daml fixtures (parity).
    - `cd solver && npx tsc --noEmit` clean.
    - The AON enumeration helper is a TOP-LEVEL recursive function in Clearing.daml (grep: not a local `let go`).
    - RULEBOOK "AllOrNone / MAQ" section filled + cites both planes.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "export PATH=/c/Users/woshv/bin:$PATH && cd daml && daml test" && cd solver && npx vitest run auction.test.ts</automated>
  </verify>
  <done>AON/MAQ clears deterministically and identically in both planes, the §4 canary + trap + noncomp stay green, golden parity holds, and the RULEBOOK section is authoritative.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Conditional auto-firming (two-pass, NOT a fixpoint) + full golden parity gate</name>
  <files>daml/Umbra/Clearing.daml, solver/src/auction.ts, daml/Umbra/Tests.daml, solver/src/auction.test.ts, RULEBOOK.md</files>
  <read_first>
    - daml/Umbra/Clearing.daml (the two-pass computeClearing wrapper from 09-02; coreClear)
    - solver/src/auction.ts (the mirrored two-pass computeClearing)
    - 09-RESEARCH.md "Conditional auto-firming — single deterministic TWO-PASS" (qualifies, provisional-p*-as-reference, NOT a fixpoint, §4 reduction) + "Pitfall 7" (two-pass must live inside computeClearing; one impl, three callers)
    - 09-PATTERNS.md "computeClearing — becomes a TWO-PASS wrapper" + "Conditional two-pass"
  </read_first>
  <behavior>
    - PASS 1: provisional clear over firm (non-conditional) orders → provP.
    - PASS 2: firm each conditional whose firmIf passes vs provP — buy firms iff provP ≤ firmIf, sell firms iff provP ≥ firmIf; drop the rest; recompute the FINAL clear over firm ++ firmed.
    - Exactly two passes — provP is the firming reference even if the final p* differs (a pathological book may firm against a provP the final clear moves past; accepted, no re-iteration).
    - §4 (no conditional orders) → firmed = all firm orders unchanged → one coreClear → still 100.00 / A=10/B=8/C=2.
    - The conditional golden fixture is identical in Daml and TS.
  </behavior>
  <action>
    Implement `qualifies` in BOTH planes: `qualifies side provP firmIf = case side of Buy -> maybe False (provP <=) firmIf; Sell -> maybe False (provP >=) firmIf` (TS: `side==='Buy' ? (firmIf!=null && provP <= firmIf) : (firmIf!=null && provP >= firmIf)`). Wire it into the PASS-2 hook left by 09-02: `firmed = [ v | v <- conditional, qualifies v.side provP v.firmIf ]`; `final = firm ++ firmed`; return `coreClear final`. The two-pass lives ENTIRELY inside `computeClearing` (Pitfall 7) — the solver and `Round.Clear` both call `computeClearing`, never firming separately.
    Add `test_conditional_*` in `Tests.daml` (clone `test_clears_at_100`): a book with a conditional order that FIRMS (band passes vs provP) and one that DROPS (band fails), asserting the exact clearing price + fills; keep any conditional demo order on the sell side (single-buyer settle invariant). Add the mirroring `conditional` scenario in `auction.test.ts` with identical expected numbers.
    Fill the RULEBOOK "Conditional (auto-firming)" section: the exact two-pass rule, provisional-p*-as-the-firming-reference, the explicit "NOT a fixpoint / no re-iteration" statement, and the buy/sell band direction — citing `qualifies` + the two-pass `computeClearing` in BOTH planes.
    FULL GATE: run the entire golden suite in both planes and confirm §4 + trap + noncomp + maq + aon + conditional all green with Daml⇄TS parity.
  </action>
  <acceptance_criteria>
    - `bash -lc "export PATH=/c/Users/woshv/bin:$PATH && cd daml && daml test"` — ALL Daml tests green: `test_clears_at_100`, `test_noncomp_*`, `test_maq_*`, `test_aon_*`, `test_conditional_*`, `test_settled_balances`, `test_atomicity`, `test_clear_rejects_bad_allocation`, `test_privacy_orders`, `test_privacy_confirmations`.
    - `cd solver && npx vitest run` — the FULL solver suite green (§4 + trap + noncomp + maq + aon + conditional + all pre-existing scenarios), numbers identical to the Daml fixtures (parity).
    - `cd solver && npx tsc --noEmit` clean.
    - The conditional logic is exactly two passes (no loop/fixpoint) and lives inside `computeClearing` in both planes.
    - RULEBOOK "Conditional (auto-firming)" section filled + cites both planes; all three per-type sections are now complete.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "export PATH=/c/Users/woshv/bin:$PATH && cd daml && daml test" && cd solver && npx vitest run</automated>
  </verify>
  <done>Conditional auto-firming clears via one deterministic two-pass rule identically in both planes; the full golden suite + §4 canary + trap are green; the RULEBOOK is complete and drift-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solver clearing proposal → Round.Clear | On-ledger recompute-and-assert re-verifies AON/MAQ + conditional via `computeClearing` — a rule not mirrored on-ledger cannot settle |
| Clearing.daml ⇄ auction.ts | Golden parity contract; a firming/enumeration divergence breaks settlement AND the golden tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering | conditional firms against a DIFFERENT reference in solver vs Round.Clear | mitigate | The two-pass lives ONLY inside `computeClearing` (one impl, three callers); the conditional golden fixture with identical numbers is the tripwire (Pitfall 7) |
| T-09-03-02 | Tampering | AON enumeration divergence (subset tiebreak, empty-safe max) | mitigate | Top-level recursive helper mirrored function-for-function; the maq/aon golden fixtures assert byte-identical numbers across planes |
| T-09-03-03 | Tampering | malformed minQty/firmIf bypassing validation | mitigate | Daml `ensure` guards (minQty in [1,quantity]; firmIf>0) reject on-ledger; `Round.Clear` recompute rejects any impossible allocation |
| T-09-03-04 | Denial of service | exponential AON enumeration on a large book | accept | Bounded to the tiny demo book; scaling caveat documented in RULEBOOK; a hard cap on AON-order count may be added in ensure/solver guard |
</threat_model>

<verification>
- Full `daml test` + `cd solver && npx vitest run` green (both planes).
- §4 = $100.00 / A=10 / B=8 / C=2 AND 99-vs-100 trap = 100 after all four order types exist.
- Every new golden fixture (maq/aon/conditional) yields identical numbers in Daml and TS.
- The conditional rule is exactly two passes (no fixpoint) and lives inside computeClearing.
- git diff confirms each Daml+TS clearing change landed in the same commit.
</verification>

<success_criteria>
- All four order types (Limit, Noncompetitive, AllOrNone/MAQ, Conditional) clear deterministically and identically in Daml and TS.
- The §4 canary + 99-vs-100 trap green; the full golden parity suite green.
- RULEBOOK is complete — the objective, trap guard, and all four order-type rules documented and cited byte-identically in both planes.
</success_criteria>

<output>
Create `.planning/phases/09-auction-depth-live-viz/09-03-SUMMARY.md` when done.
</output>
