---
phase: 09-auction-depth-live-viz
plan: 05
type: execute
wave: 4
depends_on: [09-03, 09-04]
files_modified:
  - daml/Umbra/Auction.daml
  - daml/Umbra/Tests.daml
  - solver/src/ledger.ts
  - solver/src/api.ts
  - solver/src/proofpack.ts
  - solver/src/proofpack.test.ts
  - web/daml.js
  - web/src/solver.ts
  - web/src/views/SettlementView.tsx
autonomous: true
requirements: [AUCT-04]
must_haves:
  truths:
    - "Round.Clear computes a per-desk surplus-vs-LIMIT and asserts surplusVsLimit ≥ 0 on-ledger (the provable best-ex number)"
    - "surplus-vs-REFERENCE is a DISTINCT, labeled benchmark (stub ≈ 100) that MAY be negative — never conflated with the proven number"
    - "The §4 fixture still settles A=10/B=8/C=2 @ $100.00 with three TradeConfirmations carrying the TCA fields"
    - "Each desk gets an exportable best-ex/TCA receipt embedded in the proof-pack"
  artifacts:
    - path: "daml/Umbra/Auction.daml"
      provides: "TradeConfirmation TCA fields + Round.Clear surplus computation + surplus≥0 assert + referencePrice choice arg"
      contains: "surplusVsLimit"
    - path: "daml/Umbra/Tests.daml"
      provides: "test_surplus_nonneg + extended test_settled_balances"
      contains: "test_surplus_nonneg"
    - path: "web/src/views/SettlementView.tsx"
      provides: "per-desk TCA receipt (two-distinct-surplus) + EXPORT RECEIPT ink-ghost"
      contains: "SURPLUS"
  key_links:
    - from: "daml/Umbra/Auction.daml Round.Clear"
      to: "TradeConfirmation TCA fields"
      via: "computed surplus/bp at the per-desk create site + surplus≥0 assert"
      pattern: "surplusVsLimit"
    - from: "web/src/views/SettlementView.tsx"
      to: "solver settled body"
      via: "readTradeConfirmations → api.ts → solver.ts → receipt render + proof-pack embed"
      pattern: "referencePrice"
---

<objective>
Deliver provable per-desk value (AUCT-04): extend `TradeConfirmation` with best-ex/TCA fields, compute them inside `Round.Clear`, and add an EXPLICIT on-ledger `surplusVsLimit ≥ 0` assertion — the structurally-non-negative, ledger-provable best-execution number. Surface the receipt through the solver to a post-settle per-desk TCA receipt in `SettlementView` (two DISTINCT surplus rows — proven vs-LIMIT and a labeled benchmark vs-REFERENCE that may be negative) and embed it in the existing WOW-05 proof-pack.

Purpose: surplus-vs-LIMIT is on-ledger provable (at a uniform p*, buyers pay ≤ their limit / sellers receive ≥ their limit) and must never be conflated with surplus-vs-REFERENCE (a clearly-labeled config stub ≈ $100 that may go negative). This plan does NOT touch the settlement primitives (single-funded-buyer + operator custody stay — that is Phase 11); it adds receipt fields + one assert to the existing clear.
Output: TCA fields on `TradeConfirmation`, the surplus≥0 proof + `test_surplus_nonneg`, the extended `test_settled_balances`, regenerated bindings, the solver read/surface, and the SettlementView receipt + proof-pack embed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-auction-depth-live-viz/09-CONTEXT.md
@.planning/phases/09-auction-depth-live-viz/09-RESEARCH.md
@.planning/phases/09-auction-depth-live-viz/09-PATTERNS.md
@.planning/phases/09-auction-depth-live-viz/09-UI-SPEC.md
@.planning/phases/09-auction-depth-live-viz/09-03-SUMMARY.md
@.planning/phases/09-auction-depth-live-viz/09-04-SUMMARY.md
@daml/Umbra/Auction.daml
@solver/src/ledger.ts
@RULEBOOK.md
</context>

<artifacts_produced>
New symbols this plan creates:
- Daml (`Auction.daml`): `TradeConfirmation` appended fields `ownLimit : Optional Decimal`, `referencePrice : Decimal`, `surplusVsLimit : Decimal`, `improvementVsLimitBp : Decimal`, `improvementVsReferenceBp : Decimal`; `Round.Clear` choice arg `referencePrice : Decimal` (config stub) + the surplus computation + `assertMsg "execution at least as good as limit (surplusVsLimit >= 0)"`.
- Daml tests: `test_surplus_nonneg` + extended `test_settled_balances`.
- Solver (`ledger.ts`): extended `readTradeConfirmations`; `settle`/`tamperClear` gain the `referencePrice` choice arg. (`api.ts`): TCA fields on the settled body. (`proofpack.ts`): best-ex bundle carries the TCA fields.
- Web (`solver.ts`): TCA fields on the settled types; (`SettlementView.tsx`): the per-desk TCA receipt block + `EXPORT RECEIPT ↓`.
- Regenerated artifact: `web/daml.js` (TradeConfirmation changed).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: On-ledger TCA fields + surplus≥0 proof in Round.Clear (Daml) + tests + regenerate bindings</name>
  <files>daml/Umbra/Auction.daml, daml/Umbra/Tests.daml, web/daml.js</files>
  <read_first>
    - daml/Umbra/Auction.daml (TradeConfirmation lines 85-98; Clear choice args lines 154-161; per-desk create site lines 262-271; roundBankers 2 line 184; the Option-B "pass cids in" pattern lines 154-161)
    - daml/Umbra/Clearing.daml (eligibility: buys limit ≥ p* / sells limit ≤ p* — why surplus is structurally ≥0)
    - daml/Umbra/Tests.daml (test_settled_balances — the §4 settled-balances assertion; test_clears_at_100 as fixture template)
    - 09-RESEARCH.md "TCA Receipt + Surplus Proof (AUCT-04)" (field list, surplus formula, bp formula, referencePrice choice arg, two-surplus distinction)
    - 09-PATTERNS.md "TradeConfirmation — append TCA fields"
    - RULEBOOK.md (the pinned bp formula)
  </read_first>
  <behavior>
    - For each participating desk in §4: surplusVsLimit ≥ 0 (A buy: (limit−p*)*fill; B/C sell: (p*−limit)*fill) — asserted on-ledger.
    - referencePrice is a labeled stub (≈100) passed as a Clear choice arg; improvementVsReferenceBp is signed (may be negative) — a DIFFERENT number.
    - §4 still settles A=10/B=8/C=2 @ 100.00 with three TradeConfirmations carrying the TCA fields.
  </behavior>
  <action>
    Append to `TradeConfirmation` (Optional-at-end per SCU): `ownLimit : Optional Decimal`, `referencePrice : Decimal`, `surplusVsLimit : Decimal`, `improvementVsLimitBp : Decimal`, `improvementVsReferenceBp : Decimal` (signed). Add `referencePrice : Decimal` as a `Round.Clear` choice arg (config stub — a choice body cannot read config, so pass it in, mirroring the Option-B pass-cids pattern). At the per-desk `create TradeConfirmation` site (lines 262-271), compute per desk/side: `surplusVsLimit` = Buy → `(ownLimit − priceDec) * intToDecimal filledQty`; Sell → `(priceDec − ownLimit) * intToDecimal filledQty`; for a noncompetitive order (no meaningful limit) set `ownLimit = None` and `surplusVsLimit = 0.0`. `improvementVsLimitBp` and `improvementVsReferenceBp` per the RULEBOOK-pinned formula `round((|limit − p*| / p*) * 10000)` (reference variant uses referencePrice; reference bp is SIGNED). Add `assertMsg "execution at least as good as limit (surplusVsLimit >= 0)" (surplusVsLimit >= 0.0)` — the on-ledger AUCT-04 proof. The DvP legs + single-funded-buyer invariant + `Retire` + `status = Settled` are UNCHANGED.
    Extend `test_settled_balances` to pass `referencePrice` to `Clear` and assert the three confirmations carry the TCA fields (surplusVsLimit ≥ 0; §4 balances A:10/4000 · B:12/1800 · C:13/1200 unchanged). Add `test_surplus_nonneg` (clone test_clears_at_100 + settle path) asserting each confirmation's `surplusVsLimit >= 0.0`. Update every other `Clear` call site in Tests.daml (e.g. test_atomicity, test_clear_rejects_bad_allocation) to pass the new `referencePrice` arg.
    Regenerate + commit `web/daml.js` (TradeConfirmation changed — Pitfall 4, commit 6e4bade invariant).
  </action>
  <acceptance_criteria>
    - `bash -lc "export PATH=/c/Users/woshv/bin:$PATH && cd daml && daml test"` — `test_settled_balances` (§4 balances + TCA fields), `test_surplus_nonneg`, `test_atomicity`, `test_clear_rejects_bad_allocation`, `test_clears_at_100`, privacy tests all green.
    - The §4 settle still yields A=10/B=8/C=2 @ 100.00 with three TradeConfirmations.
    - `surplusVsLimit >= 0.0` is asserted inside `Round.Clear` (grep) and the surplus-vs-reference field is signed/distinct.
    - `web/daml.js` regenerated + committed (TradeConfirmation type includes the TCA fields).
  </acceptance_criteria>
  <verify>
    <automated>bash -lc "export PATH=/c/Users/woshv/bin:$PATH && cd daml && daml test"</automated>
  </verify>
  <done>Round.Clear computes + proves surplus≥0 on-ledger, the receipt carries two distinct surplus numbers, §4 settles unchanged, and bindings are regenerated.</done>
</task>

<task type="auto">
  <name>Task 2: Surface TCA receipt through solver + proof-pack + SettlementView (two-distinct-surplus + export)</name>
  <files>solver/src/ledger.ts, solver/src/api.ts, solver/src/proofpack.ts, solver/src/proofpack.test.ts, web/src/solver.ts, web/src/views/SettlementView.tsx</files>
  <read_first>
    - solver/src/ledger.ts (readTradeConfirmations lines 240-250; settle lines 298-364 + the Clear exercise arg shape lines 349-355; tamperClear lines 381-460)
    - solver/src/api.ts (settled body post-settle branch lines 265-298 — confs mapping lines 270-283)
    - solver/src/proofpack.ts (the best-ex receipts bundle — extend with the TCA fields) + proofpack.test.ts
    - web/src/solver.ts (RoundResponse / settled types)
    - web/src/views/SettlementView.tsx (post-settle gating `settled = phase === 'settled'`; ProofPackButton; `.umbra-ink-ghost` grammar)
    - 09-UI-SPEC.md "AUCT-04 — Best-Ex / TCA Receipt" (block label, facts grid, two DISTINCT surplus rows, reference stub tag, EXPORT RECEIPT ink-ghost, red only on negative vs-reference)
  </read_first>
  <action>
    In `ledger.ts`: extend `readTradeConfirmations` (lines 240-250) to map the new TCA fields (`ownLimit`, `referencePrice`, `surplusVsLimit`, `improvementVsLimitBp`, `improvementVsReferenceBp`; Optional/numeric decode). In `settle` (line 349-355) and `tamperClear`, add the `referencePrice` choice arg to the `Clear` exercise — a labeled config stub (e.g. a module const `REFERENCE_PRICE_STUB ≈ 100.00`); settle stays byte-unchanged otherwise.
    In `api.ts`, surface the new TradeConfirmation fields on the settled body (extend the confs mapping lines 270-283) so the per-desk receipt data reaches the browser.
    In `proofpack.ts`, extend the best-ex receipts bundle to render the TCA fields (fill vs limit vs reference, surplus + bp, the two-distinct-surplus split) reusing the existing on-brand token grammar; update `proofpack.test.ts`.
    In `web/src/solver.ts`, add the TCA fields to the settled response types.
    In `web/src/views/SettlementView.tsx`, add a POST-settle per-desk TCA receipt block (only when `settled`), inserted below the DvP-legs/CTA/RoundBrief area. Per UI-SPEC: block label "Best-Ex / TCA · per desk"; per-desk card with a facts grid (CLEARING PRICE · YOUR LIMIT · REFERENCE + the `REFERENCE — PRE-AUCTION MID (STUB)` tag) and TWO DISTINCT surplus rows — (1) PROVEN vs-LIMIT: ink solid square + `ON-LEDGER · SURPLUS ≥ 0` caption + `+{surplus}` / `+{bp} bp`, never red; (2) BENCHMARK vs-REFERENCE: lighter row, `VS REFERENCE · BENCHMARK (MAY BE NEGATIVE)`, positive→ink / negative→`#E2231A` red. Add `EXPORT RECEIPT ↓` reusing `.umbra-ink-ghost` (never red/lime). Reuse existing tokens only; no new keyframe.
  </action>
  <acceptance_criteria>
    - `cd solver && npx vitest run` — the full solver suite green, including the extended `proofpack.test.ts` and any api receipt-shape test; the secret sweep still passes.
    - `cd solver && npx tsc --noEmit` clean.
    - `cd web && npm run build` succeeds; the TCA receipt renders two DISTINCT surplus rows (proven ink vs-limit; benchmark vs-reference red only when negative) with the REFERENCE stub tag.
    - `EXPORT RECEIPT ↓` uses the `.umbra-ink-ghost` grammar (grep) — no red/lime affordance.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run</automated>
  </verify>
  <done>The TCA receipt flows ledger→solver→web + proof-pack; the two surplus numbers are visually separable; export reuses the shipped ink-ghost grammar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Round.Clear (on-ledger) | The only tier that can PROVE surplus ≥ 0 (limit-compliance at a uniform p*); the assert is the proof |
| solver settled body → browser | Carries per-desk receipt numbers; the confirmation observer is the desk only — the operator-plane Settlement view aggregates, but a desk's FillCard must show only its own row |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Tampering | a receipt claiming best-ex that isn't limit-compliant | mitigate | `assertMsg surplusVsLimit >= 0` inside Round.Clear — a receipt that violates it aborts the atomic settle (on-ledger proof, not a UI claim) |
| T-09-05-02 | Repudiation | conflating surplus-vs-reference (may be negative) with the proven number | mitigate | Two DISTINCT rows; reference is a labeled stub tag; negative reference renders red and lighter — never on the proven vs-limit line |
| T-09-05-03 | Information disclosure | operator token / key echoed via the extended settled body or proof-pack | mitigate | proofpack.ts interpolates only numbers/hashes; extend the secret-sweep; no token/key/env in the receipt |
| T-09-05-04 | Tampering | stale web/daml.js after the TradeConfirmation change | mitigate | Regenerate + commit bindings (Pitfall 4); web build fails if the TCA fields are dropped |
| T-09-05-05 | Tampering | relaxing the single-funded-buyer / operator-custody settlement invariant | accept | NOT touched here — Phase 11; §4 settle path byte-unchanged except the additive referencePrice arg + TCA fields |
</threat_model>

<verification>
- `bash -lc "export PATH=/c/Users/woshv/bin:$PATH && cd daml && daml test"` — §4 settle + surplus≥0 + all settlement/privacy tests green.
- `cd solver && npx vitest run` — full suite incl. proofpack + secret sweep green.
- `cd web && npm run build` — green; receipt renders two distinct surplus rows.
- Manual (phase UAT, deferred): open the Settlement receipt, confirm surplusVsLimit ≥ 0 + reference labeled distinctly + export (per 09-VALIDATION Manual-Only).
</verification>

<success_criteria>
- Round.Clear computes + asserts surplusVsLimit ≥ 0 on-ledger; §4 settles A=10/B=8/C=2 @ $100.00 with three TCA-carrying confirmations.
- surplus-vs-LIMIT (proven, ink, never negative) and surplus-vs-REFERENCE (labeled stub, may be red) are distinct numbers throughout.
- Each desk's receipt is exportable and embedded in the WOW-05 proof-pack; secret-free.
</success_criteria>

<output>
Create `.planning/phases/09-auction-depth-live-viz/09-05-SUMMARY.md` when done.
</output>
