---
phase: 09-auction-depth-live-viz
plan: 06
type: execute
wave: 4
depends_on: [09-01, 09-03]
files_modified:
  - web/src/components/OrderTicket.tsx
autonomous: true
requirements: [AUCT-01]
must_haves:
  truths:
    - "A desk can select LIMIT · NONCOMP · MAQ · COND and submit a sealed order of that type via the shipped single SEAL ORDER confirm"
    - "Noncompetitive hides the limit input (FILL AT CLEAR caption); MAQ shows Min Acceptable Qty; Conditional shows a side-directional Firm-If band"
    - "load demo order still loads a plain Limit; the one-per-round lock + seal-wipe are byte-unchanged"
    - "Order entry runs on the desk's OWN token — never the operator token in the browser"
  artifacts:
    - path: "web/src/components/OrderTicket.tsx"
      provides: "order-type segmented selector + per-type param fields + type-aware SubmitOrder"
      contains: "orderType"
  key_links:
    - from: "web/src/components/OrderTicket.tsx selector"
      to: "Venue.SubmitOrder"
      via: "type-aware exercise (orderType/minQty/firmIf) on the desk's own DamlLedger"
      pattern: "SubmitOrder"
---

<objective>
Build the AUCT-01 order-entry UI: a segmented `LIMIT · NONCOMP · MAQ · COND` selector plus per-type param fields inside the shipped `OrderTicket`, so a desk can submit any of the four sealed order types. The exercise carries `orderType`/`minQty`/`firmIf` on the desk's own per-party plane; the shipped `SEAL ORDER` remains the single confirm and the one-per-round lock + seal-wipe are byte-unchanged.

Purpose: 09-01 wired the data model + a plain-Limit submit; this plan exposes the full order-type entry surface so the richer clearing math (09-02/09-03) is reachable end-to-end from the desk view. It is UI-only, on the desk plane, and must be visually indistinguishable from the shipped ticket (binding comp — reuse existing tokens only).
Output: The order-type selector + show/hide param fields + validation copy + a type-aware `SubmitOrder`, matching the 09-UI-SPEC AUCT-01 contract.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-auction-depth-live-viz/09-CONTEXT.md
@.planning/phases/09-auction-depth-live-viz/09-PATTERNS.md
@.planning/phases/09-auction-depth-live-viz/09-UI-SPEC.md
@.planning/phases/09-auction-depth-live-viz/09-01-SUMMARY.md
@.planning/phases/09-auction-depth-live-viz/09-03-SUMMARY.md
@web/src/components/OrderTicket.tsx
</context>

<artifacts_produced>
New symbols this plan creates:
- Web (`OrderTicket.tsx`): an order-type state + segmented selector (`LIMIT·NONCOMP·MAQ·COND`) reusing the shipped `sideBtn` grammar; per-type param inputs (Min Acceptable Qty, Firm-If band) at the 1px assist tier; per-type field show/hide + active-type descriptor + validation copy; a type-aware `SubmitOrder` exercise (orderType/minQty/firmIf).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Order-type selector + per-type params + type-aware SubmitOrder (desk plane, binding comp)</name>
  <files>web/src/components/OrderTicket.tsx</files>
  <read_first>
    - web/src/components/OrderTicket.tsx (onSeal + SubmitOrder exercise lines 100-129; sideBtn grammar lines 139-159; the WOW-03 NL sub-block lines 171-233; qty/limit inputs; ticketLocked / loadDemo)
    - 09-UI-SPEC.md "AUCT-01 — Order-Type Selector + Params" (selector position, segment labels, active-type descriptor copy, per-type field show/hide, param tiers, validation copy, states) + "Copywriting Contract" (verbatim strings) + "Color"/"Typography"/"Spacing" (reuse existing tokens only)
    - 09-PATTERNS.md "web/src/components/OrderTicket.tsx (AUCT-01 entry UI)" (reuse sideBtn/side-toggle grammar; 1px assist tier; load demo stays Limit; import path pattern)
    - 09-01-SUMMARY.md (the exact SubmitOrder payload shape wired at the Limit default)
  </read_first>
  <action>
    Insert an "Order Type" segmented selector at the top of the structured ticket — BELOW the shipped WOW-03 NL sub-block and ABOVE the side toggle — with four ghost-mono segments `LIMIT · NONCOMP · MAQ · COND` (IBM Plex Mono 9, `.16em`, uppercase; active = ink underline, inactive = opacity .45; disabled under `ticketLocked`), reusing the shipped `sideBtn`/side-toggle grammar. Add an active-type descriptor line (Inter 13/1.6 opacity .65, `umbra-rise` on change) with the verbatim copy from the UI-SPEC per type.
    Field show/hide per type: Limit → shipped side+qty+limit (unchanged). Noncompetitive → side+qty; REPLACE the limit input with the static caption row `FILL AT CLEAR — NO LIMIT PRICE`. MAQ/All-or-None → side+qty+limit + a "Min Acceptable Qty" param (mono 22 tabular, 1px ink underline assist tier, unit "BONDX"; show `= FULL FILL ONLY` hint when min == qty). Conditional → side+qty+limit + a side-directional "Firm-If band" param (mono 22, 1px underline, unit "USDCx / unit"; label "Firm If Clears ≤" for buy, "Firm If Clears ≥" for sell).
    Validation copy (Inter 13 opacity .7, inline, non-blocking hint style): MAQ min>qty → "Minimum can't exceed your order size."; MAQ min≤0 → "Enter a minimum of at least 1."; Conditional band≤0 → "Enter a firm-if price above 0."
    Type-aware `SubmitOrder` exercise: extend the shipped onSeal payload to send `orderType` = the selected type, `minQty` = the MAQ value (String) or null, `firmIf` = the conditional band (toFixed) or null; for Noncompetitive send a valid placeholder for the (still-required) `limit` per the 09-01 effective-limit encoding (match exactly what 09-01-SUMMARY documents so the Daml `ensure` accepts it). `loadDemo` stays a plain Limit; the WOW-03 `PARSE →` still prefills side/qty/limit and always yields a Limit (NL assist never switches the order type). The single `SEAL ORDER` confirm, the one-per-round lock, and the seal-wipe are byte-unchanged. Reuse existing tokens/keyframes only (no new palette/size/keyframe); honor prefers-reduced-motion.
    Import the `OrderType`/Side bindings from `@daml.js/umbra-0.1.0/lib/Umbra/{Auction,Roles,Clearing}/module` (regenerated in 09-01).
  </action>
  <acceptance_criteria>
    - `cd web && npm run build` succeeds (type-aware SubmitOrder compiles against the 09-01 regenerated bindings).
    - `cd web && npx vitest run` — existing web tests green (no regression to OrderTicket-adjacent libs).
    - Selector segments, descriptor copy, param labels, and validation copy match the 09-UI-SPEC "Copywriting Contract" verbatim (grep for the exact strings).
    - The `SEAL ORDER` remains the SINGLE confirm; `onParse`/`loadDemo` never auto-submit and never switch away from a plain Limit; no operator token / `@daml/react` operator context appears in OrderTicket (grep-clean).
    - No new tailwind token / fontSize / keyframe introduced (only existing tokens used).
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run build && npx vitest run</automated>
  </verify>
  <done>A desk can select and submit all four order types via the shipped single confirm; the ticket stays visually indistinguishable from the comp; load-demo + lock + seal-wipe unchanged; desk-plane only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desk browser (per-party token) → Venue.SubmitOrder | Order entry runs inside the active desk's own `DamlLedger` — never the operator token; the created Order stays private (no observer) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Elevation of privilege | operator token leaking into the desk-plane ticket | mitigate | OrderTicket uses the desk's own token exclusively (grep-clean of operator token / operator @daml/react context) — the shipped desk-plane invariant |
| T-09-06-02 | Tampering | malformed order-type params submitted | mitigate | Client-side validation copy + the Daml `ensure` guards (minQty in [1,quantity]; firmIf>0) reject a malformed order on-ledger; the browser check is UX only, not the security boundary |
| T-09-06-03 | Tampering | accidental double-submit / bypassing the one-per-round lock | mitigate | `SEAL ORDER` stays the single confirm; onParse/loadDemo never auto-submit; the one-per-round lock + seal-wipe are byte-unchanged |
</threat_model>

<verification>
- `cd web && npm run build && npx vitest run` — green.
- Verbatim copy matches the 09-UI-SPEC Copywriting Contract.
- grep-clean of operator token / operator @daml/react context in OrderTicket.
- Manual (phase UAT, deferred): select each type, confirm show/hide + validation + a single SEAL ORDER confirm.
</verification>

<success_criteria>
- A desk can submit noncompetitive, MAQ/all-or-none, and conditional orders in addition to plain limits, on its own token.
- The ticket matches the binding comp (existing tokens only); load-demo stays plain Limit; lock + seal-wipe unchanged.
- The single SEAL ORDER confirm is preserved; NL assist never switches the order type.
</success_criteria>

<output>
Create `.planning/phases/09-auction-depth-live-viz/09-06-SUMMARY.md` when done.
</output>
