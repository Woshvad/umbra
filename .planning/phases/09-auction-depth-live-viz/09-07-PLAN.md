---
phase: 09-auction-depth-live-viz
plan: 07
type: execute
wave: 5
depends_on: [09-05]
files_modified:
  - web/src/lib/leakage.ts
  - web/src/lib/leakage.test.ts
  - web/src/views/SettlementView.tsx
autonomous: true
requirements: [WOW-06]
must_haves:
  truths:
    - "A pure client-side sim runs the settled orders through a naive public book and shows $X lost vs $0 leaked on Umbra"
    - "The sim is deterministic and DOM-free (no ledger/solver dependency) and unit-tested"
    - "The sim panel is unmistakably NOT ledger data — dashed border + SIMULATION tag + disclaimer"
    - "$X lost renders red; $0 leaked + the $X SAVED punchline render ink (never lime)"
  artifacts:
    - path: "web/src/lib/leakage.ts"
      provides: "pure deterministic cost-of-leakage model over settled receipt numbers"
      contains: "leaked"
    - path: "web/src/lib/leakage.test.ts"
      provides: "vitest coverage ($lost>0 vs $0, deterministic)"
      contains: "leakage"
    - path: "web/src/views/SettlementView.tsx"
      provides: "post-settle leakage sim panel (dashed border, SIMULATION tag)"
      contains: "SIMULATION"
  key_links:
    - from: "web/src/views/SettlementView.tsx sim panel"
      to: "web/src/lib/leakage.ts"
      via: "pure client-side call over the settled preview numbers (no solver/ledger)"
      pattern: "leakage"
---

<objective>
Ship the WOW-06 cost-of-leakage simulator: a pure client-side lib that runs the SAME settled order set through a naive public order book (sequential marketable execution → slippage / front-run price impact → `$ lost`) beside Umbra's sealed uniform clear (`$0 leaked`), and a post-settle Settlement panel that shows the `$X saved`. It is illustrative-only and must be unmistakably NOT ledger data.

Purpose: This is the visceral "why sealed batch beats a public book" moment. It has NO ledger or solver dependency — it reads the settled per-desk receipt numbers already on the page (qty, ownLimit, clearingPrice from AUCT-04) and models a public book client-side. The dashed border + SIMULATION tag + disclaimer keep it from ever being confused with the real on-ledger receipts above it.
Output: `web/src/lib/leakage.ts` (pure, DOM-free) + `leakage.test.ts` + the Settlement sim panel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-auction-depth-live-viz/09-CONTEXT.md
@.planning/phases/09-auction-depth-live-viz/09-RESEARCH.md
@.planning/phases/09-auction-depth-live-viz/09-PATTERNS.md
@.planning/phases/09-auction-depth-live-viz/09-UI-SPEC.md
@.planning/phases/09-auction-depth-live-viz/09-05-SUMMARY.md
@web/src/lib/curve.ts
@web/src/views/SettlementView.tsx
</context>

<artifacts_produced>
New symbols this plan creates:
- Web (`lib/leakage.ts`): a pure deterministic function computing `{ publicBookLost, umbraLeaked: 0, saved }` from the settled per-desk receipt numbers (qty, ownLimit, clearingPrice) via a naive-public-book slippage/front-run model.
- Web (`lib/leakage.test.ts`): vitest coverage.
- Web (`SettlementView.tsx`): the post-settle cost-of-leakage sim panel.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure cost-of-leakage lib + unit tests (DOM-free, deterministic)</name>
  <files>web/src/lib/leakage.ts, web/src/lib/leakage.test.ts</files>
  <read_first>
    - web/src/lib/balance.ts (the pure DOM-free leaf-lib convention; `import type { Allocation } from '../solver'` pattern) + web/src/lib/curve.ts
    - web/src/lib/balance.test.ts / curve.test.ts (the vitest style to mirror)
    - 09-RESEARCH.md "WOW-06 Leakage Sim" (input = settled receipt numbers; naive-public-book model; $lost formula; $0 leaked; deterministic)
    - 09-UI-SPEC.md "WOW-06 — Cost-of-Leakage Simulator" (two columns, punchline, disclaimer — for the field/output shape the panel needs)
  </read_first>
  <behavior>
    - Given the settled per-desk receipt numbers (qty, ownLimit, clearingPrice), the public-book model produces `publicBookLost > 0` (slippage + front-run markup accumulate) while Umbra's sealed clear gives `umbraLeaked === 0`; `saved = publicBookLost`.
    - Deterministic — identical input yields identical output (no Date/random/DOM).
    - Edge cases (empty book, single order) return a sane `0`-safe result.
  </behavior>
  <action>
    Create `web/src/lib/leakage.ts` — a PURE, DOM-free, no-I/O module (mirror `balance.ts`/`curve.ts`) exporting a deterministic function that takes the settled per-desk receipt numbers (qty, ownLimit, clearingPrice; type-import `Allocation` from `../solver` if useful) and returns `{ publicBookLost, umbraLeaked: 0, saved }`. Model: walk the aggressive side sequentially through a naive public order book applying a per-unit slippage + a front-run markup, so `publicBookLost = Σ (executionPrice − uniformClear) * qty`; Umbra's sealed uniform clear yields `$0 leaked`; `saved = publicBookLost`. Keep the slippage/markup coefficients as named module constants and document them as illustrative. Use the empty-safe reduce pattern (`.reduce(..., 0)`).
    Create `web/src/lib/leakage.test.ts` (mirror `balance.test.ts`/`curve.test.ts`): assert `publicBookLost > 0` and `umbraLeaked === 0` and `saved === publicBookLost` for a representative §4-shaped input; assert determinism (same input → same output); assert 0-safe on an empty/single-order book.
  </action>
  <acceptance_criteria>
    - `cd web && npx vitest run leakage.test.ts` — green ($lost>0, $0 leaked, saved==lost, deterministic, 0-safe).
    - `web/src/lib/leakage.ts` imports NO React/DOM and NO ledger/solver client (grep-clean: no `fetch`, no `@daml`, no `document`).
    - `cd web && npx tsc --noEmit` (or `npm run build`) clean.
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx vitest run leakage.test.ts</automated>
  </verify>
  <done>A pure, deterministic, DOM-free leakage model exists with vitest coverage; no ledger/solver dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Post-settle cost-of-leakage sim panel (unmistakably not-ledger)</name>
  <files>web/src/views/SettlementView.tsx</files>
  <read_first>
    - web/src/views/SettlementView.tsx (post-settle gating `settled = phase === 'settled'`; the AUCT-04 TCA receipt block from 09-05 — the sim sits BELOW it)
    - 09-UI-SPEC.md "WOW-06 — Cost-of-Leakage Simulator" (dashed 1px ink border, SIMULATION tag, two columns, red $lost / ink $0, ink $X SAVED punchline umbra-rise, disclaimer footnote) + "Color" (never lime for the punchline) + "Copywriting Contract" (verbatim strings)
    - 09-05-SUMMARY.md (the settled receipt fields available on the page)
  </read_first>
  <action>
    Add a POST-settle cost-of-leakage sim panel to `SettlementView.tsx` (only when `settled`), inserted BELOW the AUCT-04 TCA receipts. Container: 1px DASHED ink border (real receipts above use SOLID 1px ink — the dashed border is the primary "not-ledger" signal), inner padding per UI-SPEC. Header: sub-label "Cost of Leakage · Simulation" + a right-pushed tag `SIMULATION · ILLUSTRATIVE — NOT LEDGER DATA` (mono 9 `.12em`, 1px INK border — ink not red, neutral-illustrative). Two columns from `leakage.ts` over the settled preview numbers: SIMULATED PUBLIC BOOK → `${X} LOST` (mono 22, `#E2231A` red) + `SLIPPAGE + FRONT-RUN`; UMBRA SEALED CLEAR → `$0 LEAKED` (mono 22, ink) + `SEALED UNIFORM PRICE`. Punchline `${X} SAVED VS A PUBLIC BOOK` (mono 40 tabular, INK — deliberately NOT lime, `umbra-rise` on mount). Disclaimer footnote (Inter 13/1.6 opacity .6): "Illustrative model — the same orders run through a naive public order book. No real venue; nothing here is on-ledger." Hidden pre-settle; `settled` → renders from the settled preview (pure client-side lib, no solver/ledger dependency for the sim math). Reuse existing tokens/keyframes only; honor prefers-reduced-motion (instant).
  </action>
  <acceptance_criteria>
    - `cd web && npm run build` succeeds.
    - `cd web && npx vitest run` — web suite green (incl. leakage.test.ts).
    - The sim panel uses a DASHED ink border + the `SIMULATION · ILLUSTRATIVE — NOT LEDGER DATA` tag; `$ LOST` renders red, `$0 LEAKED` + the `$X SAVED` punchline render ink (never lime) — verbatim copy per the UI-SPEC (grep the exact strings).
    - The panel reads only the settled preview numbers via `leakage.ts` — no solver/ledger call in the sim path (grep-clean).
  </acceptance_criteria>
  <verify>
    <automated>cd web && npm run build && npx vitest run</automated>
  </verify>
  <done>The Settlement view shows a clearly-labeled simulation ($X lost red vs $0 leaked ink, $X saved ink punchline) that can never be confused with the real on-ledger receipts above it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settled receipt numbers (on page) → leakage sim (client-side) | The sim is purely illustrative; it must be visibly distinct from real ledger data and introduce no ledger/solver dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07-01 | Repudiation | a viewer mistaking the simulated `$X lost` for a real on-ledger number | mitigate | Dashed border (real = solid) + `SIMULATION · ILLUSTRATIVE — NOT LEDGER DATA` tag + disclaimer footnote; the sim numbers sit visually apart from the proven receipt above |
| T-09-07-02 | Tampering | the sim reaching into ledger/solver state and coupling to live data | mitigate | leakage.ts is pure/DOM-free with no fetch/@daml/solver import (grep-clean); the panel reads only the already-settled preview numbers |
| T-09-07-03 | Information disclosure | secret leakage via a new fetch in the sim path | accept | No new network call — the sim is fully client-side; nothing new crosses the wire |
</threat_model>

<verification>
- `cd web && npx vitest run leakage.test.ts` + `cd web && npm run build && npx vitest run` — green.
- The sim is pure/DOM-free (no fetch/@daml/document) and clearly labeled a simulation.
- Manual (phase UAT, deferred): open the leakage sim on §4 orders, confirm labeled-as-simulation + a $ saved figure (per 09-VALIDATION Manual-Only).
</verification>

<success_criteria>
- The cost-of-leakage simulator shows the same orders losing $X on a simulated public book vs $0 leaked on Umbra.
- It is pure client-side, deterministic, unit-tested, and unmistakably NOT ledger data.
- $X lost is red; $0 leaked + the $X SAVED punchline are ink (never lime); disclaimer present.
</success_criteria>

<output>
Create `.planning/phases/09-auction-depth-live-viz/09-07-SUMMARY.md` when done.
</output>
