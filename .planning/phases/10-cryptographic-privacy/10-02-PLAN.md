---
phase: 10-cryptographic-privacy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - solver/package.json
  - solver/src/zk/.gitignore
  - solver/src/zk/circom.exe
  - solver/src/tlock.test.ts
  - solver/src/zk/verify.test.ts
  - .gitignore
autonomous: false
requirements: [CRYP-02, CRYP-03]
must_haves:
  truths:
    - "The crypto toolchain (tlock-js, snarkjs, circomlibjs, circomlib, prebuilt circom.exe) is installed and pure-JS (no native build) on this Windows box"
    - "Each package was human-verified on its registry before install (slopcheck could not run — packages treated as [ASSUMED])"
    - "The Wave-0 solver crypto test files exist and fail until CRYP-02/03 implement them"
    - "zk build artifacts (wasm/zkey/ptau) are gitignored; the committed proof/vkey fixtures for §4 are NOT"
  artifacts:
    - path: "solver/package.json"
      provides: "tlock-js@0.9.0 + snarkjs@0.7.6 + circomlibjs@0.1.7 deps; circomlib@2.0.5 devDep"
      contains: "tlock-js"
    - path: "solver/src/zk/circom.exe"
      provides: "prebuilt circom v2.2.3 windows-amd64 compiler (build tool, not an npm dep)"
    - path: "solver/src/tlock.test.ts"
      provides: "CRYP-02 Wave-0 test scaffold (failing until 10-03)"
    - path: "solver/src/zk/verify.test.ts"
      provides: "CRYP-03 Wave-0 test scaffold (failing until 10-04)"
  key_links:
    - from: "solver/package.json"
      to: "node_modules/tlock-js + node_modules/snarkjs"
      via: "npm install (pure-JS, no node-gyp)"
      pattern: "tlock-js|snarkjs"
---

<objective>
Install the CRYP-02 (timelock) and CRYP-03 (ZK) toolchains — all pure-JS / prebuilt-binary, verified end-to-end on this box by the researcher — behind the mandatory package-legitimacy checkpoint, and scaffold the Wave-0 solver crypto test files.

Purpose: Every downstream solver-crypto plan (tlock, zk) needs these libraries present. Because `slopcheck` cannot run on this box (no real Python), the packages are treated as `[ASSUMED]` and gated by ONE blocking human-verify against the npm registry before any install (graceful-degradation policy).

Output: `tlock-js@0.9.0`, `snarkjs@0.7.6`, `circomlibjs@0.1.7` (deps) + `circomlib@2.0.5` (dev) installed; the prebuilt `circom-windows-amd64.exe v2.2.3` downloaded to `solver/src/zk/`; gitignore entries for zk build artifacts; and failing Wave-0 test scaffolds `solver/src/tlock.test.ts` + `solver/src/zk/verify.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/10-cryptographic-privacy/10-RESEARCH.md
@.planning/phases/10-cryptographic-privacy/10-VALIDATION.md
@solver/package.json
@.gitignore
</context>

<artifacts_this_phase_produces>
- **solver/package.json:** deps `tlock-js@0.9.0`, `snarkjs@0.7.6`, `circomlibjs@0.1.7`; devDep `circomlib@2.0.5`.
- **solver/src/zk/circom.exe:** prebuilt circom v2.2.3 compiler (build tool).
- **solver/src/zk/.gitignore + root .gitignore:** ignore `*.wasm`, `*.zkey`, `*.ptau`, `*_js/` build outputs (but NOT the committed §4 `vkey.json` / `proof.json` / `public.json` fixtures).
- **solver/src/tlock.test.ts, solver/src/zk/verify.test.ts:** failing Wave-0 scaffolds.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint (slopcheck unavailable — verify on the registry)</name>
  <action>
    HALT before any install. Present the five crypto dependencies (tlock-js@0.9.0, snarkjs@0.7.6, circomlibjs@0.1.7, circomlib@2.0.5, circom.exe v2.2.3) to the developer for package-legitimacy sign-off, because `slopcheck` cannot run on this box (no real Python) so they are treated as `[ASSUMED]` per the graceful-degradation policy. Do NOT auto-approve (workflow.auto_advance is ignored for package-legitimacy gates). Wait for the developer to confirm each source on its registry / release page (see how-to-verify) before Task 2 installs anything.
  </action>
  <what-built>
    Nothing installed yet. This gate confirms the five crypto dependencies are legitimate canonical ecosystem packages before any `npm install`. RESEARCH § Package Legitimacy Audit confirmed each on the npm registry, tied to a long-lived iden3/drand source repo, and actually installed + executed end-to-end in the research session — but `slopcheck` could not run (no Python), so per the graceful-degradation policy they are treated as `[ASSUMED]` and require this one blocking human-verify.
  </what-built>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/tlock-js — confirm publisher `drand` / repo github.com/drand/tlock-js, version 0.9.0.
    2. Visit https://www.npmjs.com/package/snarkjs — confirm iden3, repo github.com/iden3/snarkjs, version ~0.7.6.
    3. Visit https://www.npmjs.com/package/circomlibjs and https://www.npmjs.com/package/circomlib — confirm iden3.
    4. Visit https://github.com/iden3/circom/releases/tag/v2.2.3 — confirm the `circom-windows-amd64.exe` asset exists.
    5. Optionally record the circom.exe release SHA-256 for reproducibility.
  </how-to-verify>
  <acceptance_criteria>
    - The reviewer confirms all five sources are the canonical iden3/drand packages/binaries and approves the install.
    - This checkpoint is NEVER auto-approved (workflow.auto_advance is ignored for package-legitimacy gates).
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with the install, or name any package to hold.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install crypto deps + circom.exe + gitignore + Wave-0 test scaffolds</name>
  <read_first>
    - 10-RESEARCH.md Installation block (lines 57-70) + Standard Stack table (lines 33-47) + Environment Availability (lines 361-374)
    - 10-VALIDATION.md Wave 0 Requirements (lines 61-70)
    - solver/package.json (existing deps — install must NOT disturb @anthropic-ai/sdk / zod / express versions)
    - .gitignore (the "Node / JS" + "Phase 8 decision proof bundles" sections — the gitignore grammar to extend)
  </read_first>
  <action>
    In solver/, run `npm install tlock-js@0.9.0 snarkjs@0.7.6 circomlibjs@0.1.7` then `npm install -D circomlib@2.0.5` (all pure-JS, no node-gyp; use `--legacy-peer-deps` only if a peer conflict surfaces, consistent with the @daml/react precedent). Download the prebuilt compiler to `solver/src/zk/circom.exe` from `https://github.com/iden3/circom/releases/download/v2.2.3/circom-windows-amd64.exe` (curl -sL). Add a `solver/src/zk/.gitignore` (and extend the root .gitignore) to ignore zk BUILD artifacts (`*.wasm`, `*.zkey`, `*.ptau`, `*_js/`, `.tmp/`) while KEEPING the committed §4 fixtures (`vkey.json`, `proof.json`, `public.json`) tracked — do NOT gitignore circom.exe (it is a pinned build tool committed for reproducibility). Create two FAILING Wave-0 scaffolds that do NOT yet import the (not-yet-existing) modules: `solver/src/tlock.test.ts` with a `describe('CRYP-02 tlock', () => { it('round-trips against drand + blocks early decrypt (implemented in 10-03)', () => { expect(false).toBe(true) }) })`, and `solver/src/zk/verify.test.ts` with `describe('CRYP-03 groth16', () => { it('accepts the §4 proof and rejects a tampered clearing (implemented in 10-04)', () => { expect(false).toBe(true) }) })`. Confirm the whole existing solver suite still passes EXCEPT these two intentional Wave-0 reds.
  </action>
  <verify>
    <automated>cd solver && node -e "require('tlock-js');require('snarkjs');require('circomlibjs');console.log('crypto deps import OK')" && ./src/zk/circom.exe --version</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "require('tlock-js');require('snarkjs');require('circomlibjs')"` prints `crypto deps import OK` (no native-build error).
    - `./solver/src/zk/circom.exe --version` prints `circom compiler 2.2.3`.
    - `solver/package.json` lists the four npm packages at the pinned versions; @anthropic-ai/sdk / zod / express are unchanged.
    - `solver/src/tlock.test.ts` and `solver/src/zk/verify.test.ts` exist and fail (the two Wave-0 reds); the rest of `npx vitest run` is green.
    - `git status` shows zk build artifacts ignored but circom.exe tracked.
  </acceptance_criteria>
  <done>The crypto toolchain installs and runs; the Wave-0 crypto test files exist (failing) for 10-03/10-04 to implement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry / GitHub release → solver node_modules | Third-party supply-chain surface entering the operator plane |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-SC | Tampering | npm installs + circom.exe download | mitigate | Blocking human-verify (Task 1) against npmjs.com / iden3 releases before install; all are canonical iden3/drand packages executed end-to-end in research; optional circom.exe SHA-256 pin |
| T-10-06 | Tampering | zk build artifacts committed by mistake | mitigate | gitignore `*.wasm`/`*.zkey`/`*.ptau`/`*_js/`; only the small §4 vkey/proof/public fixtures + circom.exe are tracked |
</threat_model>

<verification>
- Crypto deps import under Node without a native build; circom.exe reports 2.2.3.
- The two Wave-0 crypto test files exist and are red (to be turned green by 10-03/10-04).
- `git status` confirms build artifacts ignored, fixtures + circom.exe tracked.
</verification>

<success_criteria>
- All five crypto tools present and runnable; solver suite otherwise green.
- Package legitimacy gate satisfied by a human before any install.
</success_criteria>

<output>
Create `.planning/phases/10-cryptographic-privacy/10-02-SUMMARY.md` when done.
</output>
