---
phase: 10-cryptographic-privacy
plan: 03
type: execute
wave: 2
depends_on: [10-02]
files_modified:
  - solver/src/tlock.ts
  - solver/src/tlock.test.ts
autonomous: true
requirements: [CRYP-02]
must_haves:
  truths:
    - "A payload timelock-encrypted to a future drand round is UNDECRYPTABLE (throws 'too early') before that round's beacon publishes, and decrypts to the exact payload after"
    - "'Even the venue can't open it early' is a real drand-backed guarantee, not an app promise"
    - "A documented, clearly-WEAKER local offline fallback decrypts at close and is flagged OFFLINE FALLBACK · WEAKER THAN DRAND when quicknet is unreachable"
    - "tlock keys, salts, and the offline-fallback key are server-side only — never logged or returned"
  artifacts:
    - path: "solver/src/tlock.ts"
      provides: "timelockSeal/timelockOpen against quicknet + labeled offline fallback + drand round metadata"
      exports: ["timelockSeal", "timelockOpen"]
    - path: "solver/src/tlock.test.ts"
      provides: "CRYP-02 round-trip + early-block + fallback tests"
      contains: "too early"
  key_links:
    - from: "solver/src/tlock.ts"
      to: "tlock-js mainnetClient() (quicknet)"
      via: "timelockEncrypt/timelockDecrypt/roundAt"
      pattern: "timelockEncrypt|roundAt"
---

<objective>
Build the CRYP-02 timelock module: encrypt an order/reveal payload to a future drand round so it is provably undecryptable — even by the operator/solver — until the window closes, with a clearly-weaker labeled offline fallback for when quicknet is unreachable.

Purpose: This is the "even the venue can't see your order early" guarantee. It is REAL cryptography (drand League-of-Entropy quicknet, verified round-trip by the researcher on this box), composing with the CRYP-01 commit-reveal payload and Canton per-party visibility.

Output: `solver/src/tlock.ts` (encrypt-at-submit / decrypt-at-close against quicknet + an offline held-key fallback, server-side keys only) and the CRYP-02 tests turning the Wave-0 scaffold green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/10-cryptographic-privacy/10-RESEARCH.md
@.planning/phases/10-cryptographic-privacy/10-PATTERNS.md
@.planning/phases/10-cryptographic-privacy/10-VALIDATION.md
@solver/src/proof.ts
@solver/src/ledger.ts
@solver/src/tlock.test.ts
</context>

<artifacts_this_phase_produces>
- **solver/src/tlock.ts:** `timelockSeal(payload, windowMs) => Promise<{ ciphertext, targetRound, mode: 'drand' }>` (falls back to `mode: 'offline'`), `timelockOpen(ciphertext) => Promise<string>`, `drandRoundInfo(windowMs) => { targetRound, timeToBeaconMs, chainHash }`, and an internal offline held-key path. Env: `DRAND_URL`, `DRAND_CHAIN_HASH` (quicknet defaults).
- **solver/src/tlock.test.ts:** CRYP-02 round-trip + early-block + offline-fallback tests.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tlock.ts — quicknet timelock seal/open + drand round metadata + offline fallback</name>
  <read_first>
    - 10-RESEARCH.md Pattern 2 (lines 156-168 — the verified round-trip), Pitfall 6 (drand liveness / clock skew, lines 248-252), Standard Stack (tlock-js@0.9.0, quicknet chain `52db9ba7…c84e971`, period 3, genesis 1692803367, scheme bls-unchained-g1-rfc9380)
    - 10-PATTERNS.md `solver/src/tlock.ts` assignment (lines 95-108 — analog `proof.ts` for secret-safety + `ledger.ts` env-resolution lines 44-48)
    - solver/src/proof.ts (lines 25-43 — the module-private secret pattern: never log/return keys; `fileURLToPath(new URL(...))`)
  </read_first>
  <behavior>
    - `timelockSeal(payload, windowMs)` with mode 'drand': produces an AGE-armored ciphertext bound to `roundAt(Date.now()+windowMs, info)`; `timelockOpen` on it BEFORE the beacon throws an error whose message contains "too early".
    - After the target beacon exists, `timelockOpen(ciphertext)` returns the exact original payload.
    - When quicknet is unreachable (client throws), `timelockSeal` returns `mode: 'offline'` and `timelockOpen` succeeds via a server-only held key — with no threshold guarantee (weaker).
    - `drandRoundInfo(windowMs)` returns the target round number + time-to-beacon derived from `roundAt`/`roundTime` (no manual round math).
  </behavior>
  <action>
    Create solver/src/tlock.ts importing `timelockEncrypt, timelockDecrypt, roundAt, roundTime, mainnetClient, Buffer` from `tlock-js` via named CJS interop (the module is CommonJS; `defaultChainOptions` is NOT exported — use `mainnetClient()`). Resolve `DRAND_URL`/`DRAND_CHAIN_HASH` env (quicknet `https://api.drand.sh` + chain `52db9ba7…c84e971` defaults), mirroring ledger.ts PARTICIPANT resolution. Implement `timelockSeal(payload, windowMs)` → `roundAt(Date.now()+windowMs, info)` with a small safety margin, `timelockEncrypt(target, Buffer.from(payload), client)`; on any client/network throw, fall back to a LOCAL held-key seal (node:crypto AES/ChaCha or a random key held module-private, released at close) and return `mode:'offline'`. Implement `timelockOpen` (drand → `timelockDecrypt`; offline → the held key). Implement `drandRoundInfo`. Keys/salts/the offline key are module-private — NEVER exported/returned/logged (mirror proof.ts). Record the quicknet endpoint + chain hash + the offline-fallback trust nuance in DECISIONS.md.
  </action>
  <verify>
    <automated>cd solver && npx vitest run src/tlock.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - solver/src/tlock.ts exists; `timelockSeal`/`timelockOpen`/`drandRoundInfo` are exported; no key/salt is exported or logged (grep-clean of `console.log` on any key variable).
    - `tsc --noEmit` in solver is clean.
  </acceptance_criteria>
  <done>The timelock module seals to a future drand round and opens only after the beacon, with a labeled offline fallback; secrets stay server-side.</done>
</task>

<task type="auto">
  <name>Task 2: CRYP-02 tests — early-block + post-beacon round-trip + offline fallback (Wave-0 green)</name>
  <read_first>
    - solver/src/tlock.test.ts (the failing Wave-0 scaffold from 10-02)
    - 10-VALIDATION.md Per-Task Verification Map CRYP-02 rows (lines 50-51) + "near-future round or a mocked beacon for determinism" note (line 64)
    - 10-RESEARCH.md Pitfall 6 (determinism guidance)
  </read_first>
  <action>
    Replace the Wave-0 scaffold in solver/src/tlock.test.ts with real tests. Test A (round-trip + early-block): seal a payload to a NEAR-future round (a few seconds) or a MOCKED beacon for determinism; assert `timelockOpen` before the beacon throws with a "too early" message, and after the beacon (advance/await or supply the mocked beacon) returns the exact payload. Test B (offline fallback): force the drand client to throw (inject an unreachable URL or stub the client) and assert `timelockSeal` returns `mode:'offline'`, `timelockOpen` still recovers the payload, and the result is flagged weaker (mode carries the offline label). Prefer a mocked beacon so CI is deterministic and does not depend on live quicknet; note the live round-trip as an end-of-phase human-verify. Add a secret-sweep assertion: neither the returned object nor any thrown error contains a key/salt.
  </action>
  <verify>
    <automated>cd solver && npx vitest run src/tlock.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/tlock.test.ts` passes (the two Wave-0 reds are now green).
    - The early-decrypt path asserts a "too early" rejection; the post-beacon path recovers the exact payload.
    - The offline-fallback path decrypts and is flagged weaker.
    - A secret-sweep asserts no key/salt appears in any response or error.
    - Full `npx vitest run` (solver) stays green.
  </acceptance_criteria>
  <done>CRYP-02 is proven by deterministic tests: early-decrypt blocked, post-beacon recovered, offline fallback labeled weaker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solver → drand quicknet | External threshold network the timelock guarantee rests on |
| ciphertext held on-ledger during window | What a party (and the operator) holds — must be undecryptable early |
| offline-fallback key | A new server-only secret that must never leak |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Info Disclosure | operator/solver peeks during window | mitigate | tlock ciphertext undecryptable until the drand beacon (verified early-decrypt block); the operator holds only ciphertext |
| T-10-08 | Info Disclosure | salt / timelock key leakage | mitigate | Keys/salts/offline key module-private, never logged/returned (mirror proof.ts); secret-sweep test |
| T-10-09 | DoS / Trust | drand network dishonesty/outage | accept | Documented external-trust assumption; labeled `OFFLINE FALLBACK · WEAKER THAN DRAND` local held-key path keeps the demo runnable |
</threat_model>

<verification>
- `npx vitest run src/tlock.test.ts` green (early-block, post-beacon, offline fallback, secret-sweep).
- Solver `tsc --noEmit` clean; full solver suite green.
- Live quicknet round-trip = end-of-phase human-verify.
</verification>

<success_criteria>
- Timelock seal/open is real drand-backed with a labeled weaker fallback.
- No key/salt ever enters a response or log.
</success_criteria>

<output>
Create `.planning/phases/10-cryptographic-privacy/10-03-SUMMARY.md` when done.
</output>
