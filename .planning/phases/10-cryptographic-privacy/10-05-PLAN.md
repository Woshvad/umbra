---
phase: 10-cryptographic-privacy
plan: 05
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - solver/src/ledger.ts
  - solver/src/timemachine.ts
  - solver/src/timemachine.test.ts
  - web/daml.js
autonomous: true
requirements: [CRYP-01, VIZ-02]
must_haves:
  truths:
    - "The solver can gather still-live OrderCommitments for a round and forfeit each non-revealer's bond to the operator via ForfeitBond"
    - "The solver can create an on-ledger ProofAnchor recording a proof/vkey hash"
    - "The solver records a stage→ledger-offset map per round (open → committed → sealed/revealed → cleared → settled) for the VIZ-02 replay"
    - "web/daml.js is regenerated so OrderCommitment / ProofAnchor bindings exist for the desk + time-machine planes"
    - "settle() stays byte-unchanged; the crypto operations are separate exported functions (the §4 clearing path is untouched)"
  artifacts:
    - path: "solver/src/ledger.ts"
      provides: "readOrderCommitments / forfeitNonRevealed / anchorProof / currentOffset (operator-plane, token module-private)"
      exports: ["forfeitNonRevealed", "anchorProof", "currentOffset"]
    - path: "solver/src/timemachine.ts"
      provides: "in-memory stage→offset map (recordStage / getStageOffsets)"
      exports: ["recordStage", "getStageOffsets"]
    - path: "web/daml.js"
      provides: "regenerated @daml.js/umbra-0.1.0 bindings incl. OrderCommitment + ProofAnchor"
      contains: "OrderCommitment"
  key_links:
    - from: "solver/src/ledger.ts anchorProof"
      to: "createContract('Umbra.Auction:ProofAnchor', …)"
      via: "operator submit-and-wait"
      pattern: "ProofAnchor"
---

<objective>
Wire the CRYP-01 on-ledger primitives into the operator-plane solver (bond forfeit + proof anchor), add the VIZ-02 stage→offset capture, and regenerate the committed web/daml.js bindings — all additive, leaving settle() and the §4 clearing path byte-unchanged.

Purpose: The browser drives commit/reveal on each desk's own plane, but the OPERATOR-authority operations (forfeit a non-revealer's bond, anchor a proof hash) and the authentic per-stage offset map live server-side. This plan gives the solver those capabilities and refreshes the generated bindings so the desk UI and time-machine can address the new templates.

Output: `solver/src/ledger.ts` additions (`readOrderCommitments`, `forfeitNonRevealed`, `anchorProof`, exported `currentOffset`), `solver/src/timemachine.ts` (stage→offset map), and a regenerated + committed `web/daml.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/10-cryptographic-privacy/10-RESEARCH.md
@.planning/phases/10-cryptographic-privacy/10-PATTERNS.md
@.planning/phases/10-cryptographic-privacy/10-VALIDATION.md
@solver/src/ledger.ts
@solver/src/clock.ts
@daml/Umbra/Auction.daml
@README.md
</context>

<artifacts_this_phase_produces>
- **solver/src/ledger.ts:** `readOrderCommitments(roundId)`, `forfeitNonRevealed(roundId)` (exercise ForfeitBond on each still-live OrderCommitment), `anchorProof(roundId, proofHash, vkeyHash)` (create ProofAnchor), `currentOffset()` (public wrapper over the private `ledgerEnd()`). Operator token stays module-private.
- **solver/src/timemachine.ts:** an in-memory `Map<roundId, Record<stage, offset>>` with `recordStage(roundId, stage)` (captures `currentOffset()`) and `getStageOffsets(roundId)`; mirrors the clock.ts in-memory pattern.
- **solver/src/timemachine.test.ts:** stage-map unit test.
- **web/daml.js:** regenerated bindings including `OrderCommitment`, `ProofAnchor`, and `Venue.CommitOrder`/`RevealOrder`/`ForfeitBond` choices.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: ledger.ts — OrderCommitment reads + bond forfeit + proof anchor + currentOffset</name>
  <read_first>
    - solver/src/ledger.ts (whole file — `queryByEntity` lines 143-159, `createContract`/`exerciseChoice` lines 124-138, `ledgerEnd` lines 103-107, `settle` lines 332-399, the `#umbra:Module:Entity` addressing + Int/Decimal→string wire notes)
    - daml/Umbra/Auction.daml (the 10-01 OrderCommitment / ForfeitBond / ProofAnchor / AnchorProof shapes)
    - 10-PATTERNS.md `solver/src/ledger.ts` assignment (lines 136-140)
  </read_first>
  <action>
    Add to solver/src/ledger.ts (leaving `settle` and `tamperClear` BYTE-UNCHANGED — grep-confirm zero diff): `readOrderCommitments(roundId)` (copy `queryByEntity('OrderCommitment')` + filter by roundId, projecting `{ contractId, desk, commitment, bondCid }`); `forfeitNonRevealed(roundId)` that iterates the still-live OrderCommitments for the round and `exerciseChoice('Umbra.Auction:OrderCommitment', cid, 'ForfeitBond', {})` on each (operator authority) — resolves the count forfeited; `anchorProof(roundId, proofHash, vkeyHash)` that `createContract('Umbra.Auction:ProofAnchor', { operator: operatorParty, roundId, proofHash, vkeyHash })`; and `export const currentOffset = () => ledgerEnd()` (a public wrapper — the token stays module-private). All new functions are secret-free (no token/key in returns or logs), mirroring the existing exports.
  </action>
  <verify>
    <automated>cd solver && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` clean; `readOrderCommitments`/`forfeitNonRevealed`/`anchorProof`/`currentOffset` exported.
    - `git diff solver/src/ledger.ts` shows ZERO changes to the `settle` and `tamperClear` function bodies.
    - No token/key appears in any new return value or log (grep-clean).
  </acceptance_criteria>
  <done>The operator plane can forfeit non-revealers' bonds, anchor a proof hash, and read the current ledger offset.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: timemachine.ts — stage→offset capture map</name>
  <read_first>
    - solver/src/clock.ts (the in-memory `Map<roundId, RoundState>` clock pattern — copy the module-scoped map + idempotent updates)
    - solver/src/ledger.ts (the new `currentOffset` from Task 1)
    - 10-RESEARCH.md VIZ-02 (lines 14, 346-354 — solver records stage offsets; browser does per-party reads)
  </read_first>
  <behavior>
    - `recordStage(roundId, 'committed')` stores the current ledger offset for that (round, stage).
    - `getStageOffsets(roundId)` returns the recorded `{ open?, committed?, sealed?, cleared?, settled? }` map (missing stages absent).
    - Re-recording a stage is idempotent-safe (last write wins / no crash).
  </behavior>
  <action>
    Create solver/src/timemachine.ts with a module-scoped `Map<string, Partial<Record<Stage, number>>>` where `Stage = 'open'|'committed'|'sealed'|'cleared'|'settled'`. Export `recordStage(roundId, stage, offset?)` (defaults `offset` to `await currentOffset()` from ledger.ts — but keep the offset injectable so the unit test needs no ledger) and `getStageOffsets(roundId)`. Pure cache/clock state only — the ledger is authoritative. Add solver/src/timemachine.test.ts asserting record→get round-trips across stages with an injected offset function (no live ledger).
  </action>
  <verify>
    <automated>cd solver && npx vitest run src/timemachine.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/timemachine.test.ts` passes.
    - `recordStage`/`getStageOffsets` round-trip a five-stage map; missing stages are absent.
    - The offset source is injectable so the test runs ledger-free.
  </acceptance_criteria>
  <done>The solver records an authentic stage→offset map the browser replays per party.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate + commit web/daml.js with the new templates</name>
  <read_first>
    - README.md lines 152-154 (web/daml.js is committed generated bindings from `daml codegen js`, regenerated on template change)
    - STATE.md decisions [09-01]/[09-05] (prior regen-and-commit of web/daml.js — the established workflow)
    - daml/Umbra/Auction.daml (the 10-01 templates that must appear in the regenerated bindings)
  </read_first>
  <action>
    Rebuild the DAR and regenerate the TypeScript bindings (Git-Bash PATH-only): `bash -lc "export PATH=/c/Users/woshv/bin:$PATH && cd /c/Users/woshv/Desktop/Umbra/daml && daml build && daml codegen js -o ../web/daml.js .daml/dist/umbra-0.1.0.dar"`. Confirm the regenerated `web/daml.js/umbra-0.1.0/lib/Umbra/Auction/module` now exports `OrderCommitment` + `ProofAnchor` and the `Roles` module exports `Venue.CommitOrder`, and that `OrderCommitment` carries `RevealOrder`/`ForfeitBond`. This depends on 10-01 having landed the templates — build against the current daml/Umbra sources. Commit the regenerated tree (it is intentionally tracked so a fresh clone builds without the SDK).
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -10 || npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `web/daml.js/umbra-0.1.0/lib/Umbra/Auction/module` exports `OrderCommitment` and `ProofAnchor` (grep-confirm).
    - `web/daml.js/umbra-0.1.0/lib/Umbra/Roles/module` exports `Venue` with a `CommitOrder` choice.
    - `cd web && npm run build` (tsc + vite) succeeds against the regenerated bindings.
    - The regenerated tree is staged for commit.
  </acceptance_criteria>
  <done>The committed web bindings include the new commit-reveal + proof-anchor templates, so the desk UI and time-machine can address them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solver (operator token) → ledger | Operator-only ForfeitBond / ProofAnchor commands; token module-private |
| stage→offset map | Cache/clock state; the ledger offset is authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-14 | Elevation of Privilege | forfeit/anchor operations | mitigate | ForfeitBond/AnchorProof are controller operator on-ledger; the solver token stays module-private (never returned/logged) — mirrors ledger.ts SOLV-04 discipline |
| T-10-15 | Tampering | settle() regression via new code | mitigate | New functions are separate exports; `git diff` proves settle()/tamperClear() byte-unchanged; the §4 canary stays green |
| T-10-16 | Repudiation | stale bindings hide new templates | mitigate | web/daml.js regenerated + committed; web build gate confirms the new exports resolve |
</threat_model>

<verification>
- Solver `tsc --noEmit` clean; `npx vitest run src/timemachine.test.ts` green; full solver suite green.
- `git diff` proves settle()/tamperClear() byte-unchanged.
- `cd web && npm run build` succeeds with the regenerated bindings exporting OrderCommitment/ProofAnchor/CommitOrder.
</verification>

<success_criteria>
- The operator plane can forfeit bonds + anchor proofs + capture stage offsets, with settle() untouched.
- The committed web bindings include the Phase-10 templates.
</success_criteria>

<output>
Create `.planning/phases/10-cryptographic-privacy/10-05-SUMMARY.md` when done.
</output>
