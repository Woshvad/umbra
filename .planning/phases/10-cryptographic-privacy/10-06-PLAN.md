---
phase: 10-cryptographic-privacy
plan: 06
type: execute
wave: 3
depends_on: [10-03, 10-04, 10-05]
files_modified:
  - solver/src/api.ts
  - solver/src/index.ts
  - solver/src/api.test.ts
autonomous: true
requirements: [CRYP-02, CRYP-03, VIZ-02]
must_haves:
  truths:
    - "The solver exposes timelock encrypt/decrypt, ZK prove/verify/anchor + tamper-reject, and a VIZ-02 stage→offset endpoint on :4100"
    - "The off-ledger ZK verify verdict and the on-ledger proof-hash anchor are DISTINCT responses (verify off-ledger + anchor on-ledger)"
    - "Every new endpoint is zod-validated and the secret-safe { error:{code,message} } envelope never echoes the operator token, ANTHROPIC_API_KEY, tlock keys, salts, or the proof witness"
    - "The five §11 endpoints and /settle are byte-unchanged; the §4 fixture still clears $100.00"
  artifacts:
    - path: "solver/src/api.ts"
      provides: "crypto endpoints wired into AppDeps + createApp (timelock, prove/verify/anchor/tamper-proof, stage-offsets)"
      contains: "stage-offsets"
    - path: "solver/src/index.ts"
      provides: "boot wiring of tlock + zk + ledger crypto fns into buildDeps"
      contains: "generateClearingProof"
    - path: "solver/src/api.test.ts"
      provides: "crypto endpoint + secret-sweep tests (stubbed deps)"
      contains: "ANTHROPIC_API_KEY"
  key_links:
    - from: "solver/src/api.ts"
      to: "tlock.ts + zk/prove.ts + zk/verify.ts + ledger.anchorProof + timemachine.getStageOffsets"
      via: "AppDeps DI + wrap() handlers"
      pattern: "verify-proof|stage-offsets|timelock"
---

<objective>
Expose the CRYP-02 / CRYP-03 / VIZ-02 solver capabilities as HTTP endpoints on :4100, dependency-injected through `AppDeps`, zod-validated, and behind the existing secret-safe envelope — with the honest off-ledger-verify / on-ledger-anchor split reflected as distinct endpoints. The five §11 endpoints and `/settle` stay byte-unchanged.

Purpose: The browser talks only to the solver for operator-plane crypto (tlock keys, proof generation/verification, the stage→offset map). This plan is the integration hub that wires tlock.ts (10-03), zk/* (10-04), and the ledger/timemachine additions (10-05) into api.ts + index.ts.

Output: new endpoints (`POST /round/:id/timelock-encrypt`, `POST /timelock-decrypt`, `POST /round/:id/prove`, `POST /round/:id/verify-proof`, `POST /round/:id/anchor-proof`, `POST /round/:id/tamper-proof`, `GET /round/:id/stage-offsets`), AppDeps additions, boot wiring, and extended api.test.ts (secret sweep + endpoint behavior).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/10-cryptographic-privacy/10-RESEARCH.md
@.planning/phases/10-cryptographic-privacy/10-PATTERNS.md
@.planning/phases/10-cryptographic-privacy/10-VALIDATION.md
@solver/src/api.ts
@solver/src/index.ts
@solver/src/tlock.ts
@solver/src/zk/prove.ts
@solver/src/zk/verify.ts
@solver/src/ledger.ts
</context>

<artifacts_this_phase_produces>
- **solver/src/api.ts:** `AppDeps` gains `timelockEncrypt`, `timelockDecrypt`, `drandRoundInfo`, `generateProof`, `verifyProof`, `anchorProof`, `tamperProof`, `getStageOffsets`; new `wrap()`-ed handlers + zod `.strict()` bodies; the off-ledger verify verdict and the on-ledger anchor are DISTINCT endpoints.
- **solver/src/index.ts:** `BuildDepsArgs`/`buildDeps` + `main()` wire tlock.ts, zk/prove.ts, zk/verify.ts, ledger.anchorProof/forfeitNonRevealed, timemachine.getStageOffsets.
- **solver/src/api.test.ts:** crypto endpoint tests + an extended secret sweep (tlock key + proof witness sentinels asserted absent).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: api.ts — AppDeps additions + crypto endpoints (timelock, prove/verify/anchor/tamper-proof, stage-offsets)</name>
  <read_first>
    - solver/src/api.ts (whole file — `AppDeps` lines 74-134, zod schemas lines 150-176, `wrap` lines 186-190, the `/tamper-clear` handler lines 552-565, the `/proof` read handler lines 571-581, the secret-safe error middleware lines 614-621, `TERMINAL_STATUSES` lines 178-179)
    - 10-PATTERNS.md `solver/src/api.ts` assignment (lines 128-132)
    - 10-UI-SPEC.md "the verify/anchor split is a visual split" (Provenance Grammar rule 3) — the endpoints must keep verify (off-ledger) and anchor (on-ledger) DISTINCT
  </read_first>
  <action>
    Extend `AppDeps` with: `timelockEncrypt(payload, windowMs)`, `timelockDecrypt(ciphertext)`, `drandRoundInfo(windowMs)`, `generateProof(roundId)` (build the §4 input from the round's sealed views + host Poseidon commitments → `{ proof, publicSignals, sizeBytes, ms }`), `verifyProof(vkey, publicSignals, proof) => boolean`, `anchorProof(roundId, proofHash, vkeyHash)`, `tamperProof(roundId)` (perturb a public input, verify → expect false; return the verbatim rejection, mirroring tamperClear's never-throw contract), `getStageOffsets(roundId)`. Add `z.object().strict()` bodies for the encrypt/decrypt/verify/anchor inputs (reuse the tamperClearBody enum grammar; cap payload sizes per ASVS V5). Add `wrap()`-ed handlers: `POST /round/:id/timelock-encrypt`, `POST /timelock-decrypt`, `POST /round/:id/prove`, `POST /round/:id/verify-proof`, `POST /round/:id/anchor-proof`, `POST /round/:id/tamper-proof`, `GET /round/:id/stage-offsets`. Keep verify (off-ledger, returns a boolean verdict) and anchor (on-ledger, returns the anchored hashes) as SEPARATE endpoints. Throw `ApiError` (secret-free). The five §11 endpoints + `/settle` + `/tamper-clear` + `/proof` + `/proof-pack.pdf` handlers stay byte-unchanged.
  </action>
  <verify>
    <automated>cd solver && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` clean; the seven new endpoints exist with zod-validated bodies.
    - verify-proof and anchor-proof are distinct handlers (off-ledger verdict vs on-ledger anchor).
    - `git diff` shows the existing five §11 handlers + `/settle` byte-unchanged.
  </acceptance_criteria>
  <done>The solver exposes the timelock/ZK/stage-offset endpoints behind zod + the secret-safe envelope, verify and anchor kept distinct.</done>
</task>

<task type="auto">
  <name>Task 2: index.ts boot wiring + api.test.ts crypto + secret-sweep tests</name>
  <read_first>
    - solver/src/index.ts (whole file — `BuildDepsArgs` lines 52-78, `buildDeps` lines 88-133, `main()` dynamic imports lines 136-153, the settle/proof wiring lines 206-312)
    - solver/src/api.test.ts (the existing DI-stub + secret-sweep pattern — how endpoints are tested via `app.listen(0)` with stubbed deps; the ANTHROPIC_API_KEY sentinel sweep)
    - 10-VALIDATION.md CRYP-02/03/VIZ-02 rows + "secret-free envelope" security domain (V5/V7)
  </read_first>
  <action>
    In index.ts `main()`, dynamically import tlock.ts + zk/prove.ts + zk/verify.ts (alongside the existing ledger/agent/proof imports) and wire `timelockEncrypt/Decrypt`, `drandRoundInfo`, `generateProof` (composes zk/prove over `ledger.readSealedOrders`), `verifyProof`, `anchorProof` (→ `ledger.anchorProof`), `tamperProof`, and `getStageOffsets` (→ `timemachine.getStageOffsets`) through `BuildDepsArgs`/`buildDeps` into `AppDeps`. Also arm `timemachine.recordStage` at the open/close/settle transitions already present in the boot wiring (open→'open', close→'sealed'/'cleared', settle→'settled') without changing settle()'s return. Keep the index.test.ts boot-wiring path green (default the new deps like the existing optional `readProofBundle`/`buildProofPack` defaults). Extend api.test.ts: assert each new endpoint's happy path with STUBBED deps (no live ledger/drand/snarkjs), assert verify vs anchor return distinct shapes, and extend the secret sweep with a tlock-key sentinel + a proof-witness sentinel asserted ABSENT from every crypto response and error body.
  </action>
  <verify>
    <automated>cd solver && npx vitest run 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` (full solver suite) green including the new api.test.ts crypto + secret-sweep cases.
    - The secret sweep proves no operator token / ANTHROPIC_API_KEY / tlock key / salt / proof witness appears in any crypto response or error.
    - index.test.ts boot-wiring test still passes (new deps default cleanly).
    - `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The crypto endpoints are wired at boot and covered by stubbed endpoint + secret-sweep tests; the §4 path is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (:5173) → solver (:4100) | CORS-scoped operator-plane API; no credential crosses to the browser |
| solver → tlock keys / proof witness | Server-side secrets that must never enter a response/log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-17 | Info Disclosure | crypto endpoint responses leak secrets | mitigate | Secret-safe `{error:{code,message}}` envelope + extended secret sweep (tlock key + witness sentinels absent) |
| T-10-18 | Tampering | malformed crypto inputs | mitigate | zod `.strict()` bodies with size caps (ASVS V5); malformed → sanitized 400 |
| T-10-19 | Repudiation | off-ledger verify conflated with on-ledger | mitigate | verify-proof and anchor-proof are DISTINCT endpoints (off-ledger verdict vs on-ledger hash anchor) |
| T-10-20 | Tampering | regression on the byte-frozen §11/settle handlers | mitigate | `git diff` proves the five §11 handlers + /settle unchanged; §4 canary stays green |
</threat_model>

<verification>
- Full solver `npx vitest run` green (crypto endpoints + secret sweep + existing suite).
- Solver `tsc --noEmit` clean; `git diff` proves the §11/settle handlers byte-unchanged.
- Live encrypt→close→decrypt + prove→verify→anchor + stage-offsets against a booted LocalNet = end-of-phase human-verify.
</verification>

<success_criteria>
- The solver exposes timelock + ZK + stage-offset endpoints, verify vs anchor distinct, all secret-safe.
- The §11 endpoints and /settle are byte-unchanged; the §4 fixture still clears $100.00.
</success_criteria>

<output>
Create `.planning/phases/10-cryptographic-privacy/10-06-SUMMARY.md` when done.
</output>
