---
phase: 10-cryptographic-privacy
plan: 07
type: execute
wave: 4
depends_on: [10-06]
files_modified:
  - web/src/solver.ts
  - web/src/lib/cryptoUrls.test.ts
autonomous: true
requirements: [CRYP-01, CRYP-02, CRYP-03, VIZ-02]
must_haves:
  truths:
    - "The browser has a typed operator-plane client for every Phase-10 solver endpoint (timelock, prove/verify/anchor/tamper, stage-offsets)"
    - "No operator token, ANTHROPIC_API_KEY, tlock key, or port literal ever lives in the web bundle — the client carries no credential"
    - "A solver reject degrades to the shipped OFFLINE_CAPTION (SolverError OFFLINE), consistent with the existing five endpoints"
  artifacts:
    - path: "web/src/solver.ts"
      provides: "timelockEncrypt/Decrypt, generateProof/verifyProof/anchorProof/tamperProof, getStageOffsets client fns + response types"
      contains: "getStageOffsets"
    - path: "web/src/lib/cryptoUrls.test.ts"
      provides: "pure URL/shape test — no :4000, no credential in the bundle"
      contains: "credential"
  key_links:
    - from: "web/src/solver.ts"
      to: "SOLVER_BASE_URL /round/:id/{timelock-encrypt,prove,verify-proof,anchor-proof,stage-offsets}"
      via: "call<T>() (no auth header)"
      pattern: "verify-proof|stage-offsets"
---

<objective>
Add the typed operator-plane client surface for every Phase-10 solver endpoint to `web/src/solver.ts`, so the three Phase-10 web views (OrderTicket, ProofOfClearingPanel, TimeMachineView) can consume them without any of them touching solver.ts (keeping their Wave-5 plans parallel and conflict-free).

Purpose: `web/src/solver.ts` is the single operator-plane fetch layer — the browser NEVER holds the operator token or the Anthropic key. This plan mirrors the 10-06 endpoints into typed client functions + response types, reusing the shipped `call<T>()` / `SolverError` / `OFFLINE_CAPTION` grammar.

Output: `web/src/solver.ts` additions (timelock, prove/verify/anchor/tamper-proof, stage-offsets client fns + types) and a pure `web/src/lib/cryptoUrls.test.ts` proving no credential / port literal is in the bundle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/10-cryptographic-privacy/10-RESEARCH.md
@.planning/phases/10-cryptographic-privacy/10-PATTERNS.md
@.planning/phases/10-cryptographic-privacy/10-UI-SPEC.md
@web/src/solver.ts
@solver/src/api.ts
</context>

<artifacts_this_phase_produces>
- **web/src/solver.ts:** response types (`TimelockMeta`, `DrandRoundInfo`, `ProofArtifact`, `VerifyVerdict`, `AnchorResult`, `StageOffsets`) + client fns (`timelockEncrypt`, `timelockDecrypt`, `generateProof`, `verifyProof`, `anchorProof`, `tamperProof`, `getStageOffsets`), all off `SOLVER_BASE_URL` via the shipped `call<T>()`.
- **web/src/lib/cryptoUrls.test.ts:** pure URL-construction test (no `:4000`, no credential/auth header).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: web/src/solver.ts — Phase-10 crypto client fns + response types</name>
  <read_first>
    - web/src/solver.ts (whole file — `SOLVER_BASE_URL`/`solverPort`/`OFFLINE_CAPTION` lines 17-33, the response-type block lines 35-146, `call<T>()` lines 162-180, the endpoint fns lines 182-215, the `rationaleStreamUrl`/`proofPackUrl` plain-URL builders lines 217-238)
    - solver/src/api.ts (the 10-06 endpoint request/response shapes to mirror EXACTLY)
    - 10-UI-SPEC.md "Two data planes" note (CRYP-03 + VIZ-02 on the solver operator plane via web/src/solver.ts; keys stay server-side)
  </read_first>
  <action>
    Mirror the 10-06 endpoints into web/src/solver.ts using the shipped `call<T>()` (no auth header — the solver serves the operator plane open, RESEARCH Pitfall 5). Add types: `TimelockMeta` ({ ciphertext, targetRound, mode: 'drand'|'offline' }), `DrandRoundInfo` ({ targetRound, timeToBeaconMs, chainHash }), `ProofArtifact` ({ proof, publicSignals, sizeBytes, ms }), `VerifyVerdict` ({ ok: boolean }), `AnchorResult` ({ proofHash, vkeyHash }), `TamperProofResponse` ({ rejected: boolean, error: string }), `StageOffsets` ({ open?, committed?, sealed?, cleared?, settled?: number }). Add fns `timelockEncrypt(id, payload, windowMs)`, `timelockDecrypt(ciphertext)`, `generateProof(id)`, `verifyProof(id, artifact)`, `anchorProof(id, hashes)`, `tamperProof(id)`, `getStageOffsets(id)` — each via `call<T>` on `${SOLVER_BASE_URL}/…`. Reuse the SINGLE port source (`SOLVER_BASE_URL`) — NO `:4000` and NO port literal anywhere; a network reject throws `SolverError(0,'OFFLINE',OFFLINE_CAPTION)` like the existing fns.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` clean; the seven client fns + their types are exported.
    - Every path is built off `SOLVER_BASE_URL`; no `:4000` and no port literal (grep-clean).
    - No `Authorization`/operator-token/Anthropic-key literal appears in solver.ts (grep-clean).
  </acceptance_criteria>
  <done>The browser has a typed, credential-free client for every Phase-10 solver endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: cryptoUrls.test.ts — pure URL + no-credential unit test</name>
  <read_first>
    - web/src/lib/solverUrls.test.ts (the established pure URL-construction test pattern from Phase 8 — asserts no :4000, no credential)
    - web/src/solver.ts (the Task-1 additions)
  </read_first>
  <action>
    Create web/src/lib/cryptoUrls.test.ts asserting: each Phase-10 client fn targets the correct path shape off SOLVER_BASE_URL (e.g. `/round/R1/verify-proof`, `/round/R1/stage-offsets`); no constructed URL contains `:4000`; the module source carries no `Authorization` header, no operator-token literal, and no Anthropic-key literal (import the source text or assert via the exported fns + a static grep-style check). Keep it pure (no fetch, no DOM).
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/cryptoUrls.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/cryptoUrls.test.ts` passes.
    - The test proves no `:4000`, no credential, correct endpoint paths.
    - Full web `npx vitest run` stays green; `npm run build` succeeds.
  </acceptance_criteria>
  <done>A pure test locks the Phase-10 client to the single solver port with zero credential in the bundle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web bundle → solver (:4100) | Operator-plane client; must carry no credential and no port drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-21 | Info Disclosure | operator/Anthropic credential in the web bundle | mitigate | Client uses `call<T>()` with no auth header; cryptoUrls.test.ts asserts no credential literal, no :4000 |
| T-10-22 | DoS | solver down blocks the UI | accept | Network reject → shipped `SolverError OFFLINE` → `OFFLINE_CAPTION`; consistent with the shipped five endpoints |
</threat_model>

<verification>
- `npx vitest run src/lib/cryptoUrls.test.ts` green; full web suite green; `npm run build` succeeds.
- Web `tsc --noEmit` clean; grep-clean of `:4000` / operator-token / Anthropic-key in solver.ts.
</verification>

<success_criteria>
- A complete typed Phase-10 client on the single solver port with no credential in the bundle.
</success_criteria>

<output>
Create `.planning/phases/10-cryptographic-privacy/10-07-SUMMARY.md` when done.
</output>
