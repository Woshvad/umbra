---
phase: 10-cryptographic-privacy
plan: 08
type: execute
wave: 5
depends_on: [10-05, 10-07]
files_modified:
  - web/src/components/OrderTicket.tsx
autonomous: true
requirements: [CRYP-01, CRYP-02]
must_haves:
  truths:
    - "The desk ticket runs a commit → committed → timelocked → revealed / forfeited lifecycle on the desk's OWN token (never an operator token in the browser)"
    - "COMMIT & POST BOND is the one new value-locking action, with the bond+forfeit confirmation copy; REVEAL ORDER → is time-critical but non-destructive"
    - "Every crypto surface states its guarantee AND its limitation: COMMITTED (T1 solid ink), TIMELOCKED (T2 solid + DRAND tag / T3 dashed + OFFLINE-FALLBACK red tag), REVEALED (T1), FORFEITED (red)"
    - "load demo order stays a plain §4 Limit and the batch still reveals + clears $100.00; a passing commitment check is INK (never lime — lime stays the uniform-clear signal)"
    - "No new token/font/size/palette/spacing/keyframe — reuses bg-redact, umbra-wipe, the ink evidence surface, and the shipped verdict-square grammar"
  artifacts:
    - path: "web/src/components/OrderTicket.tsx"
      provides: "CRYP-01/02 commit-reveal-timelock lifecycle wrapped around the shipped seal flow"
      contains: "COMMIT & POST BOND"
  key_links:
    - from: "web/src/components/OrderTicket.tsx"
      to: "Venue.CommitOrder + OrderCommitment.RevealOrder (desk ctx.useLedger) + solver.timelockEncrypt/drandRoundInfo"
      via: "desk-plane exercise + operator-plane tlock fetch"
      pattern: "CommitOrder|RevealOrder|timelockEncrypt"
---

<objective>
Wrap the shipped Order Ticket seal flow in the CRYP-01/CRYP-02 commit → committed → timelocked → revealed / forfeited lifecycle on the desk's own JSON Ledger API v2 plane, with the three-tier provenance/honest-labeling grammar and the §4 $100.00 invariant intact.

Purpose: This is where a judge FEELS "commit a sealed order + post a bond, the venue holds only ciphertext during the window, then reveal and the ledger re-checks your commitment." All on the desk's own token — structural privacy is unchanged, the crypto strengthens it.

Output: `web/src/components/OrderTicket.tsx` extended with the lifecycle state machine, the COMMIT & POST BOND / REVEAL ORDER → CTAs, the commitment/ciphertext/bond panes (T1/T2/T3-tagged), and the forfeited loss state — reusing only shipped tokens/classes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/10-cryptographic-privacy/10-UI-SPEC.md
@.planning/phases/10-cryptographic-privacy/10-PATTERNS.md
@web/src/components/OrderTicket.tsx
@web/src/components/DeskColumn.tsx
@web/src/solver.ts
</context>

<artifacts_this_phase_produces>
- **web/src/components/OrderTicket.tsx:** a `phase: 'draft'|'committed'|'timelocked'|'revealed'|'forfeited'` state machine; `COMMIT & POST BOND` (ink-fill CTA + bond/forfeit confirmation copy) exercising `Venue.CommitOrder`; the COMMITTED pane (COMMITMENT hash + BOND POSTED + `bg-redact` sealed contents, T1 `ON-LEDGER`); the TIMELOCKED pane (T2 `TIMELOCK · DRAND QUICKNET` ciphertext + DRAND ROUND / TIME TO BEACON via `solver.timelockEncrypt`/`drandRoundInfo`; T3 dashed + red `OFFLINE FALLBACK · WEAKER THAN DRAND` variant); the `REVEAL ORDER →` CTA + `OrderCommitment.RevealOrder` exercise + ink `COMMITMENT VERIFIED ON-LEDGER` / red `COMMITMENT MISMATCH` verdict via `umbra-wipe`; the FORFEITED red loss state.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Commit lifecycle — DRAFT → COMMIT & POST BOND → COMMITTED (T1) with bg-redact sealed contents</name>
  <read_first>
    - web/src/components/OrderTicket.tsx (whole file — the shipped seal state machine: `ticketLocked` lines 87-88, `onSeal` lines 144-186, the `nlPhase` async idiom lines 81-142, the SEALED `bg-redact` row lines 488-505, the CTA grammar lines 507-516, `loadDemo` lines 110-118)
    - web/src/components/DeskColumn.tsx (`RedactedBody` red-verdict + `bg-redact` grammar lines 162-214; the 6px red square line 210)
    - 10-UI-SPEC.md "CRYP-01 + CRYP-02 — Commit · Reveal · Timelock" (lines 207-266) + Provenance Grammar (T1/T2/T3, lines 58-93) + Copywriting Contract (COMMIT & POST BOND, COMMITMENT, BOND POSTED, CONTENTS SEALED…, destructive confirmation copy lines 350-363)
  </read_first>
  <action>
    Extend OrderTicket with a `phase: 'draft'|'committed'|'timelocked'|'revealed'|'forfeited'` state machine wrapping the existing seal flow (extend the `nlPhase` idiom; the one-per-round lock + Phase-9 order-type selector stay byte-unchanged; `load demo` stays a plain §4 Limit). Replace the single SEAL ORDER confirm with `COMMIT & POST BOND` (ink-fill CTA, mono 13/700, the verbatim bond+forfeit confirmation copy from the Copywriting Contract). On commit: exercise `Venue.CommitOrder` on the desk's OWN `ctx.useLedger()` (mirroring the shipped `onSeal` Int/Decimal-as-strings pattern), passing the desk's bond USDCx holding. Render the COMMITTED card (T1 — solid 1px ink + neutral `ON-LEDGER` tag): a `COMMITMENT` caption over the `hash(order‖salt)` middle-truncated on the INK evidence surface (`#0A0A0A` bg / `#F4F1EA` text, IBM Plex Mono 13 pre-wrap tabular, full value in `title`/`aria-label`); a `BOND POSTED` row → `{n} USDCx` (mono 18); the would-be order values under the `bg-redact` stripe with the `CONTENTS SEALED — VISIBLE ONLY TO YOU AT REVEAL` caption. Use ONLY shipped tokens/classes — no new token/font/size/keyframe. A passing commit is ink, NEVER lime.
  </action>
  <verify>
    <automated>cd web && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` (tsc + vite) succeeds.
    - `COMMIT & POST BOND` exercises `Venue.CommitOrder` on the desk's own ctx (grep-clean of any operator token / @daml/react operator context).
    - The COMMITTED card renders the commitment hash literally on the ink evidence surface + BOND POSTED + `bg-redact` sealed contents, tagged `ON-LEDGER` (T1, ink — no lime).
    - `load demo` still loads the plain §4 Limit values.
  </acceptance_criteria>
  <done>The ticket commits on the desk plane and shows the on-ledger commitment + posted bond with the contents sealed under the redaction stripe.</done>
</task>

<task type="auto">
  <name>Task 2: TIMELOCKED (T2 drand / T3 offline) + REVEAL → verdict + FORFEITED</name>
  <read_first>
    - web/src/components/OrderTicket.tsx (the Task-1 lifecycle; the `umbra-wipe` overlay lines 531-537, the reduced-motion helper lines 55-61, the `animate-umbra-pulse` status-square usage lines 260-265)
    - web/src/solver.ts (the 10-07 `timelockEncrypt`/`timelockDecrypt`/`drandRoundInfo` client fns + `SolverError`/`OFFLINE_CAPTION`)
    - 10-UI-SPEC.md lifecycle states 3-6 (TIMELOCKED / REVEALED / REVEAL CTA / FORFEITED, lines 232-266) + Provenance Grammar rules 1-5 + Copywriting (TIMELOCK · DRAND QUICKNET, CIPHERTEXT, DRAND ROUND · TIME TO BEACON, OFFLINE FALLBACK · WEAKER THAN DRAND, COMMITMENT VERIFIED ON-LEDGER, COMMITMENT MISMATCH…, BOND RETURNED, BOND FORFEITED…, error-state drand copy)
  </read_first>
  <action>
    Add the TIMELOCKED card: call `solver.timelockEncrypt(roundId, payload, windowMs)` + `solver.drandRoundInfo` — on `mode:'drand'` render T2 (solid ink + ink tag `TIMELOCK · DRAND QUICKNET`): a lock glyph (CSS ink square + a 1px shackle stroke, no new asset), a `CIPHERTEXT` caption over the tlock ciphertext middle-truncated on the ink evidence surface, a `DRAND ROUND` row (mono 22) + `TIME TO BEACON` countdown (mono 18, `umbra-pulse` on the pending status square), and the guarantee caption "Undecryptable by anyone — including the venue — until the drand beacon publishes at close." On `mode:'offline'` (or a SolverError), render the T3 variant: DASHED ink border + red `OFFLINE FALLBACK · WEAKER THAN DRAND` tag + the `LOCAL KEY RELEASED AT CLOSE — NO THRESHOLD GUARANTEE` sub-line + the verbatim offline prose. At window close show the `REVEAL ORDER →` ink-fill CTA + a reveal-deadline countdown; on reveal exercise `OrderCommitment.RevealOrder` (desk plane) → on success `umbra-wipe` lifts the `bg-redact` stripe exposing the side-tinted order values + an INK 6px square + `COMMITMENT VERIFIED ON-LEDGER` + `BOND RETURNED · {n} USDCx`; on a mismatch (adversarial/demo) a RED 6px square + `COMMITMENT MISMATCH — REVEAL REJECTED` + the verbatim ledger rejection on the ink evidence surface (never summarized). Add the FORFEITED loss state (missed reveal): red square + `BOND FORFEITED — NO REVEAL BY CLOSE` + the struck/red bond amount + `BOND SLASHED TO OPERATOR POT`. All motion honors reduced-motion. The §4 batch proceeds to the shipped `100.00` clear/settle unchanged.
  </action>
  <verify>
    <automated>cd web && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds.
    - The TIMELOCKED card renders T2 (drand, ink tag) OR T3 (offline, DASHED + red tag) with guarantee AND limitation co-present; ciphertext renders literally on the ink surface.
    - REVEAL exercises `OrderCommitment.RevealOrder` on the desk plane; a match shows the ink `COMMITMENT VERIFIED ON-LEDGER` verdict + bond returned; a mismatch shows the red verdict + verbatim rejection.
    - The FORFEITED state uses the red verdict + struck bond.
    - No new token/font/size/palette/spacing/keyframe (grep the diff for any new tailwind literal or keyframe — none).
  </acceptance_criteria>
  <done>The full commit-reveal-timelock lifecycle renders with honest T1/T2/T3 labeling, ink pass / red loss verdicts, and the §4 reveal→clear unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desk browser → ledger (commit/reveal) | Desk's own token only; operator token never in the browser |
| desk browser → solver (:4100) | tlock ciphertext fetch; keys stay server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-23 | Elevation of Privilege | operator token leaking into the desk plane | mitigate | Commit/reveal exercise on the desk's own `ctx.useLedger()` (grep-clean of operator token / operator context) |
| T-10-24 | Repudiation | off-ledger/weaker mistaken for on-ledger truth | mitigate | Provenance grammar: T2 solid+ink DRAND tag vs T3 dashed+red OFFLINE-FALLBACK tag; guarantee AND limitation co-appear |
| T-10-25 | Tampering | reveal-different-order | mitigate | RevealOrder's on-ledger sha256 assert rejects a mismatch; the UI renders the verbatim ledger rejection (never a fabricated pass) |
</threat_model>

<verification>
- `cd web && npm run build` succeeds; web `tsc --noEmit` clean.
- Grep-clean of operator token / @daml/react operator context in OrderTicket.
- Live commit→timelock→reveal on a running open round (T2 real drand + the mismatch/forfeit demo) = end-of-phase human-verify.
</verification>

<success_criteria>
- The desk lifecycle commits, timelocks, reveals, and forfeits with honest T1/T2/T3 labeling on the desk's own plane.
- The §4 batch still reveals + clears $100.00; no new design token introduced.
</success_criteria>

<output>
Create `.planning/phases/10-cryptographic-privacy/10-08-SUMMARY.md` when done.
</output>
