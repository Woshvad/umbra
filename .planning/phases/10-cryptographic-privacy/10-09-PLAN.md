---
phase: 10-cryptographic-privacy
plan: 09
type: execute
wave: 5
depends_on: [10-07]
files_modified:
  - web/src/components/ProofOfClearingPanel.tsx
  - web/src/views/SettlementView.tsx
  - web/src/lib/truncate.test.ts
autonomous: true
requirements: [CRYP-03]
must_haves:
  truths:
    - "After clear, GENERATE PROOF → produces a real Groth16 proof; the public-inputs pane shows p*=100.00 + matched=10 + commitments and NO losing order (NO LOSING ORDER IN THE WITNESS)"
    - "The off-ledger VERIFY verdict sits on a DASHED T3 surface (OFF-LEDGER VERIFY · POC); the on-ledger HASH anchor sits on a SOLID T1 surface (ANCHORED ON-LEDGER) — the split IS the honest statement"
    - "A persistent POC · CRYPTOGRAPHER REVIEW PENDING tag is visible; RUN TAMPERED CLEARING shows the verbatim off-ledger rejection (proof is real, not a stub)"
    - "The panel is visually distinct from the SOLID on-ledger settlement record above it and never uses lime; EXPORT PROOF ↓ is ink-ghost"
    - "No new token/font/size/palette/spacing/keyframe — reuses the dashed-vs-solid, ink evidence surface, verdict-square, and .umbra-ink-ghost / .break-ai-force grammar"
  artifacts:
    - path: "web/src/components/ProofOfClearingPanel.tsx"
      provides: "CRYP-03 post-clear proof panel (T2 artifact / T3 off-ledger verify / T1 anchor / tamper-reject / export)"
      contains: "OFF-LEDGER VERIFY"
    - path: "web/src/views/SettlementView.tsx"
      provides: "ProofOfClearingPanel mounted below RoundBrief/ProofPackButton, gated phase cleared|settled"
      contains: "ProofOfClearingPanel"
  key_links:
    - from: "web/src/components/ProofOfClearingPanel.tsx"
      to: "solver.generateProof/verifyProof/anchorProof/tamperProof"
      via: "operator-plane fetch (web/src/solver.ts)"
      pattern: "generateProof|verifyProof|anchorProof"
---

<objective>
Build the CRYP-03 Proof-of-Correct-Clearing panel on the Settlement view — a real Groth16 proof generated + verified OFF-ledger with the on-ledger hash anchor shown on a distinct SOLID surface, honestly labeled PoC / cryptographer-review-gated, with a tamper-rejection demo.

Purpose: This is the "the AI's clearing is verifiable, not just trusted" surface — and the phase's most overclaim-prone one. The dashed T3 verify pane vs the solid T1 anchor line IS the honest "off-ledger verify + on-ledger hash anchor (Canton has no zk precompile)" statement, made visual.

Output: `web/src/components/ProofOfClearingPanel.tsx` (new) mounted in `SettlementView.tsx` below the shipped DvP legs / atomic stamp / RoundBrief / ProofPackButton, gated to `phase === 'cleared' | 'settled'`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/10-cryptographic-privacy/10-UI-SPEC.md
@.planning/phases/10-cryptographic-privacy/10-PATTERNS.md
@web/src/views/SettlementView.tsx
@web/src/components/ProofPackButton.tsx
@web/src/components/BreakTheAiPanel.tsx
@web/src/solver.ts
</context>

<artifacts_this_phase_produces>
- **web/src/components/ProofOfClearingPanel.tsx:** block label + 1px ink rule + red `POC · CRYPTOGRAPHER REVIEW PENDING` tag; the reduced-statement prose; `GENERATE PROOF →` (ink-fill, `umbra-pulse` while generating); the T2 proof artifact pane (`ZK PROOF · GROTH16` ink tag — PUBLIC INPUTS commitments + p*=100.00 + matched=10 + `NO LOSING ORDER IN THE WITNESS`; PROOF bytes + `SIZE {n}B · {ms}ms`); the T3 DASHED off-ledger VERIFY pane (`OFF-LEDGER VERIFY · POC` red tag + ink `PROOF VERIFIED OFF-LEDGER` verdict + the "no zk precompile" prose); the T1 SOLID `ANCHORED ON-LEDGER` line (proof/vkey hash + `ON-LEDGER` tag); the `RUN TAMPERED CLEARING` (.break-ai-force) verbatim-rejection demo; `EXPORT PROOF ↓` (.umbra-ink-ghost).
- **web/src/views/SettlementView.tsx:** the panel mounted post-clear.
- **web/src/lib/truncate.test.ts:** pure middle-truncation helper test.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: ProofOfClearingPanel — generate + T2 artifact / T3 off-ledger verify / T1 anchor</name>
  <read_first>
    - web/src/views/SettlementView.tsx (lines 1-60 in context — the `LeakageSimPanel` dashed T3 precedent + the operator-plane no-token discipline; the post-settle block ordering with RoundBrief/ProofPackButton/TcaReceipts imports lines 23-28)
    - web/src/components/ProofPackButton.tsx (fetch→blob→state-machine + `.umbra-ink-ghost` export CTA grammar)
    - web/src/solver.ts (10-07 `generateProof`/`verifyProof`/`anchorProof` + `SolverError`/`OFFLINE_CAPTION`)
    - 10-UI-SPEC.md "CRYP-03 — Proof of Correct Clearing" (lines 268-309) + Provenance Grammar rule 3 (verify/anchor split, lines 79-82) + rules 4-5 (raw crypto literal, ink pass / red verdicts) + Copywriting (block label, PoC tag, statement, PUBLIC INPUTS, PROOF, NO LOSING ORDER…, ZK PROOF · GROTH16, PROOF VERIFIED OFF-LEDGER, OFF-LEDGER VERIFY · POC, off-ledger prose, ANCHORED ON-LEDGER · ON-LEDGER)
  </read_first>
  <action>
    Create web/src/components/ProofOfClearingPanel.tsx (operator plane via web/src/solver.ts; NO operator token / @daml/react context). Header: block label "Proof of Correct Clearing" (Inter 10 uppercase .16em opacity .55) + 1px ink rule + a persistent RED-bordered `POC · CRYPTOGRAPHER REVIEW PENDING` tag; the verbatim reduced-statement prose (Inter 13/1.6 opacity .7). `GENERATE PROOF →` (ink-fill, mono 13/700) → `solver.generateProof(roundId)`; `GENERATING…` with an `umbra-pulse` square. On result render the T2 proof-artifact pane (SOLID 1px ink + ink tag `ZK PROOF · GROTH16`, ink evidence surface `#0A0A0A`/`#F4F1EA`, mono 13 pre-wrap tabular, padding 22px 24px): `PUBLIC INPUTS` → the commitments + `p* = 100.00` + `matched = 10` with an adjacent mono-9 `NO LOSING ORDER IN THE WITNESS` note (the losing orders are ABSENT); `PROOF` → the proof bytes middle-truncated + `SIZE {n}B · {ms}ms`. Render the T3 VERIFY pane on a DASHED 1px ink border + red `OFF-LEDGER VERIFY · POC` tag: `VERIFY PROOF` (ink-ghost) → `solver.verifyProof` → an INK 6px square + `PROOF VERIFIED OFF-LEDGER` + the verbatim "Canton has no zk-verifier precompile…" prose. Render the T1 SOLID `ANCHORED ON-LEDGER` line: `solver.anchorProof` → the proof/vkey hash (mono 13 tabular) + an ink `ON-LEDGER` tag. Middle-truncate every `0x…` with the full value in `title`/`aria-label`. Gate the whole panel to `phase === 'cleared' | 'settled'`; a SolverError → shipped `OFFLINE_CAPTION`. Use ONLY shipped tokens/classes; never lime. Extract the middle-truncation into a pure `web/src/lib/truncate.ts` + `truncate.test.ts`.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/lib/truncate.test.ts 2>&1 | tail -12 && npm run build 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `truncate.test.ts` passes; `npm run build` succeeds.
    - The verify verdict sits on a DASHED T3 surface (`OFF-LEDGER VERIFY · POC`); the anchor line sits on a SOLID T1 surface (`ANCHORED ON-LEDGER`) — distinct tiers.
    - PUBLIC INPUTS show p*=100.00 + matched=10 + commitments only, with the `NO LOSING ORDER IN THE WITNESS` note; no losing order value rendered.
    - The persistent `POC · CRYPTOGRAPHER REVIEW PENDING` tag is present; no lime anywhere in the panel (grep).
    - No operator token / @daml/react context in the component (grep-clean).
  </acceptance_criteria>
  <done>The proof panel generates + off-ledger-verifies a real proof and shows the on-ledger hash anchor on a distinct solid tier, honestly labeled PoC.</done>
</task>

<task type="auto">
  <name>Task 2: Tamper-reject demo + EXPORT PROOF + mount in SettlementView</name>
  <read_first>
    - web/src/components/BreakTheAiPanel.tsx (the `.break-ai-force` red-ghost + verbatim on-ledger rejection rendering — the exact seam to mirror for the tamper-reject)
    - web/src/views/SettlementView.tsx (whole file structure — where RoundBrief/ProofPackButton/TcaReceipts mount; the `phase` gating; the `LeakageSimPanel` insertion point below the receipts)
    - web/src/solver.ts (10-07 `tamperProof`; `proofPackUrl` for EXPORT)
    - 10-UI-SPEC.md tamper-rejection demo + EXPORT (lines 302-309) + Component Inventory "ProofOfClearingPanel … distinct from the SOLID on-ledger settlement record above it" (line 463)
  </read_first>
  <action>
    Add to ProofOfClearingPanel a `RUN TAMPERED CLEARING` control (red 1px `.break-ai-force` ghost, no confirm — it is harmless) → `solver.tamperProof(roundId)` → a RED 6px square + `TAMPERED CLEARING → PROOF REJECTED` + the verbatim verifier failure on the ink evidence surface (never summarized). Add `EXPORT PROOF ↓` (`.umbra-ink-ghost`, mono 13/700 .14em) that bundles the proof + public inputs + anchor into the WOW-05 proof-pack export (reuse the ProofPackButton `proofPackUrl` mechanism); never red/lime. Mount `<ProofOfClearingPanel {...operatorState} />` in SettlementView BELOW the shipped DvP-legs / atomic-stamp / RoundBrief / ProofPackButton / TcaReceipts (and below/adjacent the LeakageSimPanel), hidden until `phase === 'cleared' | 'settled'` — visually distinct (dashed T3 verify pane) from the SOLID on-ledger settlement record above it. Do not disturb the shipped simultaneous-settle beat or the money-shot reveal.
  </action>
  <verify>
    <automated>cd web && npm run build 2>&1 | tail -12 && npx vitest run 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds; full web `npx vitest run` green.
    - `RUN TAMPERED CLEARING` renders the verbatim off-ledger rejection (red verdict), proving the proof is real.
    - `EXPORT PROOF ↓` is ink-ghost and reuses the proof-pack export; never red/lime.
    - The panel is mounted post-clear in SettlementView, distinct from the solid settlement record; the shipped settle animation is unchanged (grep the diff — no change to the rAF settle block).
  </acceptance_criteria>
  <done>The tamper-reject demo + export ship, and the panel is mounted post-clear without disturbing the shipped settlement beats.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web bundle → solver (:4100) | Proof generate/verify/anchor/tamper on the operator plane; no credential in the browser |
| off-ledger verify vs on-ledger anchor | Distinct tiers — the honest gap made visual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-26 | Repudiation | off-ledger verify mistaken for on-ledger verification | mitigate | DASHED T3 `OFF-LEDGER VERIFY · POC` pane vs SOLID T1 `ANCHORED ON-LEDGER` line + the "no zk precompile" prose |
| T-10-27 | Info Disclosure | losing order leaked in the UI | mitigate | PUBLIC INPUTS render only p*/matched/commitments; `NO LOSING ORDER IN THE WITNESS` note; the solver never returns the witness |
| T-10-28 | Tampering | a stubbed/fake proof passed off as real | mitigate | `RUN TAMPERED CLEARING` shows the verbatim off-ledger rejection; PoC tag persistent (cryptographer-review-gated) |
</threat_model>

<verification>
- `npm run build` succeeds; `truncate.test.ts` + full web suite green; web `tsc --noEmit` clean.
- Grep-clean of operator token / lime in the panel; the shipped settle rAF block unchanged.
- Live generate→verify→anchor→tamper against a running settled round = end-of-phase human-verify.
</verification>

<success_criteria>
- A real proof generates + off-ledger-verifies with the on-ledger hash anchor on a distinct solid tier, PoC-labeled, tamper-demonstrable, exportable.
- No losing order leaks; no lime; no new design token.
</success_criteria>

<output>
Create `.planning/phases/10-cryptographic-privacy/10-09-SUMMARY.md` when done.
</output>
