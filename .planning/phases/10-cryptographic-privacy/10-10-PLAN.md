---
phase: 10-cryptographic-privacy
plan: 10
type: execute
wave: 5
depends_on: [10-05, 10-07]
files_modified:
  - web/src/views/TimeMachineView.tsx
  - web/src/components/Nav.tsx
  - web/src/App.tsx
  - web/src/views/TimeMachine.test.tsx
autonomous: true
requirements: [VIZ-02]
must_haves:
  truths:
    - "The Time Machine reconstructs each party's EXACT view at each round stage from AUTHENTIC JSON Ledger API v2 events (per-party ACS-at-offset), never staged"
    - "Each party column mounts its OWN ctx.DamlLedger with that party's token — BankB genuinely sees ∅ of BankA's order (privacy read at the wire, not filtered in render)"
    - "At the COMMITTED/TIMELOCKED stage the OPERATOR column is ALSO redacted (bg-redact) — the visual proof the venue can't see orders early (CRYP-02)"
    - "Visible cells are T1 (LEDGER EVENT @ offset); blinded cells are bg-redact + NOT VISIBLE; any derived stage is DASHED + red RECONSTRUCTED — honest labeling of derived stages"
    - "New view 06 · Time Machine appended to the shipped nav; the shipped 5 views + their nav entries are untouched; no new token/font/size/keyframe"
  artifacts:
    - path: "web/src/views/TimeMachineView.tsx"
      provides: "VIZ-02 timeline scrubber × 4 per-party columns with authentic ACS-at-offset replay"
      contains: "REWIND THE BLINDNESS"
    - path: "web/src/components/Nav.tsx"
      provides: "06 · Time Machine nav tab"
      contains: "TIME MACHINE"
    - path: "web/src/views/TimeMachine.test.tsx"
      provides: "per-party redaction test vs mocked v2 ACS"
      contains: "NOT VISIBLE"
  key_links:
    - from: "web/src/views/TimeMachineView.tsx"
      to: "per-party ctx.DamlLedger + activeAtOffset (peek.ts primitive) keyed by solver.getStageOffsets"
      via: "each party's own token reads its ACS at the stage offset"
      pattern: "activeAtOffset|getStageOffsets"
---

<objective>
Build the VIZ-02 privacy Time Machine — a new view (06) that reconstructs each party's exact view across the round timeline (open → committed/timelocked → sealed/revealed → cleared → settled) from AUTHENTIC per-party ledger events, reusing the shipped per-party plane + redaction motif, with the venue-blind beat at the timelocked stage.

Purpose: Drive home that structural privacy holds OVER TIME — and prove it, not stage it: each column reads its own ACS at each stage's offset with that party's own token, so BankB genuinely sees nothing of BankA, and even the operator is blind at the timelocked stage.

Output: `web/src/views/TimeMachineView.tsx` (new view 06), the `06 · Time Machine` nav tab + App route, and `TimeMachine.test.tsx` (mocked v2 ACS: BankB sees ∅ of BankA; operator redacted at the timelocked stage).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/10-cryptographic-privacy/10-UI-SPEC.md
@.planning/phases/10-cryptographic-privacy/10-PATTERNS.md
@web/src/views/PrivacyView.tsx
@web/src/components/DeskColumn.tsx
@web/src/lib/peek.ts
@web/src/ledger/v2react.tsx
@web/src/components/Nav.tsx
@web/src/App.tsx
@web/src/solver.ts
</context>

<artifacts_this_phase_produces>
- **web/src/views/TimeMachineView.tsx:** standard page frame (section marker 06, 1px ink rule, Space Grotesk 54 `<h1>` `REWIND THE BLINDNESS.`); a keyboard-operable timeline scrubber (5 stage nodes) on a 1px ink rail; a 4-column per-party grid (BANK-A/B/C/OPERATOR) each mounting its OWN `ctx.DamlLedger` reading its ACS at the scrubbed stage offset (from `solver.getStageOffsets`) via the `activeAtOffset` primitive — visible T1 cell / `bg-redact` `NOT VISIBLE` cell / DASHED red `RECONSTRUCTED` cell; the venue-blind operator redaction at the timelocked stage.
- **web/src/components/Nav.tsx:** the `06 · Time Machine` tab (`Screen` union gains `'timemachine'`).
- **web/src/App.tsx:** the `screen === 'timemachine'` route.
- **web/src/views/TimeMachine.test.tsx:** per-party redaction test vs mocked v2 ACS.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: TimeMachineView — scrubber × 4 per-party columns with authentic ACS-at-offset replay</name>
  <read_first>
    - web/src/views/PrivacyView.tsx (the page frame + 3/4-up grid to reuse)
    - web/src/components/DeskColumn.tsx (whole file — the per-party `ctx.DamlLedger` mount with that desk's token lines 217-233, `ActiveBody`/`RedactedBody` grammar, the `bg-redact` blinded state + red 6px verdict lines 162-214)
    - web/src/lib/peek.ts (`buildPeekRequest` with `activeAtOffset` lines 74-90 — the time-machine primitive) + web/src/ledger/v2react.tsx (`fetchAcs` lines 75-85, the per-party ACS-at-offset read)
    - web/src/solver.ts (10-07 `getStageOffsets`)
    - 10-UI-SPEC.md "VIZ-02 — Privacy Time Machine" (lines 311-341) + Provenance Grammar + Copywriting (headline, stage labels OPEN·COMMITTED/TIMELOCKED·SEALED/REVEALED·CLEARED·SETTLED, party headers, LEDGER EVENT @ {offset}, NOT VISIBLE, RECONSTRUCTED, empty-state copy) + Accessibility (keyboard scrubber)
  </read_first>
  <action>
    Create web/src/views/TimeMachineView.tsx. Standard page frame from PrivacyView/DeskView: `<main>` padding, section marker `06` + `Time Machine · Per-Party Replay` label, 1px ink rule, Space Grotesk 54 `<h1>` `REWIND THE BLINDNESS.`. Fetch the stage→offset map via `solver.getStageOffsets(roundId)` (operator plane). Render a horizontal timeline scrubber on a 1px ink rail with FIVE stage nodes (OPEN · COMMITTED/TIMELOCKED · SEALED/REVEALED · CLEARED · SETTLED, mono 9 .16em) — active = ink-filled 8px square, inactive = ink-outline; keyboard-operable (←/→ move stages, Home/End to ends, ink focus outline, accessible name `Stage: …`). Below it, a 4-column per-party grid (BANK-A/B/C/OPERATOR, mono 13 .16em headers, internal 1px ink borders, last borderless — copy DeskColumn's border/gap grammar). Each column mounts its OWN `ctx.DamlLedger` with that party's token (per-party plane — the operator token NEVER enters the browser) and reads its ACS at the SELECTED stage's offset via the `activeAtOffset` primitive (reuse the peek.ts/v2react read shape). Cells: VISIBLE (T1) → the actual contract data (mono 13 tabular) + neutral ink caption `LEDGER EVENT @ {offset}`; BLINDED → the `bg-redact` stripe + mono 9 `NOT VISIBLE`; RECONSTRUCTED/derived (T3) → DASHED cell border + red `RECONSTRUCTED` tag (only where a stage is inferred, not read from a captured event). Venue-blind beat: at COMMITTED/TIMELOCKED the OPERATOR column is ALSO `bg-redact` (only commitment + bond visible, contents redacted). Stage transition = `umbra-fade` (reduced-motion → instant). Empty state (no round captured) + `offline` → shipped `OFFLINE_CAPTION`. No new token/font/size/keyframe; the redaction stripe cells are `aria-hidden` with the state conveyed by adjacent text.
  </action>
  <verify>
    <automated>cd web && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds.
    - Each party column mounts its own `ctx.DamlLedger` with that party's token and reads ACS at the stage offset (grep-confirm no operator token in the per-party columns; the stage-offset map is the only operator-plane fetch).
    - Cells render T1 visible / `bg-redact` NOT VISIBLE / DASHED red RECONSTRUCTED per the grammar; the OPERATOR column is redacted at the timelocked stage.
    - The scrubber is keyboard-operable (←/→, Home/End) with ink focus outlines.
    - No new token/font/size/keyframe (grep the diff).
  </acceptance_criteria>
  <done>The Time Machine replays each party's authentic per-stage view with honest redaction and the venue-blind timelocked beat.</done>
</task>

<task type="auto">
  <name>Task 2: Nav 06 tab + App route + TimeMachine.test.tsx per-party redaction test</name>
  <read_first>
    - web/src/components/Nav.tsx (whole file — the `Screen` union line 5, the `TABS` array lines 7-13, the tab rendering grammar; append `06 · TIME MACHINE`)
    - web/src/App.tsx (the `screen` state + the view routing lines 99-108 — add the `timemachine` route; the `operatorState` wiring lines 58-75)
    - 10-VALIDATION.md VIZ-02 row (line 55 — `npx vitest run TimeMachine.test.tsx`, mocked v2 ACS: BankB sees ∅ of BankA; operator redacted at the timelocked stage)
    - web/src/components/DeskColumn.tsx (the redaction assertions to mirror in the test)
  </read_first>
  <action>
    Extend Nav.tsx: add `'timemachine'` to the `Screen` union and a `{ num: '06', label: 'TIME MACHINE', screen: 'timemachine', enabled: true }` tab (shipped nav grammar — mono 11, active = red border-top + ink fg; the shipped 5 tabs untouched). Wire the `screen === 'timemachine'` route in App.tsx to `<TimeMachineView {...operatorState} />`, threading the existing lifted round state. Create web/src/views/TimeMachine.test.tsx that mocks the v2 ACS reads (per-party): assert BankB's column returns EMPTY for BankA's order (renders `NOT VISIBLE` / `bg-redact`), the active/own column renders the real values (`LEDGER EVENT @ {offset}`), and the OPERATOR column is redacted at the COMMITTED/TIMELOCKED stage; assert a derived stage renders the `RECONSTRUCTED` tag. Keep the shipped 5 views/nav byte-unchanged.
  </action>
  <verify>
    <automated>cd web && npx vitest run src/views/TimeMachine.test.tsx 2>&1 | tail -15 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/views/TimeMachine.test.tsx` passes: BankB sees ∅ of BankA (NOT VISIBLE), the own column shows real data, the operator is redacted at the timelocked stage, a derived stage shows RECONSTRUCTED.
    - `06 · Time Machine` is navigable; the shipped 5 nav tabs + views are byte-unchanged.
    - Full web `npx vitest run` green; `npm run build` succeeds.
  </acceptance_criteria>
  <done>The new view is navigable and its per-party redaction (incl. venue-blind timelocked stage) is proven by an automated test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-party column → its own participant | Each column reads ACS with that party's own token — Canton disclosure is the access control |
| solver (:4100) → browser | Only the stage→offset map (public offsets), no operator token in the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-29 | Info Disclosure | render-time filtering faked as privacy | mitigate | Each column mounts its OWN ctx.DamlLedger + reads ACS-at-offset with that party's token; a rival column genuinely returns ∅ (V4 access control at the wire) |
| T-10-30 | Elevation of Privilege | operator token in the browser | mitigate | Per-party reads use each party's own token; the solver provides only the public stage→offset map (operator-token-only rule intact) |
| T-10-31 | Repudiation | derived stage passed off as authentic | mitigate | Only event-backed cells are T1 `LEDGER EVENT @ offset`; any inferred stage is DASHED + red `RECONSTRUCTED` |
</threat_model>

<verification>
- `npx vitest run src/views/TimeMachine.test.tsx` green; full web suite green; `npm run build` succeeds.
- Grep-clean of operator token in the per-party columns; shipped 5 views/nav byte-unchanged.
- Live scrub across a real run (venue-blind at timelocked) = end-of-phase human-verify.
</verification>

<success_criteria>
- The Time Machine reconstructs authentic per-party views across all five stages with honest redaction and the venue-blind timelocked beat.
- View 06 is navigable; the shipped views/nav are untouched; no new design token.
</success_criteria>

<output>
Create `.planning/phases/10-cryptographic-privacy/10-10-SUMMARY.md` when done.
</output>
