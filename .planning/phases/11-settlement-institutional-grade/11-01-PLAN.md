---
phase: 11-settlement-institutional-grade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - daml/Umbra/Instrument.daml
  - daml/Umbra/Holding.daml
  - daml/daml.yaml
  - DECISIONS.md
autonomous: true
requirements: [DFIN-01, DFIN-03]
must_haves:
  truths:
    - "A desk can hold a token-agnostic Holding of a named Instrument (BONDX / USDCx)"
    - "The operator (custodian) can Split/Reassign/Merge a Holding, conserving total amount"
    - "A Holding can carry an operator-custody lock (the Phase-10 bond lock migrates here)"
    - "daml build stays green and every existing daml test still passes (Asset path untouched)"
  artifacts:
    - path: "daml/Umbra/Instrument.daml"
      provides: "InstrumentId key type + Instrument template (token-agnostic cash/bond)"
      contains: "data InstrumentId"
    - path: "daml/Umbra/Holding.daml"
      provides: "per-desk Holding (instrument, amount, lock) + Split/Reassign/Merge + moveExactHolding"
      contains: "template Holding"
    - path: "DECISIONS.md"
      provides: "D13 — settlement-standard provenance decision (CN Token Standard vs in-repo faithful layer)"
      contains: "D13"
  key_links:
    - from: "daml/Umbra/Holding.daml"
      to: "daml/Umbra/Instrument.daml"
      via: "instrument : InstrumentId field"
      pattern: "instrument\\s*:\\s*InstrumentId"
---

<objective>
Introduce the production-grade custody primitives that retire operator-custody `Asset` from the
settlement path: a token-agnostic `Instrument` (with an `InstrumentId` key type) and a per-desk
`Holding` that carries an operator-custody `lock` (the Phase-10 bond lock migrates onto it in
11-05). Attempt real CN Token Standard (CIP-0056) interface conformance on the in-repo templates;
if that spike exceeds budget, ship the plain in-repo faithful layer — either way honestly labeled,
NEVER "Daml Finance the library" (11-RESEARCH verdict: Daml Finance is LF-1.17 and cannot vet on
this LF-2.1 / SDK-3.4.11 line).

Purpose: DFIN-01 (retire `Asset`, custody via Holding/Instrument) + DFIN-03 (token-agnostic cash
leg via a pluggable `InstrumentId`, no hardcoded "USDCx").
Output: `Instrument.daml`, `Holding.daml`, a DECISIONS.md D13 provenance record. `Asset.daml`
stays byte-untouched and the whole project still compiles + all existing `daml test` pass — this
plan only ADDS the new (as-yet-unwired) custody modules.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-settlement-institutional-grade/11-RESEARCH.md
@.planning/phases/11-settlement-institutional-grade/11-PATTERNS.md
@daml/Umbra/Asset.daml
@daml/daml.yaml
@DECISIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument.daml + Holding.daml (token-agnostic custody + lock + conserving choices)</name>
  <files>daml/Umbra/Instrument.daml, daml/Umbra/Holding.daml</files>
  <read_first>
    - daml/Umbra/Asset.daml (whole file — the custody template + Split/Merge/Reassign shape to mirror; 11-PATTERNS "Holding.daml" section)
    - daml/Umbra/Auction.daml lines 42-49 (moveExact) and 181-224 (OrderCommitment bond-LOCK model to preserve)
    - 11-RESEARCH Pattern 1 (CN interface conformance) + Anti-Pattern "A contract key on Holding" (D7/DuplicateKey — do NOT key Holding)
  </read_first>
  <action>
    Create `module Umbra.Instrument`: define `data InstrumentId = InstrumentId with issuer : Party; id : Text deriving (Eq, Show, Ord)` — the token-agnostic instrument reference (DFIN-03; replaces the hardcoded "USDCx"/"BONDX" strings). Define an `Instrument` template (signatory issuer/operator; fields `issuer : Party`, `id : Text`, `description : Text`) representing a named tokenized instrument; §4 uses `InstrumentId operator "BONDX"` (bond) and `InstrumentId operator "USDCx"` (a USDCx-labeled tokenized-deposit cash instrument). Add a helper `instrumentIdOf : Instrument -> InstrumentId`.

    Create `module Umbra.Holding` mirroring `Asset.daml`: template `Holding` with `operator : Party` (custodian, sole signatory), `owner : Party` (observer), `instrument : InstrumentId` (REPLACES `symbol : Text` — token-agnostic per DFIN-03), `amount : Decimal`, and `lock : Optional Text` (the operator-custody lock marker — the Phase-10 bond lock migrates here in 11-05; `Some reason` = locked, `None` = free). `ensure amount >= 0.0`. Provide operator-authority-alone conserving choices mirroring Asset: `Split : (ContractId Holding, ContractId Holding)` (strict `0 < splitQty < amount`), `Merge` (same instrument + owner), `Reassign : ContractId Holding` (`with newOwner`), and a top-level `moveExactHolding : ContractId Holding -> Decimal -> Party -> Update (ContractId Holding)` mirroring `Auction.moveExact` (full-move → Reassign; partial → Split then Reassign the slice). Guard every mutating choice so a LOCKED holding (`lock == Some _`) cannot be moved/split (assertMsg "holding is locked"), preserving the bond-lock guarantee. Do NOT add a contract key (D7 anti-pattern — splits create duplicate same-key holdings).

    Keep `Asset.daml` byte-untouched — these are additive, not-yet-wired modules. Directive prose only; no fenced code in this action.
  </action>
  <acceptance_criteria>
    - `daml/Umbra/Instrument.daml` exports `InstrumentId` (with `issuer`, `id`) and an `Instrument` template.
    - `daml/Umbra/Holding.daml` exports a `Holding` template with `instrument : InstrumentId`, `amount : Decimal`, `lock : Optional Text`, plus Split/Merge/Reassign (controller operator) and top-level `moveExactHolding`.
    - `Holding` has NO contract key; a locked Holding rejects Split/Reassign/Merge on-ledger.
    - `cd daml && daml build` succeeds; `Asset.daml` is unchanged (`git diff --stat daml/Umbra/Asset.daml` empty).
  </acceptance_criteria>
  <verify>
    <automated>cd daml && daml build 2>&1 | tail -3 && git diff --stat daml/Umbra/Asset.daml | grep -c Asset.daml</automated>
  </verify>
  <done>Instrument + Holding compile as new custody primitives; Asset untouched; project builds green.</done>
</task>

<task type="auto">
  <name>Task 2: CN Token Standard interface-conformance spike + DECISIONS.md D13 provenance record</name>
  <files>daml/daml.yaml, daml/Umbra/Holding.daml, daml/Umbra/Instrument.daml, DECISIONS.md</files>
  <read_first>
    - 11-RESEARCH "Standard Stack" (data-dependencies wiring block) + Assumptions A1/A2 + Open Question 1
    - daml/daml.yaml (current dependencies/build-options — the data-dependencies target)
    - DECISIONS.md D8 (LF-2.1 line) + D12 (Phase-10 honesty-label precedent)
  </read_first>
  <action>
    Best-effort attempt CN Token Standard (CIP-0056) interface conformance so the settlement
    provenance can honestly read `CN TOKEN STANDARD (CIP-0056)`: if the six `splice-api-token-*-v1-1.0.0.dar`
    artifacts are locatable on this box (cn-quickstart/quickstart/daml/dars/), copy the
    `metadata-v1` and `holding-v1` DARs into `daml/vendor/`, add them as `data-dependencies` in
    `daml.yaml`, and add `interface instance HoldingV1.Holding for Holding` (+ `MetadataV1`) to
    the `Holding` template with the appropriate `view`. Run `daml build`. IF the interface
    signatures require a registry-only context or the build fails (A1/A2), REVERT the
    data-dependencies + interface-instance change and keep the plain in-repo layer — do NOT burn
    budget forcing it, and NEVER add any LF-1.17 `daml-finance-*.dar` (Pitfall 1).

    Record the outcome in DECISIONS.md as **D13**: state which path shipped, the resulting
    on-screen/on-ledger provenance tag it authorizes — `CN TOKEN STANDARD (CIP-0056)` (interfaces
    implemented) OR `DAML-FINANCE-PATTERN (IN-REPO)` (plain in-repo layer) — and the HARD rule
    that nothing may ever be labeled "Daml Finance the library" (unbuildable on LF 2.1). Note the
    wallet-interoperability boundary (Open Q1: interfaces implemented ≠ registry-recognized;
    that is Track B / Phase 12) and that full AllocationV1 DvP conformance is out of budget (A2).
  </action>
  <acceptance_criteria>
    - `cd daml && daml build` succeeds regardless of which path shipped.
    - DECISIONS.md contains a `## D13` section naming the shipped provenance tag and the "never claim Daml Finance the library" rule.
    - No `daml-finance-*.dar` appears anywhere in `daml.yaml`.
  </acceptance_criteria>
  <verify>
    <automated>cd daml && daml build 2>&1 | tail -2 && grep -c "D13" ../DECISIONS.md && (grep -c "daml-finance" daml.yaml || echo 0)</automated>
  </verify>
  <done>Settlement provenance is decided and honestly recorded (D13); build green; no forbidden Daml-Finance DAR wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desk ↔ operator custody | the operator is Holding's sole signatory/custodian; a desk's holding is moved only by operator authority |
| repo ↔ external DAR | CN Token Standard DARs vendored as data-dependencies (supply-chain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-LOCK | Tampering | Holding move while locked | mitigate | Split/Reassign/Merge assertMsg "holding is locked" when `lock == Some _` — the bond-lock guarantee survives the Asset→Holding migration (Pitfall 2) |
| T-11-01-KEY | Denial of Service | duplicate-key holdings | mitigate | Holding carries NO contract key (D7 anti-pattern) — splits cannot DuplicateKey |
| T-11-01-PROV | Repudiation (trust) | mislabeled settlement provenance | mitigate | D13 records the honest tag; "Daml Finance the library" is forbidden; only CN-STANDARD or IN-REPO-PATTERN |
| T-11-01-SC | Tampering | vendored CN DARs | accept | official Splice/Canton Foundation artifacts already vetted on all three LocalNet participants (11-RESEARCH audit); no npm/PyPI install |
</threat_model>

<verification>
- `cd daml && daml build` green.
- `cd daml && daml test` green (no existing test changed — Asset path intact; §4 canary `test_clears_at_100` / `test_commit_reveal_clears_at_100` still pass).
- `git diff --stat daml/Umbra/Asset.daml` empty (Asset byte-untouched).
</verification>

<success_criteria>
Token-agnostic `Instrument`/`InstrumentId` + a locked-capable `Holding` with conserving
operator-authority choices exist and compile; the settlement-standard provenance is decided and
recorded (D13); every existing `daml test` still passes; `Asset.daml` unchanged.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `daml/Umbra/Instrument.daml` — `InstrumentId` (issuer, id), `Instrument` template, `instrumentIdOf`.
- `daml/Umbra/Holding.daml` — `Holding` template (operator, owner, instrument, amount, lock), choices `Split` / `Merge` / `Reassign`, top-level `moveExactHolding`.
- `DECISIONS.md` — `D13` settlement-standard provenance record + honesty rule.
- (conditional) `daml/vendor/splice-api-token-{metadata,holding}-v1-1.0.0.dar` + `data-dependencies` in `daml/daml.yaml`, if CN interface conformance built.
</artifacts>

<output>
Create `.planning/phases/11-settlement-institutional-grade/11-01-SUMMARY.md` when done.
</output>
