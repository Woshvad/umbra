---
phase: 11-settlement-institutional-grade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - daml/Umbra/Compliance.daml
  - daml/Umbra/Tests.daml
autonomous: true
requirements: [COMP-01]
must_haves:
  truths:
    - "An operator/Compliance-signed DeskEligibility credential exists per desk, keyed (operator, desk)"
    - "The credential carries accredited / jurisdiction / sanctionsClear flags with an isEligible predicate"
    - "A credential can be fetched by key and its eligibility asserted (the gate primitive for 11-05)"
    - "daml build + daml test stay green"
  artifacts:
    - path: "daml/Umbra/Compliance.daml"
      provides: "DeskEligibility keyed template + issuance choice + isEligible predicate"
      contains: "template DeskEligibility"
    - path: "daml/Umbra/Tests.daml"
      provides: "test_eligibility_issue (positive: issue + fetchByKey + isEligible)"
      contains: "test_eligibility_issue"
  key_links:
    - from: "daml/Umbra/Compliance.daml"
      to: "DeskEligibility key (operator, desk)"
      via: "key (operator, desk) : (Party, Party); maintainer key._1"
      pattern: "maintainer key\\._1"
---

<objective>
Create the on-ledger eligibility credential for COMP-01 (ERC-3643 analog): a `DeskEligibility`
template keyed `(operator, desk)`, maintainer operator, carrying `accredited` / `jurisdiction` /
`sanctionsClear` flags, issued by the operator/Compliance role (stub attestation — a real KYC
vendor is Track B, honestly labeled). This plan ships the credential + its `isEligible` predicate
+ a positive Daml test; the ACTUAL enforcement gate (fetchByKey + assertMsg inside SubmitOrder /
CommitOrder / Holding issuance) and the negative tests land in 11-05 / 11-06.

Purpose: COMP-01 — the eligibility credential primitive that gates participation on-ledger.
Output: `Compliance.daml` (new standalone module, compiles independently of Holding) + a positive
eligibility test. Parallel-safe with 11-01 (distinct files).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-settlement-institutional-grade/11-RESEARCH.md
@.planning/phases/11-settlement-institutional-grade/11-PATTERNS.md
@daml/Umbra/Auction.daml
@daml/Umbra/Tests.daml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compliance.daml — DeskEligibility keyed credential + issuance + isEligible predicate</name>
  <files>daml/Umbra/Compliance.daml</files>
  <read_first>
    - 11-RESEARCH Pattern 3 (DeskEligibility template + fetchByKey gate) + Pitfall 3 (fetchByKey authorization)
    - 11-PATTERNS "Compliance.daml" section (RoundStats operator-signed/observer-desk shape analog)
    - daml/Umbra/Auction.daml lines 270-278 (RoundStats — the operator-signatory/observer-desk keyed shape to mirror)
  </read_first>
  <action>
    Create `module Umbra.Compliance`. Define template `DeskEligibility` with fields
    `operator : Party`, `compliance : Party` (may == operator for the MVP stub),
    `desk : Party`, `accredited : Bool`, `jurisdiction : Text`, `sanctionsClear : Bool`;
    `signatory operator, compliance`; `observer desk`; `key (operator, desk) : (Party, Party)`;
    `maintainer key._1`. Add a top-level pure predicate `isEligible : DeskEligibility -> Bool`
    = `accredited && sanctionsClear` (jurisdiction is recorded/observable but not a hard gate for
    the MVP — document that inline as the honest stub boundary). Add an operator/Compliance-authority
    `Revoke` (consuming) choice so a credential can be withdrawn. Keep the module free of any
    `Holding`/`Auction` import so it compiles independently (Wave-1 parallel with 11-01).

    Provenance/honesty note in a module comment: this is a STUB attestation issued by
    operator/Compliance — the on-screen label is `STUB ATTESTATION — REAL KYC VENDOR IS TRACK B`
    (11-UI-SPEC honesty contract). Directive prose only; no fenced code in this action.
  </action>
  <acceptance_criteria>
    - `DeskEligibility` is keyed `(operator, desk)` with `maintainer key._1` and signatory `operator, compliance`, observer `desk`.
    - `isEligible` returns `accredited && sanctionsClear`.
    - `cd daml && daml build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd daml && daml build 2>&1 | tail -2 && grep -c "maintainer key._1" Umbra/Compliance.daml</automated>
  </verify>
  <done>DeskEligibility credential + isEligible predicate compile as a standalone module.</done>
</task>

<task type="auto">
  <name>Task 2: test_eligibility_issue — positive credential lifecycle test</name>
  <files>daml/Umbra/Tests.daml</files>
  <read_first>
    - daml/Umbra/Tests.daml lines 1-90 (imports + test_setup_seeds shape — the Script test idiom, allocateAll, query patterns)
    - daml/Umbra/Compliance.daml (the new template from Task 1)
  </read_first>
  <action>
    Add `import Umbra.Compliance` to Tests.daml and a `test_eligibility_issue : Script ()` that:
    allocates parties (reuse `allocateAll`), the operator (as its own Compliance) creates a
    `DeskEligibility` for bankA with `accredited = True`, `sanctionsClear = True`,
    `jurisdiction = "US"`; then `queryContractId`/`fetchByKey`-style asserts the credential is
    retrievable by key `(operator, bankA)` and that `isEligible` is `True`; also assert an
    ineligible credential (`accredited = False`) yields `isEligible == False`. This is the POSITIVE
    lifecycle proof; the on-ledger REJECTION negative tests (submit/holding) are 11-06 (they need
    the gate wired in 11-05). Do NOT change any existing test.
  </action>
  <acceptance_criteria>
    - `test_eligibility_issue` passes under `daml test`.
    - Every pre-existing test still passes (no existing test edited).
  </acceptance_criteria>
  <verify>
    <automated>cd daml && daml test 2>&1 | tail -5</automated>
  </verify>
  <done>The eligibility credential is provably issuable, key-retrievable, and its predicate is correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desk ↔ operator/Compliance | only operator+compliance can sign/issue/revoke a desk's eligibility credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-FORGE | Spoofing | forged eligibility credential | mitigate | DeskEligibility `signatory operator, compliance` — a desk cannot self-issue; only operator/Compliance authority creates it |
| T-11-02-STUB | Repudiation (trust) | over-claiming real KYC | mitigate | honest `STUB ATTESTATION — REAL KYC VENDOR IS TRACK B` label (module comment + UI in 11-10); no claim of real accreditation checks |
| T-11-02-KEY | Tampering | duplicate/ambiguous credential | mitigate | one stable keyed credential per `(operator, desk)`; keys are safe here (unlike Holding) |
</threat_model>

<verification>
- `cd daml && daml build` green.
- `cd daml && daml test` green including `test_eligibility_issue` and the untouched §4 canary.
</verification>

<success_criteria>
A keyed, operator/Compliance-signed `DeskEligibility` credential with an `isEligible` predicate
exists, compiles independently, and is proven issuable + key-retrievable by a positive Daml test;
no existing test regresses.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `daml/Umbra/Compliance.daml` — `DeskEligibility` template (keyed `(operator, desk)`), `Revoke` choice, `isEligible : DeskEligibility -> Bool`.
- `daml/Umbra/Tests.daml` — `test_eligibility_issue` (positive credential lifecycle).
</artifacts>

<output>
Create `.planning/phases/11-settlement-institutional-grade/11-02-SUMMARY.md` when done.
</output>
