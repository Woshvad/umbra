---
phase: 11-settlement-institutional-grade
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - solver/src/topology.ts
  - solver/src/topology.test.ts
  - solver/src/api.ts
  - solver/src/index.ts
  - solver/src/api.test.ts
  - scripts/localnet/guest-onboard.mjs
autonomous: true
requirements: [WOW-07, VIZ-03]
must_haves:
  truths:
    - "A credential-free solver endpoint returns the party→participant hosting map for a round"
    - "When all desks resolve to one participant the map is labeled demo-real (SAME PARTICIPANT / single-operator LocalNet)"
    - "A guest-onboarding script allocates a 4th desk party (bankD) + scoped HS256 token, never leaking the token in a QR/GET"
    - "No operator/Anthropic credential crosses the topology or guest endpoints; solver vitest stays green"
  artifacts:
    - path: "solver/src/topology.ts"
      provides: "hostingMap() — per-participant /v2/parties isLocal probe → party→participant map + demo-real flag"
      contains: "hostingMap"
    - path: "solver/src/api.ts"
      provides: "GET /round/:id/topology (credential-free) + guest-onboarding bootstrap endpoint"
      contains: "/topology"
    - path: "scripts/localnet/guest-onboard.mjs"
      provides: "allocate bankD + user + rights + scoped token → tokens.json (co-hosted, honestly labeled)"
      contains: "guest"
  key_links:
    - from: "solver/src/api.ts"
      to: "solver/src/topology.ts"
      via: "deps.hostingMap()"
      pattern: "hostingMap"
    - from: "scripts/localnet/guest-onboard.mjs"
      to: "scripts/localnet/mint-jwt.mjs"
      via: "mintJwt(guestUserId)"
      pattern: "mintJwt"
---

<objective>
Stand up the off-ledger machinery for the 4th-desk guest join (WOW-07) and the three-node
topology view (VIZ-03), reusing proven infra: the `xnode-up.mjs` party-allocation + `mint-jwt.mjs`
scoped-token pattern, and the `GET /v2/parties` `isLocal` party→participant probe. This is
independent of the settlement migration (no new Daml shape needed) so it runs in Wave 1.

Purpose: WOW-07 (guest party + scoped token, token never in the QR) + VIZ-03 (party→participant
hosting metadata for the topology view). Honest boundary: LocalNet co-hosting is demo-real; a
genuine 4th validator is the recorded limitation (live UAT).
Output: `solver/src/topology.ts`, `GET /round/:id/topology` + a guest-onboarding endpoint on
`api.ts` (credential-free), `scripts/localnet/guest-onboard.mjs`, and a stubbed-fetch
`topology.test.ts` + api secret sweep.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-settlement-institutional-grade/11-RESEARCH.md
@.planning/phases/11-settlement-institutional-grade/11-PATTERNS.md
@scripts/localnet/xnode-up.mjs
@scripts/localnet/mint-jwt.mjs
@solver/src/api.ts
@solver/src/ledger.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: topology.ts — party→participant hosting probe (isLocal) + demo-real labeling</name>
  <files>solver/src/topology.ts, solver/src/topology.test.ts</files>
  <read_first>
    - 11-RESEARCH "Party→participant hosting probe" code example + Pitfall 7 (demo-real single-operator honesty) + Assumption A4
    - scripts/localnet/xnode-up.mjs lines 23-32 (the `api(base, token, method, path)` fetch helper), 67-71 (`isLocal` probe)
    - solver/src/ledger.ts lines 8-22 (operator-token module-privacy convention — SOLV-04; topology must be credential-free)
  </read_first>
  <action>
    Create `solver/src/topology.ts` exporting a pure-ish `hostingMap` that, given an admin fetch
    function + the three participant bases (`app-user` :2975, `app-provider` :3975, `sv` :4975),
    probes each participant's `GET /v2/parties` and records, per party, the participant(s) where
    `isLocal:true`. Return a structured result: `{ nodes: [{ participant, parties }], perParty:
    Record<party, participant[]>, demoReal: boolean }` where `demoReal` is `true` when every
    desk party resolves to a single participant (emit the `SAME PARTICIPANT (LOCALNET)` /
    single-operator caption signal) and `false` when genuinely distributed (xnode). Keep the fetch
    function INJECTED (DI) so the unit test can stub `/v2/parties` responses — no live network in
    the test. The endpoint carries NO operator/Anthropic token out (credential-free per SOLV-04);
    only party ids + participant ids cross the boundary.

    Write `solver/src/topology.test.ts` (vitest): (a) all-desks-on-one-participant stub → `demoReal:true`
    + a single node with all parties; (b) distributed stub (A/B/C on three participants) →
    `demoReal:false` + three nodes; (c) a secret-sweep assert that no bearer/token string appears
    in the returned structure. Directive prose only; no fenced code in this action.
  </action>
  <acceptance_criteria>
    - `hostingMap` returns `demoReal:true` for a single-participant stub and `false` for a distributed stub.
    - No token/bearer string is present in any returned value (secret-sweep assert passes).
    - `cd solver && npx vitest run topology` is green.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run topology 2>&1 | tail -6</automated>
  </verify>
  <done>The party→participant hosting map + honest demo-real flag is derived and unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: api.ts GET /round/:id/topology + guest-onboarding endpoint + index wiring + secret sweep</name>
  <files>solver/src/api.ts, solver/src/index.ts, solver/src/api.test.ts</files>
  <read_first>
    - solver/src/api.ts lines 640-695 (the `wrap` + zod `safeParse` + `ApiError` route idiom; the read-only 404 passthrough at /proof; the cors(:5173) + secret-safe envelope)
    - solver/src/index.ts (buildDeps DI wiring — how ledger/agent deps are constructed + threaded)
    - 11-PATTERNS "api.ts" section (route patterns) + "Operator-token secrecy (SOLV-04)"
  </read_first>
  <action>
    Add to `api.ts` (AppDeps): `hostingMap: () => Promise<TopologyResult>` and
    `onboardGuest: () => Promise<GuestBootstrap>`. Wire `GET /round/:id/topology` as a
    credential-free read-only passthrough returning `deps.hostingMap()` (mirror the /proof route
    idiom; SolverError paths stay in the existing secret-safe `{ error: { code, message } }`
    envelope, never echoing token/key). Wire a guest-onboarding endpoint (`POST /guest/onboard` or
    `GET /guest/bootstrap`) returning the guest `/join` bootstrap — party id + the `/join` URL +
    roundId, but NEVER the scoped token in the response body (the token is delivered server-side to
    the `/join` page and is minted by the guest-onboard script into tokens.json; it must NOT appear
    in any GET/QR — V2/V4 security). In `index.ts` construct the real deps: `hostingMap` from
    `topology.ts` (injecting the admin fetch), `onboardGuest` returning the bootstrap; thread them
    through `buildDeps`. Extend `api.test.ts` with a secret sweep asserting neither the topology
    nor guest response ever contains a JWT/`ANTHROPIC_API_KEY`/operator-token sentinel. Keep the
    five §11 endpoints + /settle byte-unchanged.
  </action>
  <acceptance_criteria>
    - `GET /round/:id/topology` returns the hosting map; `GET/POST` guest bootstrap returns party + URL + roundId and NO token.
    - api secret-sweep test asserts no token/key sentinel in either new response body.
    - The five §11 endpoints + /settle are unchanged (`git diff` shows no deletions in their handlers).
    - `cd solver && npx vitest run` is fully green.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run 2>&1 | tail -6</automated>
  </verify>
  <done>Topology + guest-bootstrap endpoints are live, credential-free, secret-swept, and non-regressive.</done>
</task>

<task type="auto">
  <name>Task 3: guest-onboard.mjs — allocate bankD + user + rights + scoped token → tokens.json</name>
  <files>scripts/localnet/guest-onboard.mjs</files>
  <read_first>
    - scripts/localnet/xnode-up.mjs lines 58-90 (grant/provisionDesk: allocate party → user → rights → mintJwt; how tokens.json entries are shaped)
    - scripts/localnet/mint-jwt.mjs (mintJwt HS256 `{sub,aud}`, secret `unsafe`)
    - web/src/tokens.json (the `{ party, token, base }` per-desk shape the guest entry must match)
    - 11-RESEARCH Open Question 2 (guest co-hosting on an existing participant — no genuine 4th validator)
  </read_first>
  <action>
    Create `scripts/localnet/guest-onboard.mjs` mirroring `xnode-up.mjs`'s `provisionDesk`:
    allocate a `bankD` (Guest) party via `POST /v2/parties`, create user `umbra-bankD`, grant
    `CanActAs`/`CanReadAs` rights, mint its scoped HS256 token via `mintJwt('umbra-bankD')`, and
    write/merge a `bankD` entry into `web/src/tokens.json` in the SAME `{ party, token, base }`
    shape as bankA/B/C. Co-host the guest on an EXISTING participant (e.g. app-user, `base`
    matching a real desk) — LocalNet has only three participants, so a genuine 4th node is the
    recorded honest limitation, printed by the script. Also grant the guest `DeskEligibility`
    creation is NOT this script's job (that is the Setup/Compliance seed in 11-05/11-06) — this
    script only provisions the party + token. The token is written ONLY into tokens.json (the D6
    boundary); it is NEVER printed to stdout in a way that could land in a QR, and NEVER embedded
    in any URL. Guard the script to be idempotent (skip if bankD already provisioned). Real live
    run against a booted LocalNet is a live-UAT item.
  </action>
  <acceptance_criteria>
    - Running the script (node syntax-check) parses cleanly; it writes a `bankD` `{ party, token, base }` entry to tokens.json shaped identically to the existing desks.
    - The token appears only in tokens.json, never in a printed URL/QR payload.
    - `node --check scripts/localnet/guest-onboard.mjs` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/localnet/guest-onboard.mjs && echo GUEST_ONBOARD_OK</automated>
  </verify>
  <done>A reusable guest-onboarding script provisions bankD + a scoped token into tokens.json, token never in a QR/URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ solver | topology/guest endpoints are credential-free; no operator/Anthropic token crosses to the browser |
| QR payload ↔ guest token | the QR/URL must never carry the scoped guest token (delivered server-side to /join) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-LEAK | Information Disclosure | operator token via topology/guest endpoint | mitigate | credential-free endpoints; secret-sweep test asserts no token/key in responses (SOLV-04) |
| T-11-03-QR | Information Disclosure | guest token in QR/URL | mitigate | token written only to tokens.json; guest endpoint/QR carry party+URL+roundId only, never the token |
| T-11-03-OVERCLAIM | Repudiation (trust) | topology implies 3 independent institutions | mitigate | `demoReal` flag + `SAME PARTICIPANT (LOCALNET)` caption when all map to one participant (Pitfall 7) |
| T-11-03-SC | Tampering | no new npm deps | accept | reuses node:crypto HS256 + existing fetch layer; zero new solver deps |
</threat_model>

<verification>
- `cd solver && npx vitest run` fully green (topology + api secret sweep).
- `node --check scripts/localnet/guest-onboard.mjs` exits 0.
- Live 3-node hosting map + real guest onboarding against a booted LocalNet = live-UAT (enumerated in SUMMARY).
</verification>

<success_criteria>
A credential-free topology endpoint returns an honest party→participant map (demo-real flagged),
a guest-onboarding endpoint + script provision a 4th desk + scoped token without ever leaking the
token in a QR/URL, and all solver tests stay green.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `solver/src/topology.ts` — `hostingMap()`, `TopologyResult` type (`nodes`, `perParty`, `demoReal`).
- `solver/src/topology.test.ts` — single/distributed + secret-sweep tests.
- `solver/src/api.ts` — `GET /round/:id/topology`, guest-bootstrap endpoint, `AppDeps.hostingMap` / `AppDeps.onboardGuest`.
- `solver/src/index.ts` — real deps wiring for `hostingMap` / `onboardGuest`.
- `scripts/localnet/guest-onboard.mjs` — bankD party + user + rights + scoped token → tokens.json.
</artifacts>

<output>
Create `.planning/phases/11-settlement-institutional-grade/11-03-SUMMARY.md` when done.
</output>
