---
phase: 11-settlement-institutional-grade
plan: 06
type: execute
wave: 4
depends_on: ["11-05"]
files_modified:
  - daml/Umbra/Tests.daml
  - daml/Umbra/Setup.daml
autonomous: true
requirements: [DFIN-02, DFIN-03, COMP-01, WOW-07]
must_haves:
  truths:
    - "A 2-buyer x 2-seller golden clears at one uniform price and conserves cash+assets on-ledger"
    - "Round.Clear settles with a NON-USDCx cash instrument unchanged (token-agnostic proof)"
    - "An ineligible party is rejected on-ledger at BOTH Order creation and holding issuance"
    - "A guest 4th desk submits and sees only its own fill (structural per-party privacy)"
    - "The §4 canary still clears $100.00 / A=10 · B=8 · C=2"
  artifacts:
    - path: "daml/Umbra/Tests.daml"
      provides: "test_multibuyer_golden, test_cash_instrument_agnostic, test_ineligible_submit_rejected, test_ineligible_holding_rejected, test_privacy_guest_fill"
      contains: "test_multibuyer_golden"
    - path: "daml/Umbra/Setup.daml"
      provides: "seedMultiBuyerRound (2x2 golden seed) + guest/ineligible seed helpers"
      contains: "seedMultiBuyerRound"
  key_links:
    - from: "daml/Umbra/Tests.daml"
      to: "daml/Umbra/Compliance.daml"
      via: "submitMustFail on an ineligible desk's SubmitOrder / IssueHolding"
      pattern: "submitMustFail"
---

<objective>
Lock the Phase-11 generalizations with on-ledger golden + negative tests: a 2×2 multi-buyer golden
(DFIN-02), a token-agnostic cash-instrument-swap test (DFIN-03), the COMP-01 negative tests at both
enforcement points, and the guest 4th-desk own-fill privacy test (WOW-07). The §4 canary stays the
continuous reference. These tests ADD to Tests.daml/Setup.daml (after 11-05) — the multi-buyer
golden is mirrored in the solver by 11-07.

Purpose: DFIN-02 (multi-buyer golden), DFIN-03 (cash-agnostic), COMP-01 (ineligible rejected both
points), WOW-07 (guest own-fill privacy) — the verifiable acceptance evidence for the phase's
success criteria.
Output: five new Daml tests + the seed helpers they need.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-settlement-institutional-grade/11-RESEARCH.md
@.planning/phases/11-settlement-institutional-grade/11-PATTERNS.md
@daml/Umbra/Tests.daml
@daml/Umbra/Setup.daml
@daml/Umbra/Auction.daml
@daml/Umbra/Compliance.daml
</context>

<tasks>

<task type="auto">
  <name>Task 1: test_multibuyer_golden + test_cash_instrument_agnostic + seedMultiBuyerRound</name>
  <files>daml/Umbra/Setup.daml, daml/Umbra/Tests.daml</files>
  <read_first>
    - daml/Umbra/Setup.daml (seedCommitRevealRound / initialize migrated to Holdings in 11-05 — the gather-and-Clear idiom to mirror for a 2x2 batch)
    - daml/Umbra/Tests.daml (test_clears_at_100 / test_settled_balances — the clear-and-assert-balances idiom)
    - 11-RESEARCH Test Map (test_multibuyer_golden, test_cash_instrument_agnostic) + Pitfall 5 (conservation) + Pitfall 6 (§4 stays a pure reduction, prove generalization SEPARATELY)
  </read_first>
  <action>
    Add `seedMultiBuyerRound : Script …` to Setup.daml: a 2-buyer × 2-seller batch (all four desks
    eligible, Holdings minted for both cash + bond instruments) crossing at ONE uniform price;
    gather the Holding cids + Round cid for `Round.Clear`. Choose quantities/limits so the batch has
    a clean single uniform p* and balanced conservation (document the intended clear).

    In Tests.daml: `test_multibuyer_golden` runs the 2×2 through the generalized `Round.Clear` and
    asserts (a) a SINGLE uniform clearing price, (b) per-instrument conservation (Σ cash paid == Σ
    cash received; Σ bond delivered == Σ bond received), (c) each desk's post-settle Holdings match
    its fill. `test_cash_instrument_agnostic` runs a settle with a DIFFERENT cash `InstrumentId`
    (e.g. `InstrumentId operator "EURt"`) and asserts `Round.Clear` behaves identically (the cash
    leg follows the passed instrument — no hardcoded "USDCx"; DFIN-03). Do NOT modify §4 orders;
    §4 stays single-buyer-shaped.
  </action>
  <acceptance_criteria>
    - `test_multibuyer_golden` passes: one uniform price, conserved per instrument, correct per-desk fills.
    - `test_cash_instrument_agnostic` passes: a non-USDCx cash instrument settles unchanged.
    - `test_clears_at_100` still passes ($100.00 / A=10·B=8·C=2).
  </acceptance_criteria>
  <verify>
    <automated>cd daml && daml test 2>&1 | tail -8</automated>
  </verify>
  <done>The multi-buyer generalization + token-agnostic cash leg are proven on-ledger; §4 unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: COMP-01 negative tests + guest own-fill privacy test</name>
  <files>daml/Umbra/Tests.daml, daml/Umbra/Setup.daml</files>
  <read_first>
    - daml/Umbra/Tests.daml (test_privacy_orders / test_privacy_confirmations ~570/635 — the per-party query privacy idiom to mirror for the guest; submitMustFail usage in test_atomicity)
    - daml/Umbra/Roles.daml (the SubmitOrder/CommitOrder eligibility gate + Venue.IssueHolding from 11-05)
    - 11-RESEARCH Test Map (test_ineligible_submit_rejected, test_ineligible_holding_rejected, test_privacy_guest_fill) + Security Domain (structural per-party privacy)
  </read_first>
  <action>
    Add negative tests:
    - `test_ineligible_submit_rejected`: allocate a desk with NO (or `accredited=False`)
      `DeskEligibility`, `submitMustFail` its `Venue.SubmitOrder` (and `CommitOrder`) — the
      on-ledger gate rejects it. Assert the failure is the eligibility `assertMsg`, not a
      registration failure.
    - `test_ineligible_holding_rejected`: `submitMustFail` `Venue.IssueHolding` to an ineligible
      owner — holding-receipt rejected on-ledger.
    - `test_privacy_guest_fill`: seed a 4th eligible guest desk (bankD) that submits a sealed order
      and settles in a batch; assert a per-party `query @Order`/`query @TradeConfirmation` as the
      guest returns ONLY the guest's own order/fill and ZERO of A/B/C's (and symmetrically A cannot
      see the guest's) — structural per-party privacy identical to the existing desks. Add any
      guest/ineligible seed helper to Setup.daml. Keep §4 green.
  </action>
  <acceptance_criteria>
    - `test_ineligible_submit_rejected` and `test_ineligible_holding_rejected` pass via `submitMustFail` on the eligibility assert.
    - `test_privacy_guest_fill` proves the guest sees only its own order+fill (and rivals see none of the guest's).
    - Full `daml test` green including the §4 canary.
  </acceptance_criteria>
  <verify>
    <automated>cd daml && daml test 2>&1 | tail -10</automated>
  </verify>
  <done>COMP-01 rejection is proven on-ledger at both points; the guest's own-fill privacy is structural.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ineligible party ↔ ledger | rejection must be an on-ledger assertMsg failure, provable by submitMustFail |
| guest ↔ rivals | the guest's order/fill is visible only to guest+operator (structural) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-COMP | Elevation | ineligible bypass | mitigate | submitMustFail tests at both enforcement points prove the on-ledger gate rejects |
| T-11-06-PRIV | Information Disclosure | guest sees / is seen by rivals | mitigate | test_privacy_guest_fill: per-party query proves structural own-fill privacy |
| T-11-06-CONSV | Tampering | multi-buyer imbalance | mitigate | test_multibuyer_golden asserts per-instrument conservation |
| T-11-06-CANARY | Tampering | §4 regression from new seeds | mitigate | test_clears_at_100 must stay green alongside the new goldens |
</threat_model>

<verification>
- `cd daml && daml test` fully green: the five new tests + the §4 canary all pass.
</verification>

<success_criteria>
The multi-buyer generalization, token-agnostic cash leg, COMP-01 on-ledger rejection (both points),
and guest own-fill privacy are each proven by a passing Daml test, with the §4 canary unaffected.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `daml/Umbra/Setup.daml` — `seedMultiBuyerRound` (2×2 golden seed) + guest/ineligible seed helpers.
- `daml/Umbra/Tests.daml` — `test_multibuyer_golden`, `test_cash_instrument_agnostic`, `test_ineligible_submit_rejected`, `test_ineligible_holding_rejected`, `test_privacy_guest_fill`.
</artifacts>

<output>
Create `.planning/phases/11-settlement-institutional-grade/11-06-SUMMARY.md` when done.
</output>
