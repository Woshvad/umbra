---
phase: 11-settlement-institutional-grade
plan: 07
type: execute
wave: 4
depends_on: ["11-05"]
files_modified:
  - solver/src/ledger.ts
  - solver/src/settlement.ts
  - solver/src/settlement.test.ts
  - solver/src/auction.test.ts
autonomous: true
requirements: [DFIN-02, DFIN-03]
must_haves:
  truths:
    - "solver settle() gathers Holding cids (not Asset) and submits the new token-agnostic Round.Clear"
    - "settle() generalizes to N buyers × M sellers (no single-buyer assumption)"
    - "A pure TS netLegs/grossLegs mirrors the Daml Settlement layer with a 2×2 golden (Daml⇄TS parity)"
    - "The §4 solver fixture still clears $100.00 / A=10 · B=8 · C=2; verify-don't-trust + tamperClear intact"
  artifacts:
    - path: "solver/src/settlement.ts"
      provides: "pure netLegs/grossLegs mirror of Umbra.Settlement (Daml⇄TS parity for the viz + goldens)"
      contains: "netLegs"
    - path: "solver/src/ledger.ts"
      provides: "Holding-based, N-buyer settle() submitting the new Round.Clear (cashInstrument arg)"
      contains: "cashInstrument"
    - path: "solver/src/settlement.test.ts"
      provides: "2×2 multi-buyer golden + netting-conserves mirror (parity with test_multibuyer_golden)"
      contains: "multibuyer"
  key_links:
    - from: "solver/src/ledger.ts"
      to: "Umbra.Auction:Round Clear (v2)"
      via: "exerciseChoice Clear with Holding cids + cashInstrument/bondInstrument"
      pattern: "cashInstrument"
---

<objective>
Bring the solver settlement submission onto the new Batch/Instruction flow: `settle()` gathers
`Holding` contract ids (not `Asset`), generalizes past the single-buyer assumption, and passes the
new `Round.Clear` args (`cashInstrument`/`bondInstrument` InstrumentIds, Holding cids). Add a PURE
`settlement.ts` `netLegs`/`grossLegs` mirroring `Umbra.Settlement`, with a 2×2 golden proving
Daml⇄TS parity (CLEAR-05 discipline wherever settlement math is touched). The §8 clearing math
(`auction.ts`) is UNCHANGED — this is a settlement-leg + parity change.

Purpose: DFIN-02 (multi-buyer settle + netting mirror) + DFIN-03 (token-agnostic cash arg).
Output: Holding-based `settle()`, `settlement.ts`, `settlement.test.ts` 2×2 golden + parity, §4
solver fixture green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-settlement-institutional-grade/11-RESEARCH.md
@.planning/phases/11-settlement-institutional-grade/11-PATTERNS.md
@solver/src/ledger.ts
@solver/src/auction.ts
@daml/Umbra/Settlement.daml
@daml/Umbra/Auction.daml
</context>

<tasks>

<task type="auto">
  <name>Task 1: settlement.ts pure netLegs/grossLegs mirror + 2×2 golden (Daml⇄TS parity)</name>
  <files>solver/src/settlement.ts, solver/src/settlement.test.ts</files>
  <read_first>
    - daml/Umbra/Settlement.daml (buildGrossInstructions / netLegs / conservationOk — the exact math to mirror byte-for-byte in behavior)
    - solver/src/auction.ts (the existing pure §8 mirror + how goldens are structured) + solver/src/auction.test.ts (golden-fixture idiom)
    - 11-RESEARCH Test Map (settlement.test.ts netLegs 2×2) + Pitfall 5 (net per (party,instrument), conserve)
  </read_first>
  <action>
    Create `solver/src/settlement.ts`: pure functions mirroring `Umbra.Settlement` —
    `grossLegs(price, allocations, cashInstrument, bondInstrument)` and `netLegs(legs)` (one net
    leg per `(party, instrument)`), plus a `conserves(legs, instruments)` predicate. Use the same
    Instruction shape (sender/receiver/instrument/amount). This is the source the topology viz +
    receipts read (gross derivable; netted default). Keep it DOM-free / no fetch / deterministic.

    Write `solver/src/settlement.test.ts`: a 2×2 multi-buyer golden with the SAME quantities as the
    Daml `test_multibuyer_golden` (11-06) → assert identical net legs + conservation (Daml⇄TS
    parity), plus a §4 single-buyer reduction case producing A↔B 8@100 / A↔C 2@100. If the Daml and
    TS goldens use the same fixture numbers, note the parity mapping in a comment.
  </action>
  <acceptance_criteria>
    - `settlement.ts` `netLegs` produces one net leg per `(party, instrument)` and `conserves` returns true for a balanced batch.
    - `settlement.test.ts` 2×2 golden matches the Daml `test_multibuyer_golden` numbers; §4 reduction produces the two §4 legs.
    - `cd solver && npx vitest run settlement` is green.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run settlement 2>&1 | tail -6</automated>
  </verify>
  <done>A pure TS settlement mirror with a 2×2 golden holds Daml⇄TS parity for netting + conservation.</done>
</task>

<task type="auto">
  <name>Task 2: ledger.ts settle() → Holding cids + N-buyer + cashInstrument; §4 + tamperClear intact</name>
  <files>solver/src/ledger.ts, solver/src/auction.test.ts</files>
  <read_first>
    - solver/src/ledger.ts lines 324-399 (settle: Asset gather + buyer selection + Clear exercise — Option-B cids) and 401-496 (tamperClear — must keep rejecting; DO NOT break its recompute-backstop demo)
    - daml/Umbra/Auction.daml (the new Round.Clear arg names/types from 11-05) + 11-RESEARCH Pitfall 4 (v2 wire: Decimals as strings → Number(); tuples {_1,_2}; InstrumentId record shape)
  </read_first>
  <action>
    Rewrite `settle()` to gather `Holding` contract ids instead of `Asset`: `queryByEntity('Holding')`,
    match by `(owner, instrument)` + sufficient amount (coerce amounts with `Number()`; encode
    `InstrumentId` as its record shape, tuples as `{_1,_2}` — Pitfall 4). Generalize the single
    `buyAlloc`/`buyer` selection to N buyers (build the buyer cash-cid list + seller bond-cid list).
    Pass the new `Round.Clear` args: `buyerCashCids`/`sellerBondCids` (Holding cids), plus
    `cashInstrument`/`bondInstrument` InstrumentIds and the unchanged `referencePrice`. Keep the
    verify-don't-trust discipline (compute §8 locally, submit, on-ledger re-verify — NO skip path)
    and the clean secret-free `insufficient or missing <instrument> holding for <party>` error.
    Mirror the SAME gather changes into `tamperClear` (copied, not refactored — settle stays the
    canonical path; tamperClear must still submit a perturbed proposal and catch the verbatim
    on-ledger reject). Update `auction.test.ts` only if a §4 helper shape changed; the §8 math is
    byte-unchanged and the §4 fixture must stay $100.00 / A=10·B=8·C=2.
  </action>
  <acceptance_criteria>
    - `settle()` gathers Holding cids, handles N buyers, and passes `cashInstrument`/`bondInstrument`; no `Asset` gather remains on the settle path.
    - `tamperClear` still submits a perturbed proposal and resolves `{rejected:true}` on the on-ledger reject (recompute backstop intact).
    - `cd solver && npx vitest run` fully green (§4 fixture at $100.00; secret sweeps intact).
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run 2>&1 | tail -8</automated>
  </verify>
  <done>The solver settles the new token-agnostic multi-party Round.Clear on Holdings, verify-don't-trust + tamperClear intact, §4 green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solver ↔ ledger | settle submits the new Clear; the on-ledger recompute is still the backstop (verify-don't-trust) |
| Daml ⇄ TS | settlement math parity (CLEAR-05) must hold wherever legs are derived |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07-DECODE | Tampering | v2 wire misencoding (Decimal/InstrumentId/tuple) | mitigate | Number() coercion + record-shaped InstrumentId + {_1,_2} tuples (Pitfall 4); §4 fixture + secret sweep gate the wire |
| T-11-07-PARITY | Tampering | Daml⇄TS settlement drift | mitigate | settlement.test.ts 2×2 golden mirrors the Daml test_multibuyer_golden numbers |
| T-11-07-BACKSTOP | Tampering | tamperClear stops rejecting | mitigate | tamperClear keeps the perturbed-submit-and-catch path; the recompute backstop is unchanged in Round.Clear |
| T-11-07-CANARY | Tampering | §4 solver regression | mitigate | auction.ts §8 byte-unchanged; §4 vitest stays $100.00 |
</threat_model>

<verification>
- `cd solver && npx vitest run` fully green — settlement 2×2 golden parity, §4 fixture at $100.00, secret sweeps, tamperClear reject.
- Live settle against a booted LocalNet (§4 clears $100.00 on Holdings) = live-UAT (enumerated in SUMMARY).
</verification>

<success_criteria>
The solver submits the generalized token-agnostic Round.Clear on `Holding` cids for N buyers, a
pure `settlement.ts` mirror holds Daml⇄TS parity via a 2×2 golden, verify-don't-trust + tamperClear
still work, and the §4 solver fixture still clears $100.00 / A=10 · B=8 · C=2.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `solver/src/settlement.ts` — pure `grossLegs`, `netLegs`, `conserves` (Instruction shape).
- `solver/src/settlement.test.ts` — 2×2 golden (Daml⇄TS parity) + §4 reduction.
- `solver/src/ledger.ts` — Holding-based, N-buyer `settle()` + `tamperClear` submitting the new `Round.Clear` (`cashInstrument`/`bondInstrument`).
</artifacts>

<output>
Create `.planning/phases/11-settlement-institutional-grade/11-07-SUMMARY.md` when done.
</output>
