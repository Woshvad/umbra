---
phase: 11-settlement-institutional-grade
plan: 08
type: execute
wave: 5
depends_on: ["11-05", "11-06", "11-03"]
files_modified:
  - web/daml.js
  - web/src/ledgerContexts.ts
  - web/src/desks.ts
  - web/src/tokens.json
  - web/src/components/Nav.tsx
  - web/src/solver.ts
autonomous: true
requirements: [DFIN-01, WOW-07, VIZ-03]
must_haves:
  truths:
    - "web/daml.js is regenerated + committed so the frontend decodes Holding/Instrument/DeskEligibility/Settlement + the new Round.Clear"
    - "A guest desk (bankD) context + token + meta exists without breaking the 3-desk switcher"
    - "Nav exposes view 07 (TOPOLOGY); the Screen union gains 'topology'"
    - "solver.ts exposes typed getTopology + settlement-meta types for the views to consume"
    - "web build + web vitest stay green"
  artifacts:
    - path: "web/daml.js"
      provides: "regenerated @daml.js/umbra-0.1.0 bindings for all Phase-11 template changes"
      contains: "Holding"
    - path: "web/src/ledgerContexts.ts"
      provides: "ctxD (guest bankD) + DeskKey union incl. bankD"
      contains: "ctxD"
    - path: "web/src/solver.ts"
      provides: "getTopology(id) client + TopologyMeta/SettlementMeta types mirroring api.ts"
      contains: "getTopology"
  key_links:
    - from: "web/src/desks.ts"
      to: "web/src/tokens.json"
      via: "bankD guest token entry consumed by ctxD"
      pattern: "bankD"
    - from: "web/src/solver.ts"
      to: "solver/src/api.ts"
      via: "GET /round/:id/topology → getTopology"
      pattern: "topology"
---

<objective>
The frontend-enablement gate: regenerate + COMMIT `web/daml.js` after all Phase-11 template changes
(fresh-clone invariant — stale bindings silently break decode), add the guest desk (bankD) to the
per-party plumbing WITHOUT disturbing the 3-desk desktop switcher, add Nav view 07, and extend
`web/src/solver.ts` with the typed topology client + settlement-meta types the Wave-6 views consume
(so those views never edit solver.ts and stay conflict-free).

Purpose: enables DFIN-01/VIZ-03/WOW-07 frontend work — bindings + client surface + guest identity
+ nav slot. No behavior change to existing views yet.
Output: regenerated committed bindings, ctxD + bankD token/meta, Nav 07, solver.ts client surface.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-settlement-institutional-grade/11-RESEARCH.md
@.planning/phases/11-settlement-institutional-grade/11-PATTERNS.md
@web/src/ledgerContexts.ts
@web/src/desks.ts
@web/src/tokens.json
@web/src/components/Nav.tsx
@web/src/solver.ts
@solver/src/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate + commit web/daml.js for all Phase-11 template changes</name>
  <files>web/daml.js</files>
  <read_first>
    - 11-PATTERNS "Regenerate + commit bindings (fresh-clone invariant)" + 11-RESEARCH Runtime State (Build artifacts row)
    - DECISIONS.md D10 (the @daml.js/umbra-0.1.0 bindings are the typed schema; the v2react shim consumes them)
    - daml/daml.yaml (sdk 3.4.11; codegen via `daml codegen js`)
  </read_first>
  <action>
    Build the DAR (`cd daml && daml build`) then regenerate the JS/TS bindings with
    `daml codegen js` into `web/daml.js/` so `@daml.js/umbra-0.1.0` carries the new
    `Holding`/`Instrument`/`DeskEligibility`/`Settlement` templates + `InstrumentId` + the changed
    `Round.Clear` / `OrderCommitment` shapes. COMMIT the regenerated `web/daml.js/` in lockstep
    (the established every-template-change step; stale bindings break decode silently). Confirm the
    generated modules for the new templates exist. Do NOT hand-edit generated files.
  </action>
  <acceptance_criteria>
    - `web/daml.js` contains generated modules for `Holding`, `Instrument`, `Compliance` (DeskEligibility), and `Settlement`.
    - `cd web && npm run build` succeeds against the regenerated bindings.
  </acceptance_criteria>
  <verify>
    <automated>ls web/daml.js/lib/Umbra | grep -Ei "Holding|Instrument|Compliance|Settlement" && cd web && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>The frontend bindings decode every Phase-11 template change; committed in lockstep.</done>
</task>

<task type="auto">
  <name>Task 2: Guest desk (bankD) plumbing — ctxD + token + meta, switcher untouched</name>
  <files>web/src/ledgerContexts.ts, web/src/desks.ts, web/src/tokens.json</files>
  <read_first>
    - web/src/ledgerContexts.ts (ctxA/B/C + DeskKey union + ctxFor map — the pattern to extend)
    - web/src/desks.ts (DESKS meta + tokens typing + httpBaseUrlFor — how a desk resolves its token/base)
    - web/src/tokens.json (the `{party, token, base}` shape; bankD entry written by guest-onboard.mjs in 11-03)
    - 11-UI-SPEC (guest is OUTSIDE the top-bar switcher — mobile /join only; topology GUEST card appears once joined)
  </read_first>
  <action>
    Add `ctxD = make('bankD')` to ledgerContexts.ts and extend the `DeskKey` union to include
    `'bankD'`; add `bankD` to `ctxFor`. Add a `GUEST` `DeskMeta` (code `GUEST`, role `Guest`) as a
    separate export in desks.ts WITHOUT adding it to the primary `DESKS` array — the desktop
    PartySwitcher/Header keep iterating the three primary desks (guest is mobile-only per UI-SPEC),
    so no ripple into Header/PartySwitcher. Ensure `tokens.json` has a `bankD` entry in the same
    `{party, token, base}` shape (provisioned by guest-onboard.mjs; if absent in dev, add a
    placeholder entry so types resolve — the real token is minted at onboarding, never hardcoded
    beyond tokens.json). `httpBaseUrlFor('bankD')` resolves via the bankD `base`.
  </action>
  <acceptance_criteria>
    - `DeskKey` includes `'bankD'`; `ctxD`/`ctxFor.bankD` exist; a `GUEST` meta is exported but NOT in `DESKS`.
    - `tokens.json` has a `bankD` `{party, token, base}` entry; `httpBaseUrlFor('bankD')` resolves.
    - `cd web && npm run build` succeeds (no switcher/Header ripple).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "ctxD" web/src/ledgerContexts.ts && grep -c "bankD" web/src/tokens.json && cd web && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>The guest desk has an isolated per-party context + token + meta; the 3-desk switcher is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Nav view 07 + solver.ts topology client + settlement-meta types</name>
  <files>web/src/components/Nav.tsx, web/src/solver.ts</files>
  <read_first>
    - web/src/components/Nav.tsx (TABS + Screen union — the exact 06 entry to extend to 07)
    - web/src/solver.ts lines 180-400 (getRound/solvePreview/settle/getStageOffsets client idiom + response types; the SOLVER_BASE_URL call<T> pattern; no :port literal, no auth header)
    - solver/src/api.ts (the topology + settlement response shapes to mirror EXACTLY — mirror authoritative api.ts shapes, not an approximate sketch)
  </read_first>
  <action>
    In Nav.tsx add `{ num:'07', label:'TOPOLOGY', screen:'topology' as const, enabled:true }` to
    TABS and add `'topology'` to the `Screen` union. In solver.ts add `getTopology(id)` calling
    `GET /round/:id/topology` via the existing `call<T>()`/`SOLVER_BASE_URL` (no port literal, no
    auth header, no operator token) returning a `TopologyMeta` type that mirrors `topology.ts`
    `TopologyResult` EXACTLY (`nodes`, `perParty`, `demoReal`). Also add the settlement-meta types
    the SettlementView delta (11-11) needs — the provenance tag (`CN TOKEN STANDARD (CIP-0056)` /
    `DAML-FINANCE-PATTERN (IN-REPO)`), the `{N}` instruction count, netted-vs-gross legs, and the
    cash instrument symbol — as additive fields on the settle/preview response types, mirroring the
    authoritative `api.ts` shapes. Do NOT add a `:4100` literal (reuse `SOLVER_BASE_URL`). `SolverError
    'OFFLINE'` behavior is unchanged.
  </action>
  <acceptance_criteria>
    - Nav shows a 07 TOPOLOGY tab; `Screen` includes `'topology'`.
    - `getTopology` + `TopologyMeta`/settlement-meta types exist in solver.ts, mirroring api.ts shapes; no `:4100` literal added.
    - `cd web && npm run build` + `cd web && npx vitest run` are green.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "topology" web/src/components/Nav.tsx web/src/solver.ts && cd web && npm run build 2>&1 | tail -3 && npx vitest run 2>&1 | tail -4</automated>
  </verify>
  <done>The nav slot + typed topology client + settlement-meta types are ready for the Wave-6 views; no view edits solver.ts later.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ solver | solver.ts stays credential-free (no operator token / :port literal / auth header) |
| generated bindings ↔ ledger | stale bindings silently mis-decode — must regenerate + commit in lockstep |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08-STALE | Tampering | stale web/daml.js decode | mitigate | regenerate + commit bindings in lockstep (fresh-clone invariant); build gate against them |
| T-11-08-CRED | Information Disclosure | operator token / port leak in bundle | mitigate | solver.ts client reuses SOLVER_BASE_URL + call<T>; no :4100 literal, no auth header, no operator token |
| T-11-08-GUEST | Information Disclosure | guest token hardcoded in web source | mitigate | bankD token lives only in tokens.json (D6 boundary); never inline in components |
</threat_model>

<verification>
- `cd web && npm run build` green against regenerated bindings.
- `cd web && npx vitest run` green.
- `web/daml.js` contains the new template modules and is committed.
</verification>

<success_criteria>
Regenerated committed bindings decode every Phase-11 template; the guest desk has an isolated
context/token/meta without disturbing the 3-desk switcher; Nav exposes view 07; and solver.ts
exposes the typed topology client + settlement-meta types the Wave-6 views consume — with web build
+ vitest green.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `web/daml.js` — regenerated `@daml.js/umbra-0.1.0` (Holding/Instrument/Compliance/Settlement + changed Round.Clear/OrderCommitment).
- `web/src/ledgerContexts.ts` — `ctxD`, `DeskKey` incl. `bankD`, `ctxFor.bankD`.
- `web/src/desks.ts` — `GUEST` DeskMeta (separate from `DESKS`), `httpBaseUrlFor('bankD')`.
- `web/src/tokens.json` — `bankD` entry.
- `web/src/components/Nav.tsx` — 07 TOPOLOGY tab + `'topology'` Screen.
- `web/src/solver.ts` — `getTopology(id)`, `TopologyMeta`, settlement-meta types.
</artifacts>

<output>
Create `.planning/phases/11-settlement-institutional-grade/11-08-SUMMARY.md` when done.
</output>
