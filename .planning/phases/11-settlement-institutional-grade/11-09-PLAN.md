---
phase: 11-settlement-institutional-grade
plan: 09
type: execute
wave: 6
depends_on: ["11-03", "11-08"]
files_modified:
  - web/src/views/TopologyView.tsx
  - web/src/components/TopologyNode.tsx
  - web/src/views/Topology.test.tsx
  - web/src/App.tsx
autonomous: true
requirements: [VIZ-03]
must_haves:
  truths:
    - "View 07 shows three participant node cards (+ GUEST once joined) each hosting a desk's order as a redaction stripe"
    - "An operator/synchronizer node is drawn inverted (ink) and sees a count, never contents"
    - "On settle the SVG edges light red and AtomicStamp spans the whole diagram (one atomic tx across nodes)"
    - "The HARD DEMO-REAL · SINGLE-OPERATOR LOCALNET badge is non-removable while nodes map to one participant"
  artifacts:
    - path: "web/src/views/TopologyView.tsx"
      provides: "view 07 (TimeMachine chrome + node grid + SVG edges + demo-real badge + AtomicStamp)"
      contains: "DEMO-REAL"
    - path: "web/src/components/TopologyNode.tsx"
      provides: "participant/operator node card (redaction stripe + PARTICIPANT caption)"
      contains: "NOT VISIBLE"
    - path: "web/src/views/Topology.test.tsx"
      provides: "pure node/edge derivation tests (demo-real caption + no rival contents)"
      contains: "demoReal"
  key_links:
    - from: "web/src/views/TopologyView.tsx"
      to: "web/src/solver.ts getTopology"
      via: "getTopology(roundId) → hosting map"
      pattern: "getTopology"
    - from: "web/src/App.tsx"
      to: "TopologyView"
      via: "screen === 'topology' render branch"
      pattern: "topology"
---

<objective>
Build view 07 Network Topology (VIZ-03): three participant node cards each hosting a desk's order as
a cross-node redaction stripe, an inverted operator/synchronizer node that sees only a count, SVG
connector edges that light red with an `AtomicStamp` spanning all nodes on settle, and the HARD
non-removable `DEMO-REAL · SINGLE-OPERATOR LOCALNET` honesty badge. Bound to 11-UI-SPEC tokens and
modeled on `TimeMachineView` chrome + the redaction motif.

Purpose: VIZ-03 — the three-node topology + atomic cross-node settle visualization, honestly labeled.
Output: `TopologyView.tsx`, `TopologyNode.tsx`, a pure derivation test, App route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-settlement-institutional-grade/11-UI-SPEC.md
@.planning/phases/11-settlement-institutional-grade/11-PATTERNS.md
@web/src/views/TimeMachineView.tsx
@web/src/views/TimeMachine.test.tsx
@web/src/components/AtomicStamp.tsx
@web/src/components/RedactionBar.tsx
@web/src/solver.ts
@web/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: TopologyNode.tsx + pure node/edge derivation core + Topology.test.tsx</name>
  <files>web/src/components/TopologyNode.tsx, web/src/views/Topology.test.tsx</files>
  <read_first>
    - web/src/views/TimeMachineView.tsx lines 16-63 (the pure DOM-free core exports + RECONSTRUCTED honesty grammar to mirror) + TimeMachine.test.tsx (node-env .tsx test idiom)
    - web/src/components/DeskColumn.tsx + RedactionBar.tsx (ColumnHeader code grammar + redaction stripe to reuse)
    - 11-UI-SPEC "VIZ-03 — 07 Network Topology" (node card spec, codes BLUEROCK/MERIDIAN/HALWARD/GUEST, PARTICIPANT/SAME PARTICIPANT captions, operator inverted node)
    - web/src/solver.ts (TopologyMeta type from 11-08)
  </read_first>
  <action>
    Create `TopologyNode.tsx`: a participant node card (1px ink border, paper fill, `24px 22px 26px`
    padding) with a `ColumnHeader`-style mono code + role, an `ORDER RESIDENT` label, the order
    rendered as a `bg-redact` redaction stripe with `NOT VISIBLE` (mono-9) — contents are the
    owner's alone (cross-node privacy is structural; never render a rival's contents), and a small
    `PARTICIPANT · {node-id}` caption (or `SAME PARTICIPANT (LOCALNET)` when the hosting map resolves
    every node to one participant). Support an inverted operator/synchronizer variant (ink fill
    `#0A0A0A`, paper text, `OPERATOR · SYNCHRONIZER`, sees a COUNT only).

    Export a PURE DOM-free derivation core from TopologyView (or a lib) — `nodesFromHosting(meta)`
    and `edges(...)` — mapping the `TopologyMeta` hosting map onto node cards + connector edges, and
    a `demoRealCaption(meta)` helper. Write `Topology.test.tsx` (node-env) covering: (a)
    single-participant meta → every node shows `SAME PARTICIPANT (LOCALNET)` + the `demoReal` badge
    is present; (b) distributed meta → per-participant captions; (c) no node ever carries a rival's
    order contents (only redaction stripes for cross-node orders). Reuse only inherited
    tailwind/keyframes. Directive prose only; no fenced code.
  </action>
  <acceptance_criteria>
    - `TopologyNode` renders a participant card (redaction stripe + caption) and an inverted operator variant.
    - The pure core derives nodes/edges + demo-real caption from `TopologyMeta`; `Topology.test.tsx` covers single + distributed + no-rival-contents.
    - `cd web && npx vitest run Topology` is green.
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx vitest run Topology 2>&1 | tail -6</automated>
  </verify>
  <done>Node cards + a unit-tested pure derivation core with honest demo-real captions exist.</done>
</task>

<task type="auto">
  <name>Task 2: TopologyView.tsx (chrome + node grid + SVG edges + demo-real badge + AtomicStamp) + App route</name>
  <files>web/src/views/TopologyView.tsx, web/src/App.tsx</files>
  <read_first>
    - web/src/views/TimeMachineView.tsx lines 484-499 (the verbatim section-marker + rule + display headline chrome to mirror)
    - web/src/components/AtomicStamp.tsx (the `1 TRANSACTION · ATOMIC` finality stamp to span the diagram)
    - 11-UI-SPEC "VIZ-03" (headline `ONE TRANSACTION. EVERY NODE.`, honesty badge copy + limitation sub-caption, states loading/empty/resident/settled, red edges on settle, reduced-motion)
    - web/src/App.tsx (the screen render branches + operatorState threading to add a 'topology' branch)
  </read_first>
  <action>
    Create `TopologyView.tsx` with the verbatim shipped chrome (`<main> padding 30px 48px 64px` →
    `07` marker + `Network Topology · Cross-Node Settlement` label → 1px ink rule → `font-display
    text-54` headline `ONE TRANSACTION. EVERY NODE.`). Directly under the headline render the HARD
    honesty badge in the red-square mono-9 tag grammar: `DEMO-REAL · SINGLE-OPERATOR LOCALNET` + the
    limitation sub-caption (`True 3-validator topology needs three institutions each running a
    validator — a recorded limitation.`), NON-REMOVABLE while nodes resolve to one participant.
    Render a CSS-grid of `TopologyNode` cards (3 participants + the GUEST card once a guest has
    joined + the inverted operator/synchronizer node) with an SVG connector overlay (1px ink idle
    edges node→synchronizer). On settle (`phase === 'settled'`) light the edges `#E2231A` red and
    overlay `AtomicStamp` (`1 TRANSACTION · ATOMIC`) across the whole diagram. States: loading
    (`READING NODE TOPOLOGY…`, `animate-umbra-pulse`) → empty (`No round to map yet.` + the
    empty-state body) → resident → settled; `SolverError 'OFFLINE'` → `OFFLINE_CAPTION`. Data via
    `getTopology(roundId)`; honor reduced-motion. In App.tsx add the `screen === 'topology' &&
    <TopologyView {...operatorState} />` branch. Reuse only inherited tokens/keyframes; no new
    tailwind literal. Directive prose only; no fenced code.
  </action>
  <acceptance_criteria>
    - View 07 renders the TimeMachine-style chrome + `ONE TRANSACTION. EVERY NODE.` headline + the non-removable `DEMO-REAL · SINGLE-OPERATOR LOCALNET` badge.
    - Nodes host redacted orders; on settle edges are red + AtomicStamp spans the diagram; empty/loading/offline states present.
    - App routes `screen==='topology'` to TopologyView.
    - `cd web && npm run build` + `cd web && npx vitest run` green.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "DEMO-REAL · SINGLE-OPERATOR LOCALNET" web/src/views/TopologyView.tsx && cd web && npm run build 2>&1 | tail -3 && npx vitest run 2>&1 | tail -4</automated>
  </verify>
  <done>View 07 renders the three-node topology + atomic cross-node settle with a non-removable honesty badge, routed in App.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| topology render ↔ ledger-derived hosting | the view presents ledger-derived hosting metadata; cross-node order contents are never rendered (redaction only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09-OVERCLAIM | Repudiation/Spoofing (trust) | topology implies 3 independent institutions | mitigate | HARD non-removable `DEMO-REAL · SINGLE-OPERATOR LOCALNET` badge + `SAME PARTICIPANT (LOCALNET)` per-node caption when one participant (Pitfall 7) |
| T-11-09-DISCLOSE | Information Disclosure | rival order contents rendered cross-node | mitigate | node cards render only redaction stripes for cross-node orders (structural privacy) |
| T-11-09-CRED | Information Disclosure | operator token in bundle | mitigate | data via credential-free getTopology (SOLVER_BASE_URL); no operator token / @daml/react context |
</threat_model>

<verification>
- `cd web && npm run build` + `cd web && npx vitest run` green (incl. Topology.test.tsx).
- The demo-real badge string is present and non-removable.
- Live 3-node hosting + cross-node atomic settle across a booted xnode LocalNet = live-UAT (SUMMARY).
</verification>

<success_criteria>
View 07 renders three participant nodes hosting redacted orders + an inverted synchronizer node,
lights red edges + an AtomicStamp spanning all nodes on settle, and carries a non-removable
`DEMO-REAL · SINGLE-OPERATOR LOCALNET` honesty badge — bound to the UI-SPEC tokens and routed in App.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `web/src/components/TopologyNode.tsx` — participant/operator node card (redaction stripe + PARTICIPANT caption).
- `web/src/views/TopologyView.tsx` — view 07 (chrome + node grid + SVG edges + demo-real badge + AtomicStamp) + pure `nodesFromHosting`/`edges`/`demoRealCaption` core.
- `web/src/views/Topology.test.tsx` — single/distributed + no-rival-contents tests.
- `web/src/App.tsx` — `screen==='topology'` render branch.
</artifacts>

<output>
Create `.planning/phases/11-settlement-institutional-grade/11-09-SUMMARY.md` when done.
</output>
