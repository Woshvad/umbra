---
phase: 11-settlement-institutional-grade
plan: 10
type: execute
wave: 6
depends_on: ["11-03", "11-08"]
files_modified:
  - web/src/views/JoinView.tsx
  - web/src/components/QrJoin.tsx
  - web/src/lib/qrPayload.ts
  - web/src/lib/qrPayload.test.ts
  - web/src/views/TheatreView.tsx
  - web/src/main.tsx
  - web/package.json
autonomous: false
requirements: [WOW-07, COMP-01]
must_haves:
  truths:
    - "A QR block on 03 Theatre encodes only the /join URL + roundId — never the guest token"
    - "A mobile-first /join route lets the guest submit ONE sealed bid on its own scoped token"
    - "After settle the guest sees ONLY its own fill (structural per-party privacy)"
    - "An ineligible guest submit renders the on-ledger reject VERBATIM (COMP-01), not a render guard"
    - "Both surfaces carry the HARD DEV TOKEN — PRODUCTION GUEST AUTH IS OIDC (PHASE 12) label"
  artifacts:
    - path: "web/src/views/JoinView.tsx"
      provides: "mobile /join guest desk (OrderTicket + FillCard reuse, own scoped token, SEAL GUEST ORDER)"
      contains: "SEAL GUEST ORDER"
    - path: "web/src/components/QrJoin.tsx"
      provides: "SVG QR (ink-on-paper) + honesty label, no secret in payload"
      contains: "OIDC"
    - path: "web/src/lib/qrPayload.ts"
      provides: "pure join-URL+roundId payload builder (no-secret assertion)"
      contains: "roundId"
  key_links:
    - from: "web/src/views/JoinView.tsx"
      to: "web/src/ledgerContexts.ts ctxD"
      via: "guest mounts inside ctxD.DamlLedger with the bankD token"
      pattern: "ctxD"
    - from: "web/src/components/QrJoin.tsx"
      to: "web/src/lib/qrPayload.ts"
      via: "QR encodes buildJoinPayload(roundId) — URL + roundId only"
      pattern: "buildJoinPayload"
---

<objective>
Ship the 4th-desk guest join (WOW-07): a QR block on `03 Theatre` encoding only the `/join` URL +
roundId (never the token), and a mobile-first `/join` route that reuses `OrderTicket` + the private
`FillCard` on the guest's OWN scoped token so it submits one sealed bid and sees only its own fill.
Both surfaces carry the HARD `DEV TOKEN — PRODUCTION GUEST AUTH IS OIDC (PHASE 12)` label; an
ineligible submit renders the on-ledger reject verbatim (COMP-01). Includes the plan-time
package-legitimacy gate for the QR generator.

Purpose: WOW-07 (guest QR → mobile own-fill) + COMP-01 (verbatim on-ledger reject surface).
Output: `JoinView.tsx`, `QrJoin.tsx`, pure `qrPayload.ts` (+test), Theatre QR host, `/join` route.
This plan has a blocking package-legitimacy checkpoint → `autonomous: false`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-settlement-institutional-grade/11-UI-SPEC.md
@.planning/phases/11-settlement-institutional-grade/11-RESEARCH.md
@.planning/phases/11-settlement-institutional-grade/11-PATTERNS.md
@web/src/views/DeskView.tsx
@web/src/components/OrderTicket.tsx
@web/src/components/FillCard.tsx
@web/src/views/TheatreView.tsx
@web/src/main.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy gate — qrcode.react (or hand-rolled SVG fallback)</name>
  <action>Pause for the plan-time package-legitimacy gate (11-UI-SPEC requirement): the human confirms qrcode.react@4.2.0's registry metadata (publisher, permissive license, downloads, no install/telemetry) and either approves the install or directs the zero-dep hand-rolled SVG fallback. No code is written in this task.</action>
  <what-built>
    Nothing installed yet. This is the 11-UI-SPEC plan-time package-legitimacy gate (required before
    the QR dependency enters the plan). 11-RESEARCH Package Legitimacy Audit already vetted
    `qrcode.react@4.2.0`: npm registry, ~11 years old (created 2014), 6.46M weekly downloads,
    source github.com/zpao/qrcode.react, permissive license (ISC), publisher zpao/react-community,
    SVG output (`QRCodeSVG`) → stylable ink-on-paper, no network/telemetry. Verdict: Approved.
  </what-built>
  <how-to-verify>
    1. Confirm on npmjs.com/package/qrcode.react: version 4.2.0, permissive license, publisher, weekly downloads, last-publish recency.
    2. Confirm no install/postinstall script and no network/telemetry.
    3. Choose ONE: (a) approve `npm install qrcode.react@4.2.0 --legacy-peer-deps` (the same peer-dep convention as @daml/react); OR (b) direct the executor to use the hand-rolled SVG QR encoder fallback (zero new dep) — both are honest per UI-SPEC.
  </how-to-verify>
  <resume-signal>Type "approve qrcode.react" or "use hand-rolled SVG" (or describe concerns).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: qrPayload.ts (no-secret payload) + QrJoin.tsx (ink-on-paper SVG + honesty label)</name>
  <files>web/src/lib/qrPayload.ts, web/src/lib/qrPayload.test.ts, web/src/components/QrJoin.tsx, web/package.json</files>
  <read_first>
    - 11-UI-SPEC "WOW-07 — Guest QR host" (QR contrast rule: ink #0A0A0A modules on paper #F4F1EA quiet-zone; SVG not <img>; no secret in payload; honesty label copy)
    - 11-RESEARCH Package Legitimacy Audit (qrcode.react) + Security Domain (guest token leakage → QR carries only URL+roundId)
    - web/src/index.css (scoped token classes) — reuse inherited tokens only
  </read_first>
  <action>
    Create pure `web/src/lib/qrPayload.ts`: `buildJoinPayload(origin, roundId)` → the `/join` URL
    with a `?round=<id>` query — URL + roundId ONLY, never a token/secret. Add
    `qrPayload.test.ts` asserting the payload contains the round id + `/join` and NEVER a JWT-like
    substring (no `eyJ` / bearer / token). Create `QrJoin.tsx` rendering an SVG QR of that payload —
    if Task 1 approved qrcode.react, use `QRCodeSVG` (`level="M"`, ink `#0A0A0A` modules, paper
    `#F4F1EA` background + quiet zone, no external fetch); if the fallback was chosen, use the
    zero-dep hand-rolled SVG encoder. Add the heading `SCAN TO JOIN AS A 4TH DESK`, the sub-caption,
    and the HARD honesty label `DEV TOKEN — PRODUCTION GUEST AUTH IS OIDC (PHASE 12)` in the
    red-square mono-9 tag grammar. If qrcode.react was approved, add it to `web/package.json` and
    install with `--legacy-peer-deps`. Directive prose only; no fenced code.
  </action>
  <acceptance_criteria>
    - `buildJoinPayload` yields a URL + roundId with NO token; `qrPayload.test.ts` asserts no `eyJ`/bearer substring.
    - `QrJoin` renders an SVG (not <img>) with ink modules on paper + the OIDC honesty label; no external fetch.
    - `cd web && npx vitest run qrPayload` + `cd web && npm run build` green.
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx vitest run qrPayload 2>&1 | tail -5 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>A no-secret QR payload + an ink-on-paper SVG QR with the OIDC honesty label render and pass the no-secret test.</done>
</task>

<task type="auto">
  <name>Task 3: JoinView.tsx (/join mobile guest desk) + Theatre QR host + /join route</name>
  <files>web/src/views/JoinView.tsx, web/src/views/TheatreView.tsx, web/src/main.tsx</files>
  <read_first>
    - web/src/views/DeskView.tsx lines 23-108 (per-party plane: DeskBody inside ctx.DamlLedger; OrderTicket + FillCard reuse — the exact pattern to mirror for the guest on ctxD)
    - web/src/components/OrderTicket.tsx (the one-per-round lock + seal-wipe + CTA relabel seam) + FillCard.tsx (YOUR FILL / Visible only to you)
    - 11-UI-SPEC "WOW-07 — mobile /join route" (mobile-first layout, max-width 480px, 16px padding, text-40 headline, JOIN THE DARK., SEAL GUEST ORDER, honesty label, COMP-01 verbatim reject copy) + "03 Theatre QR host block"
    - web/src/main.tsx (the render entry — how to branch /join by window.location.pathname without a router lib)
  </read_first>
  <action>
    Create `JoinView.tsx`: a mobile-first single-column (`max-width:480px`, 16px padding) guest desk
    that mounts inside `ctxD.DamlLedger` with the bankD scoped token (per-party plane — every hook
    reads only the guest's own contracts; structural privacy identical to A/B/C). Minimal chrome
    (`UMBRA · GUEST` row + `DESK D · GUEST` tag + the HARD `DEV SCOPED TOKEN — NO SECRET IN THE QR ·
    REAL GUEST AUTH IS OIDC (PHASE 12)` label), headline `JOIN THE DARK.` (`text-40`), then reuse
    `OrderTicket` with the CTA relabeled `SEAL GUEST ORDER` (one-per-round lock + seal-wipe
    unchanged; `onParse` never auto-seals), and `FillCard` (`YOUR FILL` / `Visible only to you`)
    after settle — the guest can never see another desk's order/fill. COMP-01: if a submit hits an
    ineligible party, render the on-ledger reject VERBATIM on a 1px-ink evidence surface (heading
    `SUBMISSION REJECTED — NOT ELIGIBLE` + the raw assertMsg text + the stub sub-caption) — never a
    render-time guard. Touch targets ≥ 44px (existing paddings satisfy — do not shrink).

    On `03 Theatre` (TheatreView.tsx) add the QR host block BELOW the dark stage (does not disturb
    the countdown/reveal beat): heading + `QrJoin` + the honesty label. In `main.tsx`, branch on
    `window.location.pathname === '/join'` (or a `?join` marker) to render `JoinView` standalone
    (no desktop Header/Nav) instead of `App`. Reuse only inherited tokens/keyframes. Directive prose
    only; no fenced code.
  </action>
  <acceptance_criteria>
    - `/join` renders the mobile guest desk on ctxD (own scoped token), with `SEAL GUEST ORDER` + the DEV-token honesty label + FillCard own-fill.
    - An ineligible submit renders the verbatim on-ledger reject (`SUBMISSION REJECTED — NOT ELIGIBLE` + raw assertMsg), not a render guard.
    - The Theatre QR host block renders `QrJoin` below the stage without disturbing the countdown/reveal.
    - `cd web && npm run build` + `cd web && npx vitest run` green.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "SEAL GUEST ORDER" web/src/views/JoinView.tsx && grep -c "SUBMISSION REJECTED — NOT ELIGIBLE" web/src/views/JoinView.tsx && cd web && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>The guest joins via QR → mobile /join, submits one sealed bid on its own token, sees only its own fill, and ineligible rejects render verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QR payload ↔ guest token | the QR/URL carries only /join URL + roundId; the scoped token is delivered server-side, never in the QR |
| guest ↔ rivals | the guest's order/fill is visible only to guest+operator (structural per-party plane) |
| npm registry ↔ repo | QR generator dependency (supply chain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10-QR | Information Disclosure | guest token in QR/URL | mitigate | qrPayload carries only URL+roundId; qrPayload.test asserts no JWT/bearer substring |
| T-11-10-PRIV | Information Disclosure | guest sees rival contents | mitigate | JoinView mounts on ctxD (own token) — structural per-party privacy, not render filter |
| T-11-10-COMP | Elevation | ineligible guest submits | mitigate | on-ledger reject rendered VERBATIM (COMP-01), never a render-time guard |
| T-11-10-AUTH | Repudiation (trust) | over-claiming production guest auth | mitigate | HARD `DEV TOKEN — PRODUCTION GUEST AUTH IS OIDC (PHASE 12)` label on both QR host + /join |
| T-11-10-SC | Tampering | qrcode.react install | mitigate | blocking package-legitimacy checkpoint (Task 1); metadata-verified (6.46M/wk, ISC, no telemetry); hand-rolled SVG fallback if not approved |
</threat_model>

<verification>
- `cd web && npm run build` + `cd web && npx vitest run` green (incl. qrPayload.test.ts).
- QR payload contains no token (no `eyJ`/bearer); both surfaces show the OIDC honesty label.
- Live QR-on-a-real-phone + guest submit against a booted LocalNet = live-UAT (SUMMARY).
</verification>

<success_criteria>
A guest scans a QR on 03 Theatre (URL + roundId only, no token), lands on a mobile /join page,
submits one sealed bid on its own scoped token, and sees only its own fill; ineligible submits
render the verbatim on-ledger reject; both surfaces carry the DEV-token OIDC honesty label; the QR
generator passed the plan-time legitimacy gate.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `web/src/lib/qrPayload.ts` (+ `qrPayload.test.ts`) — `buildJoinPayload` (URL + roundId, no secret).
- `web/src/components/QrJoin.tsx` — SVG QR (ink-on-paper) + `DEV TOKEN — PRODUCTION GUEST AUTH IS OIDC (PHASE 12)` label.
- `web/src/views/JoinView.tsx` — mobile /join guest desk (ctxD, `SEAL GUEST ORDER`, FillCard own-fill, verbatim COMP-01 reject).
- `web/src/views/TheatreView.tsx` — QR host block below the stage.
- `web/src/main.tsx` — `/join` standalone route.
- `web/package.json` — `qrcode.react@4.2.0` (if approved).
</artifacts>

<output>
Create `.planning/phases/11-settlement-institutional-grade/11-10-SUMMARY.md` when done.
</output>
