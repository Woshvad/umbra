---
phase: 11-settlement-institutional-grade
plan: 11
type: execute
wave: 6
depends_on: ["11-07", "11-08"]
files_modified:
  - web/src/views/SettlementView.tsx
  - web/src/components/DvpLegs.tsx
autonomous: true
requirements: [DFIN-01, DFIN-02, DFIN-03, WOW-07]
must_haves:
  truths:
    - "The settlement legs sub-label reads Delivery vs Payment · Batch/Instruction · {N} instructions with the ALLOCATED → APPROVED → SETTLED finality micro-grammar"
    - "A HARD provenance tag reads CN TOKEN STANDARD (CIP-0056) or DAML-FINANCE-PATTERN (IN-REPO), driven by data — never DAML FINANCE"
    - "A NETTED ⇄ GROSS LEGS toggle (default NETTED) renders net-per-party or gross legs, conserving totals"
    - "The cash arrow reads the instrument symbol from data (defaults USDCx), never a hardcoded literal; GUEST row shows when present"
    - "The §4 fixture still renders $100.00 / A=10 · B=8 · C=2 with the simultaneous-settle beat intact"
  artifacts:
    - path: "web/src/views/SettlementView.tsx"
      provides: "Batch/finality/netting/provenance/token-agnostic/guest deltas on view 05"
      contains: "ALLOCATED → APPROVED → SETTLED"
    - path: "web/src/components/DvpLegs.tsx"
      provides: "instruction-count sub-label + token-agnostic cash symbol + netted/gross legs"
      contains: "instructions"
  key_links:
    - from: "web/src/views/SettlementView.tsx"
      to: "web/src/solver.ts settlement-meta"
      via: "provenance tag + netting + cash symbol read from the settle/preview meta"
      pattern: "provenance|netted|cashSymbol"
---

<objective>
Apply the settlement deltas to view 05 (SettlementView / DvpLegs) that surface the Phase-11
production-grade settlement: the Batch/Instruction sub-label + `ALLOCATED → APPROVED → SETTLED`
finality micro-grammar, the HARD data-driven provenance tag (`CN TOKEN STANDARD (CIP-0056)` or
`DAML-FINANCE-PATTERN (IN-REPO)` — never `DAML FINANCE`), a `NETTED ⇄ GROSS LEGS` toggle
(default NETTED), a token-agnostic cash symbol read from data, and the guest (Desk D) row when
present — all while preserving the shipped simultaneous-settle beat and the §4 canary render.

Purpose: DFIN-01 (finality grammar + provenance) + DFIN-02 (netting toggle) + DFIN-03 (token-agnostic
cash symbol) + WOW-07 (guest aggregate row). Bound to 11-UI-SPEC.
Output: SettlementView + DvpLegs deltas; §4 still renders $100.00 / A=10·B=8·C=2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-settlement-institutional-grade/11-UI-SPEC.md
@.planning/phases/11-settlement-institutional-grade/11-PATTERNS.md
@web/src/views/SettlementView.tsx
@web/src/components/DvpLegs.tsx
@web/src/components/BalanceTable.tsx
@web/src/solver.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DvpLegs.tsx — Batch/instruction sub-label + finality grammar + token-agnostic cash symbol + netted/gross</name>
  <files>web/src/components/DvpLegs.tsx</files>
  <read_first>
    - web/src/components/DvpLegs.tsx (whole file — the `Delivery vs Payment · {legs.length} legs` sub-label, the `← {cash} USDCx` hardcoded literal to make token-agnostic, the shared settleProgress track — never sequence legs)
    - 11-UI-SPEC "Settlement deltas" (sub-label `Delivery vs Payment · Batch/Instruction · {N} instructions` — UI-SPEC lines 149/233/236 superseded by the RESEARCH RECONCILIATION: never render the literal "Daml Finance"; finality `ALLOCATED → APPROVED → SETTLED`, netting toggle, token-agnostic `← {cash} {CASH_SYMBOL}`)
    - web/src/solver.ts settlement-meta types (from 11-08)
  </read_first>
  <action>
    Update `DvpLegs` props to accept the instruction count `N`, the netted/gross mode, and the cash
    instrument `symbol` (default `USDCx`). Relabel the sub-label to `Delivery vs Payment · Batch/Instruction
    · {N} instructions` (NEVER the literal "Daml Finance" — the library is unsatisfiable on this stack; the honest provenance tag is `CN TOKEN STANDARD (CIP-0056)` / `DAML-FINANCE-PATTERN (IN-REPO)`) and add the `ALLOCATED → APPROVED → SETTLED` finality
    micro-grammar (mono-9, opacity .6). Replace the hardcoded `← {cash} USDCx` with `← {cash}
    {symbol}` read from data (DFIN-03 — no hardcoded literal). Support NETTED (one net leg per
    party·instrument) vs GROSS (per buyer↔seller legs) rendering off the passed legs; keep the
    SINGLE shared `settleProgress` clock so all legs snap simultaneously (never sequence). Reuse
    only inherited tokens/keyframes. Directive prose only; no fenced code.
  </action>
  <acceptance_criteria>
    - The sub-label reads `Delivery vs Payment · Batch/Instruction · {N} instructions`; the finality grammar `ALLOCATED → APPROVED → SETTLED` renders.
    - The cash arrow reads `← {cash} {symbol}` from data via a `{symbol}`/`{cashSymbol}` binding (no hardcoded `USDCx` literal in the arrow label).
    - The literal string "Daml Finance" appears NOWHERE in DvpLegs.tsx or SettlementView.tsx (honest provenance; the library is unsatisfiable on this stack).
    - NETTED and GROSS both render off the shared settleProgress (legs still simultaneous).
    - `cd web && npm run build` green.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "ALLOCATED → APPROVED → SETTLED" web/src/components/DvpLegs.tsx && ! grep -rq "Daml Finance" web/src/components/DvpLegs.tsx web/src/views/SettlementView.tsx && grep -Eq "\{[[:space:]]*(cash)?[Ss]ymbol[[:space:]]*\}" web/src/components/DvpLegs.tsx && cd web && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>DvpLegs shows Batch/instruction finality grammar, a token-agnostic cash symbol, and netted/gross legs on the shared simultaneous clock.</done>
</task>

<task type="auto">
  <name>Task 2: SettlementView.tsx — provenance tag + netting toggle + guest row; §4 canary intact</name>
  <files>web/src/views/SettlementView.tsx</files>
  <read_first>
    - web/src/views/SettlementView.tsx (whole file — the rAF simultaneous-settle beat lines 104-155, legsFromPreview/balanceRowsFromPreview, the BEFORE §4 balances + DESK_ORDER; the post-settle block where the tag/toggle mount)
    - 11-UI-SPEC "Settlement deltas" (provenance tag HARD honesty, NETTED ⇄ GROSS toggle default ON, guest own-fill on settle, §4 stays single-buyer-shaped rendering $100.00) + "Honesty Labeling Contract"
    - web/src/solver.ts settlement-meta (provenance, instructionCount, netted, cashSymbol) from 11-08
  </read_first>
  <action>
    In SettlementView: read the settlement-meta from the settle/preview response and (a) render a
    mono-9 ink-border provenance tag reading `CN TOKEN STANDARD (CIP-0056)` or
    `DAML-FINANCE-PATTERN (IN-REPO)` driven by data — NEVER `DAML FINANCE` the library (HARD honesty
    label per D13 / UI-SPEC); (b) add a `NETTED ⇄ GROSS LEGS` mono tag toggle (default NETTED) that
    switches the legs passed to `DvpLegs` between net-per-party and gross while the balances table
    totals stay conserved; (c) pass the instruction count + cash symbol through to `DvpLegs`; (d)
    include the guest (`GUEST` / Desk D) in the aggregate legs + balance rows WHEN present (the guest
    still sees only its own leg via /join). PRESERVE the single-rAF simultaneous-settle beat + the
    §4 BEFORE balances so §4 still renders `$100.00` / A=10·B=8·C=2 (continuous canary) — do NOT
    disturb the shipped reveal/settle motion. Reuse only inherited tokens/keyframes; no new tailwind
    literal. Directive prose only; no fenced code.
  </action>
  <acceptance_criteria>
    - The provenance tag renders `CN TOKEN STANDARD (CIP-0056)` or `DAML-FINANCE-PATTERN (IN-REPO)` from data; the string `DAML FINANCE` (as a standalone library claim) never renders.
    - A `NETTED ⇄ GROSS LEGS` toggle (default NETTED) switches legs while balance totals stay conserved.
    - The guest row appears in the aggregate when present; §4 still renders $100.00 / A=10·B=8·C=2 with the simultaneous-settle beat intact.
    - `cd web && npm run build` + `cd web && npx vitest run` green.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "DAML-FINANCE-PATTERN (IN-REPO)\|CN TOKEN STANDARD (CIP-0056)" web/src/views/SettlementView.tsx && grep -c "NETTED" web/src/views/SettlementView.tsx && cd web && npm run build 2>&1 | tail -3 && npx vitest run 2>&1 | tail -4</automated>
  </verify>
  <done>View 05 surfaces the honest provenance tag, netting toggle, finality grammar, token-agnostic cash + guest row, with the §4 canary render + simultaneous-settle beat intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settlement render ↔ provenance truth | the provenance tag must reflect the ACTUAL shipped settlement layer (D13), never over-claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11-PROV | Repudiation (trust) | mislabeled settlement provenance | mitigate | data-driven tag `CN TOKEN STANDARD (CIP-0056)` / `DAML-FINANCE-PATTERN (IN-REPO)`; `DAML FINANCE` the library never rendered (D13) |
| T-11-11-NET | Tampering | netting toggle drifts balances | mitigate | net vs gross both conserve balance-table totals; §4 canary render is the gate |
| T-11-11-CANARY | Tampering | §4 render regression / broken settle beat | mitigate | preserve the single-rAF simultaneous beat + §4 BEFORE balances; web build + vitest gate |
| T-11-11-CRED | Information Disclosure | operator token in bundle | mitigate | SettlementView stays on the solver plane via solver.ts — no operator token / @daml/react context |
</threat_model>

<verification>
- `cd web && npm run build` + `cd web && npx vitest run` green.
- The provenance tag + netting toggle + finality grammar + token-agnostic cash render; §4 still $100.00 / A=10·B=8·C=2.
- Live settled-round render (Batch legs, provenance, netting, guest row) against a booted stack = live-UAT (SUMMARY).
</verification>

<success_criteria>
View 05 surfaces the Batch/Instruction finality grammar, a data-driven honest provenance tag
(never the Daml Finance library), a default-on netting toggle that conserves, a token-agnostic cash
symbol, and the guest aggregate row — while the §4 fixture still renders $100.00 / A=10 · B=8 · C=2
with the simultaneous-settle beat intact.
</success_criteria>

<artifacts>
## Artifacts this plan produces
- `web/src/components/DvpLegs.tsx` — Batch/`{N} instructions` sub-label, `ALLOCATED → APPROVED → SETTLED` grammar, token-agnostic `← {cash} {symbol}`, netted/gross rendering.
- `web/src/views/SettlementView.tsx` — provenance tag (D13-honest), `NETTED ⇄ GROSS LEGS` toggle, instruction count + cash symbol wiring, guest aggregate row; §4 canary render preserved.
</artifacts>

<output>
Create `.planning/phases/11-settlement-institutional-grade/11-11-SUMMARY.md` when done.
</output>
