---
phase: 13-platform-baseline-adjacent-track-b-ongoing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - solver/package.json
  - solver/package-lock.json
  - solver/src/telemetry.ts
  - solver/src/telemetry.test.ts
  - solver/src/logger.ts
  - solver/src/logger.test.ts
  - alerts/umbra-rules.yml
autonomous: false
requirements: [OPS-01]
user_setup:
  - service: opentelemetry-collector
    why: "Live OTLP export + Grafana/Prometheus dashboards (UAT only — console exporter is the offline default)"
    env_vars:
      - name: OTEL_EXPORTER_OTLP_ENDPOINT
        source: "Your OTLP collector URL (unset in dev → ConsoleSpanExporter/ConsoleMetricExporter)"
must_haves:
  truths:
    - "With OTEL_EXPORTER_OTLP_ENDPOINT UNSET, telemetry initializes against ConsoleSpanExporter + ConsoleMetricExporter (no collector needed)"
    - "Manual nested spans carry a round.id attribute; the ledger-exercise span is labeled a CLIENT span (not in-participant)"
    - "The JSON logger redacts token/key/authorization/secret/env fields; every line is JSON with ts/level/msg"
    - "Metric instruments umbra.rounds.opened / umbra.clear.latency_ms / umbra.settle.latency_ms / umbra.agent.verified_total / umbra.webhook.delivery_total exist"
  artifacts:
    - path: "solver/src/telemetry.ts"
      provides: "NodeSDK boot (OTLP-or-console), tracer + span helpers, meter + named instruments"
      exports: ["initTelemetry", "shutdownTelemetry", "withSpan", "instruments"]
    - path: "solver/src/logger.ts"
      provides: "Secret-redacting JSON logger"
      exports: ["log", "redact"]
    - path: "alerts/umbra-rules.yml"
      provides: "Prometheus/Alertmanager threshold rules (config-only, not delivered live)"
      contains: "solver-down"
  key_links:
    - from: "solver/src/telemetry.ts"
      to: "@opentelemetry/sdk-node NodeSDK"
      via: "exporter selection on OTEL_EXPORTER_OTLP_ENDPOINT presence"
      pattern: "OTEL_EXPORTER_OTLP_ENDPOINT"
    - from: "solver/src/logger.ts"
      to: "@opentelemetry/api trace.getActiveSpan"
      via: "trace.id correlation"
      pattern: "getActiveSpan"
---

<objective>
Establish OPS-01 observability primitives as standalone, unit-tested solver modules: an OpenTelemetry NodeSDK bootstrap that degrades to console exporters when no collector is configured, a zero-dependency secret-redacting JSON logger, and a committed Prometheus/Alertmanager alert-rules file. This is the ONLY plan in Phase 13 that adds a new npm dependency (the five official CNCF OpenTelemetry packages), so it is gated by a dependency-legitimacy checkpoint before install.

Purpose: give the solver production-grade tracing/metrics/logging that works offline (console fallback) and against a real collector (OTLP) at UAT, without leaking secrets.
Output: `solver/src/telemetry.ts` (+test), `solver/src/logger.ts` (+test), `alerts/umbra-rules.yml`, five OTel deps in `solver/package.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-RESEARCH.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-PATTERNS.md
@solver/src/agent.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Dependency-legitimacy checkpoint for the 5 OpenTelemetry packages</name>
  <what-built>Nothing yet — this gate MUST pass before the OTel install task runs. RESEARCH tagged all five OTel packages `[ASSUMED]` because slopcheck was unavailable at research time (13-RESEARCH.md "## Package Legitimacy Audit").</what-built>
  <read_first>
    - .planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-RESEARCH.md lines 79-92 (Package Legitimacy Audit table + no-postinstall note)
  </read_first>
  <action>Human-verify each of the five `@opentelemetry/*` packages is legitimate BEFORE the install task runs — confirm the official CNCF `open-telemetry` npm org + canonical source repo and an empty `scripts.postinstall`, then approve. This gate is blocking and never auto-approvable (threat T-13-SC); a `[SLOP]`/`[SUS]` verdict on any package stops the plan.</action>
  <how-to-verify>
    1. Confirm each package resolves to the official CNCF org page on npmjs.com:
       - https://www.npmjs.com/package/@opentelemetry/api
       - https://www.npmjs.com/package/@opentelemetry/sdk-node
       - https://www.npmjs.com/package/@opentelemetry/exporter-trace-otlp-http
       - https://www.npmjs.com/package/@opentelemetry/exporter-metrics-otlp-http
       - https://www.npmjs.com/package/@opentelemetry/sdk-metrics
    2. Confirm publisher scope is `open-telemetry` and source repo is github.com/open-telemetry/opentelemetry-js.
    3. Confirm no install-time script: `npm view @opentelemetry/sdk-node scripts.postinstall` returns empty for each package.
  </how-to-verify>
  <acceptance_criteria>
    - All 5 packages confirmed under the official `open-telemetry` npm org with the canonical repo.
    - `npm view <pkg> scripts.postinstall` is empty for all 5 packages.
    - Threat T-13-SC is dispositioned before any install runs.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed to the install, or list any package to remove/replace.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install OTel deps + telemetry.ts (NodeSDK OTLP-or-console) + logger.ts</name>
  <files>solver/package.json, solver/package-lock.json, solver/src/telemetry.ts, solver/src/telemetry.test.ts, solver/src/logger.ts, solver/src/logger.test.ts</files>
  <read_first>
    - 13-RESEARCH.md lines 62-71 (exact install command, --legacy-peer-deps), lines 165-222 (Patterns 1-3), lines 336-345 (Pitfall 1-2)
    - 13-PATTERNS.md lines 99-105 (telemetry+logger analog: agent.ts boot-resource DI + secret-free logging)
    - solver/src/agent.ts lines 210-215 (module-scope `_client` boot-once resource) and lines 142-148 (secret-free `console.error` discipline — fixed string + err.name only)
  </read_first>
  <action>
    From `solver/`, run the RESEARCH install command with `--legacy-peer-deps` (the established `@anthropic-ai/sdk`/`@daml/react` convention) pinning `@opentelemetry/api@1.9.1`, `@opentelemetry/sdk-node@0.220.0`, `@opentelemetry/exporter-trace-otlp-http@0.220.0`, `@opentelemetry/exporter-metrics-otlp-http@0.220.0`, `@opentelemetry/sdk-metrics@2.9.0`. Verify the `resourceFromAttributes` and `ConsoleSpanExporter` import paths against the installed `.d.ts` files before relying on them (Pitfall 2 — the 0.2xx line drifts).
    Create `solver/src/telemetry.ts` as a boot-once module (mirror agent.ts `_client`): export `initTelemetry()` that constructs a `NodeSDK` selecting `OTLPTraceExporter`/`OTLPMetricExporter` when `process.env.OTEL_EXPORTER_OTLP_ENDPOINT` is set, else `ConsoleSpanExporter`/`ConsoleMetricExporter` wrapped in a `PeriodicExportingMetricReader`; export `shutdownTelemetry()` (calls `sdk.shutdown()`); export `withSpan(name, roundId, fn)` using `trace.getTracer('umbra-solver').startActiveSpan` that sets attribute `round.id` and, for the ledger leg, `span.kind=client`; export `instruments` — a record of `meter.createCounter('umbra.rounds.opened')`, `meter.createHistogram('umbra.clear.latency_ms')`, `meter.createHistogram('umbra.settle.latency_ms')`, `meter.createCounter('umbra.agent.verified_total')`, `meter.createCounter('umbra.webhook.delivery_total')`. Accept an optional injected exporter/reader parameter so the test drives a fake with no live collector.
    Create `solver/src/logger.ts`: export `redact(obj)` recursively replacing values whose key matches `/^(.*token.*|.*key.*|authorization|cookie|secret|password|env)$/i` with `'[REDACTED]'`, and `log(level, msg, fields)` emitting `JSON.stringify({ ts: ISO, level, msg, ...redact(fields) })`, adding `trace.id` from `trace.getActiveSpan()?.spanContext().traceId` when present. Never log `err.message` or a raw error — callers pass `err.name` only (agent.ts discipline).
    Write co-located `telemetry.test.ts` (inject a fake span exporter + metric reader; assert console-fallback selected when the env var is unset, a span carries `round.id`, the ledger span is `client`-kind, and the five instruments exist) and `logger.test.ts` (assert token/key/authorization/secret/env keys are `[REDACTED]`, non-secret keys pass through, output parses as JSON with ts/level/msg).
    NEVER inline OTel SDK implementation code in this action — verify import paths at implement time.
  </action>
  <acceptance_criteria>
    - `solver/package.json` lists all five `@opentelemetry/*` packages at the pinned versions.
    - `solver/src/telemetry.ts` contains `export function initTelemetry`, `export function shutdownTelemetry`, `export function withSpan`, `export const instruments`.
    - `solver/src/telemetry.ts` selects `ConsoleSpanExporter` when `OTEL_EXPORTER_OTLP_ENDPOINT` is unset (assert via injected-exporter test).
    - `solver/src/logger.ts` contains `export const log` and `export const redact`; a field named `authorization` (and `apiKey`, `operatorToken`, `env`) serializes as `[REDACTED]`.
    - `npx vitest run src/telemetry.test.ts src/logger.test.ts` exits 0.
    - `npm run typecheck` (or `npx tsc --noEmit`) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/telemetry.test.ts src/logger.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Telemetry + logger modules exist, unit-tested, with the console fallback proven and secret redaction proven; five OTel deps installed.</done>
</task>

<task type="auto">
  <name>Task 3: alerts/umbra-rules.yml threshold config</name>
  <files>alerts/umbra-rules.yml</files>
  <read_first>
    - 13-RESEARCH.md line 136 (alert-rules responsibility), lines 41, 483 (thresholds: solver-down, clear-latency-p95, webhook-failure-rate; live delivery is a UAT gate)
    - 13-PATTERNS.md lines 163-165 (No Analog — author from RESEARCH thresholds, config-only)
  </read_first>
  <action>
    Create `alerts/umbra-rules.yml` as a Prometheus/Alertmanager-style rule group named `umbra` containing at least three alert rules: `SolverDown` (venue up gauge == 0 for 1m), `ClearLatencyP95High` (histogram_quantile 0.95 of `umbra_clear_latency_ms` over 5m above a threshold), and `WebhookFailureRateHigh` (ratio of `umbra_webhook_delivery_total{status="failed"}` over total above a threshold). Each rule carries `severity` and an honest annotation noting live delivery is a UAT/ops gate (no Alertmanager runs in-repo). Keep it valid YAML that a Prometheus config loader would accept.
  </action>
  <acceptance_criteria>
    - `alerts/umbra-rules.yml` parses as valid YAML.
    - The file contains alert names `SolverDown`, `ClearLatencyP95High`, `WebhookFailureRateHigh`.
    - Each alert references a metric name matching the instruments defined in telemetry.ts (umbra.rounds/clear/settle/agent/webhook, dot→underscore in PromQL).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('alerts/umbra-rules.yml','utf8'); for (const n of ['SolverDown','ClearLatencyP95High','WebhookFailureRateHigh']) if(!y.includes(n)) throw new Error('missing '+n); console.log('ok')"</automated>
  </verify>
  <done>Alert-rules YAML committed with the three baseline thresholds; live delivery labeled a UAT gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → solver build | Untrusted package code enters at install |
| span attributes / log fields → exporter/console | New serialization surface for secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-SC | Tampering | npm OTel installs | mitigate | Blocking dependency-legitimacy checkpoint (Task 1) verifies official CNCF org + no postinstall before install; `[ASSUMED]` packages are never auto-approved |
| T-13-01 | Information Disclosure | span attributes / log fields (telemetry.ts, logger.ts) | mitigate | Redacting logger drops token/key/authorization/secret/env keys; only round.id + non-secret fields set as span attributes; unit test asserts redaction |
| T-13-02 | Information Disclosure | error logging | mitigate | Log a fixed string + err.name only, never err.message / raw object (agent.ts discipline) |
</threat_model>

<verification>
- `cd solver && npx vitest run src/telemetry.test.ts src/logger.test.ts` — green.
- `cd solver && npx tsc --noEmit` — clean.
- `node -e` YAML check on alerts/umbra-rules.yml — ok.
- Confirm no secret sentinel appears in span attributes or logger output (redaction test).
</verification>

<success_criteria>
- Console-fallback telemetry works with no collector; five named instruments exist; redacting logger proven; alert rules committed. §4 fixture untouched (no clearing/settlement code changed).
</success_criteria>

## Artifacts this phase produces (this plan)

- Files: `solver/src/telemetry.ts`, `solver/src/telemetry.test.ts`, `solver/src/logger.ts`, `solver/src/logger.test.ts`, `alerts/umbra-rules.yml`
- Exports (telemetry.ts): `initTelemetry()`, `shutdownTelemetry()`, `withSpan(name, roundId, fn)`, `instruments`
- Exports (logger.ts): `log(level, msg, fields)`, `redact(obj)`
- Metric instrument names: `umbra.rounds.opened`, `umbra.clear.latency_ms`, `umbra.settle.latency_ms`, `umbra.agent.verified_total`, `umbra.webhook.delivery_total`
- Env vars introduced: `OTEL_EXPORTER_OTLP_ENDPOINT` (optional; unset → console exporters)
- New deps: `@opentelemetry/{api,sdk-node,exporter-trace-otlp-http,exporter-metrics-otlp-http,sdk-metrics}`
- Config: `alerts/umbra-rules.yml` (alerts `SolverDown`, `ClearLatencyP95High`, `WebhookFailureRateHigh`)

<output>
Create `.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-01-SUMMARY.md` when done.
</output>
