---
phase: 13-platform-baseline-adjacent-track-b-ongoing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - solver/src/secrets.ts
  - solver/src/secrets.test.ts
  - solver/src/status.ts
  - solver/src/status.test.ts
  - ops/vault/docker-compose.vault.yml
  - scripts/rotate-secret.mjs
  - docs/SECRETS-RUNBOOK.md
autonomous: true
requirements: [OPS-02]
user_setup:
  - service: hashicorp-vault
    why: "Live secret storage + rotation (UAT only — the `env` backend is the offline default and preserves current .env behavior)"
    env_vars:
      - name: SECRETS_PROVIDER
        source: "Set to `vault` to use HashiCorp Vault KV v2; default `env` reads .env"
      - name: VAULT_ADDR
        source: "Vault server address, e.g. http://127.0.0.1:8200 (dev compose)"
      - name: VAULT_TOKEN
        source: "Vault token (dev-mode root token labeled UNSAFE-DEV in compose)"
must_haves:
  truths:
    - "The `env` SecretsProvider backend returns current .env values byte-for-byte (default; nothing breaks when Vault is absent)"
    - "The `vault` backend reads KV v2 over raw fetch (GET /v1/secret/data/umbra/<key>) and returns only the value"
    - "A resolved secret is never returned from a serializing path, never logged, never spread into a response"
    - "GET /status JSON and /status.html expose ONLY aggregate health (venue up/down, phase, last clear price/time, uptime, build) — zero individual order data and zero secrets"
  artifacts:
    - path: "solver/src/secrets.ts"
      provides: "SecretsProvider abstraction with env|vault backends selected by SECRETS_PROVIDER"
      exports: ["createSecretsProvider", "SecretsProvider"]
    - path: "solver/src/status.ts"
      provides: "Pure /status JSON builder + /status.html brand-styled document builder (token-free, aggregate-only)"
      exports: ["buildStatus", "renderStatusHtml"]
    - path: "ops/vault/docker-compose.vault.yml"
      provides: "Dev-mode Vault service (UNSAFE-DEV root token)"
      contains: "vault"
  key_links:
    - from: "solver/src/secrets.ts vault backend"
      to: "VAULT_ADDR /v1/secret/data/umbra/<key>"
      via: "raw fetch (no node-vault SDK)"
      pattern: "v1/secret/data/umbra"
    - from: "solver/src/status.ts"
      to: "GET /status"
      via: "renderStatusHtml reads the buildStatus shape"
      pattern: "PUBLIC HEALTH"
---

<objective>
Deliver OPS-02 as standalone modules: a `SecretsProvider` abstraction (env|vault backends, raw-fetch KV v2, no SDK) that preserves current `.env` behavior by default, plus the S1 public status page — a token-free, aggregate-only `/status` JSON builder and a self-contained brand-styled `/status.html` document. Ships the dev Vault docker-compose service, a rotation script, and a runbook. Endpoint wiring into the Express app is done in plan 13-07; this plan builds and unit-tests the pure modules and infra.

Purpose: get secrets out of `.env` into a vault-capable abstraction with rotation, and expose the ONE deliberately-public health surface (S1) that never leaks a private order.
Output: `secrets.ts` (+test), `status.ts` (+test), Vault compose, `rotate-secret.mjs`, `SECRETS-RUNBOOK.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-RESEARCH.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-PATTERNS.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-UI-SPEC.md
@solver/src/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: secrets.ts — SecretsProvider (env|vault), raw-fetch KV v2, module-private</name>
  <files>solver/src/secrets.ts, solver/src/secrets.test.ts</files>
  <read_first>
    - 13-PATTERNS.md lines 29-39 (analog = auth.ts; copy module-private-secret + lazy-env + raw-fetch backend VERBATIM)
    - solver/src/auth.ts lines 32-47 (lazy `requireEnv`, secret-free throw) and lines 69-90 (raw form-POST backend; non-2xx throws `HTTP ${status}` only, response body NEVER interpolated)
    - 13-RESEARCH.md lines 44-45 (vault decision), line 60 (raw KV v2 fetch, not node-vault), lines 464-466 (Open Question 3: dev root token)
  </read_first>
  <action>
    Create `solver/src/secrets.ts` exporting an interface `SecretsProvider` with `get(name: string): Promise<string>` and a factory `createSecretsProvider()` that selects the backend by `process.env.SECRETS_PROVIDER` (`env` default | `vault`). The `env` backend returns `process.env[name]` (throwing a secret-free `${name} is unset` like auth.ts `requireEnv`) so current behavior is byte-for-byte preserved. The `vault` backend mirrors auth.ts `acquireToken`: raw `fetch` to `${VAULT_ADDR}/v1/secret/data/umbra/<name>` with header `X-Vault-Token: ${VAULT_TOKEN}` (or AppRole), non-2xx throws `Vault HTTP ${status}` (status ONLY — never interpolate the response body), and returns `json.data.data[name]` (KV v2 nesting). Read `VAULT_ADDR`/`VAULT_TOKEN` lazily inside functions; hold them module-private; NEVER return/log/spread the token. Managed secret names: `ANTHROPIC_API_KEY`, the operator token, party tokens.
    Write `secrets.test.ts`: env backend returns a set process.env value and throws secret-free on unset; vault backend (stubbed fetch returning a KV v2 `{data:{data:{KEY:'v'}}}`) returns the nested value and throws `Vault HTTP 4xx` without echoing the fetch response body; a secret-sweep asserts the VAULT_TOKEN sentinel never appears in any thrown message.
  </action>
  <acceptance_criteria>
    - `solver/src/secrets.ts` contains `export interface SecretsProvider` and `export function createSecretsProvider`.
    - Default (unset SECRETS_PROVIDER) uses the `env` backend and returns `process.env` values unchanged.
    - vault backend fetches `.../v1/secret/data/umbra/` and reads `data.data.<name>`; a 403 throws a message containing `Vault HTTP 403` and NOT the response body or the token.
    - `npx vitest run src/secrets.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/secrets.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>SecretsProvider exists with both backends, env-default preserved, vault raw-fetch KV v2, secret-free throws — all unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: status.ts — token-free aggregate /status JSON + /status.html (S1)</name>
  <files>solver/src/status.ts, solver/src/status.test.ts</files>
  <read_first>
    - 13-PATTERNS.md lines 65-71 (analog = api.ts buildIndicative aggregate discipline; token-free serving; self-contained brand HTML)
    - solver/src/api.ts lines 348-366 (buildIndicative — pure aggregate builder that withholds order-derivable data)
    - 13-UI-SPEC.md lines 123-149 (S1 spec: fields, states, layout, copy, brand tokens, honest-label tag)
  </read_first>
  <action>
    Create `solver/src/status.ts` exporting `buildStatus(input)` — a PURE function mapping venue health inputs (health: 'operational'|'degraded'|'offline', current round `phase` name only, optional `lastClearPrice`/`lastClearAt`, `uptimeSeconds`, `build` version string) to the aggregate JSON shape. It MUST accept ONLY aggregate/health inputs and MUST NOT accept or emit any individual order, desk identity, per-desk fill, bid/limit, token, or operator identifier (mirror buildIndicative's withholding discipline). Export `renderStatusHtml(status)` returning a self-contained HTML string: inlines the brand tokens as literal CSS (`background:#F4F1EA; color:#0A0A0A;`, lime `#D6FB3C` up-dot, red `#E2231A` down state), `<link>` Google Fonts Space Grotesk + IBM Plex Mono with `system-ui`/`monospace` fallbacks, the `UMBRA` wordmark at 30px Space Grotesk `-.02em`, a persistent honest-label tag `PUBLIC HEALTH · NO PRIVATE ORDER DATA`, metric rows `PHASE`/`LAST CLEAR`/`UPTIME`/`BUILD`, the health pill (`OPERATIONAL`/`DEGRADED`/`OFFLINE`), the idle empty-state (`NO ACTIVE ROUND`), and a client-side auto-refresh poll of `/status` with the feed-unreachable notice `STATUS UNAVAILABLE — cannot reach the venue. Retrying…`. No React, no Tailwind, no auth context.
    Write `status.test.ts`: assert the JSON body contains only the allowed aggregate keys and NO order/secret sentinels (order-sweep + secret-sweep); assert renderStatusHtml contains the honest tag, the four row labels, and the health-state strings, and does NOT contain any token/key sentinel or order field.
  </action>
  <acceptance_criteria>
    - `solver/src/status.ts` contains `export function buildStatus` and `export function renderStatusHtml`.
    - buildStatus output keys are a subset of {health, phase, lastClearPrice, lastClearAt, uptimeSeconds, build} — no order/desk/limit/token field.
    - renderStatusHtml output contains the strings `PUBLIC HEALTH · NO PRIVATE ORDER DATA`, `PHASE`, `LAST CLEAR`, `UPTIME`, `BUILD`, `OPERATIONAL`, and the `UMBRA` wordmark.
    - A secret-sweep test asserts no `ANTHROPIC_API_KEY`/operator-token sentinel and no order field appears in either output.
    - `npx vitest run src/status.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/status.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Aggregate-only /status JSON + brand-styled /status.html builders exist and are proven order-free + secret-free.</done>
</task>

<task type="auto">
  <name>Task 3: Vault dev compose + rotate-secret.mjs + SECRETS-RUNBOOK.md</name>
  <files>ops/vault/docker-compose.vault.yml, scripts/rotate-secret.mjs, docs/SECRETS-RUNBOOK.md</files>
  <read_first>
    - 13-RESEARCH.md lines 45-46 (vault dev compose + rotation script/runbook), lines 464-466 (dev root token labeled UNSAFE-DEV; AppRole is the prod path)
    - 13-CONTEXT.md lines 45-46 (managed secrets; rotation writes a new KV version; live zero-downtime rotation is UAT)
  </read_first>
  <action>
    Create `ops/vault/docker-compose.vault.yml` adding a HashiCorp Vault dev-mode service (image `hashicorp/vault`, `VAULT_DEV_ROOT_TOKEN_ID` a fixed value clearly labeled UNSAFE-DEV in a comment, port 8200, `IPC_LOCK` cap) — a standalone compose file (not merged into the LocalNet compose). Create `scripts/rotate-secret.mjs` — a Node ESM script that writes a NEW KV v2 version of a named secret to `${VAULT_ADDR}/v1/secret/data/umbra/<key>` via raw fetch using `VAULT_TOKEN` (no node-vault); it prints only the key name + new version number, never the value. Create `docs/SECRETS-RUNBOOK.md` documenting: the env-vs-vault switch (`SECRETS_PROVIDER`), booting the dev Vault, seeding the managed secrets (ANTHROPIC_API_KEY / operator token / party tokens), the rotate procedure, the AppRole prod path, and the honest note that live zero-downtime rotation is a UAT gate.
  </action>
  <acceptance_criteria>
    - `ops/vault/docker-compose.vault.yml` parses as valid YAML and defines a `vault` service with a comment marking the root token UNSAFE-DEV.
    - `scripts/rotate-secret.mjs` writes to a path containing `v1/secret/data/umbra/` and prints no secret value (grep the source for the write path + a no-value-print assertion).
    - `docs/SECRETS-RUNBOOK.md` documents SECRETS_PROVIDER, the rotate step, and labels live rotation a UAT gate.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const y=fs.readFileSync('ops/vault/docker-compose.vault.yml','utf8'); if(!/vault/.test(y)||!/UNSAFE-DEV/.test(y)) throw new Error('compose'); const r=fs.readFileSync('scripts/rotate-secret.mjs','utf8'); if(!r.includes('v1/secret/data/umbra')) throw new Error('rotate path'); console.log('ok')"</automated>
  </verify>
  <done>Dev Vault compose, rotation script, and runbook committed; live rotation labeled UAT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solver → Vault HTTP | Secret material crosses the wire; token authenticates |
| public internet → /status /status.html | Deliberately token-free surface — must expose zero private data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Information Disclosure | secrets.ts vault backend | mitigate | VAULT_TOKEN module-private (mirror auth.ts oidcClientSecret); non-2xx throws status only, response body never interpolated; secret-sweep test |
| T-13-04 | Information Disclosure | status.ts /status + /status.html | mitigate | Aggregate-only by construction (buildIndicative discipline); order-sweep + secret-sweep tests assert no order/desk/limit/token field appears |
| T-13-05 | Spoofing | dev Vault root token | accept | Dev-mode root token labeled UNSAFE-DEV; AppRole documented as the prod path (mirrors the `unsafe` HMAC dev-token precedent); live Vault is a UAT gate |
</threat_model>

<verification>
- `cd solver && npx vitest run src/secrets.test.ts src/status.test.ts` — green.
- `cd solver && npx tsc --noEmit` — clean.
- Node YAML/path check on compose + rotate script — ok.
- Manual: `/status.html` string contains the honest tag and no order fields (asserted in status.test.ts).
</verification>

<success_criteria>
- SecretsProvider (env default preserved, vault raw-fetch KV v2) + token-free aggregate status builders exist and are proven order-free + secret-free; dev Vault infra + rotation + runbook committed. §4 fixture untouched.
</success_criteria>

## Artifacts this phase produces (this plan)

- Files: `solver/src/secrets.ts` (+test), `solver/src/status.ts` (+test), `ops/vault/docker-compose.vault.yml`, `scripts/rotate-secret.mjs`, `docs/SECRETS-RUNBOOK.md`
- Exports (secrets.ts): `SecretsProvider` interface, `createSecretsProvider()`
- Exports (status.ts): `buildStatus(input)`, `renderStatusHtml(status)`
- Env vars introduced: `SECRETS_PROVIDER` (env|vault), `VAULT_ADDR`, `VAULT_TOKEN`
- Infra: dev Vault compose service; `scripts/rotate-secret.mjs` rotation script
- UI surface S1: the `/status.html` document body (served by the endpoint wired in 13-07)

<output>
Create `.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-02-SUMMARY.md` when done.
</output>
