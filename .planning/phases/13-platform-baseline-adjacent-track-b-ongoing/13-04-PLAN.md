---
phase: 13-platform-baseline-adjacent-track-b-ongoing
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - solver/src/webhooks.ts
  - solver/src/webhooks.test.ts
  - solver/src/sandbox.ts
  - solver/src/sandbox.test.ts
autonomous: true
requirements: [OPS-04]
must_haves:
  truths:
    - "A webhook payload is HMAC-SHA256 signed over `${timestamp}.${rawBody}` with a per-subscription secret; the signature verifies via timingSafeEqual"
    - "A stale X-Umbra-Timestamp is rejected by the replay-guard"
    - "Delivery retries with exponential backoff (~5 attempts, jittered) and records a delivery log {status, attempts, lastError}; a timer can never double-fire"
    - "The webhook payload never contains a secret (the per-subscription secret is module-private, never echoed)"
    - "The sandbox seed builder deterministically produces the canonical §4 batch that clears exactly $100.00 with A=10/B=8/C=2"
  artifacts:
    - path: "solver/src/webhooks.ts"
      provides: "HMAC signing, subscription registry, retry/backoff, delivery log"
      exports: ["signPayload", "createWebhooks", "WebhookEvent"]
    - path: "solver/src/sandbox.ts"
      provides: "Deterministic §4 fixture seed builder (asserts $100.00)"
      exports: ["sandboxFixtureOrders", "assertSandboxClears"]
  key_links:
    - from: "solver/src/webhooks.ts"
      to: "node:crypto createHmac + timingSafeEqual"
      via: "sha256 signature over ts.rawBody"
      pattern: "createHmac\\('sha256'"
    - from: "solver/src/sandbox.ts"
      to: "auction.ts computeClearing / §4 golden"
      via: "assertSandboxClears asserts $100.00 / A=10 B=8 C=2"
      pattern: "100"
---

<objective>
Deliver OPS-04 as two standalone modules: a signed, retried webhook emitter (HMAC-SHA256 signature, per-subscription secret, subscription registry, exponential-backoff retry with a delivery log) and a deterministic sandbox-round fixture that always seeds the canonical §4 batch and asserts it clears exactly $100.00. Firing webhooks off the live lifecycle seams and exposing the `POST /sandbox/round` + registration endpoints happens in plan 13-09; this plan builds and unit-tests the pure emitter + fixture.

Purpose: give integrators reliable, verifiable round-lifecycle webhooks and a stable, never-drifting §4 sandbox contract.
Output: `webhooks.ts` (+test), `sandbox.ts` (+test).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-RESEARCH.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-PATTERNS.md
@solver/src/auth.ts
@solver/src/clock.ts
@solver/src/auction.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: webhooks.ts — HMAC sign + subscription registry + retry/backoff + delivery log</name>
  <files>solver/src/webhooks.ts, solver/src/webhooks.test.ts</files>
  <read_first>
    - 13-PATTERNS.md lines 75-83 (analog: auth.ts node crypto + per-secret discipline; clock.ts timer hygiene for retry; fired off existing seams)
    - solver/src/auth.ts lines 14-19 (secret module-private, never in payload/logs), lines 69-90 (raw-fetch backend, non-2xx status-only errors)
    - solver/src/clock.ts lines 62-74 (single-setTimeout-per-unit + always-clearTimeout — copy for jittered retry so a timer never double-fires)
    - 13-RESEARCH.md lines 260-269 (Pattern 6 HMAC), lines 51-52 (headers + at-least-once + delivery log), lines 372-376 (Pitfall 7 secret leak)
  </read_first>
  <behavior>
    - signPayload(secret, ts, rawBody) returns `sha256=<hex>` = HMAC-SHA256 over `${ts}.${rawBody}`.
    - A verify helper using timingSafeEqual returns true for a correct signature, false otherwise.
    - A stale timestamp (older than the replay window) is rejected.
    - deliver() retries a failing sink up to ~5 attempts with backoff, then records {status:'failed', attempts, lastError} in the delivery log; a succeeding sink records {status:'delivered', attempts}.
    - The emitted payload body contains the event data but NEVER the subscription secret.
  </behavior>
  <action>
    Create `solver/src/webhooks.ts` exporting: a `WebhookEvent` union (`round.opened`|`round.sealed`|`round.cleared`|`round.settled`|`fill.posted`); `signPayload(secret, ts, rawBody)` using `createHmac('sha256', secret).update(`${ts}.${rawBody}`).digest('hex')` prefixed `sha256=`; a `verifySignature` using `timingSafeEqual`; and `createWebhooks(deps?)` returning `{ register, unregister, emit, deliveryLog }`. `register` stores a subscription `{ url, secret, events[] }` in a private `Map` (secret module-private, never returned). `emit(event, data)` serializes the payload, signs it, and POSTs to each matching subscription with headers `X-Umbra-Signature`, `X-Umbra-Timestamp`, `X-Umbra-Event`, `X-Umbra-Delivery`; on non-2xx/throw it retries with exponential backoff + jitter (~5 attempts) using the clock.ts single-timer + always-clearTimeout hygiene so a retry timer never double-fires; every attempt updates the in-memory delivery log `{ status, attempts, lastError }`. Inject the `fetch`/timer deps so the test drives a failing then succeeding sink with no real network. The payload must carry only event data (round id, phase, aggregate numbers) — never a secret/token.
    Write `webhooks.test.ts`: assert signPayload/verifySignature round-trip (timingSafeEqual), a stale timestamp rejects, a failing-then-recovering sink records the right attempts/status, and a secret-sweep asserts the subscription secret + ANTHROPIC_API_KEY/operator-token sentinels never appear in any emitted payload or the delivery log.
  </action>
  <acceptance_criteria>
    - `solver/src/webhooks.ts` contains `export function signPayload(` using `createHmac('sha256'`, and `export function createWebhooks`.
    - A correct signature verifies via timingSafeEqual; a tampered one fails.
    - A failing sink retries and the delivery log ends `status:'failed'` with the attempt count; a good sink ends `status:'delivered'`.
    - Secret-sweep: no subscription secret / API key / operator token appears in any payload or the delivery log.
    - `npx vitest run src/webhooks.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/webhooks.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Webhook emitter signs (HMAC), replay-guards, retries with backoff + delivery log, leaks no secret — all unit-tested with injected fetch/timers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sandbox.ts — deterministic §4 fixture that asserts $100.00</name>
  <files>solver/src/sandbox.ts, solver/src/sandbox.test.ts</files>
  <read_first>
    - 13-CONTEXT.md line 53 (sandbox always seeds+clears the canonical §4 fixture; reuses §8 core + settle; asserts $100.00; isolated from real rounds)
    - 13-RESEARCH.md lines 366-370 (Pitfall 6 — additive; assert $100.00/A=10/B=8/C=2; keep auction.ts byte-unchanged)
    - solver/src/auction.ts (the pure §8 `computeClearing`/`matchedAt` — import and reuse, do not modify)
    - solver/src/auction.test.ts (the existing §4 golden fixture values: p*=100.00, A=10/B=8/C=2)
  </read_first>
  <behavior>
    - sandboxFixtureOrders() returns the canonical §4 order views (the three desks A/B/C bids that clear at $100.00 with fills A=10/B=8/C=2).
    - assertSandboxClears() runs computeClearing over the fixture and throws unless clearingPrice === 100.00 and allocations match A=10/B=8/C=2.
  </behavior>
  <action>
    Create `solver/src/sandbox.ts` importing the pure §8 helpers from `auction.ts` (do NOT modify auction.ts). Export `sandboxFixtureOrders()` returning the canonical §4 `OrderView[]` (identical to the auction.test.ts golden batch — the deterministic, never-varying fixture). Export `assertSandboxClears()` that calls `computeClearing(sandboxFixtureOrders())`, asserts `clearingPrice === 100.00` and the allocation set equals `{A:10, B:8, C:2}` (use the same equality discipline as agent.ts allocationsEqual), and throws a fixed message on any mismatch. This module is the source of truth the `POST /sandbox/round` endpoint (13-09) will reuse to seed + settle the sandbox round isolated from real rounds.
    Write `sandbox.test.ts` asserting `assertSandboxClears()` passes for the fixture (p*=100.00, A=10/B=8/C=2) and that a mutated fixture makes it throw.
  </action>
  <acceptance_criteria>
    - `solver/src/sandbox.ts` contains `export function sandboxFixtureOrders` and `export function assertSandboxClears`.
    - `assertSandboxClears()` passes: clearingPrice is exactly 100.00 and fills are A=10/B=8/C=2.
    - auction.ts is byte-unchanged (grep git diff — no edit to auction.ts).
    - `npx vitest run src/sandbox.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/sandbox.test.ts src/auction.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Deterministic §4 sandbox fixture + $100.00 assertion exist, reusing the byte-unchanged §8 core; the golden auction.test.ts still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solver → subscriber URL | Signed payload crosses to an external sink |
| subscriber → (future) inbound | Signature+timestamp guard forgery/replay |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Spoofing/Tampering | webhooks.ts signature | mitigate | HMAC-SHA256 over ts.rawBody + timingSafeEqual verify + X-Umbra-Timestamp replay-guard |
| T-13-10 | Information Disclosure | webhook payload | mitigate | Per-subscription secret module-private; secret-sweep asserts no secret/token in payload or delivery log |
| T-13-11 | Tampering | sandbox fixture drift | mitigate | assertSandboxClears asserts $100.00/A=10/B=8/C=2; auction.ts byte-unchanged; golden test gate |
</threat_model>

<verification>
- `cd solver && npx vitest run src/webhooks.test.ts src/sandbox.test.ts src/auction.test.ts` — green.
- `cd solver && npx tsc --noEmit` — clean.
- Confirm auction.ts unchanged (git diff empty for solver/src/auction.ts).
</verification>

<success_criteria>
- Signed/retried webhook emitter (proven HMAC + replay-guard + backoff + no-leak) and a deterministic §4 sandbox fixture ($100.00) exist standalone; the §4 golden stays green.
</success_criteria>

## Artifacts this phase produces (this plan)

- Files: `solver/src/webhooks.ts` (+test), `solver/src/sandbox.ts` (+test)
- Exports (webhooks.ts): `signPayload(secret, ts, rawBody)`, `verifySignature`, `createWebhooks(deps?)`, `WebhookEvent` union
- Exports (sandbox.ts): `sandboxFixtureOrders()`, `assertSandboxClears()`
- Webhook events: `round.opened`, `round.sealed`, `round.cleared`, `round.settled`, `fill.posted`
- Webhook headers: `X-Umbra-Signature`, `X-Umbra-Timestamp`, `X-Umbra-Event`, `X-Umbra-Delivery`

<output>
Create `.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-04-SUMMARY.md` when done.
</output>
