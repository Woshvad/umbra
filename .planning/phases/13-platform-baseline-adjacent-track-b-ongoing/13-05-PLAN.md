---
phase: 13-platform-baseline-adjacent-track-b-ongoing
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - solver/src/fix.ts
  - solver/src/fix.test.ts
autonomous: true
requirements: [OPS-05]
must_haves:
  truths:
    - "parseFix decodes a FIX 4.4 NewOrderSingle (35=D) reading 55 Symbol, 54 Side, 38 OrderQty, 44 Price, 40 OrdType into a validated sealed-order shape"
    - "BodyLength(9) and CheckSum(10) are computed to the exact FIX spec ranges and match a known-good reference message"
    - "buildFix(parseFix(m)) round-trips a canonical NewOrderSingle byte-for-byte"
    - "A malformed frame maps to a 35=8 ExecutionReport reject and NEVER throws"
    - "Session Logon(35=A)/Heartbeat(35=0)/Logout(35=5) with a monotonic MsgSeqNum are handled; no secret/operator/Anthropic credential appears in any FIX output"
  artifacts:
    - path: "solver/src/fix.ts"
      provides: "Hand-rolled FIX 4.4 order-entry subset (framing + parse 35=D + build 35=8 + session)"
      exports: ["parseFix", "buildFix", "bodyLength", "checkSum", "handleFixMessage", "fixToSubmitOrder"]
  key_links:
    - from: "solver/src/fix.ts fixToSubmitOrder"
      to: "a validated sealed Venue.SubmitOrder shape"
      via: "zod re-validation like agent.ts orderSchema"
      pattern: "safeParse"
    - from: "solver/src/fix.ts checkSum/bodyLength"
      to: "a known-good FIX 4.4 vector"
      via: "unit test asserts exact values"
      pattern: "10="
---

<objective>
Deliver OPS-05 as a standalone hand-rolled FIX 4.4 order-entry subset acceptor: pure framing helpers (BodyLength/CheckSum), a `NewOrderSingle` (35=D) parser that maps to a validated sealed order, an `ExecutionReport` (35=8) builder, and a minimal session layer (Logon/Heartbeat/Logout with a monotonic MsgSeqNum). No FIX-engine dependency (dependency-minimalism). Exposing the HTTP-wrapped raw-FIX endpoint happens in plan 13-09; this plan builds and unit-tests the pure protocol logic against a known-good vector.

Purpose: let an institutional desk submit a sealed bid from its existing OMS over a familiar protocol, mapping to `Venue.SubmitOrder`, without exposing any credential.
Output: `fix.ts` (+test).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-RESEARCH.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-PATTERNS.md
@solver/src/agent.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fix.ts — framing + parse 35=D + build 35=8 + session (with known-good vector)</name>
  <files>solver/src/fix.ts, solver/src/fix.test.ts</files>
  <read_first>
    - 13-PATTERNS.md lines 87-95 (analog: agent.ts parseOrder untrusted-input → zod-validated domain → never-throw degrade; pure framing helpers like priceEqual/allocationsEqual)
    - solver/src/agent.ts lines 90-94 (orderSchema zod shape to re-validate against), lines 82-150 region (parseOrder never-throw-on-error discipline)
    - 13-RESEARCH.md lines 271-282 (Pattern 7 framing spec + HTTP-wrapped primary + honest label), lines 348-352 (Pitfall 3 BodyLength/CheckSum off-by-one)
  </read_first>
  <behavior>
    - bodyLength(msg) = byte count from after `9=<n>\x01` up to and including the `\x01` before `10=`.
    - checkSum(msg) = (sum of all bytes up to and including the `\x01` before `10=`) mod 256, 3-digit zero-padded.
    - Both match a precomputed known-good FIX 4.4 NewOrderSingle vector.
    - parseFix decodes SOH-delimited tag=value pairs; a 35=D message yields {msgType:'D', symbol, side, orderQty, price, ordType}.
    - fixToSubmitOrder maps 54 Side (1 Buy / 2 Sell), 38 OrderQty, 44 Price, 40 OrdType(2 Limit), 55 Symbol(BONDX) → a zod-validated {side,qty,limit}; an out-of-range/foreign field → null (no throw).
    - handleFixMessage on a valid 35=D returns a 35=8 ExecutionReport with OrdStatus 0 (New); on malformed input returns a 35=8 with OrdStatus 8 (Rejected) — never throws.
    - Session: Logon(A)/Heartbeat(0)/Logout(5) recognized; MsgSeqNum(34) is monotonic per session.
    - buildFix(parseFix(m)) === m for the canonical vector.
  </behavior>
  <action>
    Create `solver/src/fix.ts` with pure helpers `bodyLength(msg)`, `checkSum(msg)`, `parseFix(raw)` (SOH `\x01` split into a tag→value map), and `buildFix(fields)` (re-serialize with correct 8/9/…/10 ordering, computing 9 and 10 last). Export `fixToSubmitOrder(fields)` mapping the 35=D fields to a sealed-order shape and re-validating with a zod schema mirroring agent.ts `orderSchema` (55 Symbol must equal `BONDX`; 54 Side 1→Buy/2→Sell; 38→qty; 44→limit; 40 OrdType 2→Limit) returning `null` on any invalid/foreign field (never throw). Export `handleFixMessage(raw, session)` returning the framed `ExecutionReport` (35=8) string — OrdStatus 0 New on success, OrdStatus 8 Rejected on a malformed/unmapped message — plus updating a monotonic `MsgSeqNum` (34). Handle session `Logon`(35=A)/`Heartbeat`(35=0)/`Logout`(35=5) with SenderCompID(49)/TargetCompID(56). Never interpolate any operator token / Anthropic key into any FIX output; the acceptor is behind the operator boundary and credential-free.
    Write `fix.test.ts`: assert bodyLength + checkSum equal the precomputed values for a hard-coded known-good NewOrderSingle vector; assert `buildFix(parseFix(vector)) === vector`; assert 35=D → correct fixToSubmitOrder mapping; assert a malformed frame yields a 35=8 reject and does NOT throw; assert MsgSeqNum monotonicity; and a secret-sweep asserting no operator-token/ANTHROPIC_API_KEY sentinel appears in any built message.
  </action>
  <acceptance_criteria>
    - `solver/src/fix.ts` contains `export function parseFix`, `export function buildFix`, `export function bodyLength`, `export function checkSum`, `export function fixToSubmitOrder`, `export function handleFixMessage`.
    - bodyLength + checkSum match the known-good vector's precomputed values (exact).
    - `buildFix(parseFix(vector)) === vector` (round-trip byte-exact).
    - A malformed frame returns a 35=8 reject and `handleFixMessage` does not throw.
    - Secret-sweep: no operator-token/ANTHROPIC_API_KEY sentinel in any FIX output.
    - `npx vitest run src/fix.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/fix.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Hand-rolled FIX 4.4 subset (framing/parse/build/session) is proven against a known-good vector, never throws on malformed input, and leaks no credential.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external OMS → FIX acceptor | Untrusted framed FIX bytes enter the mapper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-12 | Tampering/DoS | fix.ts parse | mitigate | Bounds-checked parse + BodyLength/CheckSum validation; malformed → 35=8 reject, never throw |
| T-13-13 | Spoofing (session) | fix.ts MsgSeqNum | mitigate | Monotonic MsgSeqNum(34) per session (session-integrity control, ASVS V3) |
| T-13-14 | Information Disclosure | FIX ExecutionReport output | mitigate | Credential-free acceptor; secret-sweep asserts no operator/Anthropic secret in any FIX output |
| T-13-15 | Tampering (prompt injection via FIX text) | fixToSubmitOrder | mitigate | zod re-validation (mirror orderSchema); only mapped numeric/enum fields reach the domain; no free-text reaches the AI |
</threat_model>

<verification>
- `cd solver && npx vitest run src/fix.test.ts` — green.
- `cd solver && npx tsc --noEmit` — clean.
- Confirm the checksum/bodylength vector assertions pass (regression against the FIX spec).
</verification>

<success_criteria>
- The FIX 4.4 order-entry subset parses/builds/rounds-trips against a known-good vector, maps 35=D → a validated sealed order, rejects malformed frames without throwing, and leaks no credential. §4 fixture untouched.
</success_criteria>

## Artifacts this phase produces (this plan)

- Files: `solver/src/fix.ts` (+test)
- Exports: `parseFix(raw)`, `buildFix(fields)`, `bodyLength(msg)`, `checkSum(msg)`, `fixToSubmitOrder(fields)`, `handleFixMessage(raw, session)`
- Protocol surface: FIX 4.4 subset — NewOrderSingle(35=D) in, ExecutionReport(35=8) out, session Logon(A)/Heartbeat(0)/Logout(5)

<output>
Create `.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-05-SUMMARY.md` when done.
</output>
