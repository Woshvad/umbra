---
phase: 13-platform-baseline-adjacent-track-b-ongoing
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - daml/Umbra/Rfq.daml
  - daml/Umbra/Tests.daml
autonomous: true
requirements: [ADJ-02]
must_haves:
  truths:
    - "A requester posts an RfqRequest; one or more dealers respond with a FIRM signed Quote (signatory operator, dealer) — the quote is a binding commitment"
    - "The requester accepts the best quote and it settles a 1×1 batch via the SAME atomic Umbra.Settlement.settleBatch DvP path — cash and bond conserve"
    - "All new templates are KEYLESS (fetch-by-ContractId + assertMsg), like Holding.daml"
    - "RFQ privacy is structural: a Quote is observed only by its requester; a dealer sees only the request it is an observer of"
    - "The §4 secondary-market fixture still clears $100.00 (RFQ is additive; Round.Clear / Clearing.daml byte-unchanged)"
  artifacts:
    - path: "daml/Umbra/Rfq.daml"
      provides: "Keyless RfqRequest + firm Quote templates + AcceptQuote choice reusing settleBatch"
      contains: "template RfqRequest"
    - path: "daml/Umbra/Tests.daml"
      provides: "ADJ-02 scenarios: firm quote, accept-best settles 1×1, conservation, privacy"
      contains: "test_rfq"
  key_links:
    - from: "daml/Umbra/Rfq.daml AcceptQuote"
      to: "Umbra.Settlement.settleBatch"
      via: "1×1 batch DvP (reuse, not reinvent)"
      pattern: "settleBatch"
    - from: "daml/Umbra/Rfq.daml Quote"
      to: "signatory operator, dealer"
      via: "firm binding commitment"
      pattern: "signatory operator, dealer"
---

<objective>
Deliver ADJ-02 as a new keyless Daml module: an RFQ side-mode where a requester posts an `RfqRequest`, dealers respond with FIRM signed `Quote`s, and the requester accepts the best quote — settling a 1×1 batch via the existing `Umbra.Settlement.settleBatch` atomic DvP path (no new settlement mechanism). All templates are keyless (fetch-by-ContractId + assert) per D7. Solver orchestration and the web panel land in later plans; this plan builds the on-ledger templates + Daml Script scenarios.

Purpose: prove the same atomic Canton DvP machinery serves illiquid single-bond RFQ trades, with firm signatory-bound quotes and structural per-party privacy.
Output: `daml/Umbra/Rfq.daml`, RFQ scenarios in `daml/Umbra/Tests.daml`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-RESEARCH.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-PATTERNS.md
@daml/Umbra/Holding.daml
@daml/Umbra/Settlement.daml
@daml/Umbra/Instrument.daml
@daml/Umbra/Clearing.daml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rfq.daml — keyless RfqRequest + firm Quote + AcceptQuote (settleBatch)</name>
  <files>daml/Umbra/Rfq.daml</files>
  <read_first>
    - 13-PATTERNS.md lines 117-124 (analog: Holding.daml keyless fetch-by-cid + assert; Settlement.settleBatch DvP; firm quote via signatory dealer)
    - daml/Umbra/Holding.daml lines 34-44 (keyless template, no contract key, ensure), lines 51-80 (fetch-by-cid + assertMsg pattern in Split/Merge/Reassign), lines 109-117 (moveExactHolding helper)
    - daml/Umbra/Settlement.daml lines 33-38 (Instruction record), lines 63-76 (buildGrossInstructions), lines 170-193 (settleBatch signature + DvP-integrity checks)
    - daml/Umbra/Instrument.daml lines 20-24 (InstrumentId), daml/Umbra/Clearing.daml line 31 (Side = Buy | Sell)
    - 13-RESEARCH.md lines 382-414 (RFQ template sketch), lines 300-306 (Anti-Patterns: no contract keys, reuse settleBatch), Open Question / Assumption A6 (marshal cids the way settle does)
  </read_first>
  <action>
    Create `daml/Umbra/Rfq.daml` (module `Umbra.Rfq`) importing `InstrumentId` from Umbra.Instrument, `Side` from Umbra.Clearing, and `settleBatch`/`Instruction` from Umbra.Settlement. Define a KEYLESS `template RfqRequest` with fields `operator`, `requester`, `instrument : InstrumentId`, `side : Side`, `quantity : Int`; `signatory operator, requester`; `observer` = the dealer set who may quote (the explicit observer set is the privacy control — do NOT add stray observers). Define a KEYLESS `template Quote` with `operator`, `dealer`, `requester`, `instrument : InstrumentId`, `price : Decimal`, `quantity : Int`; `signatory operator, dealer` (dealer's signature makes the quote FIRM/binding); `observer requester`. On `RfqRequest` add `choice AcceptQuote` controlled by `requester` taking `quoteCid : ContractId Quote` and the source Holding cids (bond source from the seller side, cash source from the buyer side — marshaled the way settle gathers cids): `q <- fetch quoteCid` then `assertMsg` the quote's `instrument == instrument`, `requester == requester`, and `quantity` matches; build a 1×1 `[Instruction]` for the (requester, dealer) pair at `q.price` (bond leg + cash leg, reusing the Settlement pairing shape) and call `settleBatch operator instructions sources` — the SAME atomic all-or-nothing DvP path. NO contract keys anywhere; every cross-contract read is `fetch` + `assertMsg` (D7). Add a doc comment: firm quote = dealer signatory; RFQ reuses settleBatch, not a new engine.
  </action>
  <acceptance_criteria>
    - `daml/Umbra/Rfq.daml` contains `template RfqRequest`, `template Quote`, and `choice AcceptQuote`.
    - `Quote` is `signatory operator, dealer` (firm) with `observer requester`; `RfqRequest` is `signatory operator, requester`.
    - No `key`/`maintainer` appears in Rfq.daml (keyless — grep returns nothing).
    - AcceptQuote calls `settleBatch` and does `fetch quoteCid` + `assertMsg` (no contract-key lookup).
    - `daml build` (Bash, cwd daml/) succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd daml && /c/Users/woshv/bin/daml build 2>&1 | tail -5</automated>
  </verify>
  <done>Keyless RFQ templates compile: firm signed Quote, requester-accept settles via settleBatch, structural per-party privacy.</done>
</task>

<task type="auto">
  <name>Task 2: Tests.daml — ADJ-02 RFQ scenarios (firm quote, accept-best settle, conservation, privacy)</name>
  <files>daml/Umbra/Tests.daml</files>
  <read_first>
    - daml/Umbra/Tests.daml (existing §4 golden + settlement/conservation test structure — mirror the seedAndClose + sorted before/after balance-equality style used for settlement atomicity)
    - daml/Umbra/Settlement.daml lines 126-158 (conservationOk helper for the conservation assertion)
    - 13-VALIDATION.md line 49 (ADJ-02 test contract: RfqRequest→firm signed Quote→accept-best settles 1×1 via settleBatch, conserving cash+asset; keyless)
  </read_first>
  <action>
    Add Daml Script scenarios to `daml/Umbra/Tests.daml` covering ADJ-02: `test_rfq_accept_settles` — a requester posts an RfqRequest, one dealer creates a firm Quote (with a funded Holding), the requester exercises AcceptQuote, and the resulting Holdings show the bond delivered to the requester and cash delivered to the dealer, with per-instrument conservation (Σ before == Σ after, reuse conservationOk / a sorted before/after Holding snapshot equality). `test_rfq_best_quote` — two dealers quote different prices, the requester accepts the better quote, and settlement uses that price. `test_rfq_quote_is_firm` — assert the Quote is signatory-dealer (a submitMustFail proving the dealer's authority is required to create it). `test_rfq_privacy` — a per-party query proving a rival dealer does NOT see the requester's accepted Quote / another dealer's Quote (observer=requester only). Do NOT touch the §4 golden tests or Clearing.daml.
  </action>
  <acceptance_criteria>
    - `daml/Umbra/Tests.daml` contains `test_rfq_accept_settles`, `test_rfq_best_quote`, `test_rfq_quote_is_firm`, `test_rfq_privacy`.
    - The accept-settle test asserts per-instrument conservation (before == after) and the bond/cash legs landed with the right owners.
    - The §4 golden test (`test_clears_at_100` or equivalent) is unchanged and still passes.
    - `daml test` (Bash, cwd daml/) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd daml && /c/Users/woshv/bin/daml test 2>&1 | tail -15</automated>
  </verify>
  <done>RFQ scenarios prove firm-quote/accept-best/1×1-settle/conservation/privacy; the §4 golden stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dealer → Quote | A firm price commitment enters the ledger |
| requester → AcceptQuote | Triggers atomic settlement of a 1×1 batch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-16 | Tampering (quote forgery) | Rfq.daml Quote | mitigate | Quote is `signatory operator, dealer` — a quote cannot exist without the dealer's authority; AcceptQuote asserts instrument/requester/quantity match via fetch+assertMsg |
| T-13-17 | Information Disclosure (RFQ leak) | Rfq.daml observers | mitigate | Explicit observer sets only (Quote observed by requester; RfqRequest by the dealer set) — no stray observers; privacy test proves rivals blind |
| T-13-18 | Tampering (settlement bypass) | AcceptQuote settle path | mitigate | Reuses settleBatch's DvP-integrity checks (custodian/owner/instrument match); atomic all-or-nothing; §4 golden + daml test regression gate |
</threat_model>

<verification>
- `cd daml && /c/Users/woshv/bin/daml build` — succeeds.
- `cd daml && /c/Users/woshv/bin/daml test` — exit 0 (RFQ scenarios + §4 golden green).
- Confirm Clearing.daml and the §4 golden test are byte-unchanged.
</verification>

<success_criteria>
- Keyless RFQ (firm signed quote → accept-best → 1×1 settleBatch DvP, conserving, private) compiles and its scenarios pass; §4 still clears $100.00.
</success_criteria>

## Artifacts this phase produces (this plan)

- Files: `daml/Umbra/Rfq.daml`; RFQ scenarios appended to `daml/Umbra/Tests.daml`
- Daml templates: `RfqRequest` (signatory operator, requester), `Quote` (signatory operator, dealer — firm)
- Daml choices: `AcceptQuote` (controller requester → settleBatch 1×1 DvP)
- Tests: `test_rfq_accept_settles`, `test_rfq_best_quote`, `test_rfq_quote_is_firm`, `test_rfq_privacy`

<output>
Create `.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-06-SUMMARY.md` when done.
</output>
