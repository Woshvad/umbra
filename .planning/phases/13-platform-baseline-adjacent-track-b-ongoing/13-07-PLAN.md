---
phase: 13-platform-baseline-adjacent-track-b-ongoing
plan: 07
type: execute
wave: 2
depends_on: [13-01, 13-02, 13-03]
files_modified:
  - solver/src/index.ts
  - solver/src/api.ts
  - solver/src/ledger.ts
  - solver/src/agent.ts
  - solver/.env.example
  - solver/src/api.test.ts
  - solver/src/index.test.ts
autonomous: true
requirements: [OPS-01, OPS-02, OPS-03]
must_haves:
  truths:
    - "telemetry initializes FIRST in index.ts main(); sdk shuts down on SIGTERM"
    - "The clear/settle request path emits nested spans (request→clear-compute→ledger-exercise(client)→agent) carrying round.id, plus latency histograms + counters"
    - "GET /health returns readiness/liveness; GET /status returns aggregate health JSON; GET /status.html serves the token-free brand page — all reachable without a token and free of order/secret data"
    - "ANTHROPIC_API_KEY (and the operator/party token seam) resolves through the SecretsProvider; the env backend preserves current .env behavior byte-for-byte; no secret reaches the browser or a log"
    - "The idempotency middleware guards mutating POSTs and the FSM guards settle/close; the §4 fixture still clears $100.00 (existing api.test.ts + golden green)"
  artifacts:
    - path: "solver/src/api.ts"
      provides: "GET /health, GET /status, GET /status.html + span-wrapped handlers + idempotency mw + FSM guard"
      contains: "/status.html"
    - path: "solver/.env.example"
      provides: "OTEL/SECRETS_PROVIDER/VAULT env keys documented"
      contains: "SECRETS_PROVIDER"
  key_links:
    - from: "solver/src/index.ts main()"
      to: "telemetry.initTelemetry (first import)"
      via: "boot ordering (Pitfall 1)"
      pattern: "initTelemetry"
    - from: "solver/src/api.ts /status.html"
      to: "status.renderStatusHtml"
      via: "token-free route"
      pattern: "renderStatusHtml"
    - from: "solver/src/index.ts"
      to: "secrets.createSecretsProvider"
      via: "ANTHROPIC_API_KEY resolution → createAgent client"
      pattern: "createSecretsProvider"
---

<objective>
Wire the OPS-01/02/03 foundation modules (telemetry, logger, secrets, status, idempotency, fsm — all built in Wave 1) into the running solver: telemetry-first boot with SIGTERM shutdown and span/metric instrumentation on the clear/settle path, the token-free `/status` + `/status.html` + `/health` endpoints (S1), SecretsProvider-resolved credentials, the idempotency middleware, and the FSM transition guard. Thin wiring only — the heavy logic lives in the Wave-1 modules. The §4 fixture and the existing endpoint contracts stay byte-compatible.

Purpose: turn the foundation modules into live observability, a public status page, vault-capable secret resolution, and idempotent/legal-transition-guarded mutation on the actual Express app.
Output: updated `index.ts`, `api.ts`, `ledger.ts`, `agent.ts`, `.env.example`, extended tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-RESEARCH.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-PATTERNS.md
@solver/src/index.ts
@solver/src/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: index.ts boot — telemetry-first, SIGTERM shutdown, SecretsProvider wiring, .env.example</name>
  <files>solver/src/index.ts, solver/src/agent.ts, solver/src/ledger.ts, solver/.env.example, solver/src/index.test.ts</files>
  <read_first>
    - solver/src/index.ts lines 1-25 (boot order comment + dotenv-first), lines 106-146 (buildDeps + openRound wiring), lines 190-230 (main() constructs createAgent), lines 478-535 (buildDeps call + app.listen + main guard)
    - 13-RESEARCH.md lines 336-345 (Pitfall 1 — telemetry import FIRST + sdk.shutdown on SIGTERM), lines 328-329 (new env vars for .env.example)
    - solver/src/agent.ts lines 210-215 (module-private `_apiKey`/`_client`), lines 290-301 (createAgent accepts an injected `client`)
    - 13-PATTERNS.md lines 29-39 (secrets.ts analog) + lines 99-105 (telemetry boot-first)
  </read_first>
  <action>
    In `index.ts` `main()`: import and call `initTelemetry()` BEFORE the dynamic ledger/agent imports (Pitfall 1) and register `shutdownTelemetry()` on `SIGTERM`/`SIGINT`. Replace the raw boot `console.log` with the redacting `log()` from logger.ts (secret-free `:${port} as ${operatorParty}` line preserved). Construct a `SecretsProvider` via `createSecretsProvider()` and use it to resolve `ANTHROPIC_API_KEY` at boot; when present, construct the Anthropic client and pass it as `createAgent({ client, computeClearing, matchedAt })` (agent.ts already prefers an injected client over its module-private one, and keyless-degrades when absent — so the keyless fallback is preserved). Route the ledger operator/party-token resolution through the same provider seam such that the `env` backend returns exactly today's value (byte-for-byte; do NOT change ledger.ts's proven token-handling behavior — only add the provider seam so the `vault` backend can swap in at UAT). Add to `solver/.env.example`: `OTEL_EXPORTER_OTLP_ENDPOINT` (commented — unset uses console), `SECRETS_PROVIDER=env`, `VAULT_ADDR`, `VAULT_TOKEN`. Extend `index.test.ts` to assert telemetry init is called before listen and that the injected-agent client path still fires openRound+openRoundClock (existing wiring test extended, not replaced).
  </action>
  <acceptance_criteria>
    - `solver/src/index.ts` calls `initTelemetry()` before the ledger/agent dynamic imports and registers `shutdownTelemetry` on SIGTERM.
    - `index.ts` constructs the agent client from `SecretsProvider.get('ANTHROPIC_API_KEY')` (env backend === current process.env value) and injects it into `createAgent`; the keyless-degrade path is preserved when the key is absent.
    - `solver/.env.example` contains `SECRETS_PROVIDER`, `VAULT_ADDR`, `VAULT_TOKEN`, `OTEL_EXPORTER_OTLP_ENDPOINT`.
    - `npx vitest run src/index.test.ts` exits 0; `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/index.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Boot initializes telemetry first, shuts it down on SIGTERM, resolves secrets through the provider (env default preserved), and documents the new env keys.</done>
</task>

<task type="auto">
  <name>Task 2: api.ts — spans + metrics on clear/settle path + GET /health, /status, /status.html</name>
  <files>solver/src/api.ts, solver/src/ledger.ts, solver/src/api.test.ts</files>
  <read_first>
    - solver/src/api.ts lines 348-366 (buildIndicative aggregate discipline for /status inputs), lines 368-372 (createApp + express.json + CORS), lines 399-460 (GET /round/:id + settle region — the clear/settle path to span-wrap)
    - solver/src/telemetry.ts (from 13-01: withSpan + instruments) and solver/src/status.ts (from 13-02: buildStatus + renderStatusHtml)
    - 13-RESEARCH.md lines 190-209 (Pattern 2 nested spans + instrument names), lines 372-376 (Pitfall 7 secret leak on new surfaces)
  </read_first>
  <action>
    In `api.ts` `createApp`, wrap the clear/settle request handling in nested spans via `withSpan` — an outer `round.settle`/`round.clear` span carrying `round.id`, an inner `clear.compute` span around `computeClearing`, and (in ledger.ts) a `ledger.exercise` CLIENT-kind span around the JSON Ledger API v2 exercise call (label it honestly a client span, not in-participant). Record `umbra.clear.latency_ms`/`umbra.settle.latency_ms` histograms and increment `umbra.agent.verified_total{source}` / `umbra.rounds.opened` at their seams. Register three token-free routes: `GET /health` (returns `{status:'ok', uptimeSeconds}` liveness/readiness), `GET /status` (returns `buildStatus(...)` built from aggregate health inputs — current phase via the FSM `sealedAlias`, last clear price/time, uptime, build/version — NEVER an individual order), and `GET /status.html` (returns `renderStatusHtml(...)` with `Content-Type: text/html`). These three routes take NO auth context. Keep the existing five §11 endpoints byte-compatible (additive only). Extend `api.test.ts` with: /health returns ok; /status body contains only aggregate keys and passes an order-sweep + secret-sweep (ANTHROPIC_API_KEY + operator-token sentinels absent); /status.html contains the honest tag and no order/secret field.
  </action>
  <acceptance_criteria>
    - `api.ts` registers `GET /health`, `GET /status`, `GET /status.html` and they respond without any token.
    - The settle/clear handler is wrapped in `withSpan` with a `round.id` attribute; the ledger exercise is a client-kind span in ledger.ts.
    - /status + /status.html pass an order-sweep + secret-sweep (no order field, no ANTHROPIC_API_KEY/operator-token sentinel).
    - The existing five §11 endpoints' response shapes are unchanged (existing api.test.ts assertions still pass).
    - `npx vitest run src/api.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/api.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Observability spans/metrics instrument the clear/settle path; token-free /health, /status, /status.html serve aggregate-only, secret-free data (S1 live).</done>
</task>

<task type="auto">
  <name>Task 3: api.ts reliability — idempotency middleware + FSM guard</name>
  <files>solver/src/api.ts, solver/src/api.test.ts</files>
  <read_first>
    - solver/src/api.ts line 370 (express.json first — the idempotency mw must register AFTER it), lines 221-230 (ApiError), lines 428-460 (settle handler + TERMINAL_STATUSES double-settle guard to fold the FSM into)
    - solver/src/idempotency.ts + solver/src/fsm.ts (from 13-03: idempotencyMiddleware + transition)
    - 13-RESEARCH.md lines 459-462 (Open Question 2 — apply to POSTs carrying Idempotency-Key)
  </read_first>
  <action>
    In `api.ts` `createApp`, register the `idempotencyMiddleware` (from 13-03) AFTER `express.json()` so mutating POSTs carrying an `Idempotency-Key` are deduped (a replay returns the original response; a same-key-different-body → 422 IDEMPOTENCY_KEY_REUSED). In the settle and close handlers, gate the status change through `transition(current, target)` (from fsm.ts) so a settle-before-clear / double-settle is rejected as 409 ILLEGAL_TRANSITION — folding in / complementing the existing TERMINAL_STATUSES double-settle guard (keep behavior at least as strict; do not weaken the 409 double-settle). Extend `api.test.ts`: a replayed POST /round with the same key+body returns the identical response and does not re-open; a same-key-different-body → 422; a settle on a non-cleared round → 409 ILLEGAL_TRANSITION; the §4 clear/settle still returns $100.00 with matched 10.
  </action>
  <acceptance_criteria>
    - The idempotency middleware is registered after express.json and dedupes POSTs by Idempotency-Key (replay + 422 cross-body proven in api.test.ts).
    - Settle/close route status changes through `transition()`; a settle-before-clear returns 409 ILLEGAL_TRANSITION.
    - The §4 path still clears $100.00 / matched 10 (existing golden assertions green).
    - `npx vitest run src/api.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/api.test.ts src/auction.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Mutating POSTs are idempotent and the round lifecycle is FSM-guarded (409 on illegal transitions); §4 stays $100.00.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public → /status /status.html /health | Token-free surfaces — must expose zero private/secret data |
| client → mutating POST | Retries + illegal-sequence attempts |
| solver → telemetry exporter/log | Span attributes + logs are new leak vectors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-19 | Information Disclosure | /status /status.html + span attributes/logs | mitigate | Aggregate-only buildStatus; order-sweep + secret-sweep on both endpoints; redacting logger; only round.id as a span attribute |
| T-13-20 | Tampering | idempotency mw / FSM guard | mitigate | Idempotency dedup (replay/422); transition() 409 on illegal edges; ledger Round.status authoritative backstop |
| T-13-21 | Information Disclosure | SecretsProvider wiring | mitigate | ANTHROPIC_API_KEY resolved server-side, injected as a client; never returned/logged/echoed; env backend preserves current behavior |
| T-13-06/07/09 (carried) | Tampering/Disclosure | reused Wave-1 mitigations | mitigate | Wave-1 module tests + api.test.ts secret sweep extended to the new endpoints |
</threat_model>

<verification>
- `cd solver && npx vitest run` (full solver suite) — green.
- `cd solver && npx tsc --noEmit` — clean.
- Confirm the §4 golden (auction.test.ts) still clears $100.00 / A=10 B=8 C=2.
- Confirm /status + /status.html secret-sweep + order-sweep pass.
</verification>

<success_criteria>
- Telemetry-first boot + SIGTERM shutdown; instrumented clear/settle path; token-free S1 status page + /health live; SecretsProvider-resolved credentials (env default preserved); idempotent + FSM-guarded mutations; §4 still $100.00 and all existing endpoint contracts intact.
</success_criteria>

## Artifacts this phase produces (this plan)

- Endpoints: `GET /health`, `GET /status`, `GET /status.html` (token-free)
- Boot wiring: `initTelemetry()` first + `shutdownTelemetry()` on SIGTERM; redacting `log()` adopted; `SecretsProvider` resolves ANTHROPIC_API_KEY into the injected agent client
- Middleware: `idempotencyMiddleware` on mutating POSTs; `transition()` FSM guard on settle/close
- Metrics emitted: `umbra.rounds.opened`, `umbra.clear.latency_ms`, `umbra.settle.latency_ms`, `umbra.agent.verified_total{source}`
- Env vars documented: `OTEL_EXPORTER_OTLP_ENDPOINT`, `SECRETS_PROVIDER`, `VAULT_ADDR`, `VAULT_TOKEN`

<output>
Create `.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-07-SUMMARY.md` when done.
</output>
