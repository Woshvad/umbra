---
phase: 13-platform-baseline-adjacent-track-b-ongoing
plan: 08
type: execute
wave: 2
depends_on: [13-06]
files_modified:
  - daml/Umbra/Issuance.daml
  - daml/Umbra/Tests.daml
  - web/daml.js
autonomous: true
requirements: [ADJ-03]
must_haves:
  truths:
    - "An Issuer offers a bond tranche; desks submit sealed bids; the SAME §8 computeClearing allocates the issuance at ONE uniform price"
    - "Winners receive minted Holdings (create Holding conforming to HoldingV1.Holding, CIP-0056) at the uniform issuance price"
    - "A Coupon choice pays cash pro-rata to current bond holders (deterministic amounts) with a per-round guard against double-pay"
    - "A Redeem choice repays principal at maturity and retires the bond Holdings, conserving cash"
    - "All new templates are keyless (fetch-by-cid + assert); the §4 secondary-market bond is untouched (Clearing.daml / Round.Clear byte-unchanged); web/daml.js regenerated + committed"
  artifacts:
    - path: "daml/Umbra/Issuance.daml"
      provides: "Uniform-price primary issuance over §8 + Coupon/Redeem lifecycle choices (keyless)"
      contains: "module Umbra.Issuance"
    - path: "daml/Umbra/Tests.daml"
      provides: "ADJ-03 scenarios: uniform-price mint, coupon pro-rata, redeem retires + conserves"
      contains: "test_issuance"
    - path: "web/daml.js"
      provides: "Regenerated TS bindings including Rfq + Issuance templates"
      contains: "Issuance"
  key_links:
    - from: "daml/Umbra/Issuance.daml"
      to: "Umbra.Clearing.computeClearing"
      via: "uniform issuance price (reuse §8, not a parallel mechanism)"
      pattern: "computeClearing"
    - from: "daml/Umbra/Issuance.daml Coupon/Redeem"
      to: "Umbra.Holding (mint / transfer / retire)"
      via: "keyless create/moveExactHolding"
      pattern: "Holding"
---

<objective>
Deliver ADJ-03 as a new keyless Daml module: primary bond issuance that reuses the existing §8 uniform-price `computeClearing` to allocate a new tranche at one uniform price and mint `Holding`s (CIP-0056 conformant) to winners, plus post-settlement `Coupon` (issuer pays cash pro-rata to current holders) and `Redeem` (repay principal, retire holdings) lifecycle choices. Regenerates + commits `web/daml.js` now that both new templates (Rfq from 13-06, Issuance here) exist. Solver orchestration and the optional web panel land in later plans.

Purpose: prove the auction engine is issuance-capable (not just secondary-market) and add a bond lifecycle, reusing the proven clearing + custody, without perturbing §4.
Output: `daml/Umbra/Issuance.daml`, ADJ-03 scenarios in `Tests.daml`, regenerated `web/daml.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-RESEARCH.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-PATTERNS.md
@daml/Umbra/Holding.daml
@daml/Umbra/Clearing.daml
@daml/Umbra/Instrument.daml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Issuance.daml — uniform-price mint (reuse §8) + Coupon + Redeem (keyless)</name>
  <files>daml/Umbra/Issuance.daml</files>
  <read_first>
    - 13-PATTERNS.md lines 127-133 (analog: computeClearing uniform price + Holding mint + Settlement; Coupon enumerates holders via ACS; keyless; §4 untouched)
    - daml/Umbra/Clearing.daml lines 276-277 (computeClearing signature — reuse for the uniform issuance price), line 31 (Side), lines 37-48 (Allocation)
    - daml/Umbra/Holding.daml lines 34-44 (mint via create Holding), lines 88-102 (HoldingV1 interface conformance), lines 109-117 (moveExactHolding for cash transfers)
    - daml/Umbra/Instrument.daml lines 20-24 (InstrumentId), lines 31-42 (Instrument template)
    - 13-RESEARCH.md lines 416-424 (issuance+coupon sketch), lines 468-470 (Open Question 4 — coupon holder enumeration + per-round marker to avoid double-pay)
  </read_first>
  <action>
    Create `daml/Umbra/Issuance.daml` (module `Umbra.Issuance`) importing `computeClearing`/`Side`/`Allocation` from Umbra.Clearing, `Holding`/`moveExactHolding` from Umbra.Holding, and `InstrumentId`/`Instrument` from Umbra.Instrument. Define a KEYLESS `template IssuanceRound` (fields: `operator`, `issuer`, `bondInstrument : InstrumentId`, `cashInstrument : InstrumentId`, `trancheSize : Int`, and the sealed issuance-bid order views). Add a `choice ClearIssuance` (controller operator/issuer) that calls `computeClearing` over the issuance bids to get the SINGLE uniform issuance price + per-winner allocation, then for each winning allocation `create Holding with operator; owner = winner; instrument = bondInstrument; amount = filledQty` (mint, conforming to HoldingV1), and moves the winners' cash to the issuer via `moveExactHolding` — asserting per-instrument conservation on-ledger. Add a keyless `choice Coupon` on the issuance/instrument (controller operator/issuer) that enumerates current bond `Holding`s (passed as cids, gathered the way readSealedOrders gathers the ACS) and pays cash pro-rata to each holder deterministically, with a per-round coupon marker (a created `CouponPaid` contract or a passed guard) preventing double-pay. Add a keyless `choice Redeem` that repays principal to holders and retires (archives) the bond Holdings, conserving cash. NO contract keys anywhere; every cross-contract read is `fetch` + `assertMsg`. Do NOT touch Clearing.daml or the §4 Round.Clear path (issuance is additive; the secondary-market bond is untouched).
  </action>
  <acceptance_criteria>
    - `daml/Umbra/Issuance.daml` declares `module Umbra.Issuance`, a keyless `IssuanceRound` template, and choices `ClearIssuance`, `Coupon`, `Redeem`.
    - ClearIssuance calls `computeClearing` (reuses §8 — no parallel clearing logic) and `create Holding` for winners.
    - No `key`/`maintainer` appears in Issuance.daml (keyless — grep returns nothing).
    - Clearing.daml is byte-unchanged (git diff empty).
    - `daml build` (Bash, cwd daml/) succeeds.
  </acceptance_criteria>
  <verify>
    <automated>cd daml && /c/Users/woshv/bin/daml build 2>&1 | tail -5</automated>
  </verify>
  <done>Keyless issuance module compiles: uniform-price mint via reused §8, Coupon pro-rata, Redeem retires — Clearing.daml untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Tests.daml — ADJ-03 scenarios (uniform-price mint, coupon, redeem) + §4 regression</name>
  <files>daml/Umbra/Tests.daml</files>
  <read_first>
    - daml/Umbra/Tests.daml (existing §4 golden + settlement/conservation scenario structure to mirror)
    - 13-VALIDATION.md line 50 (ADJ-03 test contract: uniform-price mint; Coupon pro-rata; Redeem retires + conserves; §4 secondary bond untouched)
  </read_first>
  <action>
    Add Daml Script scenarios to `Tests.daml`: `test_issuance_uniform_price` — an issuer offers a tranche, several desks submit sealed issuance bids, `ClearIssuance` allocates at ONE uniform price and mints the winners' bond Holdings + moves cash to the issuer, conserving per instrument. `test_issuance_coupon_prorata` — after issuance, `Coupon` pays cash pro-rata to the minted holders (deterministic amounts) and a second Coupon for the same period is rejected (no double-pay). `test_issuance_redeem` — `Redeem` at maturity repays principal to holders and archives the bond Holdings, conserving cash. `test_issuance_sec_bond_untouched` (or reuse the §4 golden) — assert the §4 secondary-market fixture still clears $100.00 / A=10 B=8 C=2 alongside the issuance scenarios. Do NOT modify the §4 golden test body.
  </action>
  <acceptance_criteria>
    - `Tests.daml` contains `test_issuance_uniform_price`, `test_issuance_coupon_prorata`, `test_issuance_redeem`.
    - The uniform-price test asserts a single issuance price + minted Holdings + conservation.
    - The coupon test asserts pro-rata amounts and a double-pay rejection.
    - The §4 golden still passes ($100.00 / A=10 B=8 C=2).
    - `daml test` (Bash, cwd daml/) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd daml && /c/Users/woshv/bin/daml test 2>&1 | tail -15</automated>
  </verify>
  <done>Issuance/coupon/redeem scenarios pass; the §4 golden stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate + commit web/daml.js (Rfq + Issuance bindings)</name>
  <files>web/daml.js</files>
  <read_first>
    - 13-RESEARCH.md lines 329-330 (regenerate + commit web/daml.js after any Daml template addition — fresh-clone invariant), lines 431 (JSON Ledger API v2 codegen note)
    - .planning/STATE.md line 144 (prior regen precedent 6e4bade; generated bindings package @daml.js/umbra-0.1.0)
    - CLAUDE.md (daml runs via Bash `/c/Users/woshv/bin/daml`, cwd daml/; NEVER add Claude git attribution)
  </read_first>
  <action>
    After both Rfq.daml (13-06) and Issuance.daml compile, run the Daml→JS codegen (Bash, `/c/Users/woshv/bin/daml codegen js` per the project workflow) to regenerate the TS bindings, refreshing `web/daml.js` so the new `Umbra.Rfq` and `Umbra.Issuance` templates/choices are available to the web client. Stage and (per the project commit discipline) prepare `web/daml.js` for commit — the fresh-clone invariant requires the generated bindings be committed. Do NOT hand-edit generated output. Author/committer stays `woshvad`; NO Claude attribution / Co-Authored-By / "Generated with".
  </action>
  <acceptance_criteria>
    - `web/daml.js` contains generated modules for `Rfq` and `Issuance` (grep the regenerated tree for `Rfq` and `Issuance`).
    - The regenerated bindings match the current DAR (codegen ran cleanly; no hand edits).
    - `cd web && npx tsc --noEmit` (or the web build) still compiles against the refreshed bindings.
  </acceptance_criteria>
  <verify>
    <automated>ls web/daml.js && grep -rl "Rfq" web/daml.js | head -1 && grep -rl "Issuance" web/daml.js | head -1</automated>
  </verify>
  <done>web/daml.js regenerated with Rfq + Issuance bindings and staged for commit (no Claude attribution); fresh-clone invariant preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| issuer → ClearIssuance | Mints new bond supply — over-mint risk |
| operator → Coupon/Redeem | Pays/repays cash — double-pay / over-pay risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-22 | Tampering (over-mint) | Issuance.daml ClearIssuance | mitigate | Reuses §8 computeClearing for a single uniform price + on-ledger per-instrument conservation assert; minted amount == cleared allocation |
| T-13-23 | Tampering (double coupon) | Issuance.daml Coupon | mitigate | Per-round coupon marker prevents a second payment for the same period; deterministic pro-rata amounts; test asserts double-pay rejection |
| T-13-24 | Tampering (§4 perturbation) | shared computeClearing | mitigate | Clearing.daml byte-unchanged; §4 golden + daml test regression gate; issuance is additive |
</threat_model>

<verification>
- `cd daml && /c/Users/woshv/bin/daml build` — succeeds.
- `cd daml && /c/Users/woshv/bin/daml test` — exit 0 (issuance + RFQ + §4 golden green).
- `web/daml.js` regenerated (Rfq + Issuance present); web still compiles.
- Confirm Clearing.daml + the §4 golden test body are byte-unchanged.
</verification>

<success_criteria>
- Keyless uniform-price issuance (reusing §8) mints Holdings at one price; Coupon pays pro-rata (no double-pay); Redeem retires + conserves; §4 stays $100.00; web/daml.js regenerated + committed.
</success_criteria>

## Artifacts this phase produces (this plan)

- Files: `daml/Umbra/Issuance.daml`; ADJ-03 scenarios in `daml/Umbra/Tests.daml`; regenerated `web/daml.js`
- Daml templates: `IssuanceRound` (keyless)
- Daml choices: `ClearIssuance` (reuse §8 → mint Holdings), `Coupon` (pro-rata, double-pay-guarded), `Redeem` (retire + conserve)
- Tests: `test_issuance_uniform_price`, `test_issuance_coupon_prorata`, `test_issuance_redeem`
- Regenerated bindings: `@daml.js/umbra` now includes `Umbra.Rfq` + `Umbra.Issuance`

<output>
Create `.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-08-SUMMARY.md` when done.
</output>
