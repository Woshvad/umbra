---
phase: 13-platform-baseline-adjacent-track-b-ongoing
plan: 09
type: execute
wave: 3
depends_on: [13-04, 13-05, 13-07]
files_modified:
  - solver/src/api.ts
  - solver/src/index.ts
  - solver/src/clock.ts
  - solver/src/api.test.ts
  - solver/src/index.test.ts
  - solver/src/clock.test.ts
autonomous: true
requirements: [OPS-04, OPS-05]
must_haves:
  truths:
    - "round.opened fires off the open seam; round.sealed/cleared/settled + fill.posted fire off the close/clear/settle seams — each an HMAC-signed, retried webhook"
    - "Authenticated webhook register/unregister endpoints manage subscriptions; no secret is ever echoed"
    - "POST /sandbox/round deterministically seeds + clears + settles the canonical §4 fixture at exactly $100.00 (A=10/B=8/C=2), isolated from real rounds"
    - "POST /fix accepts a raw FIX 4.4 NewOrderSingle string and returns a raw ExecutionReport string; a malformed frame → 35=8 reject (never a 500 throw)"
    - "The five §11 endpoints + /settle stay byte-compatible; the §4 golden stays green; no operator/Anthropic secret appears in any webhook/sandbox/FIX response"
  artifacts:
    - path: "solver/src/api.ts"
      provides: "POST /sandbox/round, POST /fix, webhook register/unregister endpoints + lifecycle webhook emits"
      contains: "/sandbox/round"
  key_links:
    - from: "solver/src/index.ts open seam + clock.ts close/settle seam"
      to: "webhooks.emit(event, data)"
      via: "lifecycle event firing"
      pattern: "emit\\("
    - from: "solver/src/api.ts POST /sandbox/round"
      to: "sandbox.assertSandboxClears"
      via: "deterministic §4 fixture $100.00"
      pattern: "assertSandboxClears"
    - from: "solver/src/api.ts POST /fix"
      to: "fix.handleFixMessage"
      via: "HTTP-wrapped raw-FIX acceptor"
      pattern: "handleFixMessage"
---

<objective>
Wire the OPS-04 webhook emitter + sandbox fixture and the OPS-05 FIX acceptor (all built in Wave 1) into the running solver: fire signed/retried lifecycle webhooks off the existing open/close/clear/settle seams, expose authenticated subscription register/unregister endpoints, add the deterministic `POST /sandbox/round` (§4 @ $100.00) and the HTTP-wrapped `POST /fix` raw-FIX endpoint. Thin wiring — logic lives in webhooks.ts / sandbox.ts / fix.ts. The five §11 endpoints and the §4 golden stay intact.

Purpose: deliver reliable round-lifecycle webhooks, a stable §4 sandbox contract for integrators, and a FIX order-entry surface for institutional OMSs.
Output: updated `api.ts`, `index.ts`, `clock.ts`, extended tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-RESEARCH.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-PATTERNS.md
@solver/src/api.ts
@solver/src/index.ts
@solver/src/clock.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fire lifecycle webhooks off the open/close/clear/settle seams + register endpoints</name>
  <files>solver/src/index.ts, solver/src/clock.ts, solver/src/api.ts, solver/src/index.test.ts, solver/src/clock.test.ts</files>
  <read_first>
    - 13-PATTERNS.md lines 75-83 (fired off existing seams: round.opened at index open path, close/clear/settle through clock.forceClose + api settle; fill.posted per readTradeConfirmations)
    - solver/src/index.ts lines 106-146 (buildDeps openRound wiring — the open seam), lines 478-500 (buildDeps call + openRoundClock)
    - solver/src/clock.ts lines 62-74 (close routine — the close/sealed seam), lines 91-93 (forceClose)
    - solver/src/api.ts lines 100 (readTradeConfirmations for fill.posted), settle handler region ~428-460
    - 13-RESEARCH.md lines 454-457 (Open Question 1 — fire round.settled once + fill.posted per confirmation)
  </read_first>
  <action>
    Construct the webhook emitter (`createWebhooks` from 13-04) once at boot and thread it through `buildDeps`/`AppDeps` (a new optional dep). Fire `round.opened` off the index.ts open seam (where openRound + openRoundClock run), `round.sealed` off the clock.ts close routine (window sealed at close), `round.cleared` and `round.settled` off the api.ts settle handler, and `fill.posted` once PER confirmation read from `readTradeConfirmations` at settle (Open Question 1 recommendation). Each emit passes only aggregate/round data (id, phase, aggregate numbers) — never a secret/order-content. Add authenticated `POST /webhooks` (register `{url, secret, events}`) and `DELETE /webhooks/:id` (unregister) endpoints on createApp, zod-validated (`.strict()`), returning the secret-safe envelope (the subscription secret is stored module-private and NEVER echoed in the response). Extend index.test.ts/clock.test.ts to assert the emitter fires on the open + close seams (inject a spy emitter).
  </action>
  <acceptance_criteria>
    - `round.opened` fires off the open seam and `round.sealed` off clock.ts close (asserted via an injected spy emitter).
    - `round.cleared`/`round.settled`/`fill.posted` fire off the settle path (fill.posted once per confirmation).
    - `POST /webhooks` registers a subscription (zod-validated) and the response body does NOT contain the subscription secret.
    - `npx vitest run src/index.test.ts src/clock.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/index.test.ts src/clock.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Signed lifecycle webhooks fire off every seam; subscription register/unregister endpoints exist; no secret echoed.</done>
</task>

<task type="auto">
  <name>Task 2: POST /sandbox/round — deterministic §4 fixture ($100.00), isolated</name>
  <files>solver/src/api.ts, solver/src/index.ts, solver/src/api.test.ts</files>
  <read_first>
    - solver/src/sandbox.ts (from 13-04: sandboxFixtureOrders + assertSandboxClears)
    - solver/src/api.ts lines 368-397 (createApp + POST /round shape to mirror), lines 428-460 (settle path to reuse)
    - 13-CONTEXT.md line 53 (sandbox always seeds+clears §4, reuses §8+settle, asserts $100.00, isolated from real rounds)
  </read_first>
  <action>
    Add `POST /sandbox/round` on createApp: it deterministically seeds the canonical §4 fixture (via `sandboxFixtureOrders`), runs the clear + settle path, and asserts the result is exactly $100.00 with fills A=10/B=8/C=2 (`assertSandboxClears`) before returning a stable sandbox response `{ roundId: 'SANDBOX-...', clearingPrice: 100.00, allocations, matched: 10, sandbox: true }`. The sandbox round is ISOLATED from real rounds (a distinct id namespace; it never mutates a real round's state) and NEVER varies from $100.00. Wire any needed dep through buildDeps. Extend api.test.ts asserting the sandbox response is exactly $100.00 / A=10 B=8 C=2, is marked `sandbox:true`, does not collide with a real round id, and passes a secret-sweep.
  </action>
  <acceptance_criteria>
    - `POST /sandbox/round` returns clearingPrice 100.00 and fills A=10/B=8/C=2 with `sandbox:true`.
    - The sandbox round id is namespaced (does not overwrite a real round).
    - Secret-sweep: no ANTHROPIC_API_KEY/operator-token sentinel in the response.
    - `npx vitest run src/api.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/api.test.ts src/sandbox.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>The §4 sandbox endpoint deterministically clears $100.00, isolated and secret-free.</done>
</task>

<task type="auto">
  <name>Task 3: POST /fix — HTTP-wrapped raw-FIX order-entry acceptor</name>
  <files>solver/src/api.ts, solver/src/api.test.ts</files>
  <read_first>
    - solver/src/fix.ts (from 13-05: handleFixMessage + fixToSubmitOrder)
    - solver/src/api.ts lines 221-230 (ApiError), lines 303-307 (wrap), lines 368-372 (createApp/express.json)
    - 13-RESEARCH.md line 282 (HTTP-wrapped raw-FIX as the primary, offline-testable), lines 58 (Alternatives — HTTP-wrapped primary)
  </read_first>
  <action>
    Add `POST /fix` on createApp accepting a raw FIX string body (text/plain or a `{fix}` field), passing it to `handleFixMessage` (from 13-05) and returning the raw `ExecutionReport` (35=8) string — OrdStatus 0 New when the 35=D maps to a valid sealed order (which the handler forwards to `Venue.SubmitOrder` via the desk seam), OrdStatus 8 Rejected on a malformed/unmapped frame. A malformed input must NOT produce a 500 — the acceptor returns a framed 35=8 reject (never throws). Honest label in a route comment: "FIX 4.4 subset — order entry only; live OMS interop is a UAT gate." Never interpolate any operator/Anthropic credential into the response. Extend api.test.ts: a known-good NewOrderSingle returns a 35=8 New; a malformed frame returns a 35=8 Rejected with a 200/4xx (not a 500 throw); a secret-sweep asserts no credential in the FIX response.
  </action>
  <acceptance_criteria>
    - `POST /fix` returns a raw ExecutionReport (35=8) string; a valid 35=D → OrdStatus 0, a malformed frame → OrdStatus 8 (no 500).
    - Secret-sweep: no operator-token/ANTHROPIC_API_KEY sentinel in the response.
    - The five §11 endpoints + /settle remain byte-compatible (existing api.test.ts green).
    - `npx vitest run src/api.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/api.test.ts src/fix.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>The HTTP-wrapped FIX endpoint maps 35=D → a sealed order and returns a 35=8 report; malformed frames reject without throwing; credential-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| solver → subscriber URL | Signed lifecycle payloads leave the venue |
| client → /webhooks register | Authenticated subscription management |
| OMS → POST /fix | Untrusted FIX frames enter the mapper |
| integrator → POST /sandbox/round | Public-ish deterministic fixture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-25 | Spoofing/Tampering | webhook emit | mitigate | HMAC-SHA256 + timestamp replay-guard (13-04); payloads carry only aggregate/round data |
| T-13-26 | Information Disclosure | /webhooks register response | mitigate | Subscription secret stored module-private, never echoed; secret-sweep test |
| T-13-27 | Tampering/DoS | POST /fix | mitigate | fix.ts bounds-checked parse + BodyLength/CheckSum; malformed → 35=8 reject, never a 500 |
| T-13-28 | Tampering (fixture drift) | POST /sandbox/round | mitigate | assertSandboxClears asserts $100.00/A=10/B=8/C=2; isolated id namespace; §4 golden gate |
</threat_model>

<verification>
- `cd solver && npx vitest run` (full solver suite) — green.
- `cd solver && npx tsc --noEmit` — clean.
- Confirm the §4 golden still clears $100.00 and the five §11 endpoints are byte-compatible.
- Secret-sweep across /webhooks, /sandbox/round, /fix responses.
</verification>

<success_criteria>
- Signed/retried lifecycle webhooks + register/unregister endpoints live; deterministic §4 sandbox endpoint ($100.00, isolated); HTTP-wrapped FIX acceptor (35=D→35=8, malformed-safe, credential-free); §4 golden green; §11 contracts intact.
</success_criteria>

## Artifacts this phase produces (this plan)

- Endpoints: `POST /sandbox/round`, `POST /fix`, `POST /webhooks` (register), `DELETE /webhooks/:id` (unregister)
- Lifecycle webhook emits: `round.opened` (open seam), `round.sealed` (close seam), `round.cleared`/`round.settled`/`fill.posted` (settle seam)
- AppDeps additions: the webhook emitter threaded through buildDeps
- Honest label: "FIX 4.4 subset — order entry only; live OMS interop is a UAT gate"

<output>
Create `.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-09-SUMMARY.md` when done.
</output>
