---
phase: 13-platform-baseline-adjacent-track-b-ongoing
plan: 10
type: execute
wave: 3
depends_on: [13-07]
files_modified:
  - solver/src/agent.ts
  - solver/src/agent.test.ts
autonomous: true
requirements: [ADJ-01]
must_haves:
  truths:
    - "proposeCompeting(views, configs) runs N agent configs concurrently and returns a ranked leaderboard + a winner + the deterministic result"
    - "The deterministic §8 recompute is the REFEREE: a proposal is eligible/verified ONLY if it equals the deterministic result (priceEqual && allocationsEqual)"
    - "Ranking is by (matchedVolume desc, surplus desc) among verified proposals; the winner is narrative, never a settlement input"
    - "The deterministic clear still settles unconditionally — AI stays off the settlement path; a failing/timeout config degrades to verified:false and never throws"
    - "The §4 fixture stays $100.00 (proposeClearing byte-unchanged; proposeCompeting is additive)"
  artifacts:
    - path: "solver/src/agent.ts"
      provides: "proposeCompeting(views, configs) — additive competing-solver ranking on the verify gate"
      exports: ["proposeCompeting", "CompetingResult", "SolverConfig"]
  key_links:
    - from: "solver/src/agent.ts proposeCompeting"
      to: "computeClearing (referee) + priceEqual/allocationsEqual"
      via: "verify-only eligibility"
      pattern: "allocationsEqual"
---

<objective>
Deliver ADJ-01 as an additive extension to `agent.ts`: `proposeCompeting(views, configs)` runs N solver configs (varying model/prompt/temperature) concurrently, marks each proposal verified ONLY when it equals the deterministic §8 result (the referee), and returns a narrative leaderboard ranked by (matched volume, surplus). The existing single-agent `proposeClearing` is byte-unchanged; the deterministic clear still settles unconditionally (AI off the settlement path). This plan builds + unit-tests the ranking with a mocked SDK; the API endpoint + web leaderboard land in later plans.

Purpose: race competing AI solvers as a narrative leaderboard, refereed by the deterministic recompute, without ever risking correctness.
Output: `proposeCompeting` in `agent.ts` + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-RESEARCH.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-PATTERNS.md
@solver/src/agent.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agent.ts — proposeCompeting(views, configs) additive ranking on the verify gate</name>
  <files>solver/src/agent.ts, solver/src/agent.test.ts</files>
  <read_first>
    - 13-PATTERNS.md lines 109-113 (analog: agent.ts itself — reuse the verify gate wholesale; referee = deterministic §8; failing config degrades to verified:false via withTimeout+catch)
    - solver/src/agent.ts lines 179-189 (priceEqual + allocationsEqual predicates), lines 197-208 (AgentResult), lines 243-249 (withTimeout), lines 290-326 (createAgent + proposeClearing structure to mirror), lines 356-358 (the exact-match verify gate)
    - 13-RESEARCH.md lines 284-297 (Pattern 8 proposeCompeting sketch), lines 557-558 (referee threat mitigation)
  </read_first>
  <behavior>
    - Given N configs, each producing a proposal, only proposals that satisfy priceEqual && allocationsEqual vs computeClearing are `verified`.
    - The leaderboard ranks verified proposals by (matchedVolume desc, then surplus desc); the winner is leaderboard[0] or null when none verified.
    - A config whose SDK call throws / times out contributes a `verified:false` entry (excluded from ranking) and proposeCompeting never throws.
    - The returned `deterministic` block always carries the authoritative §8 numbers; on the §4 fixture that is $100.00 / A=10 B=8 C=2.
  </behavior>
  <action>
    Add to `agent.ts` (additive — do NOT modify `proposeClearing`) an exported `SolverConfig` type (`{ id, model, temperature, systemPrompt? }`), a `CompetingResult` type (`{ winner: RankedProposal | null; leaderboard: RankedProposal[]; deterministic: ClearingResult-ish }`), and `proposeCompeting(views, configs)` on the createAgent factory. It computes `det = computeClearing(views)` as the referee, runs each config concurrently via `Promise.all` reusing the existing per-call internals (each wrapped in `withTimeout` + catch so a failure/timeout yields `verified:false`, never throws), marks a proposal `verified` ONLY when `priceEqual(p, det.clearingPrice) && allocationsEqual(alloc, det.allocations)`, computes each proposal's `matchedVolume` (matchedAt) + `surplus`, ranks the verified set by (matchedVolume desc, surplus desc), and returns `{ winner, leaderboard, deterministic }`. The narrative winner is NEVER used to settle — document that the deterministic clear settles unconditionally (AI off the settlement path). Each leaderboard entry carries an honest config tag (model + temperature).
    Write agent.test.ts cases (mocked SDK): three configs where two agree with §8 (verified) and one diverges (unverified, excluded); ranking order by matched then surplus; a throwing/timeout config → verified:false, no throw; the deterministic block is $100.00/A=10/B=8/C=2 on the §4 fixture; a secret-sweep asserting the ANTHROPIC_API_KEY sentinel is absent from the CompetingResult.
  </action>
  <acceptance_criteria>
    - `solver/src/agent.ts` contains `export ... proposeCompeting`, `SolverConfig`, `CompetingResult`; `proposeClearing` is byte-unchanged (git diff shows only additions).
    - Only equal-to-deterministic proposals are `verified`; a diverging proposal is excluded from the ranked leaderboard.
    - A throwing/timeout config yields `verified:false` and proposeCompeting does not throw.
    - On the §4 fixture the `deterministic` block is clearingPrice 100.00 / A=10 B=8 C=2.
    - Secret-sweep: no ANTHROPIC_API_KEY sentinel in the result.
    - `npx vitest run src/agent.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/agent.test.ts src/auction.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>proposeCompeting ranks verified proposals (referee = deterministic §8), degrades failing configs, never throws, keeps AI off the settlement path; §4 stays $100.00.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N AI configs → ranking | Multiple untrusted proposals compete; only verified ones rank |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-29 | Tampering (unfair clear) | proposeCompeting | mitigate | Deterministic §8 is the referee — only equal-to-deterministic proposals are eligible; the deterministic clear settles unconditionally (AI off settlement path) |
| T-13-30 | Denial of Service | a hanging config | mitigate | Each config wrapped in withTimeout + catch → verified:false; proposeCompeting never throws |
| T-13-31 | Information Disclosure | CompetingResult | mitigate | Reuses agent.ts module-private key discipline; secret-sweep asserts no key in the result |
</threat_model>

<verification>
- `cd solver && npx vitest run src/agent.test.ts src/auction.test.ts` — green.
- `cd solver && npx tsc --noEmit` — clean.
- Confirm `proposeClearing` is byte-unchanged (git diff additive-only in agent.ts).
</verification>

<success_criteria>
- Competing-solver ranking is additive, referee-gated (verify-only eligibility), failure-tolerant, and off the settlement path; §4 stays $100.00.
</success_criteria>

## Artifacts this phase produces (this plan)

- Files: `solver/src/agent.ts` (extended) + `solver/src/agent.test.ts` (extended)
- Exports: `proposeCompeting(views, configs)`, `SolverConfig`, `CompetingResult`
- Guarantee: deterministic §8 is the referee; the leaderboard is narrative, never a settlement input

<output>
Create `.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-10-SUMMARY.md` when done.
</output>
