---
phase: 13-platform-baseline-adjacent-track-b-ongoing
plan: 11
type: execute
wave: 4
depends_on: [13-08, 13-09, 13-10]
files_modified:
  - solver/src/ledger.ts
  - solver/src/api.ts
  - solver/src/index.ts
  - solver/src/ledger.test.ts
  - solver/src/api.test.ts
  - solver/src/index.test.ts
autonomous: true
requirements: [ADJ-01, ADJ-02, ADJ-03]
must_haves:
  truths:
    - "A competing-solvers endpoint returns the ranked leaderboard + winner + deterministic result (proposeCompeting); the deterministic clear still settles"
    - "RFQ endpoints post an RfqRequest, list firm signed quotes, and accept the best — settling a 1×1 batch via the on-ledger AcceptQuote → settleBatch DvP"
    - "Issuance endpoints clear a tranche at one uniform price (mint Holdings), pay a coupon pro-rata, and redeem at maturity"
    - "All new endpoints are zod-validated with the secret-safe {error:{code,message}} envelope; no operator/Anthropic secret is echoed; the browser never holds a credential"
    - "The five §11 endpoints + /settle stay byte-compatible; the §4 golden stays green"
  artifacts:
    - path: "solver/src/ledger.ts"
      provides: "RFQ + issuance/coupon/redeem exercise wrappers over JSON Ledger API v2"
      contains: "AcceptQuote"
    - path: "solver/src/api.ts"
      provides: "POST /competing, RFQ endpoints, issuance endpoints"
      contains: "/competing"
  key_links:
    - from: "solver/src/api.ts POST /competing"
      to: "agent.proposeCompeting"
      via: "AppDeps"
      pattern: "proposeCompeting"
    - from: "solver/src/ledger.ts acceptQuote"
      to: "Umbra.Rfq AcceptQuote (settleBatch)"
      via: "JSON Ledger API v2 exercise, cids gathered like settle()"
      pattern: "AcceptQuote"
    - from: "solver/src/ledger.ts clearIssuance"
      to: "Umbra.Issuance ClearIssuance"
      via: "exercise → mint Holdings"
      pattern: "ClearIssuance"
---

<objective>
Wire the adjacent capabilities into the running solver: expose the ADJ-01 competing-solvers leaderboard endpoint (reusing `proposeCompeting`), and orchestrate the ADJ-02 RFQ (post request → list firm quotes → accept best → settle via `AcceptQuote`/`settleBatch`) and ADJ-03 issuance (clear tranche → mint Holdings; coupon; redeem) flows through `ledger.ts` exercise wrappers over JSON Ledger API v2 + zod-validated `api.ts` endpoints. The five §11 endpoints and the §4 golden stay intact; every new endpoint is secret-safe and the browser never holds a credential.

Purpose: make competing solvers, RFQ, and issuance/coupon reachable from the web layer, orchestrated server-side, settling through the proven on-ledger DvP.
Output: RFQ/issuance wrappers in `ledger.ts`; competing/RFQ/issuance endpoints in `api.ts`; deps in `index.ts`; extended tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-RESEARCH.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-PATTERNS.md
@solver/src/ledger.ts
@solver/src/api.ts
@daml/Umbra/Rfq.daml
@daml/Umbra/Issuance.daml
</context>

<tasks>

<task type="auto">
  <name>Task 1: ledger.ts — RFQ orchestration (post/quote/list/accept → settleBatch)</name>
  <files>solver/src/ledger.ts, solver/src/ledger.test.ts</files>
  <read_first>
    - 13-PATTERNS.md lines 117-124 (RFQ reuses settleBatch; gather cids the way api.ts/settle does — Assumption A6)
    - solver/src/ledger.ts (existing exercise pattern over JSON Ledger API v2: package-NAME `#umbra:Module:Entity` addressing, Int/Decimal as strings out, cid re-query before exercise; the settle() cid-gathering pattern to mirror; `_operatorToken` module-private)
    - daml/Umbra/Rfq.daml (RfqRequest / Quote / AcceptQuote signatures from 13-06)
    - .planning/STATE.md line 124 (Option-B cid marshaling: Int/Decimal as strings, ContractId branded string cast at the exercise site)
  </read_first>
  <action>
    Add to `ledger.ts` keyless-Daml exercise wrappers over JSON Ledger API v2 (address templates by package-NAME `#umbra:Umbra.Rfq:...`): `postRfq(requester, side, quantity)` (create RfqRequest), `createQuote(dealer, requester, price, quantity)` (create the firm Quote), `listQuotes(rfqCid)` (query the requester-visible Quotes), and `acceptQuote(rfqCid, quoteCid)` which gathers the bond/cash source Holding cids the way `settle()` gathers cids (re-query cids before exercise; Int/Decimal as strings; branded-ContractId cast at the exercise site) and exercises `AcceptQuote`, returning a secret-free settle summary. The operator token stays module-private (mirror `_operatorToken`); nothing crosses out but the operator's public party id + the settlement result. Extend ledger.test.ts with DI-stubbed exercise calls asserting the correct choice + argument marshaling shape (no live ledger) and a secret-sweep.
  </action>
  <acceptance_criteria>
    - `ledger.ts` contains `postRfq`, `createQuote`, `listQuotes`, `acceptQuote`; `acceptQuote` exercises `AcceptQuote` and gathers source cids like settle().
    - Choice args marshal Int/Decimal as strings; the operator token never appears in a return value (secret-sweep).
    - `npx vitest run src/ledger.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/ledger.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>RFQ exercise wrappers post/quote/list/accept over JSON Ledger API v2, reusing the settle() cid-gathering, secret-free.</done>
</task>

<task type="auto">
  <name>Task 2: ledger.ts — issuance orchestration (clear/mint, coupon, redeem)</name>
  <files>solver/src/ledger.ts, solver/src/ledger.test.ts</files>
  <read_first>
    - daml/Umbra/Issuance.daml (IssuanceRound / ClearIssuance / Coupon / Redeem signatures from 13-08)
    - solver/src/ledger.ts (the settle() gather pattern + `readSealedOrders` ACS-query pattern to reuse for Holder enumeration in Coupon)
    - 13-RESEARCH.md lines 468-470 (Open Question 4 — coupon holder enumeration via ACS query + per-round marker)
  </read_first>
  <action>
    Add to `ledger.ts`: `openIssuance(issuer, bondInstrument, cashInstrument, trancheSize)` (create IssuanceRound), `clearIssuance(issuanceCid)` (exercise ClearIssuance → the uniform issuance price + minted Holdings summary), `payCoupon(instrumentRef)` which enumerates current bond `Holding` cids via an ACS query (like `readSealedOrders`) and exercises `Coupon` with the deterministic pro-rata cash, and `redeem(instrumentRef)` (exercise Redeem → retire holdings). All keyless (cids gathered + passed; re-query before exercise). Return secret-free summaries; operator token stays module-private. Extend ledger.test.ts with DI-stubbed assertions on the exercised choices + marshaling + secret-sweep.
  </action>
  <acceptance_criteria>
    - `ledger.ts` contains `openIssuance`, `clearIssuance`, `payCoupon`, `redeem`; `payCoupon` enumerates holders via an ACS query.
    - `clearIssuance` exercises `ClearIssuance`; returns the single uniform price + minted-holdings summary.
    - Secret-sweep: operator token never in a return value.
    - `npx vitest run src/ledger.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/ledger.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Issuance/coupon/redeem exercise wrappers exist over JSON Ledger API v2, keyless, secret-free.</done>
</task>

<task type="auto">
  <name>Task 3: api.ts endpoints — /competing + RFQ + issuance + index.ts deps</name>
  <files>solver/src/api.ts, solver/src/index.ts, solver/src/api.test.ts, solver/src/index.test.ts</files>
  <read_first>
    - solver/src/api.ts lines 84-143 (AppDeps — add proposeCompeting + RFQ/issuance deps), lines 221-241 (ApiError + zod schema pattern), lines 303-307 (wrap), lines 368-397 (createApp route registration)
    - solver/src/index.ts lines 106-146 (buildDeps), lines 190-230 (createAgent), lines 478-500 (buildDeps call) — thread the new deps
    - solver/src/agent.ts proposeCompeting (from 13-10)
    - 13-PATTERNS.md lines 144-147 (error envelope + zod on every new endpoint)
  </read_first>
  <action>
    Add to `AppDeps` the new capabilities (proposeCompeting from agent.ts; postRfq/listQuotes/acceptQuote and openIssuance/clearIssuance/payCoupon/redeem from ledger.ts) and thread them through `index.ts` `buildDeps`. On createApp register zod-`.strict()`-validated endpoints, each using `wrap` + the secret-safe `{error:{code,message}}` envelope: `POST /competing` (body = solver configs → returns `{winner, leaderboard, deterministic}`; note in a comment that this is a NARRATIVE leaderboard, never a settlement input), `POST /rfq` (post request), `GET /rfq/:id/quotes` (list firm quotes), `POST /rfq/:id/accept` (accept a quote → settle), `POST /issuance` (open + clear a tranche), `POST /issuance/:id/coupon`, `POST /issuance/:id/redeem`. All are additive — the five §11 endpoints + /settle stay byte-compatible. Extend api.test.ts (DI-stubbed deps): /competing returns a ranked leaderboard with the deterministic §4 block at $100.00; /rfq/accept returns a settle summary; /issuance clears at one uniform price; a malformed body → 400 INVALID_BODY; a secret-sweep asserts no operator-token/ANTHROPIC_API_KEY sentinel in any new response.
  </action>
  <acceptance_criteria>
    - `api.ts` registers `POST /competing`, `POST /rfq`, `GET /rfq/:id/quotes`, `POST /rfq/:id/accept`, `POST /issuance`, `POST /issuance/:id/coupon`, `POST /issuance/:id/redeem`, each zod-validated with the secret-safe envelope.
    - /competing returns `{winner, leaderboard, deterministic}` with the §4 deterministic block at $100.00.
    - Secret-sweep passes on all new endpoints; the five §11 endpoints remain byte-compatible.
    - `npx vitest run src/api.test.ts src/index.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/api.test.ts src/index.test.ts src/auction.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Competing/RFQ/issuance endpoints live, zod-validated + secret-safe, deps threaded via index.ts; §4 golden + §11 contracts intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → RFQ/issuance/competing endpoints | Untrusted request bodies |
| solver → JSON Ledger API v2 | Exercise choices under operator authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-32 | Tampering (RFQ quote forgery) | ledger.ts acceptQuote | mitigate | On-ledger AcceptQuote asserts instrument/requester/quantity; firm quote is dealer-signatory; settleBatch DvP-integrity checks |
| T-13-33 | Tampering (issuance over-mint) | ledger.ts clearIssuance | mitigate | On-ledger ClearIssuance reuses §8 + conservation asserts; mint == cleared allocation |
| T-13-34 | Information Disclosure | new endpoints + ledger wrappers | mitigate | Secret-safe envelope; operator token module-private (mirror _operatorToken); secret-sweep on every new response |
| T-13-35 | Tampering (AI settlement) | /competing | mitigate | Leaderboard is narrative-only; deterministic §8 + Round.Clear settle; documented in the route |
</threat_model>

<verification>
- `cd solver && npx vitest run` (full solver suite) — green.
- `cd solver && npx tsc --noEmit` — clean.
- Confirm §4 golden ($100.00) + five §11 endpoints byte-compatible.
- Secret-sweep across /competing, /rfq*, /issuance* responses.
</verification>

<success_criteria>
- Competing-solvers leaderboard, RFQ post/quote/accept→settle, and issuance clear/coupon/redeem are reachable, zod-validated, secret-safe, and settle through the proven on-ledger DvP; §4 stays $100.00; §11 contracts intact.
</success_criteria>

## Artifacts this phase produces (this plan)

- Endpoints: `POST /competing`, `POST /rfq`, `GET /rfq/:id/quotes`, `POST /rfq/:id/accept`, `POST /issuance`, `POST /issuance/:id/coupon`, `POST /issuance/:id/redeem`
- ledger.ts wrappers: `postRfq`, `createQuote`, `listQuotes`, `acceptQuote`, `openIssuance`, `clearIssuance`, `payCoupon`, `redeem`
- AppDeps additions: `proposeCompeting` + the RFQ/issuance ledger wrappers, threaded via buildDeps

<output>
Create `.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-11-SUMMARY.md` when done.
</output>
