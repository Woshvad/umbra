---
phase: 13-platform-baseline-adjacent-track-b-ongoing
plan: 12
type: execute
wave: 5
depends_on: [13-11]
files_modified:
  - web/src/solver.ts
  - web/src/components/SolverLeaderboard.tsx
  - web/src/views/AgentView.tsx
  - web/src/lib/leaderboard.ts
  - web/src/lib/leaderboard.test.ts
autonomous: true
requirements: [ADJ-01]
must_haves:
  truths:
    - "web/src/solver.ts gains typed client fns for competing/RFQ/issuance that mirror the api.ts wire shapes EXACTLY and ride the single SOLVER_BASE_URL (no :4000, no auth header, no operator/Anthropic credential in the bundle)"
    - "SolverLeaderboard (S2) renders on the Agent view below the proposal/rationale grid, ranking proposals with VERIFIED (ink) / UNVERIFIED (red) badges and a distinct DETERMINISTIC §8 CLEAR — AUTHORITATIVE marker"
    - "The narrative winner is marked with ink weight / a rule — never lime (lime is reserved to the true clear)"
    - "Empty / loading (COMPUTING) / offline (OFFLINE_CAPTION) / ranked states all render; the persistent tag LEADERBOARD · NARRATIVE — NOT A SETTLEMENT INPUT is shown"
    - "No new tailwind token/fontSize/keyframe; grep-clean of operator-token / @daml/react / Anthropic-key literals"
  artifacts:
    - path: "web/src/solver.ts"
      provides: "Client fns + types for competing/RFQ/issuance endpoints"
      exports: ["getCompeting", "postRfq", "getRfqQuotes", "acceptRfqQuote", "openIssuance", "clearIssuance", "payCoupon", "redeemIssuance"]
    - path: "web/src/components/SolverLeaderboard.tsx"
      provides: "S2 competing-solvers leaderboard panel (operator-plane display only)"
      contains: "LEADERBOARD"
  key_links:
    - from: "web/src/components/SolverLeaderboard.tsx"
      to: "web/src/solver.ts getCompeting"
      via: "lifted response, no token"
      pattern: "getCompeting"
    - from: "web/src/views/AgentView.tsx"
      to: "SolverLeaderboard"
      via: "additive panel below the proposal/rationale grid"
      pattern: "SolverLeaderboard"
---

<objective>
Add the web client seam for all adjacent capabilities (competing/RFQ/issuance client fns + types on `web/src/solver.ts`, mirroring the api.ts wire shapes exactly) and build the S2 competing-solvers leaderboard panel on the Agent view. The leaderboard is a NARRATIVE display (ranked, VERIFIED/UNVERIFIED badged, with the deterministic clear marked authoritative) — never a settlement input. The subsequent RFQ (S3) and issuance (S4) panels read the client fns added here.

Purpose: give the web layer typed, credential-free access to the new endpoints and surface the competing-solvers race per the UI-SPEC S2 contract.
Output: `solver.ts` client additions + `SolverLeaderboard.tsx` mounted on `AgentView.tsx` + a pure lib + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-UI-SPEC.md
@web/src/solver.ts
@web/src/views/AgentView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: web/src/solver.ts — competing/RFQ/issuance client fns + types (credential-free)</name>
  <files>web/src/solver.ts, web/src/lib/leaderboard.ts, web/src/lib/leaderboard.test.ts</files>
  <read_first>
    - web/src/solver.ts lines 17-33 (SOLVER_BASE_URL + solverPort + OFFLINE_CAPTION), lines 154-219 (call<T>() + the existing typed client fns to mirror), lines 264-296 (plain-URL builders — no :4000, no auth)
    - solver/src/api.ts (the authoritative wire shapes for /competing, /rfq*, /issuance* from 13-11 — mirror EXACTLY; STATE.md 10-07 lesson: mirror the authoritative api.ts shape over an approximate sketch)
    - 13-UI-SPEC.md lines 153-168 (S2 leaderboard data contract: rank, config tag, matched, surplus, VERIFIED/UNVERIFIED, authoritative marker)
  </read_first>
  <action>
    Extend `web/src/solver.ts` with types mirroring the api.ts responses EXACTLY (a `CompetingResponse` = `{winner, leaderboard: RankedProposal[], deterministic}` where each `RankedProposal` carries `{configTag, model, temperature, matchedVolume, surplus, verified}`; plus RFQ + issuance response types for S3/S4) and typed client fns riding the existing `call<T>()`/`SOLVER_BASE_URL`: `getCompeting(configs)` → `POST /competing`; `postRfq`, `getRfqQuotes`, `acceptRfqQuote` → the /rfq* endpoints; `openIssuance`, `clearIssuance`, `payCoupon`, `redeemIssuance` → the /issuance* endpoints. Every fn uses the single `SOLVER_BASE_URL` (no `:4000`, no auth header, no operator/Anthropic credential) and throws `SolverError('OFFLINE')` on network reject like the existing client. Add a pure `web/src/lib/leaderboard.ts` `rankForDisplay(leaderboard)` helper (sort/format for the S2 rows, badge label VERIFIED/UNVERIFIED) with a co-located test asserting the ranking/badge mapping and that constructed URLs contain no `:4000` and no credential (a `?raw` source scan like the existing solverUrls test).
  </action>
  <acceptance_criteria>
    - `web/src/solver.ts` exports `getCompeting`, `postRfq`, `getRfqQuotes`, `acceptRfqQuote`, `openIssuance`, `clearIssuance`, `payCoupon`, `redeemIssuance` and the mirrored response types.
    - Every new fn uses `SOLVER_BASE_URL` (no literal `:4000`, no auth header) — asserted by the source scan test.
    - `web/src/lib/leaderboard.ts` `rankForDisplay` maps verified→VERIFIED, unverified→UNVERIFIED and sorts by (matched desc, surplus desc).
    - `cd web && npx vitest run src/lib/leaderboard.test.ts` exits 0; web tsc/build clean.
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx vitest run src/lib/leaderboard.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Credential-free client fns + types for competing/RFQ/issuance exist mirroring the api.ts shapes; the leaderboard display helper is unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: SolverLeaderboard.tsx (S2) mounted on AgentView</name>
  <files>web/src/components/SolverLeaderboard.tsx, web/src/views/AgentView.tsx</files>
  <read_first>
    - 13-UI-SPEC.md lines 153-168 (S2 full spec: component, row contract, authoritative marker, states, honest tag, ink-not-lime rule) + lines 82-97 (color discipline) + lines 65-78 (typography tokens)
    - web/src/views/AgentView.tsx (existing AgentProposal/AgentRationale grid + BreakTheAiPanel placement — mount S2 as a distinct block below the grid)
    - web/src/components/AgentProposal.tsx (reuse the VERIFIED badge styling — mono 9px .12em border) and the animate-umbra-pulse COMPUTING idiom from Theatre
  </read_first>
  <action>
    Create `web/src/components/SolverLeaderboard.tsx` (operator-plane display only — reads a lifted `CompetingResponse` via `getCompeting`; NO operator token, NO `@daml/react` context, grep-clean like AgentProposal). Render the row contract from UI-SPEC S2: rank numeral (mono 11px/600 opacity .6, zero-padded), solver config tag (mono 9px .12em ink border, e.g. `HAIKU · t0.0`), matched volume + surplus (mono 15px/600 tabular-nums), and a VERIFIED badge (ink, reuse AgentProposal badge styling) when the proposal equals the deterministic result / UNVERIFIED badge (red `#E2231A`) when it diverged. Add the always-present authoritative block `DETERMINISTIC §8 CLEAR — AUTHORITATIVE` (ink, mono 9px tag) visually separated by a heavier ink rule, restating the clearing price/matched that will actually settle; mark the narrative winner (rank 1 among verified) with ink weight / a rule — NEVER lime. Render the states: empty (`NO COMPETING PROPOSALS YET` + body), loading (`animate-umbra-pulse` `COMPUTING`), offline (`OFFLINE_CAPTION`), ranked (list). Show the persistent honest tag `LEADERBOARD · NARRATIVE — NOT A SETTLEMENT INPUT`. Use ONLY existing tailwind tokens/fontSizes/keyframes (no new ones). Mount it on `AgentView.tsx` as a distinct block below the AgentProposal/AgentRationale grid.
  </action>
  <acceptance_criteria>
    - `web/src/components/SolverLeaderboard.tsx` renders rank/config-tag/matched/surplus rows + VERIFIED(ink)/UNVERIFIED(red) badges + the `DETERMINISTIC §8 CLEAR — AUTHORITATIVE` marker + the `LEADERBOARD · NARRATIVE — NOT A SETTLEMENT INPUT` tag.
    - The winner marker uses ink weight/rule, never lime (grep the component: no `#D6FB3C` on the leaderboard).
    - Grep-clean: no operator-token / `@daml/react` / Anthropic-key literal in the component.
    - `AgentView.tsx` mounts `SolverLeaderboard` below the existing grid.
    - `cd web && npx vitest run` (leaderboard/component tests) exits 0; web build clean.
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>The S2 leaderboard renders on the Agent view with correct badges, the authoritative deterministic marker, the honest narrative tag, ink-not-lime winner, and no credential in the bundle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser bundle → solver | Client must carry no operator/Anthropic credential |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-36 | Information Disclosure | web/src/solver.ts client fns | mitigate | All fns ride SOLVER_BASE_URL with no auth header; source-scan test asserts no :4000/credential in the bundle |
| T-13-37 | Tampering (misrepresent AI as authoritative) | SolverLeaderboard | mitigate | Honest tag `NOT A SETTLEMENT INPUT` + distinct `DETERMINISTIC §8 CLEAR — AUTHORITATIVE` marker; winner marked ink, never lime |
</threat_model>

<verification>
- `cd web && npx vitest run` — green.
- `cd web && npx tsc --noEmit` (or `npm run build`) — clean.
- Grep the bundle sources for `:4000`, operator-token, and Anthropic-key literals — none.
</verification>

<success_criteria>
- Credential-free client fns for all adjacent endpoints exist; the S2 leaderboard renders the ranked, badged, authoritatively-marked, narrative-tagged panel on the Agent view with no lime and no credential in the bundle.
</success_criteria>

## Artifacts this phase produces (this plan)

- Files: `web/src/solver.ts` (extended), `web/src/components/SolverLeaderboard.tsx`, `web/src/views/AgentView.tsx` (mount), `web/src/lib/leaderboard.ts` (+test)
- Client fns: `getCompeting`, `postRfq`, `getRfqQuotes`, `acceptRfqQuote`, `openIssuance`, `clearIssuance`, `payCoupon`, `redeemIssuance`
- Component: `SolverLeaderboard` (S2) on the Agent view
- Lib: `rankForDisplay(leaderboard)`

<output>
Create `.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-12-SUMMARY.md` when done.
</output>
