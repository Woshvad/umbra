---
phase: 13-platform-baseline-adjacent-track-b-ongoing
plan: 13
type: execute
wave: 6
depends_on: [13-12]
files_modified:
  - web/src/components/RfqPanel.tsx
  - web/src/views/DeskView.tsx
  - web/src/components/RfqPanel.test.tsx
autonomous: true
requirements: [ADJ-02]
must_haves:
  truths:
    - "RfqPanel (S3) renders on the Desk view below OrderTicket: compose an RFQ (instrument BONDX / side / qty) → REQUEST QUOTE → firm signed quotes → ACCEPT BEST QUOTE"
    - "Firm quotes are tagged FIRM · SIGNED; the best quote is highlighted with a 1px-ink emphasis + BEST marker"
    - "Accepting the best quote settles via the SAME atomic DvP path — reusing DvpLegs + AtomicStamp (no new settlement grammar)"
    - "Desk-plane only: submits on the desk's OWN token; grep-clean of operator token / @daml/react / Anthropic key"
    - "Empty (NO QUOTES YET) / awaiting / offline (OFFLINE_CAPTION) / settled states render; a reject renders verbatim on an ink evidence surface; single confirm on accept (no dialog)"
  artifacts:
    - path: "web/src/components/RfqPanel.tsx"
      provides: "S3 RFQ side-mode panel (desk-plane)"
      contains: "REQUEST QUOTE"
  key_links:
    - from: "web/src/components/RfqPanel.tsx"
      to: "web/src/solver.ts postRfq/getRfqQuotes/acceptRfqQuote"
      via: "desk-plane client, own token"
      pattern: "acceptRfqQuote"
    - from: "web/src/components/RfqPanel.tsx"
      to: "DvpLegs + AtomicStamp"
      via: "reuse the atomic-DvP settlement visuals"
      pattern: "AtomicStamp"
---

<objective>
Build the S3 RFQ side-mode panel on the Desk view: compose an RFQ, view firm signed quotes, accept the best, and see it settle via the SAME atomic Canton DvP visuals (reusing `DvpLegs` + `AtomicStamp`). Desk-plane only (the desk's own token; no operator/Anthropic credential). Minimal single-bond flow per the UI-SPEC S3 contract.

Purpose: surface the RFQ capability as a small additive panel that reuses the proven settlement visuals and honestly labels the 1×1 DvP.
Output: `RfqPanel.tsx` mounted on `DeskView.tsx` + component test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-UI-SPEC.md
@web/src/views/DeskView.tsx
@web/src/components/OrderTicket.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: RfqPanel.tsx (S3) mounted on DeskView</name>
  <files>web/src/components/RfqPanel.tsx, web/src/views/DeskView.tsx, web/src/components/RfqPanel.test.tsx</files>
  <read_first>
    - 13-UI-SPEC.md lines 172-188 (S3 full spec: component, flow states, CTAs, honesty, reject convention) + lines 100-118 (copywriting contract: REQUEST QUOTE / ACCEPT BEST QUOTE / NO QUOTES YET / FIRM · SIGNED) + lines 82-97 (color: Buy #2B3AF2 / Sell #FF3D9A, red reject)
    - web/src/components/OrderTicket.tsx (reuse the Side toggle Buy/Sell, the qty input, the single-confirm one-tap pattern, and the grep-clean desk-plane discipline — no operator token / @daml/react)
    - web/src/components/DvpLegs.tsx + web/src/components/AtomicStamp.tsx (reuse the atomic-DvP settlement visuals for the settled state — no new grammar)
    - web/src/solver.ts (postRfq / getRfqQuotes / acceptRfqQuote from 13-12)
  </read_first>
  <action>
    Create `web/src/components/RfqPanel.tsx` (desk-plane — submits on the desk's OWN token; grep-clean of operator token / `@daml/react` / Anthropic key, matching OrderTicket). Implement the S3 flow states from the UI-SPEC: idle/compose (instrument fixed `BONDX` label, the OrderTicket Side toggle Buy `#2B3AF2` / Sell `#FF3D9A`, a mono qty input, CTA `REQUEST QUOTE` ink-fill primary → `postRfq`); posted/awaiting (`AWAITING QUOTES…` reduced-motion-friendly pulse; poll `getRfqQuotes`); quotes-in (each row: dealer id mono, price mono 15px/600 tabular-nums, tag `FIRM · SIGNED` ink-border; the best quote highlighted 1px-ink + `BEST` marker; CTA `ACCEPT BEST QUOTE` single confirm, NO dialog → `acceptRfqQuote`); settling/settled (reuse `DvpLegs` for the 1×1 batch draw-on + `AtomicStamp` `SETTLED · ATOMIC` — no new settlement grammar); empty (`NO QUOTES YET` + body); error/offline (reuse `OFFLINE_CAPTION`; a reject rendered verbatim on an ink evidence surface, matching the BreakTheAiPanel verbatim-reject convention). Use ONLY existing tailwind tokens/fontSizes/keyframes. Mount `RfqPanel` on `DeskView.tsx` below `OrderTicket`.
    Write `RfqPanel.test.tsx` (stubbed solver.ts): compose→REQUEST QUOTE calls postRfq; quotes-in highlights the best + shows FIRM · SIGNED; ACCEPT BEST QUOTE calls acceptRfqQuote and renders the AtomicStamp settled state; offline shows OFFLINE_CAPTION; a reject renders verbatim. Assert grep-clean (no operator-token/@daml/react/Anthropic literal).
  </action>
  <acceptance_criteria>
    - `web/src/components/RfqPanel.tsx` renders the compose→quotes→accept→settled flow with `REQUEST QUOTE` + `ACCEPT BEST QUOTE` CTAs, `FIRM · SIGNED` tags, a `BEST` marker, and the `NO QUOTES YET` empty state.
    - The settled state reuses `DvpLegs` + `AtomicStamp` (no new settlement grammar).
    - Grep-clean: no operator-token / `@daml/react` / Anthropic-key literal in the component.
    - `DeskView.tsx` mounts `RfqPanel` below `OrderTicket`.
    - `cd web && npx vitest run src/components/RfqPanel.test.tsx` exits 0; web build clean.
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx vitest run src/components/RfqPanel.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>The S3 RFQ panel renders the full desk-plane flow, reuses the atomic-DvP visuals, honestly labels FIRM·SIGNED / the 1×1 settle, and carries no credential.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desk browser → solver RFQ endpoints | Desk-plane requests on the desk's own token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-38 | Information Disclosure | RfqPanel bundle | mitigate | Desk-plane only; grep-clean of operator token / @daml/react / Anthropic key; rides SOLVER_BASE_URL |
| T-13-39 | Tampering (misrepresent settlement) | RfqPanel settled state | mitigate | Reuses the real DvpLegs + AtomicStamp visuals; honest label that RFQ is a 1×1 DvP reusing the same machinery (no separate engine implied) |
</threat_model>

<verification>
- `cd web && npx vitest run src/components/RfqPanel.test.tsx` — green.
- `cd web && npx tsc --noEmit` (or build) — clean.
- Grep RfqPanel for operator-token/@daml/react/Anthropic-key — none.
</verification>

<success_criteria>
- The S3 RFQ panel delivers the full compose→quote→accept→atomic-settle flow on the Desk view, reusing the settlement visuals, honestly labeled, credential-free.
</success_criteria>

## Artifacts this phase produces (this plan)

- Files: `web/src/components/RfqPanel.tsx` (+test), `web/src/views/DeskView.tsx` (mount)
- Component: `RfqPanel` (S3) on the Desk view, consuming `postRfq`/`getRfqQuotes`/`acceptRfqQuote`
- Reused visuals: `DvpLegs`, `AtomicStamp` (no new settlement grammar)

<output>
Create `.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-13-SUMMARY.md` when done.
</output>
