---
phase: 13-platform-baseline-adjacent-track-b-ongoing
plan: 14
type: execute
wave: 6
depends_on: [13-12]
files_modified:
  - web/src/components/IssuancePanel.tsx
  - web/src/views/TheatreView.tsx
  - web/src/components/IssuancePanel.test.tsx
autonomous: true
requirements: [ADJ-03]
must_haves:
  truths:
    - "IssuancePanel (S4, OPTIONAL) renders a primary-issuance uniform-price clear reusing CrossingChart + PriceReveal, tagged PRIMARY ISSUANCE · UNIFORM-PRICE"
    - "The uniform issuance price reveal is the one place lime (#D6FB3C) is allowed on Phase 13 UI (it IS a real uniform-price clear)"
    - "A compact LifecycleRow shows COUPON PAID (pro-rata) / REDEEMED states, tagged LIFECYCLE"
    - "Theatre-plane only; grep-clean of operator token / @daml/react / Anthropic key; no new tailwind token/fontSize/keyframe"
    - "This surface is explicitly droppable — if plan scope is tight the executor MAY defer it (record the deferral; no partial UI)"
  artifacts:
    - path: "web/src/components/IssuancePanel.tsx"
      provides: "S4 issuance/coupon panel (optional, reuses CrossingChart + PriceReveal)"
      contains: "PRIMARY ISSUANCE"
  key_links:
    - from: "web/src/components/IssuancePanel.tsx"
      to: "web/src/solver.ts openIssuance/clearIssuance/payCoupon/redeemIssuance"
      via: "theatre-plane client"
      pattern: "clearIssuance"
    - from: "web/src/components/IssuancePanel.tsx"
      to: "CrossingChart + PriceReveal"
      via: "reuse the crossing/reveal visuals"
      pattern: "PriceReveal"
---

<objective>
Build the OPTIONAL S4 issuance/coupon panel on the Theatre view: a compact primary-issuance uniform-price clear (reusing `CrossingChart` + `PriceReveal` — the one place lime is allowed, as it IS a real uniform-price clear) plus a coupon/redemption lifecycle row. This surface is explicitly droppable per the UI-SPEC — if plan scope is tight the executor MAY defer it, recording the deferral rather than shipping partial UI.

Purpose: minimally demonstrate that the auction engine is issuance-capable end-to-end in the UI, reusing existing crossing/reveal visuals.
Output: `IssuancePanel.tsx` mounted on `TheatreView.tsx` + test (or a recorded deferral).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-UI-SPEC.md
@web/src/views/TheatreView.tsx
@web/src/components/CrossingChart.tsx
@web/src/components/PriceReveal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: IssuancePanel.tsx (S4, optional) mounted on TheatreView — or record deferral</name>
  <files>web/src/components/IssuancePanel.tsx, web/src/views/TheatreView.tsx, web/src/components/IssuancePanel.test.tsx</files>
  <read_first>
    - 13-UI-SPEC.md lines 192-202 (S4 full spec: optional/minimal, placement, component, states, tag, "If deferred: record the deferral; no partial UI") + lines 90-96 (lime reserved to the money-shot clear — S4 issuance reveal is the allowed use) + lines 106-107 (S4 has no primary CTA)
    - web/src/components/CrossingChart.tsx (reuse `mode="assembling"|"locked"` for pre-clear/cleared) and web/src/components/PriceReveal.tsx (the lime uniform-price reveal slab)
    - web/src/views/TheatreView.tsx (existing CrossingChart + PriceReveal usage — mount S4 as a single compact block; do not add a nav tab)
    - web/src/solver.ts (openIssuance/clearIssuance/payCoupon/redeemIssuance from 13-12)
  </read_first>
  <action>
    If plan scope allows, create `web/src/components/IssuancePanel.tsx` (theatre-plane display; grep-clean of operator token / `@daml/react` / Anthropic key). Render the S4 states: pre-clear (reuse `CrossingChart mode="assembling"`), cleared (reuse `CrossingChart mode="locked"` + `PriceReveal` for the uniform issuance price — the ONE allowed lime use — plus a minted-holdings summary from `clearIssuance`), and lifecycle (a compact `LifecycleRow` showing `COUPON PAID` pro-rata / `REDEEMED` states via `payCoupon`/`redeemIssuance`, ink rows + mono values + a `LIFECYCLE` tag). Persistent tag `PRIMARY ISSUANCE · UNIFORM-PRICE`. Use ONLY existing tailwind tokens/fontSizes/keyframes. Mount as a single compact block on `TheatreView.tsx` (no nav tab). Write `IssuancePanel.test.tsx` (stubbed solver.ts) asserting the assembling→cleared→lifecycle states render, the reveal uses PriceReveal, the COUPON PAID/REDEEMED rows show, and grep-clean.
    IF plan scope is tight (context budget pressured or the ledger issuance orchestration surfaced complications): DO NOT ship partial UI — instead record the deferral in the SUMMARY (S4 is explicitly droppable per UI-SPEC) and leave TheatreView unchanged.
  </action>
  <acceptance_criteria>
    - EITHER: `web/src/components/IssuancePanel.tsx` renders the pre-clear/cleared/lifecycle states reusing `CrossingChart` + `PriceReveal`, shows `PRIMARY ISSUANCE · UNIFORM-PRICE` + `COUPON PAID`/`REDEEMED` (`LIFECYCLE` tag), is grep-clean (no operator-token/@daml/react/Anthropic literal), lime appears ONLY on the PriceReveal reveal, and is mounted on `TheatreView.tsx`; `cd web && npx vitest run src/components/IssuancePanel.test.tsx` exits 0.
    - OR: the SUMMARY records an explicit S4 deferral (no partial UI shipped; TheatreView unchanged) — an acceptable outcome per the UI-SPEC.
    - Web build/tsc stays clean either way.
  </acceptance_criteria>
  <verify>
    <automated>cd web && npx tsc --noEmit && (npx vitest run src/components/IssuancePanel.test.tsx || echo "S4 deferred — verify SUMMARY records the deferral")</automated>
  </verify>
  <done>The S4 issuance panel renders the uniform-price clear + coupon/redeem lifecycle (lime only on the reveal), OR the deferral is recorded with no partial UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theatre browser → solver issuance endpoints | Theatre-plane requests, credential-free |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-40 | Information Disclosure | IssuancePanel bundle | mitigate | Theatre-plane display only; grep-clean of operator token / @daml/react / Anthropic key; rides SOLVER_BASE_URL |
| T-13-41 | Tampering (design fidelity — lime misuse) | IssuancePanel | mitigate | Lime restricted to the PriceReveal uniform-price reveal (a real clear); all other elements ink/red per UI-SPEC |
</threat_model>

<verification>
- `cd web && npx tsc --noEmit` — clean.
- If built: `cd web && npx vitest run src/components/IssuancePanel.test.tsx` — green; grep IssuancePanel for credential literals — none; lime only on the reveal.
- If deferred: the SUMMARY records the deferral and TheatreView is unchanged.
</verification>

<success_criteria>
- The optional S4 issuance/coupon panel is delivered (uniform-price reveal + lifecycle, lime only on the reveal, credential-free) OR cleanly deferred with the deferral recorded — no partial UI.
</success_criteria>

## Artifacts this phase produces (this plan)

- Files (if built): `web/src/components/IssuancePanel.tsx` (+test), `web/src/views/TheatreView.tsx` (mount)
- Component: `IssuancePanel` (S4, optional) consuming `openIssuance`/`clearIssuance`/`payCoupon`/`redeemIssuance`
- Reused visuals: `CrossingChart` (assembling/locked), `PriceReveal` (lime uniform-price reveal)
- Note: S4 is explicitly droppable — a recorded deferral is an acceptable artifact

<output>
Create `.planning/phases/13-platform-baseline-adjacent-track-b-ongoing/13-14-SUMMARY.md` when done.
</output>
