# Phase 13 — Live UAT Checklist

**Status:** Built · offline-verified · live UAT pending (2026-07-10)
**Why deferred:** These four items require external infrastructure absent on this box. Every seam is proven offline — solver `vitest` 291/291, web `vitest` 136/136 + build, `daml test` exit 0 (incl. `test_clears_at_100` = $100.00, 4× `test_rfq_*`, issuance/coupon/redeem), each new module carries a co-located secret-sweep. Verification status = `human_needed` (8/8 requirements substantively delivered + offline-verified). Security = SECURED (42/42, 0 high/critical). This mirrors the "Built · live UAT pending" pattern from Phases 8–12. Each live item has a **verified graceful offline fallback**, so nothing here is a gap.

## How to run the live stack
Boot the 3-node Canton LocalNet (`:3975/:2975/:4975`) + solver (`:4100`) + web (`:5173`) per `.planning` memory / `scripts/localnet/`. For the OTel + Vault items, bring up the optional collector + `ops/vault/docker-compose.vault.yml` and set the env switches.

## Live checks

- [ ] **OPS-01 observability (live collector):** set `OTEL_EXPORTER_OTLP_ENDPOINT` to a running OTLP collector; drive a round and confirm the nested request→§8-compute→ledger-client→agent spans arrive with the `round.id` attribute, the four metrics (`umbra.rounds.opened` / `clear.latency_ms` / `settle.latency_ms` / `agent.verified_total`) record, and a Prometheus/Alertmanager rule from `alerts/umbra-rules.yml` (e.g. `SolverDown`) trips. *(Offline-verified: console-exporter fallback when the env var is unset; span/metric emission + secret-redaction proven in vitest.)*
- [ ] **OPS-02 secrets from live Vault + rotation:** boot the dev Vault compose, `SECRETS_PROVIDER=vault`, seed `secret/umbra/*`; confirm the solver reads `ANTHROPIC_API_KEY`/operator token from Vault (env backend byte-unchanged when unset), then `scripts/rotate-secret.mjs` writes a new KV version and the solver re-reads it. *(Offline-verified: env backend byte-for-byte; vault backend KV-v2 shape parsed via stubbed fetch; `VAULT_TOKEN` module-private.)*
- [ ] **OPS-02 public status page (live):** hit `GET /status` + `/status.html` with NO token from a browser; confirm it renders venue up/round-phase/last-clear/uptime and **exposes zero private order data**. *(Offline-verified: order-sweep + secret-sweep tests; aggregate allow-list is structural.)*
- [ ] **OPS-04 webhooks (live sink):** register a real HTTPS sink; run a round; confirm `round.opened/sealed/cleared/settled` + `fill.posted` deliver, the `X-Umbra-Signature` HMAC verifies at the sink, and a failing sink triggers the backoff-retry ladder + delivery-log entry. *(Offline-verified: HMAC `timingSafeEqual` + replay guard + retry ladder in vitest; emits never block settlement.)*
- [ ] **OPS-05 FIX from a real OMS:** point a FIX-4.4-speaking OMS/test client at the acceptor (`POST /fix` HTTP-wrapped, or the TCP listener if enabled); send a `NewOrderSingle (35=D)` for BONDX; confirm it maps to a sealed order and returns an `ExecutionReport (35=8)` ack. *(Offline-verified: parse/build/BodyLength/CheckSum against a known-good vector; malformed → reject, never throws.)*
- [ ] **OPS-03 idempotency + FSM (live):** replay a mutating POST with the same `Idempotency-Key`+body → original response returned (settle runs once); same key with a different body → 422; a transient 5xx is NOT cached (retry re-runs); an illegal lifecycle transition → 409. *(Offline-verified end-to-end in vitest incl. the post-review 5xx-not-cached + method/path-namespacing regression tests.)*
- [ ] **ADJ-01 competing solvers (live key):** with a real `ANTHROPIC_API_KEY`, `POST /competing {roundId, configs}` races N configs; confirm the leaderboard ranks verified proposals and the **deterministic §8 clear still settles** (AI off the settlement path). *(Offline-verified: mocked-SDK ranking + referee tests; key-sentinel absent from every result.)*
- [ ] **ADJ-02 RFQ settle on live LocalNet:** a requester posts an RFQ, dealers return firm signed quotes, accept-best settles a 1×1 batch via `settleBatch` on real Canton, conserving cash+bond; per-desk quote privacy holds. *(Offline-verified: `daml test` `test_rfq_*` incl. privacy + firm-quote + accept-settles.)*
- [ ] **ADJ-03 issuance + coupon on live LocalNet:** an issuer opens a tranche, desks bid, the uniform-price clear mints Holdings; a Coupon pays pro-rata (double-pay rejected) and Redeem retires holdings, all conserving. *(Offline-verified: `daml test` `test_issuance_uniform_price` / `_coupon_prorata` / `_redeem`.)*

## Recorded honest limitations / accepted risks (not defects)
- **OTel "→ Canton" span** is a ledger-API **client** span, not an in-participant span — Canton itself is not instrumentable from the solver (labeled honestly in `telemetry.ts`).
- **Dev Vault root token** (T-13-05, security accepted-risk): the `ops/vault` compose uses an UNSAFE-DEV root token for local use; production uses AppRole (documented in `docs/SECRETS-RUNBOOK.md`). A live production Vault + zero-downtime rotation is a UAT/ops gate.
- **FIX 4.4 subset** — order entry only (NewOrderSingle → sealed order + ExecutionReport ack, minimal session). Full session recovery / resend / a live counterparty OMS handshake are UAT.
- **Multi-instance persistence** — idempotency / webhook / subscription stores are in-memory for the single-operator demo; a Postgres-backed swap is documented, not built.
- **External/business gates scheduled here (not code):** a cryptographer's review (gates Phase 10 crypto for production), a KYC/AML vendor (backs COMP-01), and a SOC 2 engagement — noted, not built.
