---
phase: 14-agentic-payments-x402
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - solver/src/api.ts
  - solver/src/index.ts
  - solver/.env.example
  - solver/src/x402.wiring.test.ts
  - .planning/phases/14-agentic-payments-x402/14-UAT.md
autonomous: true
requirements: [PAY-01]
tags: [x402, wiring, config, di, uat]

user_setup:
  - service: canton-x402-facilitator
    why: "canton-cc backend settles real Canton Coin via the FTP facilitator (live UAT only; self backend needs none)"
    env_vars:
      - name: X402_FACILITATOR_URL
        source: "FTP Canton x402 facilitator base URL (e.g. https://dev.cantrustai.xyz/...) — confirmed at UAT via GET /supported"
      - name: X402_FACILITATOR_KEY
        source: "FTP facilitator auth token — resolved via SecretsProvider.get, server-side only, never committed"

must_haves:
  truths:
    - "With X402_ENABLED unset/false the solver is byte-unchanged: the two metered routes and the §4 money-shot demo behave exactly as before (the primary invariant)"
    - "With metering enabled the gate is attached to EXACTLY GET /round/:id/solve-preview and POST /competing; every never-metered path (/health, /status, GET /round/:id, /settle, /sandbox/round, /fix, /rfq*, /issuance*) is never 402"
    - "index.ts main() reads X402_* env (default-OFF), resolves X402_FACILITATOR_KEY via SecretsProvider (module-private), constructs the FacilitatorLedger from ledger.listHoldings/moveFee, and threads a PaymentGate through buildDeps"
    - ".env.example documents every X402_* key with X402_ENABLED=false as the default that keeps the demo byte-unchanged"
    - "14-UAT.md records the live gates (real FTP facilitator, real $CC on DevNet, real x402 client, GET /supported network/asset confirm)"
  artifacts:
    - path: "solver/src/api.ts"
      provides: "AppDeps.x402? optional DI slot + per-route gate attachment + optional /status availability line"
      contains: "x402"
    - path: "solver/src/index.ts"
      provides: "X402_* config read + SecretsProvider facilitator key + facilitator/gate construction + buildDeps threading"
      contains: "X402_ENABLED"
    - path: "solver/.env.example"
      provides: "documented X402_* config block (default-OFF)"
      contains: "X402_ENABLED=false"
    - path: ".planning/phases/14-agentic-payments-x402/14-UAT.md"
      provides: "live-gate checklist for the canton-cc + real-$CC-on-DevNet path"
      contains: "X402_FACILITATOR"
  key_links:
    - from: "solver/src/api.ts createApp"
      to: "deps.x402.middleware on the two metered routes"
      via: "per-route pre-handler (default disabled no-op)"
      pattern: "x402.*middleware"
    - from: "solver/src/index.ts main()"
      to: "createFacilitator + createX402Gate → buildDeps({ x402 })"
      via: "boot construction threaded through BuildDepsArgs"
      pattern: "createX402Gate|createFacilitator"
---

<objective>
Wire the Plan 01 gate + Plan 02 facilitator into the live solver: an optional `AppDeps.x402` DI slot defaulted to a disabled no-op (so existing tests + the §4 demo are byte-unchanged), per-route attachment on exactly the two metered AI endpoints, `X402_*` config read in `main()` with the facilitator key resolved through `SecretsProvider`, `.env.example` documentation, and a `14-UAT.md` recording the external live gates. Optionally advertise x402 availability on the existing `/status` surface (droppable).

Purpose: Close PAY-01 end-to-end offline — deliver Success Criteria 1-4 as a running, default-OFF gated solver, honoring every never-metered allow-list entry and keeping the securities DvP + AI-off-settlement invariants intact. The live FTP facilitator + real $CC on DevNet are recorded as UAT (the same SV-sponsorship gate as Phase 12).

Output: modified `solver/src/api.ts` + `solver/src/index.ts` + `solver/.env.example`, a new `solver/src/x402.wiring.test.ts`, and `.planning/phases/14-agentic-payments-x402/14-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-agentic-payments-x402/14-CONTEXT.md
@.planning/phases/14-agentic-payments-x402/14-RESEARCH.md
@.planning/phases/14-agentic-payments-x402/14-PATTERNS.md
@.planning/phases/14-agentic-payments-x402/14-VALIDATION.md
@solver/src/x402.ts
@solver/src/facilitator.ts
@solver/.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: attach the gate in api.ts (optional DI, per-route, allow-list-safe)</name>
  <files>solver/src/api.ts, solver/src/x402.wiring.test.ts</files>
  <read_first>
    - `solver/src/api.ts` lines 242-246 (`AppDeps.idempotency?` optional DI slot — the shape to mirror for `x402?`), 583-594 (`createApp` + `express.json()` + `cors(ALLOWED_ORIGIN)` + `deps.idempotency ?? createIdempotency()`), 817-850 (`GET /round/:id/solve-preview`, the wrap()'d metered handler), 1350-1371 (`POST /competing`, the metered handler — roundId is in the BODY), 1287-1289 (the `/fix` per-route `express.text` middleware precedent), 1466-1481 (the secret-safe error middleware), 623-639 (`buildStatusInput` — the aggregate `/status` source, for the optional availability line).
    - `solver/src/x402.ts` — `PaymentGate`, `X402Options`, `createX402Gate` (Plan 01 exports).
    - 14-RESEARCH.md Pitfall 7 (gating a free/lifecycle endpoint by accident — prefer per-route on exactly the two handlers) + the never-metered allow-list in 14-CONTEXT.md §Area 3.
    - 14-PATTERNS.md §"solver/src/api.ts" (optional DI dep + defaulted unit + per-route attach + NEVER-gate list).
  </read_first>
  <action>
    Modify `solver/src/api.ts`: (1) add `x402?: PaymentGate` to the `AppDeps` interface next to `idempotency?` (import `PaymentGate` + `createX402Gate` from `./x402.js`), with a docblock: "OPTIONAL x402 gate. When absent, createApp builds a DISABLED no-op gate so existing endpoints/tests + the §4 demo are byte-unchanged." (2) In `createApp`, default it once: `const x402 = deps.x402 ?? createX402Gate({ enabled: false })`. (3) Attach `x402.middleware` as a PER-ROUTE pre-handler on EXACTLY the two metered routes — insert it as the middleware arg before the `wrap(...)` handler on `GET /round/:id/solve-preview` (api.ts:818) and `POST /competing` (api.ts:1350), mirroring the `/fix` per-route precedent. Do NOT register it app-wide; do NOT attach it to any other route. Leave every never-metered path free: `/health`, `/status`, `/status.html`, `POST /round` + lifecycle, `POST /round/:id/settle`, `GET /round/:id`, `/sandbox/round`, `/fix`, `/rfq*`, `/issuance*`. (4) OPTIONAL (droppable): in `buildStatusInput`, add a single aggregate, non-secret `x402: { enabled, network }`-style availability field sourced from the gate options — no price/payTo/secret, aggregate-only (mirror the /status allow-list discipline); drop it if it complicates the secret-sweep. Create `solver/src/x402.wiring.test.ts`: build the real `createApp` with a minimal deps stub and (a) NO `x402` dep → assert the two metered routes respond normally (no 402) — byte-unchanged default; (b) an injected ENABLED `createX402Gate` (stub facilitator) → assert both `GET /round/:id/solve-preview` and `POST /competing` return 402 with an `accepts[]` body, while `/health`, `/status`, `GET /round/:id`, `POST /round/:id/settle`, `/sandbox/round` are NEVER 402 (the free-path allow-list); (c) secret-sweep — no operator/ANTHROPIC/X402_FACILITATOR_KEY sentinel in any 402 body/header.
  </action>
  <acceptance_criteria>
    - `AppDeps` has `x402?: PaymentGate` and `createApp` defaults `deps.x402 ?? createX402Gate({ enabled: false })` (grep: `x402\\?: PaymentGate`, `createX402Gate\\(\\{ enabled: false`).
    - `x402.middleware` appears on exactly the two metered routes and nowhere app-wide (grep: `x402.middleware` count == 2; no `app.use(x402` present).
    - `cd solver && npx vitest run src/x402.wiring.test.ts -t "free"` passes (never-metered paths never 402).
    - `cd solver && npx vitest run src/x402.wiring.test.ts -t "byte-unchanged"` passes (no x402 dep ⇒ metered routes normal).
    - `cd solver && npx vitest run src/api.test.ts` — the existing 200+ api suite stays green (default no-op ⇒ byte-unchanged).
    - `cd solver && npx tsc --noEmit` clean.
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/x402.wiring.test.ts src/api.test.ts && npx tsc --noEmit</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: X402_* config + facilitator/gate construction + buildDeps threading in index.ts + .env.example</name>
  <files>solver/src/index.ts, solver/.env.example, solver/src/x402.wiring.test.ts</files>
  <read_first>
    - `solver/src/index.ts` lines 63-122 (`BuildDepsArgs` — where `webhooks?`/optional deps are declared), 132-234 (`buildDeps` — defaulting + returning optional deps into `AppDeps`), 275-276 (env-with-default read pattern), 306-334 (SecretsProvider resolution of ANTHROPIC_API_KEY + module-private client + keyless degradation — the exact pattern for the facilitator key), 356-368 (webhooks/clock construction in main()), 618-677 (the `buildDeps({...})` call site), 679 (`createApp(deps)`).
    - `solver/src/x402.ts` (`createX402Gate`, `PaymentGate`, `X402Options`) + `solver/src/facilitator.ts` (`createFacilitator`, `FacilitatorLedger`) + `solver/src/ledger.ts` (`listHoldings`, `moveFee`, `operatorParty`).
    - `solver/.env.example` lines 10-40 (`SOLVER_PORT`/`ROUND_SECONDS` block + the `SECRETS_PROVIDER`/`VAULT_*` doc-block style to mirror).
    - 14-RESEARCH.md §"Config" + Pitfall 6 (atomic price scale) + Assumptions A1-A5 (network/asset/URL env-driven, UAT-confirmed).
    - 14-PATTERNS.md §"solver/src/index.ts" (Analog A env reads, B SecretsProvider secret, C thread through BuildDepsArgs+buildDeps+call-site) + §".env.example".
  </read_first>
  <action>
    Modify `solver/src/index.ts`: (1) add `x402?: PaymentGate` to `BuildDepsArgs` (import `PaymentGate` from `./x402.js`), default it in `buildDeps` to `undefined` and return it in the AppDeps object (so `createApp`'s `?? createX402Gate({ enabled:false })` default holds when boot metering is off) — mirror how `webhooks?` threads through. (2) In `main()`, after the agent block (index.ts:334), read the config: `X402_ENABLED` (parse `=== 'true'`, default false), `X402_FACILITATOR` (`self`|`canton-cc`, default `self`), `X402_NETWORK`, `X402_ASSET`, `X402_PRICE`, `X402_PAY_TO` (default `ledger.operatorParty` as the venue party), `X402_FACILITATOR_URL` — all via `process.env.X ?? default`. (3) Resolve `X402_FACILITATOR_KEY` through the existing `secrets` provider EXACTLY like ANTHROPIC_API_KEY (try/catch, module-private, degrade to undefined when absent — the env backend throws `${name} is unset`); the key is used ONLY for the `canton-cc` backend and is never logged/returned. (4) Construct the facilitator: build a `FacilitatorLedger` port from `{ listHoldings: ledger.listHoldings, moveFee: ledger.moveFee }`, then `const facilitator = createFacilitator({ backend: X402_FACILITATOR, ledger: facilitatorLedger, facilitatorUrl: X402_FACILITATOR_URL, facilitatorKey: x402FacilitatorKey })`. (5) Construct the gate: `const x402 = createX402Gate({ facilitator, enabled: X402_ENABLED, network: X402_NETWORK, asset: X402_ASSET, price: X402_PRICE, payTo: X402_PAY_TO })`. (6) Pass `x402` into the `buildDeps({...})` call site. Keep the boot log secret-free (no key). Modify `solver/.env.example`: after the `ROUND_SECONDS` line add a `── PAY-01 x402 metered access ──` block documenting each `X402_*` key, stating `X402_ENABLED=false` is the DEFAULT that keeps the money-shot demo byte-unchanged, that `X402_PRICE` is the fee as a decimal at a documented USDCx-2dp scale, that `X402_NETWORK`/`X402_ASSET`/`X402_FACILITATOR_URL` are UAT-confirmed placeholders (do not hard-code a Canton id), and that `X402_FACILITATOR_KEY` is server-side-only (never committed — mirror the ANTHROPIC_API_KEY/VAULT_TOKEN notes); leave `X402_ENABLED=false` and the secret key empty. Extend `solver/src/x402.wiring.test.ts` with a boot-wiring assertion: call the exported `buildDeps` with a stub `x402` PaymentGate and assert the returned `AppDeps.x402` is that same gate (threading proof), and that `buildDeps` with no `x402` leaves it undefined (default-OFF).
  </action>
  <acceptance_criteria>
    - `BuildDepsArgs` has `x402?: PaymentGate` and `buildDeps` returns it into AppDeps (grep: `x402\\?: PaymentGate` in index.ts; `x402` in the returned object).
    - `main()` reads `X402_ENABLED`, `X402_FACILITATOR`, `X402_NETWORK`, `X402_ASSET`, `X402_PRICE`, `X402_PAY_TO`, `X402_FACILITATOR_URL` via `process.env` with defaults (grep: `X402_ENABLED`, `X402_FACILITATOR`) and resolves `X402_FACILITATOR_KEY` via `secrets.get` (grep: `X402_FACILITATOR_KEY` inside a try/catch near the ANTHROPIC_API_KEY block).
    - `createFacilitator(` and `createX402Gate(` are called in `main()` and the gate is passed to `buildDeps({...})` (grep both present).
    - `solver/.env.example` contains `X402_ENABLED=false` and documents all X402_* keys (grep: `X402_ENABLED=false`, `X402_FACILITATOR_KEY`).
    - `cd solver && npx vitest run src/x402.wiring.test.ts -t "threading"` passes; `cd solver && npx vitest run src/index.test.ts` stays green (boot wiring byte-compatible).
    - `cd solver && npx tsc --noEmit` clean; boot log carries no secret (grep the new log call — no key var interpolated).
  </acceptance_criteria>
  <verify>
    <automated>cd solver && npx vitest run src/x402.wiring.test.ts src/index.test.ts && npx tsc --noEmit</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: 14-UAT.md — the live/external gate checklist + phase regression sweep</name>
  <files>.planning/phases/14-agentic-payments-x402/14-UAT.md</files>
  <read_first>
    - 14-RESEARCH.md §"Validation Architecture" → "Live-UAT gates" + §"Manual-Only Verifications" (14-VALIDATION.md) — the exact external gates to record.
    - 14-CONTEXT.md §"Out of scope (external/UAT gates)" + §"Deferred Ideas".
    - The Phase 12 external-gate precedent (STATE.md — SV-sponsorship gate) so the honest "Built · offline-verified · live UAT pending" labeling matches the house style.
  </read_first>
  <action>
    Create `.planning/phases/14-agentic-payments-x402/14-UAT.md` following the project's "Built · offline-verified · live UAT pending" pattern. Record, as a checklist, the four live gates from 14-VALIDATION.md §Manual-Only + 14-RESEARCH.md §Live-UAT: (1) a real x402 client pays the enabled gate end-to-end (402 → pay → 200 + X-PAYMENT-RESPONSE) against `/solve-preview`; (2) the `self` backend settles a real USDCx transfer on a live DevNet node (funded desk USDCx cid → venue Holding grows); (3) the `canton-cc` backend settles real Canton Coin via the live FTP facilitator (`X402_FACILITATOR=canton-cc` + `X402_FACILITATOR_URL` + a CC-funded venue party — the SV-sponsorship gate, same as Phase 12); (4) the exact Canton CAIP-2 `X402_NETWORK` id + Canton Coin `X402_ASSET` id + facilitator paths confirmed against the FTP facilitator's `GET /supported`, plus the v1-vs-v2 envelope version confirmed (Assumptions A1-A5). For each: the exact enable/run command, the expected observable, and the honest note that it is offline-mocked / `self`-covered today. State clearly that NONE of these block the offline phase build (self is DevNet-capable now; canton-cc is offline-mocked). Do NOT run a live ledger here — this is a documentation deliverable. Finish by noting the phase regression sweep to run before verification: full solver `npx vitest run` green + `npx tsc --noEmit` clean + `cd daml && daml test` §4 golden $100.00 + a secret-sweep pass.
  </action>
  <acceptance_criteria>
    - `.planning/phases/14-agentic-payments-x402/14-UAT.md` exists and lists all four live gates with an enable/run command + expected observable each (grep: `X402_FACILITATOR`, `GET /supported`, `self`, `canton-cc`).
    - It explicitly states none of the gates block the offline build and marks the canton-cc/real-$CC path as the Phase-12-style SV-sponsorship gate.
    - It records the pre-verification regression sweep (full solver vitest + tsc + daml test §4 $100.00 + secret-sweep).
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/14-agentic-payments-x402/14-UAT.md && grep -q "GET /supported" .planning/phases/14-agentic-payments-x402/14-UAT.md && grep -q "canton-cc" .planning/phases/14-agentic-payments-x402/14-UAT.md && echo OK</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser / x402 client → solver | The gate is the only new untrusted-input surface; CORS stays :5173-only for the browser, the gate is accountless per-request for machine clients |
| boot env / SecretsProvider → main() | `X402_FACILITATOR_KEY` crosses from the secret store into a module-private variable, used only for canton-cc |
| gate scope → route table | The gate must touch EXACTLY two routes; a mis-attachment could meter a free/lifecycle/settlement endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-11 | Elevation | Gate accidentally meters a free/lifecycle/settlement endpoint | mitigate | PER-ROUTE attachment on exactly `/solve-preview` + `/competing`; a wiring test asserts `/health`,`/status`,`GET /round/:id`,`/settle`,`/sandbox/round` are never 402 |
| T-14-12 | Information Disclosure | Facilitator key / operator token / ANTHROPIC key reaching a 402 body, header, or boot log | mitigate | Key resolved via `SecretsProvider.get` at boot, module-private, canton-cc-only; secret-safe error middleware (api.ts:1471); secret-free boot log; wiring-test secret-sweep with an X402_FACILITATOR_KEY sentinel |
| T-14-13 | Elevation | CORS widening | mitigate | `cors({ origin: ALLOWED_ORIGIN })` (:5173) is untouched — the gate adds no origin and is never `*` |
| T-14-14 | Tampering | Enabling metering perturbs the §4 money-shot demo | mitigate | Default `X402_ENABLED=false` ⇒ `createX402Gate({ enabled:false })` no-op; api/index suites stay byte-green; the §4 golden clears $100.00 with the gate present |
| T-14-15 | Tampering | Fee leaking onto the securities DvP path | mitigate | The gate wraps only the two AI COMPUTE endpoints; `/settle`/`Round.Clear`/§8 are never gated and byte-unchanged; the fee is a standalone operator-custody transfer (Plan 02) |
| T-14-SC | Tampering | npm supply chain | accept | Zero new packages this phase (14-RESEARCH.md Package Legitimacy Audit); nothing added to package.json |
</threat_model>

<verification>
- `cd solver && npx vitest run` — the FULL solver suite green (existing ~291 + x402/facilitator/wiring), proving default-OFF byte-compatibility.
- `cd solver && npx tsc --noEmit` — clean.
- `cd daml && daml test` — §4 golden clears $100.00 with the gate in the tree (no Daml changed).
- Grep: `x402.middleware` on exactly the two metered routes; never-metered allow-list free; `X402_ENABLED=false` default in .env.example; no secret in the boot log; CORS unchanged.
- `14-UAT.md` present with the four live gates + regression sweep.
</verification>

<success_criteria>
- Default-OFF is proven byte-unchanged at the whole-app level (full solver suite + §4 $100.00); metering-on gates exactly the two AI endpoints and honors every never-metered allow-list entry.
- `main()` reads all `X402_*` config (default-OFF), resolves the facilitator key via SecretsProvider (module-private, canton-cc-only), and threads a `PaymentGate` through `buildDeps` → `createApp`.
- `.env.example` documents the config; `14-UAT.md` records the external live gates (canton-cc + real $CC = the Phase-12-style SV gate); no secret reaches the browser or a log.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 03 slice)

**Modified `solver/src/api.ts`:**
- `AppDeps.x402?: PaymentGate` optional DI slot; `createApp` default `deps.x402 ?? createX402Gate({ enabled:false })`
- `x402.middleware` attached per-route on `GET /round/:id/solve-preview` + `POST /competing` only
- Optional aggregate `/status` x402-availability line (droppable, secret-free)

**Modified `solver/src/index.ts`:**
- `BuildDepsArgs.x402?: PaymentGate` threaded through `buildDeps` → AppDeps
- `main()` reads `X402_ENABLED`, `X402_FACILITATOR`, `X402_NETWORK`, `X402_ASSET`, `X402_PRICE`, `X402_PAY_TO`, `X402_FACILITATOR_URL`; resolves `X402_FACILITATOR_KEY` via SecretsProvider (module-private)
- Constructs `FacilitatorLedger` (from `ledger.listHoldings`/`moveFee`) → `createFacilitator` → `createX402Gate` → `buildDeps({ x402 })`

**Modified `solver/.env.example`:** documented `X402_*` block (default `X402_ENABLED=false`).

**New env keys:** `X402_ENABLED`, `X402_FACILITATOR`, `X402_NETWORK`, `X402_ASSET`, `X402_PRICE`, `X402_PAY_TO`, `X402_FACILITATOR_URL`, `X402_FACILITATOR_KEY`.

**New files:** `solver/src/x402.wiring.test.ts`, `.planning/phases/14-agentic-payments-x402/14-UAT.md`.
</artifacts>

<output>
Create `.planning/phases/14-agentic-payments-x402/14-03-SUMMARY.md` when done.
</output>
