// FillCard (UI-SPEC "02 — DESK VIEW", YOUR FILL, lines 167-168) — STUB. Plan-02 Task 2
// fills this with the desk's own post-settlement TradeConfirmation. This stub keeps
// DeskView's Task-1 build green; it carries no operator token / context.
import type { Ctx, DeskKey } from '../ledgerContexts'

type Props = {
  ctx: Ctx
  deskKey: DeskKey
}

export default function FillCard(_props: Props) {
  return null
}
