// HoldingsPanel (UI-SPEC "02 — DESK VIEW", Right — Holdings, lines 165-166) — STUB.
// Plan-02 Task 2 fills this with the desk's own live BONDX/USDCx sum. This stub keeps
// DeskView's Task-1 build green; it carries no operator token / context.
import type { Ctx } from '../ledgerContexts'

type Props = {
  ctx: Ctx
}

export default function HoldingsPanel(_props: Props) {
  return null
}
